# Reconcile Committed and Discarded changes

analyze and evaluate code differences for applicable issues from the notes/thread_implementation_delta_analysis.md file:

## Summary of All Differences section

### Critical Diffferences sub-section

Perform the following:

1. **1. RestrictedUnpickler allowlist**
    - Restore the discarded version's of the allowlist for RestricedUnpickler to include all 13 activation types
2. **3. Weight magnitude validation**
    - Restore the discarded version's check of weight magnitude in result validatio

### Architectural Differences sub-section

Perform a detailed analysis of the committed and discarded architectural approaches for the following differences:
evaluate strengths and weakness as well as risks and guardrails

1. **4. Protocol organization**
    - Discarded: standalone dataclasses and functions
    - Committed: class-based `BinaryFrame` and `WorkerProtocol` and static methods
2. **5. Registry WebSocket storage**
    - Discarded: Websocket refs stored in registry
    - Committed: maintains registry as pure data

### Minor Differences sub-section

Perform the following:

1. **8. Queue Maxsize**
    - Ensure that queue maxsize is 1024 and uses a named constant (not a literal)
2. **9. Max Dimensions**
    - Ensure that max dimensions is 10
3. **10. Settings naming**
    - Ensure that the `remote_workers_*` name is used (not `worker_*`)
4. **11. Env var**
    - Ensure that `CASCOR_AUTH_TOKEN` env var is used (not `CASCOR_API_KEY`)

---
