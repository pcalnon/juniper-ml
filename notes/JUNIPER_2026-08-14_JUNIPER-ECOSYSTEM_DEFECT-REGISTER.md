# Juniper Defect Register — Extracted from the API Primer

**Project**: Juniper — Cascade Correlation Neural Network Research Platform
**Repository**: pcalnon/juniper-ml (register spans the whole ecosystem)
**Author**: Paul Calnon
**License**: MIT License
**Status**: Living register — verified against working copies on 2026-08-14
**Last Updated**: 2026-09-04
**Source**: [`JUNIPER_2026-08-13_JUNIPER-ECOSYSTEM_API-DESIGN-AND-IMPLEMENTATION-PRIMER.md`](JUNIPER_2026-08-13_JUNIPER-ECOSYSTEM_API-DESIGN-AND-IMPLEMENTATION-PRIMER.md)

---

## 1. What this is

The API primer uses the Juniper codebase as its worked-example corpus. In doing so it asserts, in passing, a large number of concrete problems in that code — because a defect is the most instructive example of a principle. Those assertions were scattered across 9,863 lines of teaching prose, where they are unusable as work items.

This register extracts every such claim, re-verifies it against current source, and records it with both anchors: the primer passage that makes the claim, and the `file:line` where the code lives.

**Provenance marker.** Entries marked **`†`** are **register-original**: verified against source today, but *not* asserted by the primer. They were discovered during re-verification, most often by asking "does this sibling repo have the same gap?" — a question the primer never asked. Everything unmarked was asserted in the primer and re-verified here.
Where an otherwise-primer-sourced entry carries a sub-claim the primer never made, that sub-claim is labelled **(register-derived)** in place. With those markers applied, the claim "this register contains no new *findings*" is precisely true for every unmarked entry, and honestly false for the marked ones.

It does contain some new *analysis*, and it is worth being precise about where. Three passages go beyond the primer: [§2.3](#23-the-pattern-worth-naming) groups fifteen entries into a single failure mode and recommends a drift check that does not exist; [§2.2](#22-the-four-highest-value-items) ranks by consequence, which the primer never did;
and [`APD-CASCOR-005`](#apd-cascor-005--api-key-comparison-short-circuits-on-match-in-two-of-three-copies) records an internal contradiction between two primer passages.

### Identifier namespace

Entries are `APD-<REPO>-<NNN>` — **A**PI-**P**rimer **D**efect. This is deliberately *not* the existing `F-<REPO>-NNN` namespace used by the canopy E2E arc (`F-CANOPY-001`, `F-CASCOR-002`); the two are unrelated and must not be cross-referenced by number.

Retired IDs are never reused. `APD-CCLIENT-003` was merged into `APD-CCLIENT-004` and its number is retired; `APD-CASCOR-001` was split into `APD-CASCOR-001a` and `APD-CASCOR-001b`.

### Inclusion criterion

A finding is included when it is a **concrete, actionable problem a maintainer could ticket**: a bug, a security gap, a specification violation, a bypass, drift between copies of shared code, a missing guard, a stale comment that misleads, dead code, or an ergonomics defect with a real cost.

Deliberately excluded: general API teaching that merely mentions Juniper; neutral descriptions of how something works; design choices the primer explicitly endorses (the seedless-nonce `dataset_id` escape hatch, the 501-not-503 capability signal, the `ZIP_STORED` streaming trade-off, cascor's verb-shaped training routes); and anything about third-party code (Starlette's 307 `redirect_slashes` default, `requests`, mypy).

Where a claim was arguable, it is included with **Confidence: low** and a note, rather than silently dropped. Over-inclusion is recoverable; a missed defect is not.

### Status is verified, not inherited

Every `OPEN` in this document was confirmed by reading the file **today**. Line numbers are current and may differ from the primer's, which are preserved separately so both anchors resolve. Four extraction passes ran independently over disjoint ranges of the primer; overlapping findings were merged, and each merged entry carries the union of its evidence. A fifth pass audited every primer line anchor for faithfulness, which is what produced the `†` markers above.

### Closing a row — the five touches (resident: do not move this to a handoff)

**Recorded here 2026-09-03, after it went missing.** This protocol governed every close from
round ~25 onward, but it was written down **only in the handoff chain** — each round's document
restating it for the next. Round 32 dropped the section, and a residency check then found the
definition in no `notes/` document, no `docs/` page, no `AGENTS.md`, and no agent memory —
while §4.1's correction note dated 2026-08-28 still *cites* it by name ("the fifth touch is
necessary but not sufficient").
A rule that lives only in the document that hands off the work stops existing the first time one
successor omits it, and nothing fails loudly when it does. It lives in the register now.

Closing one entry touches **five** places. Miss one and the register disagrees with itself while
every count-based check still passes:

1. Its **§4 table row** — status cell becomes `**FIXED (<pr>)**`.
2. Its **§3 detail entry** Status field — *only if the row has one*; most do not. **A row with no §3
   entry needs four touches, not five** — do not go looking for a detail entry to edit.
3. Its **§5.1 verification row** — the PR and what was actually verified.
4. The **§2 Status paragraph** — add the id to the enumerated list and update the open count.
5. The header **`Last Updated`** date.

Then run a whole-file **`grep -n 'APD-<ID>'`** and **read every hit** — ids also live in prose
notes, cross-references and other rows' cells. That same 2026-08-28 note records why reading is
required and not
just grepping: a closure once left a sentence standing because it named the ids *inside a
parenthetical about a different row*, so the grep found it and the author skimmed past it.

**`**FIXED` is the machine-readable token.** `util/ad-hoc/register_open_set.py` keys on it
(`\| (APD-[A-Z]+-\d+[ab]?) ` then `"**FIXED" in line`). A WON'T FIX close still writes `**FIXED`
with the qualification *inside* the marker. Inventing a second marker leaves the row counted OPEN.
Operator runbook (cwd pitfall, third reading vs the two §4 counters):
[`docs/REFERENCE.md` § Defect Register Close Protocol](../docs/REFERENCE.md#defect-register-close-protocol).

**A correction-only register PR is legitimate** — closing nothing, fixing what drifted.

**Cite this document by anchor text, not by its own line numbers.** Intra-document `:NNN` citations
have gone stale in this register on essentially every round that edited it, and the handoff lineage
has twice had to publish whole remapping tables (a `+15` shift after one PR, a `+16` after another).
The failure is quicker than that suggests: the session that wrote this very section measured three
line numbers, then added a note **earlier** in the file, and shipped all three pointing ~19 lines
short — inside the same PR, in the paragraph warning about stale anchors. **Measuring a line number
and then editing above it invalidates the measurement**, and nothing in review catches it. Quote the
section name or a distinctive phrase; those survive edits, and a reader can `grep` them.

### "Parked" has three shapes, and that is why the count keeps changing

**Recorded 2026-09-03.** Successive rounds have counted the parked/unparked split as **14/8**, then
**16/6**, then **14/4** — each session concluding its predecessor had miscounted, each re-deriving
carefully from source. Nobody miscounted. **The register never defined what parks a row**, and park
text is written in three structurally different places:

| Shape | Where the sentence physically sits | Example |
|---|---|---|
| **Row-level** | In the row's own §4 entry or §3 detail | `APD-CASCOR-005` — "owner decision, not a task"; `APD-ML-001` — "Record it; do not action it" |
| **Group-level** | One sentence parking a *named set*, with nothing in the member rows | `APD-DATA-026`–`-032` etc. — "Treat the remaining ten as one owner decision; do not action any of them unilaterally". **`APD-DATA-028` has no row-level park at all**, only this |
| **Foreign-cell** | Park language for row X living inside **row Y's** §5.1 verification cell | `APD-ECO-001` — "blocked on `APD-ECO-001`'s owner decision", inside `APD-CCLIENT-005`'s cell; `APD-ECO-003` — "which of their methods warrant the kwarg is part of that row's decision", inside `APD-RCLIENT-002`'s cell |

Count only the first shape and 4 rows are parked. Count the first two and it is 14. Count all three
and it is 16. **All three numbers are correct**; they answer three different questions, and the
lineage has been arguing about arithmetic when the disagreement was definitional.

**Operating rule: all three shapes park a row.** A park exists to stop a session actioning a row
unilaterally; where the sentence ended up is an artifact of which round wrote it, not a statement
about its force. The conservative reading can only ever *prevent* unilateral action, never license
it, so it is the one that fails safe. That makes the split **16 parked / 1 unparked** at 17 open,
the single unparked row being `APD-DATA-019` — which §4.1's note **"`APD-DATA-018` /
`-019` — re-verified 2026-09-02"** restates as
deliberately *not* part of the `juniper-data` REST group.

> *(Recount 2026-09-04: was "16 parked / 2 unparked at 18 open" until `APD-DATA-018` closed. This
> sentence is exactly the kind the § above warns about — it **counts**, so no whole-file
> `grep -n 'APD-<ID>'` would have routed a closing session here on the strength of the id alone.
> It survives the sweep only because it happens to name both ids; the count beside them is what
> goes stale, and the count is the part a reader trusts.)*

**A "row-level park only" rule was proposed and is rejected here**, because it reads `APD-DATA-028`
as disposable: that row's *only* protection is the group sentence. Adopting it would re-run the
`APD-ECO-002` mis-park from the opposite direction — there, a row was parked by a bucket label with
no reason of its own; here, a row would be *un*parked because its reason lives one paragraph away.

**This does not license the reverse, either.** Unparked ≠ actionable. Both unparked rows still
require an owner decision before any code can be written — `-018`'s cap is a rejection-vs-truncation
and rows-vs-bytes question, `-019`'s remedy needs `total` estimated, cached or absent, and the last
is a response-shape change for existing clients. **As of 2026-09-03 the set of rows a session may
action without asking the owner first is empty**, and that is a fact about how far this register has
been worked, not an obstacle to route around.

**The limit of the sweep, found 2026-09-03: it is ID-keyed, so it cannot see a claim that names no
ID.** `grep -n 'APD-<ID>'` finds every mention of an id; it finds nothing in a sentence like "three
prefixes read zero" or "the third grouping to reach zero", which is *about* the register's state
but names nothing. Such a sentence went stale for three days at `:139` after a correctly-executed
four-touch close, precisely because the close was complete and the sweep was clean. **After a
close, also re-read any sentence that counts or ranks something** — those are the ones no grep will
route you to. `util/ad-hoc/register_status_crosscheck.py` covers the ID-keyed half by checking §4,
§2 and §5.1 name the same ids from three independent reads; the ID-free half has no automation and
is why this paragraph exists.

---

## 2. Summary

| Repository                  | Security | Correctness | Robustness | Maintainability | Ergonomics |  Total |
|-----------------------------|---------:|------------:|-----------:|----------------:|-----------:|-------:|
| `juniper-data`              |        4 |          10 |          6 |               7 |          9 | **36** |
| `juniper-service-core`      |        1 |           0 |          5 |               6 |          4 | **16** |
| `juniper-cascor-client`     |        0 |           3 |          2 |               4 |          3 | **12** |
| `juniper-data-client`       |        0 |           3 |          1 |               3 |          1 |  **8** |
| `juniper-cascor`            |        2 |           2 |          0 |               3 |          0 |  **7** |
| `juniper-observability`     |        0 |           0 |          0 |               4 |          0 |  **4** |
| `juniper-recurrence-client` |        0 |           1 |          1 |               3 |          0 |  **5** |
| Cross-client / ecosystem    |        0 |           0 |          3 |               3 |          1 |  **7** |
| `juniper-ml` (meta)         |        0 |           0 |          0 |               1 |          0 |  **1** |
| **Total**                   |    **7** |      **19** |     **18** |          **34** |     **18** | **96** |

**Status:** the 96 entries above were all confirmed present in current source on 2026-08-14.
**Seventy-nine have since been fixed** — `APD-DATA-002`, `APD-DATA-006`, `APD-DATA-034`, `APD-DATA-036`, `APD-CASCOR-002`, (2026-08-16) `APD-DATA-001` † / `APD-CASCOR-004` †, (2026-08-17) `APD-DATA-003` / `APD-CASCOR-006` † plus `APD-SVCCORE-003` / `APD-SVCCORE-010` / `APD-OBS-001`, and (2026-08-20) `APD-DATA-035` † / `APD-CASCOR-001b` plus `APD-CASCOR-001a`, then `APD-DATA-004`, and (2026-08-21) `APD-DCLIENT-001` / `APD-DCLIENT-003`, then `APD-CCLIENT-004` / `APD-RCLIENT-001` and `APD-CCLIENT-002`, then `APD-SVCCORE-008` / `APD-OBS-002`, and `APD-OBS-003` / `APD-OBS-004`, then (2026-08-22) `APD-DATA-009`, and (2026-08-23) `APD-DATA-013` / `APD-DATA-014`, then `APD-DATA-007`, and `APD-DATA-011` / `APD-DATA-012`, then `APD-DATA-010`, and `APD-SVCCORE-006` / `APD-SVCCORE-014`, then `APD-SVCCORE-009` / `APD-SVCCORE-017`, and (2026-08-24) `APD-SVCCORE-004` / `APD-SVCCORE-007`, then `APD-DCLIENT-002` and `APD-DCLIENT-007`, then `APD-CCLIENT-010` and `APD-RCLIENT-003`, and `APD-DCLIENT-005` / `APD-DCLIENT-006`, then `APD-DCLIENT-004` / `APD-CCLIENT-005`, and (2026-08-25) `APD-RCLIENT-002`, then `APD-CCLIENT-006`, and `APD-DCLIENT-008`, then `APD-CCLIENT-007` / `APD-CCLIENT-009` / `APD-CCLIENT-013`, and `APD-CCLIENT-012`, then `APD-CCLIENT-011`, and `APD-DATA-015`, then `APD-DATA-020` / `APD-DATA-021`, and (2026-08-26) `APD-DATA-023`, then `APD-DATA-025`, and `APD-ECO-002`, then `APD-DATA-005` / `APD-DATA-024`, and `APD-CASCOR-003` (partial), then (2026-08-28) `APD-CCLIENT-001`, and `APD-DATA-033`, then `APD-ECO-005`, and `APD-SVCCORE-011` / `APD-SVCCORE-015`, then `APD-SVCCORE-012`, and `APD-SVCCORE-001`, then `APD-SVCCORE-005`, and (2026-08-29) `APD-SVCCORE-013` / `APD-SVCCORE-016`, and `APD-ECO-006`, then (2026-08-30) `APD-RCLIENT-005`, and (2026-08-31) `APD-CCLIENT-008`, then (2026-09-01) `APD-ECO-007`, and (2026-09-02) `APD-DATA-016`, then (2026-09-04) `APD-DATA-018` — leaving **17 open**; each is marked at its detail entry and in its §4 table row, and all seventy-nine are recorded in [§5](#5-fixed-findings-before-and-since-the-primer) with their PR and verification.
**Four groupings now have no open row at all**, in the order they reached zero: `juniper-observability` (4/4, closed by [ml#1245](https://github.com/pcalnon/juniper-ml/pull/1245)), `juniper-data-client` (8/8, closed by [dclient#171](https://github.com/pcalnon/juniper-data-client/pull/171)), `juniper-service-core` (16/16, 2026-08-29) and `juniper-cascor-client` (12/12 — thirteen `APD-CCLIENT-*` ids exist, but `-003` is retired into `-004`; closed by [cascor#610](https://github.com/pcalnon/juniper-cascor/pull/610) / [cclient#148](https://github.com/pcalnon/juniper-cascor-client/pull/148), 2026-08-31). **Do not trust that list to be current** — re-derive with `util/ad-hoc/register_open_set.py` and read which prefixes are absent from its open set. Of `juniper-service-core`'s sixteen, two closed as decisions rather than code changes (`APD-SVCCORE-007` disclosure, `APD-SVCCORE-016` won't-fix) and one as a docstring with its name deliberately unchanged (`APD-SVCCORE-013`); those three are closed, not waived, and each records the reasoning where the next reader will meet it.

> *(Corrected 2026-09-03. This sentence read "the **third** grouping … three prefixes read zero" for the three days after `APD-CCLIENT-008` closed and made it four. **The close was not sloppy — it was complete.** `APD-CCLIENT-008` took all four of its touches: §4 table row, §5.1 verification row, the §2 status list above, and the header date. The sentence survived anyway because the §4 close protocol's whole-file sweep is **`grep -n 'APD-<ID>'`**, and this sentence **names no ID at all**. That is one step past `:634`'s warning, where the missed sentence at least contained IDs and only needed reading. **A claim about the register's own state that names no ID is invisible to the ID-keyed protocol** — so the ordinal and the derived count are now gone, and the standing instruction is to re-derive rather than read. Cross-checking the whole register for other ID-free state claims found no second instance; the severity claims below survive because they are about closed classes, which cannot go stale by closing more rows. `util/ad-hoc/register_status_crosscheck.py` pins the ID-keyed half: §4's `**FIXED` set, the §2 prose list and the §5.1 verification rows must name the same ids, and it reads all three independently.)*
**The `Security` class and all three §2.3 drift groups are now closed.** What remains is `C` / `R` / `M` / `E` work with no shared theme left to exploit — each open row is now its own item rather than an instance of a pattern.
**Every `Security` entry in this register is now `FIXED`.** All seven — `APD-DATA-001`–`-004`, `APD-CASCOR-004`, `APD-CASCOR-006`, `APD-SVCCORE-003` — are closed; every remaining open row is `C` / `R` / `M` / `E`. **`juniper-data` now has no open `Correctness` row either** — all ten are closed, and what is left there is `R` / `M` / `E`. That is a statement about this register's contents, not about the services: coverage is uneven by construction (§6), and `juniper-canopy` / `juniper-cascor-worker` were barely visited by the primer at all.
That closes every item in §2.2's ranked list **and every copy-drift row in §2.3** — the `OPTIONS`/CORS row was the last one open, and fixing it in both forks is what finally made it encodable as a drift gate.
Three further findings were already fixed before this register was written and are likewise listed in §5 rather than counted in the 96: two between the primer's publication and this register, and one earlier still.
No extracted claim failed to reproduce, but one claim reproduced with its provenance inverted — see `APD-SVCCORE-016`.

### 2.1 Reachability — read this before triaging any Security entry

Severity here classifies the *defect*. It does not account for deployment topology, and topology removes the external attacker from most of the `Security` entries:

- **`juniper-data` publishes no host port** in the reference stack. It sits only on the `backend` and `data` networks, both `internal: true` (`juniper-deploy/docker-compose.yml:136-143`, which notes a `ports:` mapping there "would be silently ineffective"). Its reachable attacker is a sibling container that already holds a valid API key.
- **`juniper-cascor` publishes loopback-only**, attested by `JUNIPER_CASCOR_LOOPBACK_PUBLISH_ATTESTED` and enforced by `make preflight` plus `tests/test_compose_bind_posture_attestation.py`.
- **`juniper-service-core` is only partially adopted.** Its HTTP middleware and security modules have exactly one production consumer (`juniper-recurrence`); its WebSocket modules have none. Which module an entry lives in decides whether it is live or latent — see the §4.2 preamble, which classifies all sixteen.

The entries that survive this filter as genuinely reachable are those in the bare/dev profile, where the services bind directly. Triage accordingly; do not read a `Security` label as "exploitable from the internet today".

### 2.2 The four highest-value items

Ranked by consequence divided by cost, not by count.

> **Worked through 2026-08-14 to 2026-08-16. All four items are FIXED.** The ranking held up in
> practice: item 1's fix was indeed a port of already-tested lines from two siblings, and item 2 did
> turn out to need two PRs because the handler is byte-identical in both services. Item 4's warning
> below — that consolidating the forks would activate `APD-SVCCORE-003` — is unchanged, and the fix
> chosen for item 4 deliberately **did not** consolidate: it ported the throttle into each copy,
> leaving that warning still governing any future consolidation.
>
> | Item                                | Fixed by                                                                 | Notes                    |
> |-------------------------------------|--------------------------------------------------------------------------|--------------------------|
> | 1 — `APD-DATA-002` + `APD-DATA-036` | [juniper-data#261](https://github.com/pcalnon/juniper-data/pull/261)     | One patch, as predicted  |
> | 2 — `APD-CASCOR-002`                | [juniper-cascor#516](https://github.com/pcalnon/juniper-cascor/pull/516) |                          |
> | 2 — `APD-DATA-034`                  | [juniper-data#262](https://github.com/pcalnon/juniper-data/pull/262)     |                          |
> | 3 — `APD-DATA-006`                  | [juniper-data#263](https://github.com/pcalnon/juniper-data/pull/263)     |                          |
> | 4 — `APD-DATA-001`                  | [juniper-data#266](https://github.com/pcalnon/juniper-data/pull/266)     | Ported, not consolidated |
> | 4 — `APD-CASCOR-004`                | [juniper-cascor#524](https://github.com/pcalnon/juniper-cascor/pull/524) | Ported, not consolidated |
>
> The §2.3 drift-check recommendation was also built: [juniper-ml#1103](https://github.com/pcalnon/juniper-ml/pull/1103).
> Its `pre-auth-throttle` row was promoted `KNOWN_GAP` → `ENFORCED` when item 4 landed, which is the
> self-maintaining half of that ledger working exactly as designed.

1. **`APD-DATA-002` — the request body limit is bypassable.** A chunked request with no `Content-Length` streams past the 10 MiB cap entirely. The only unauthenticated memory-exhaustion vector in the register when the service runs in the open bare/dev profile — and the fix is twelve already-written, already-tested lines sitting in **two** sibling repos
   (`juniper-cascor/src/api/middleware.py:100-110`, `juniper-service-core/juniper_service_core/middleware.py:113-131`). Highest consequence over lowest cost. `APD-DATA-036` is a second defect on the same line and ships with the same patch.
2. **`APD-CASCOR-002` + `APD-DATA-034` † — a blanket `ValueError` handler reports server faults as `400`.** Present in **both** running services. `PydanticSerializationError` subclasses `ValueError`, so a server-side serialisation fault is returned to the caller as a client error and never appears in 5xx alerting.
   It has already bitten once — cascor's `coerce_native_scalars` exists solely to dodge it, and `juniper-data` has no such workaround, so it is *less* protected. An observability blind spot across the whole HTTP surface of two services.
3. **`APD-DATA-006` — a `GET` can silently undo a write.** `record_access` rewrites the whole metadata document under a lock the tag-update path does not take. The race is likelier than it first appears: on the download path `record_access` is scheduled with `call_soon`
   (`routes/datasets.py:698`), so it runs on the loop thread and can land squarely between the tag update's two `asyncio.to_thread` hops. Silent write loss triggered by a *safe* method is the class nobody suspects.
4. **`APD-DATA-001` † / `APD-CASCOR-004` † — the 401 path is unthrottled in both running services.** — **FIXED** ([juniper-data#266](https://github.com/pcalnon/juniper-data/pull/266), [juniper-cascor#524](https://github.com/pcalnon/juniper-cascor/pull/524)). `juniper-ml#1082` added `FailedAuthThrottle` to `juniper-service-core`, but **neither service imported that middleware** — both carry their own copies. Real, but ranked fourth deliberately:
   the credentials are high-entropy keys from Docker secrets, so online guessing is not the threat; what the throttle actually buys is CPU and log-flood control. The shippable fix is porting `FailedAuthThrottle` into the two copies — *not* "retire the copies", which is a migration project, not an equal-cost alternative.
   **Both entries are `†`.** The primer describes the mechanism and states the shared-package fix landed (1058, 1344); it never mentions a juniper-data or juniper-cascor copy of `SecurityMiddleware`, and `FailedAuthThrottle` appears zero times in its 9,863 lines. The copy divergence is this register's finding, not the primer's.
   **What the fix took, for the next port of this shape.** Both PRs mirror service-core's optional fourth constructor parameter (`failed_auth_throttle: FailedAuthThrottle | None = None`, defaulting to an *enabled* throttle), so neither `app.py` call site changed and no settings field was added — the shared package and juniper-recurrence expose no env knob either, and inventing one per fork would have widened the divergence this entry is about.
   The port is two-part and the second half is the easy one to lose: `check()` runs before authentication, but `record_failure()` is gated on the response being a 401, so a `check()`-only port is a throttle that never accumulates.
   Each fork's suite therefore carries an arm that fails if `record_failure` is dropped (verified by mutation in both).
   One pre-existing cascor test, `test_failed_auth_does_not_increment_rate_limit`, fires exactly the default budget of 10 failed attempts and then asserts a valid key still passes; it now builds a generous throttle so it keeps measuring the *identity-keyed limiter's* counters rather than tripping the new gate.

**A loop worth noticing before acting on #4.** The tempting fix for the divergence — adopt the shared middleware everywhere — is precisely what would make `APD-SVCCORE-003` live. That entry (a `getattr`-with-default settings lookup that silently skips the WebSocket Origin allowlist when the field is absent or misspelled) is the
highest *potential* consequence in the register, and it is inert today only because nothing imports `juniper_service_core.websocket`. cascor's copy reads those settings as hard attributes, so a typo is an `AttributeError` there. **On this axis the local copy is the stricter one, and consolidation would be a regression unless `_setting` is hardened first.**

### 2.3 The pattern worth naming

Fifteen entries share one shape: **a guard adopted in one copy of near-identical code and not in its siblings.** They are not all the same mechanism, and the distinction matters for what fixes them:

**Copy drift** — a service maintains its own copy of shared code and misses a fix. A drift check against `juniper-service-core` would catch these. **All of this group is now closed, and every row is encoded**: each has been ported (or, for the two rows with no shared implementation, fixed independently in both forks) and promoted to `ENFORCED`. The `OPTIONS`/CORS row was the last one open — it had landed in *no* copy, so there was nothing to derive a marker from until both forks were fixed.

> **That drift check now exists**: `juniper-ml/tests/test_service_fork_drift.py`
> ([juniper-ml#1103](https://github.com/pcalnon/juniper-ml/pull/1103)). It encodes this table as a
> registry of named guards — deliberately *not* a file diff, because these forks diverge
> legitimately and sometimes intentionally (juniper-data holds API keys in a `list` so
> `compare_digest` runs per key without a set-membership timing side-channel, where service-core uses
> a `set`; a diff would bury the signal). It is **two-sided**: rows already fixed are `ENFORCED` and
> must stay present, and rows still open are `KNOWN_GAP` and asserted to be still *absent*, so
> closing one fails the gate and prompts promotion rather than letting the ledger rot. **No
> `KNOWN_GAP` rows remain**; all six guards are `ENFORCED`. The last one promoted,
> `cors-outside-auth`, needed a mechanism the others did not: its regression shape is two
> `add_middleware` calls swapping places, so every marker is present either way and a presence-only
> check would pass on the very defect it guards. That row is an **ordered** site — the markers must
> appear in a declared sequence — with its own negative controls in the always-on structural class.
> The gate runs against sibling checkouts in `docs-full-check.yml`.

| Guard                                       | Landed in                                     | Missing from                                                                                                     | Gate                                |
|---------------------------------------------|-----------------------------------------------|------------------------------------------------------------------------------------------------------------------|-------------------------------------|
| Streaming body cap (CR-024)                 | `juniper-cascor`, then `juniper-service-core` | ~~`juniper-data`~~ — **fixed** (`APD-DATA-002`, data#261)                                                        | `ENFORCED`                          |
| `Content-Length` parse guard (400, not 500) | `juniper-cascor`, `juniper-service-core`      | ~~`juniper-data`~~ — **fixed** (`APD-DATA-036`, data#261)                                                        | `ENFORCED`                          |
| Blank-API-key filter                        | `juniper-service-core`                        | ~~`juniper-data`, `juniper-cascor`~~ — **both fixed** (`APD-DATA-003` data#267, `APD-CASCOR-006` † cascor#527)   | `ENFORCED`                          |
| Pre-auth throttle (`juniper-ml#1082`) †     | `juniper-service-core`                        | ~~`juniper-data`, `juniper-cascor`~~ — **both fixed** (`APD-DATA-001` † data#266, `APD-CASCOR-004` † cascor#524) | `ENFORCED`                          |
| CORS outside auth (was: `OPTIONS` bypass)   | *(nowhere)*                                   | ~~`juniper-cascor`, `juniper-data`~~ — **both fixed** (`APD-CASCOR-001b` cascor#540, `APD-DATA-035` † data#273)   | `ENFORCED`                          |
| Narrow serialisation-error handling         | *(nowhere)*                                   | ~~`juniper-cascor`, `juniper-data`~~ — **both fixed** (`APD-CASCOR-002` cascor#516, `APD-DATA-034` † data#262)   | `ENFORCED`                          |

The pre-auth-throttle row is `†`: the pairing of the shared fix against the two unpatched copies is register-original, not a primer claim.

> **A marker caveat learned promoting that row.** The gate checks source markers, so it verifies a
> *name*, not a behaviour — the original single marker `("FailedAuthThrottle",)` would have gone
> green on a bare `import`, and the half-port that actually matters (pre-auth `check()` wired,
> `record_failure` omitted → a throttle that never accumulates) would have sailed through it. The
> promoted row therefore asserts **two** markers per fork, `FailedAuthThrottle` *and*
> `record_failure`. That is still only a structural proxy: real behavioural coverage lives in each
> fork's own suite, and a green `ENFORCED` gate is evidence the pieces are present, never that the
> port is correct. Worth applying the same scepticism to the remaining rows.

**Sibling-package drift** — three independently released client packages that solved the same problem differently. No shared code, so no drift check applies; this needs a written cross-client convention.

| Guard                            | Landed in                                          | Missing from                                                                           |
|----------------------------------|----------------------------------------------------|----------------------------------------------------------------------------------------|
| Idempotent-only retry (XREPO-11) | `juniper-data-client`, `juniper-recurrence-client` | ~~`juniper-cascor-client` (`APD-CCLIENT-001`)~~ — **fixed**, cclient#143: all three clients now restrict retry to idempotent methods. **This was the last open row in this table**, four months after the siblings landed XREPO-11 |
| Base-URL normalisation           | `juniper-data-client`, `juniper-recurrence-client` | ~~`juniper-cascor-client` (`APD-CCLIENT-005`)~~ — **fixed**, cclient#129               |
| Base-URL `netloc` validation     | `juniper-recurrence-client`                        | ~~`juniper-data-client` (`APD-DCLIENT-004`), `juniper-cascor-client` (`APD-CCLIENT-005`)~~ — **fixed**, data-client#165 + cclient#129: all three clients now normalise **and** validate |

**Same-file inconsistency** — one author, one file, one hardened path and one not. Nothing structural would have caught these; they are ordinary review misses.

| Guard                           | Present at                    | Absent at                                                   |
|---------------------------------|-------------------------------|-------------------------------------------------------------|
| Listener fault isolation        | `_dispatch_disconnect`        | ~~`_dispatch` (`APD-CCLIENT-006`)~~ — **fixed**, cclient#135: the disconnect guard ported |
| Redaction of raw exception text | the `except Exception` branch | ~~the adjacent `except HTTPException` branch~~ — **fixed** at the source (`APD-DATA-004`, data#275) |

The first group is the actionable one, and it is the argument for a drift check: six of the fifteen are copies of `juniper-service-core` code that diverged silently. The other two groups need conventions and review attention respectively, not tooling.

**The sibling-package-drift group is CLOSED** (2026-08-21). `APD-DCLIENT-001` — exceptions carrying no `status_code` / `detail` / `response` — was the same defect as `APD-CCLIENT-004` and `APD-RCLIENT-001` in the two other clients. It was fixed first in `juniper-data-client` ([#158](https://github.com/pcalnon/juniper-data-client/pull/158)) precisely so there was something concrete to port, then ported to `juniper-cascor-client` ([#123](https://github.com/pcalnon/juniper-cascor-client/pull/123)) and `juniper-recurrence-client` ([juniper-recurrence#124](https://github.com/pcalnon/juniper-recurrence/pull/124)). The shared contract: keyword-only `status_code` / `detail` / `response` on the base class, `detail` holding the server's payload unmodified, a `__reduce__` override so the context survives pickle/copy, and each package's own test double populating the same fields.

No drift gate can enforce this — three separately released packages share no code — so the enforcement is documentary: each package's `AGENTS.md` now carries an "exception context" section naming the three constraints a refactor must not break, and each cites the other two. That is weaker than `test_service_fork_drift.py` and is *supposed* to be: inventing a gate for packages with no shared code would be the same error as inventing a drift marker with no reference implementation.

**`juniper-recurrence-client` is not a standalone repository.** It is a sub-package of the `pcalnon/juniper-recurrence` monorepo at `juniper-recurrence-client/juniper_recurrence_client/`. The §4.6 anchors are written bare and read as though the repo were its own; they are relative to that subdirectory.

**Unfiled fork-drift observation — worker-disconnect cleanup, named differently in each fork.**
*Not a numbered entry: it is register-original, was never asserted by the primer, and adding a row
would perturb the 96-entry accounting the rest of this document rests on. Recorded here because it
was otherwise living in exactly one place — the squashed commit body of cascor#604 (`3af59bd`),
where no reader of this register would ever meet it.* Verified 2026-09-03:

| Fork | Symbol | Defined | Called |
|---|---|---|---|
| `juniper-service-core` (shared) | `WorkerCoordinator.release_worker_tasks` | — | `websocket/worker_stream.py:203`, `:358` |
| `juniper-cascor` (fork) | `WorkerCoordinator.handle_worker_disconnect` | `src/api/workers/coordinator.py:185` | `src/api/websocket/worker_stream.py:158` |

Both forks already have the remedy, so **nothing is broken today** — which is precisely what makes
it drift rather than a defect. The two public names cannot be checked against each other, and
`tests/test_service_fork_drift.py` covers neither symbol (`grep -c` over both names → **0**). The
guard registry described above keys on *named guards present or absent*, so a divergence in which
name the guard wears is invisible to it: the shape it cannot see is **same behaviour, different
public API**. Filing this as a row, or extending the registry to carry a per-fork symbol alias, is
an owner call — recording it is not.

---

## 3. Critical and security findings — detail

Entries below carry full detail. Everything else is tabulated in §4 with the same anchors.

### APD-DATA-001 † — The 401 path consumes no rate-limit budget

|                |                                                                                                                                                                                                                                                                |
|----------------|----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Security                                                                                                                                                                                                                                                       |
| **Status**     | **FIXED** — [juniper-data#266](https://github.com/pcalnon/juniper-data/pull/266); the juniper-cascor sibling `APD-CASCOR-004` † in [juniper-cascor#524](https://github.com/pcalnon/juniper-cascor/pull/524)                                                    |
| **Source**     | `juniper-data/juniper_data/api/middleware.py:110-150`                                                                                                                                                                                                          |
| **Primer**     | I.5 — line 1058 (mechanism only)                                                                                                                                                                                                                               |
| **Provenance** | **Register-original.** The primer describes the shared-package mechanism and says the gap "is now fixed (juniper-ml#1082)" (1058, 1344). It never mentions a juniper-data copy of `SecurityMiddleware`; `FailedAuthThrottle` appears zero times in the primer. |
| **Confidence** | High                                                                                                                                                                                                                                                           |

**Problem.** `SecurityMiddleware` calls `APIKeyAuth`, which *raises* on failure, before it ever reaches the rate limiter. A failed authentication therefore consumes no budget at all.

**Impact.** Credential guessing and garbage-credential floods are bounded only by how fast the process can reject them. The limiter protects the authenticated surface and leaves the authentication surface itself open.

**Why this is still open after `juniper-ml#1082`.** The fix landed in `juniper-service-core`. Both services import their **own copies**:

```text
juniper-cascor/src/api/app.py:20        from api.middleware import ... SecurityMiddleware
juniper-data/juniper_data/api/app.py:18 from .middleware import ... SecurityMiddleware
```

`grep -rn FailedAuthThrottle` across both repos returned **zero** hits (re-verified 2026-08-15, still zero). The shared fix was unreachable from either running service.

**Fix.** Port `FailedAuthThrottle` into both copies. Retiring the copies in favour of the shared middleware is the better end state but a much larger project — and read §2.2's closing paragraph first.

**Fixed as described** — ported, not consolidated, so §2.2's loop warning still governs any future consolidation.
Both PRs mirror service-core's optional fourth constructor parameter defaulting to an *enabled* throttle, so no `app.py` call site changed; no settings field was added, matching the shared package and juniper-recurrence.
Both halves are wired (`check()` before auth, `record_failure()` gated on a 401) — a `check()`-only port would be a throttle that never accumulates, and each fork carries an arm that fails if `record_failure` is dropped.
The `pre-auth-throttle` drift row moved `KNOWN_GAP` → `ENFORCED` in the same arc, with a second marker added because the original name-only marker would have gone green on a bare `import`.

---

### APD-DATA-002 — Request body limit is bypassable by a chunked request

|                |                                                                                                                    |
|----------------|--------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Security                                                                                                           |
| **Status**     | **FIXED** — [juniper-data#261](https://github.com/pcalnon/juniper-data/pull/261) (with `APD-DATA-036`, same patch) |
| **Source**     | `juniper-data/juniper_data/api/middleware.py:79-83`                                                                |
| **Primer**     | I.2 / I.4 — lines 757-759                                                                                          |
| **Confidence** | High                                                                                                               |

**Problem.** The entire check is `if content_length is not None and int(content_length) > self._max_bytes`. A chunked request that sends no `Content-Length` makes the first conjunct false, so the comparison never runs and nothing stream-caps the body.

**Impact.** The 10 MiB limit is advisory. Verified empirically in the primer: an 11 MiB chunked body was read in full and surfaced as a 422 JSON-decode error, never a 413.

**Evidence.** The shared implementation at `juniper-service-core/juniper_service_core/middleware.py:113-131` always stream-reads `POST`/`PUT`/`PATCH` against a cumulative cap, and its comment names this exact bypass as "the classic bypass". The fix never propagated. `APD-DATA-036` is a second, distinct defect on the same line.

---

### APD-DATA-003 — A blank API key enables authentication that accepts an empty key

|                |                                                                                                                                                                                                             |
|----------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Security                                                                                                                                                                                                    |
| **Status**     | **FIXED** — [juniper-data#267](https://github.com/pcalnon/juniper-data/pull/267); the juniper-cascor sibling `APD-CASCOR-006` † in [juniper-cascor#527](https://github.com/pcalnon/juniper-cascor/pull/527) |
| **Source**     | `juniper-data/juniper_data/api/security.py:54`; `juniper_data/api/settings.py:155-160`                                                                                                                      |
| **Primer**     | I.5 — lines 1041-1042                                                                                                                                                                                       |
| **Confidence** | High                                                                                                                                                                                                        |

**Problem.** `self._api_keys = list(dict.fromkeys(api_keys)) if api_keys else []` de-duplicates but does not filter blanks, and the settings validator is inconsistent: the comma-separated-string branch filters (`:159`), the list branch returns `v` untouched (`:160`).

**Impact.** `JUNIPER_DATA_API_KEYS='[""]'` parses to `['']`, sets `_enabled = True`, and validates an empty `X-API-Key`. This is strictly worse than authentication being off, because the deployment believes it is protected — the exact failure mode the ecosystem's boot-time `enforce_auth_posture` check exists to prevent, defeated after boot.

**Evidence.** `juniper-service-core/juniper_service_core/security.py:44` carries the filter with a comment naming this failure. Not propagated.

**Reachability caveat (register-derived).** The reference deployment defaults `JUNIPER_DATA_REQUIRE_AUTH` to `"true"` (`juniper-deploy/docker-compose.yml:163`). With that set, boot runs `enforce_auth_posture`, which resolves keys through `real_keys` (`juniper_service_core/auth_posture.py:59-69`, called at `:112`) — the same blank filter — so a blank key is a **boot failure**, not a silent open service.
Triggering this defect therefore needs *both* the JSON list form `'[""]'` (the string branch filters) *and* `require_auth=false`. Real, and narrower than it first reads.

**Fixed at the point of use, not by relying on the boot check** (which can be turned off).
Both forks now filter blanks / whitespace-only / non-string entries in `APIKeyAuth.__init__` before deriving `_enabled`, and both settings validators filter the list branch as well — closing the string-vs-list inconsistency that made the JSON form the reachable shape.
juniper-data keeps its `list` container (`hmac.compare_digest` per key, SEC-01/JD-SEC-02) rather than adopting service-core's `set`; cascor already used a set, so its line is byte-identical to the canonical one.
One ordering subtlety surfaced during the fix and is worth keeping: `dict.fromkeys` hashes every element, so the filter must run **before** the de-duplication or a malformed env value containing an unhashable entry raises `TypeError` — the first draft had it backwards and a new test caught it.
The `blank-api-key-filter` drift row moved `KNOWN_GAP` → `ENFORCED` with a two-marker predicate.
The prior bare `.strip()` marker was sufficient to *detect* this fix — neither fork's `security.py` contained a strip beforehand — but it was not specific to it: any unrelated strip later added to that module would have flipped the guard green with the filter still absent.
Pairing it with `isinstance(k, str)` ties the marker to the guard's shape rather than to an incidental call.

---

### APD-DATA-006 — A `GET` can silently undo a concurrent tag edit

|                |                                                                                                    |
|----------------|----------------------------------------------------------------------------------------------------|
| **Severity**   | Correctness                                                                                        |
| **Status**     | **FIXED** — [juniper-data#263](https://github.com/pcalnon/juniper-data/pull/263)                   |
| **Source**     | `juniper-data/juniper_data/storage/base.py:217-222`; `juniper_data/api/routes/datasets.py:785-794` |
| **Primer**     | II.6 — lines 4197-4200, 4275-4279                                                                  |
| **Confidence** | High                                                                                               |

**Problem.** Two writers of the same `DatasetMeta` document use asymmetric locking. `record_access` — fired on every metadata read and every artifact download — does read-modify-write of the whole document under `self._version_lock`. `update_dataset_tags` does its own read-modify-write across two `asyncio.to_thread` hops and takes **no lock at all**, so the lock protects nothing against it.

**Impact.** A plain `GET` can overwrite a concurrent tag edit with a pre-edit snapshot. Silent data loss triggered by a *safe* method, which is why it would never be suspected.

**Note on the existing rationale.** The docstring at `base.py:214-216` reasons about a race — but only for the access *counter*, calling it "informational so this is an acceptable trade-off". The code rewrites the entire document, so the trade-off analysis does not cover what it actually does.

---

### APD-SVCCORE-003 — Unvalidated settings lookup silently defaults security controls

|                |                                                                                                                                                                                                       |
|----------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Security                                                                                                                                                                                              |
| **Status**     | **FIXED** — ml#1154 (closes `APD-SVCCORE-010` in the same change)                                                                                                                                     |
| **Source**     | `juniper-service-core/juniper_service_core/websocket/control_stream.py:66-69` — 13 call sites across both `_setting` copies (9 in `control_stream.py`, 4 in `training_stream.py:193-194`, `:210-211`) |
| **Primer**     | III.7 — lines 7950-7951, 7962, 7964-7968                                                                                                                                                              |
| **Confidence** | High                                                                                                                                                                                                  |

**Problem.** `_setting` is `getattr(settings, name, default)` guarded by `getattr(app.state, "settings", None)` — both defaulted. A misspelled or absent settings field is indistinguishable from an unconfigured one. `SettingsBase` declares only four fields; eleven distinct tunables are read this way, entirely unvalidated.

**The primer frames this as a deliberate trade, and that framing belongs here.** At 7950-7951 it calls it "the most interesting trade here", one that "deliberately gives up validation", and at 7964-7965 it names the benefit plainly: "genuine decoupling: the shared package never imports a consuming service's settings class, and each service declares only the tunables it uses." That benefit is real. This entry argues the trade is mispriced for the subset below, not that the design is thoughtless.

**Impact (register-derived).** Several of those tunables are **security controls**: `ws_control_allowed_origins` (the fail-closed Origin allowlist, `control_stream.py:114`), `disable_ws_control_endpoint` (`:99`), `ws_control_rate_limit_per_sec` (`:246`), and the three handshake-cooldown parameters (`:89-91`). Writing `ws_control_rate_limit_per_second` where the library reads `..._per_sec` reverts the control WebSocket to library defaults — silently, forever, with no log line and no test able to see it.

The primer names only that one rate-limit typo pair (7967-7968) and gives an aggregate count without a list; enumerating *which* of the eleven are security controls is this register's analysis. It is what turns a maintainability trade into a security entry.

**Fix (ml#1154) — the trade is kept, the cost is removed.**
`juniper_service_core/websocket/tunables.py` declares all eleven tunables with their defaults and a `security` flag, and both handlers now resolve through it.
Nothing imports a consuming service's settings class, so the decoupling the primer praises is intact.
Two things change: the default lives in the registry rather than at each of the 13 call sites (so the set is *declared* and therefore auditable at all), and a miss that looks like a misspelling is logged at WARNING naming both spellings — the `..._per_second` / `..._per_sec` case the primer names now produces a log line instead of silence.
`audit(settings)` is the boot-time counterpart, reporting which security controls are running on library defaults and which look like typos.
A `resolve()` call for an undeclared name raises rather than defaulting, so a library-side typo cannot reach production either.
This also closes `APD-SVCCORE-010`: the byte-identical `_setting` bodies are replaced by one shared resolver, with the two thin module-local wrappers delegating to it.

**Still latent, and the consolidation warning is now weaker but not void.**
`juniper_service_core.websocket` still has no production consumer, so this remains a latent library defect (§4.2 preamble).
What changed is §2.2's loop: adopting the shared middleware no longer silently reverts a misspelled security control, because the misspelling is now loud.
cascor's hard-attribute reads are still *stricter* for the six security controls (an `AttributeError` beats a WARNING), so consolidation still trades strictness for reach — it is now a judgement call rather than a straight regression.

---

### APD-SVCCORE-001 — Worker binary-frame cap has no cumulative limit across an attachment list

|                |                                                                                                                |
|----------------|----------------------------------------------------------------------------------------------------------------|
| **Severity**   | Robustness                                                                                                     |
| **Status**     | **FIXED** — [juniper-ml#1446](https://github.com/pcalnon/juniper-ml/pull/1446), 2026-08-28                     |
| **Source**     | `juniper-service-core/juniper_service_core/websocket/worker_stream.py:323` (receive), `:329` (per-frame check) |
| **Primer**     | I.4 — lines 754-755                                                                                            |
| **Confidence** | High                                                                                                           |

**Problem.** `len(raw_bytes) > _MAX_BINARY_SIZE` (100 MB) is evaluated *after* `await websocket.receive()` has fully materialised the frame. More importantly, the cap is **per-frame with no cumulative accumulator**: `_handle_task_result` loops over `attachment_names` and receives one frame each (`:322-333`), so a single result submission permits `len(attachment_names) × cap`.

**Impact.** An authenticated worker can force repeated large allocations across one logical submission. Requires an already-registered, already-authenticated worker — the `/ws/workers` handshake is fail-closed (4001 on a bad key, 4008 on a shape-invalid registration), so this is not an unauthenticated vector.

**Severity caveat — the 100 MB constant is effectively unreachable.** uvicorn's `ws_max_size` defaults to **16 MiB** (`uvicorn.config.Config`, verified 0.46.0) and nothing in `juniper-service-core`, `juniper-cascor` or `juniper-data` overrides it. A frame larger than 16 MiB is rejected by the protocol layer before `receive()` returns, so `_MAX_BINARY_SIZE` can never fire and the maximum forced allocation per frame is 16 MiB, not 100 MB.
That is what moves this from Security to Robustness: the real defect is the missing cumulative cap, plus a dead constant that reads as protection it does not provide.

**The shared package is the *weaker* copy — recorded 2026-08-28.** `juniper-cascor`'s fork bounds the loop's cardinality: `_MAX_TENSOR_MANIFEST_ENTRIES = 32` (`src/api/websocket/worker_stream.py:33`, enforced `:391`, mirroring `src/api/workers/protocol.py:64`), whose own comment names this failure mode — *"so a malformed/hostile result cannot force the worker handler into an unbounded binary-receive loop"*. `juniper-service-core` has **no cardinality cap anywhere**, and no `workers/protocol.py` at all; `N` is whatever the consumer-injected `WorkerTaskProtocol.result_attachments()` returns. So cascor's worst case is bounded at `32 × 16 MiB` while the shared package's is unbounded `N × 16 MiB`.
This inverts the usual sibling reading — the fork is not the copy that lags. **Acted on in ml#1446**: cascor's manifest cap was ported rather than a new number invented, and the aggregate byte budget cascor *also* lacks was added alongside it. `JsonTaskProtocol` (the `APD-SVCCORE-015` default, ml#1441) sidesteps the row entirely by declaring no attachments, so adopting it was never a fix for consumers that need binary frames.

**Fix (ml#1446).** Two independent bounds, because the defect has two halves: `_MAX_ATTACHMENTS = 32` (cardinality, checked **before the first `receive()`**) and `_MAX_TOTAL_BINARY_SIZE` (aggregate bytes per submission). The aggregate is set equal to the per-frame cap by a stated principle rather than an invented magnitude — *one submission may not deliver more bytes than a single frame was already permitted to carry* — and the two are kept as **independent policies that merely coincide today**, pinned by a structural arm, so raising one cannot silently raise the other. The severity caveat above is unchanged and still governs: uvicorn's 16 MiB `ws_max_size` means the reachable ceiling was `32 × 16 MiB`; what this closes is the unbounded-`N` half.

---

### APD-OBS-001 — `X-Request-ID` is propagated unvalidated

|                |                                                                                                  |
|----------------|--------------------------------------------------------------------------------------------------|
| **Severity**   | Maintainability                                                                                  |
| **Status**     | **FIXED** — ml#1156                                                                              |
| **Source**     | `juniper-observability/juniper_observability/middleware/request_id.py:36` (ingest), `:40` (echo) |
| **Primer**     | I.10 — lines 1109, 2101-2103                                                                     |
| **Confidence** | Medium                                                                                           |

**Problem.** The inbound header is accepted verbatim — no length cap, no character allowlist, no shape validation — stored in a process-wide `ContextVar`, embedded in every log record, and echoed back on the response.

**Severity caveat — both "unmitigated" items are in fact contained.** The register's earlier Security rating rested on two claims that do not survive verification:

- **Length.** h11's `max_incomplete_event_size` defaults to **16384** bytes and caps the *entire* request head, and uvicorn leaves `h11_max_incomplete_event_size` at `None` (verified h11 0.16.0 / uvicorn 0.46.0). A multi-megabyte header value is rejected at the protocol layer and never reaches ASGI, so the "amplified into every log record" scenario cannot occur through HTTP.
- **Injection.** h11 rejects CR/LF in header values outright — `h11.Connection.send` with a value containing `\r\n` raises `LocalProtocolError: Illegal header value`. The response echo at `:40` is therefore not a header-injection vector either.

**What remains, and why it is still worth fixing.** An unvalidated external value flows into a `ContextVar` that any consumer may write to a line-oriented sink, and the containment above is entirely incidental — it depends on the h11 defaults and the JSON formatter's `json.dumps` escaping, neither of which this package controls or asserts.
Restate the work item as: **apply a length cap and a character allowlist on ingress, before that containment is assumed.** The house helper for exactly this class already exists — `_sanitize_for_log` in `juniper-service-core/juniper_service_core/websocket/control_security.py:29` — and is simply not applied here.

**Fixed (ml#1156) — as restated, on ingress.**
`is_valid_request_id` applies a 128-character cap and an allowlist (`[A-Za-z0-9._:-]`, anchored at both ends), and an inbound value that fails either is **replaced with a fresh UUID4** rather than sanitized: stripping characters would propagate an ID the client never sent, correlating to nothing on either side, whereas a fresh UUID is at least honestly this server's own.
Rejection is deliberately **silent** — logging every rejected header would hand an attacker the log-flood lever this guard exists to bound.
The allowlist was chosen to keep real correlation IDs working (UUID, ULID, W3C `traceparent`, `service:id`), all pinned by tests.
Verified by mutation: making the validator return `True` fails 7 arms.
The containment the caveat above describes is now asserted locally instead of borrowed from h11's defaults.

**Provenance (register-derived).** Two claims in the earlier text were not the primer's: that `juniper-data-client` propagates the value outbound across service hops (the primer's client-side references read `X-Request-ID` *off responses*, the opposite direction), and that `_sanitize_for_log` exists and is unapplied (`_sanitize_for_log` appears zero times in the primer). Both are verified against source; neither is an extraction.

---

### APD-CASCOR-001a — Middleware ordering comment is wrong

|                |                                                                                           |
|----------------|-------------------------------------------------------------------------------------------|
| **Severity**   | Maintainability                                                                           |
| **Status**     | **FIXED** — [juniper-cascor#540](https://github.com/pcalnon/juniper-cascor/pull/540)      |
| **Source**     | `juniper-cascor/src/api/app.py:644-646` (the comment); `:621` (CORS), `:630` (body limit) |
| **Primer**     | Appendix A Q57 — lines 9592-9594                                                          |
| **Confidence** | High                                                                                      |

**Problem.** The in-code comment states the execution order as `RequestId → Prometheus → Security → SecurityHeaders → CORS`, omitting `RequestBodyLimitMiddleware` (`:630`), which under Starlette's LIFO `add_middleware` actually runs between `SecurityHeaders` and CORS.

**Impact.** A wrong ordering comment sitting beside security-critical middleware is worse than none — it is the artefact the next maintainer will trust when reordering.

**Fix.** A one-line comment edit. Split from `APD-CASCOR-001b` precisely because the fixes are disjoint: this one is a comment, that one changes request handling and needs tests.

**Fixed together after all.** The split was sound as analysis but not as sequencing: `001b`'s fix *is* a reorder, and a reorder cannot land beside an ordering comment that the reorder has just made doubly wrong. Rewriting the comment was forced by the code change, so both closed in cascor#540. The same wrong comment existed verbatim in `juniper-data` — a sibling the register never recorded, because `APD-CASCOR-001a` was filed as cascor-only — and was corrected in data#273.

---

### APD-CASCOR-001b — CORS sits behind auth, so preflights are answered 401

|                |                                                                                                                                                        |
|----------------|--------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Correctness                                                                                                                                            |
| **Status**     | **FIXED** — [juniper-cascor#540](https://github.com/pcalnon/juniper-cascor/pull/540); the juniper-data sibling `APD-DATA-035` † in [juniper-data#273](https://github.com/pcalnon/juniper-data/pull/273) |
| **Source**     | `juniper-cascor/src/api/app.py:621` (CORS registered first → innermost), `:641` (`SecurityMiddleware`); `src/api/middleware.py:189-198` (`_is_exempt`) |
| **Primer**     | Appendix A Q57 — lines 9592-9594                                                                                                                       |
| **Confidence** | High                                                                                                                                                   |

**Problem.** Because CORS is registered first it executes *innermost*, so `SecurityMiddleware` sees cross-origin preflights before CORS does — and `_is_exempt` keys on path only (`return path in EXEMPT_PATHS`, `middleware.py:198`), with no `OPTIONS` bypass.

**Impact.** A preflight to any non-exempt `/v1/*` path is answered 401 rather than with CORS headers, so browser clients on a configured origin cannot preflight authenticated endpoints — preflights carry no `X-API-Key` by specification.

**Fix.** Either an `OPTIONS` bypass in `_is_exempt` or a middleware reorder. Both change request handling and need tests, which is why this is tracked separately from the comment fix. `APD-DATA-035` † is the same defect in `juniper-data`.

**Fixed by the reorder, deliberately.** The two options are not equivalent. An `OPTIONS` bypass in `_is_exempt` exempts *every* `OPTIONS` request from auth **and** rate limiting; `CORSMiddleware` short-circuits only a genuine preflight — one carrying `Access-Control-Request-Method` — so a plain `OPTIONS` request still authenticates. Both forks now pin that distinction as a test in its own right, so the narrower surface cannot regress silently.

**A second symptom, not recorded in the original entry:** with CORS innermost, an ordinary cross-origin request rejected by auth also returned **no** CORS headers, so a browser reported an opaque CORS failure instead of surfacing the 401. Running CORS outermost fixes both, which is likely why this was hard to diagnose from the client side. Verified before and after against the real app factories; mutation-tested (4 of 5 new arms fail against the unfixed ordering in each fork).

---

### APD-CASCOR-002 — `ValueError` handler reclassifies server faults as client errors

|                |                                                                                                                                                                                                       |
|----------------|-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Correctness                                                                                                                                                                                           |
| **Status**     | **FIXED** — [juniper-cascor#516](https://github.com/pcalnon/juniper-cascor/pull/516); the juniper-data sibling `APD-DATA-034` in [juniper-data#262](https://github.com/pcalnon/juniper-data/pull/262) |
| **Source**     | `juniper-cascor/src/api/app.py:678-684`; mechanism recorded at `src/api/models/common.py:83-94`                                                                                                       |
| **Primer**     | Appendix A Q58 — lines 9596-9598                                                                                                                                                                      |
| **Confidence** | High                                                                                                                                                                                                  |

**Problem.** A blanket `@app.exception_handler(ValueError)` returns `400 VALIDATION_ERROR`. `pydantic_core.PydanticSerializationError` subclasses `ValueError`, so a *server-side serialisation fault* is returned to the client as a 400 with a stripped detail.

**Impact.** Server defects are invisible to 5xx alerting and misattributed to the caller, and the generic `"Invalid request parameters"` destroys the diagnostic. The narrow workaround (`coerce_native_scalars`) is applied only inside `success_response`, so any other `ValueError`-subclass server fault is still misclassified. The failure mode is survivable only because a helper was written specifically to dodge it. `APD-DATA-034` † is the same handler in `juniper-data`, which has no equivalent workaround.

---

### APD-CCLIENT-001 — Non-idempotent methods are auto-retried with no idempotency key

|                |                                                                                             |
|----------------|---------------------------------------------------------------------------------------------|
| **Severity**   | Correctness                                                                                 |
| **Status**     | **FIXED** — [juniper-cascor-client#143](https://github.com/pcalnon/juniper-cascor-client/pull/143) |
| **Source**     | `juniper-cascor-client/juniper_cascor_client/constants.py:37`; applied at `client.py:89-95` |
| **Primer**     | I.7 — lines 296, 355, 1574-1590                                                             |
| **Confidence** | High                                                                                        |

**Problem.** `RETRY_ALLOWED_METHODS = ["GET", "POST", "DELETE", "PUT", "PATCH"]` is handed to urllib3's `Retry`, so it transparently replays non-idempotent mutations on any of `[429, 502, 503, 504]`. There is no idempotency key anywhere in the stack (`APD-ECO-001`).

**Impact.** The contract violation is unambiguous: the client replays methods RFC 9110 §9.2.2 defines as non-idempotent, inside the HTTP adapter, where the caller never learns it happened. What that costs varies per call site, and the earlier framing of this entry overstated it:

- **The genuinely duplicating call site is `save_snapshot` (`client.py:295`).** A transient 502 on `POST /v1/snapshots` produces a **duplicate snapshot row** — a real, silent, unreported side effect with no server-side guard.
- **The lifecycle mutations are accidentally protected.** A replayed `POST /v1/training/start` does **not** start a second training run: cascor 409s when the FSM is not startable (`juniper-cascor/src/api/routes/training.py:117`), and `POST /v1/network` 409s likewise (`routes/network.py:31`). 409 is **not** in `RETRYABLE_STATUS_CODES`, so the replay surfaces to the caller as `JuniperCascorConflictError` — confusing, but not duplicating.

**That mitigation is accidental, not designed.** It is a property of cascor's lifecycle FSM rejecting a second start, not of any idempotency contract. It covers exactly the endpoints whose server happens to hold conflicting state, and covers `save_snapshot` not at all. Any new mutating endpoint without an FSM guard inherits the unmitigated behaviour.

**Evidence.** Both siblings carry the fix — `juniper-data-client` is `["HEAD","GET","PUT"]` with RFC 9110 §9.2.2 cited in-comment (`constants.py:59-67`), `juniper-recurrence-client` is `["HEAD","GET"]` (`constants.py:54`). cascor-client received neither. Its own `tests/test_retry_policy.py` pins the *status* forcelist and asserts nothing about the method allow-list.

---

### APD-DATA-004 — Batch-create leaks raw exception detail beside a branch that redacts it

|                |                                                                          |
|----------------|--------------------------------------------------------------------------|
| **Severity**   | Security                                                                 |
| **Status**     | **FIXED** — [juniper-data#275](https://github.com/pcalnon/juniper-data/pull/275) |
| **Source**     | `juniper-data/juniper_data/api/routes/datasets.py:423-431` vs `:433-447` |
| **Primer**     | II.8 — lines 4749-4758                                                   |
| **Confidence** | High                                                                     |

**Problem.** Two `except` blocks apply opposite disclosure policies. The `HTTPException` branch at `:423` copies `e.detail` into the per-item `error` field verbatim; the sibling `except Exception` branch **ten lines below it, at `:433`**, mints a 12-hex correlation id and returns only that, with an ERR-08 comment (`:434-437`) explaining why raw strings "can leak filesystem paths or internal type details".

**Impact.** The 501 detail at `:165-168` interpolates a raw `ImportError` message — which routinely carries a module or filesystem path — into an `HTTPException`. Batch-create's `HTTPException` branch then copies that verbatim into an API response body, defeating the ERR-08 control that sits ten lines below it in the same function.

**Correction, established while fixing it (register-original).** This entry names the amplifier, not the source, and its scope is too narrow in one respect and too wide in another.

- **Too narrow: the leak was never batch-specific.** `create_dataset` raises the `HTTPException`, so single-create returns the same detail to the caller *directly*. Reproduced on both paths. Batch-create doubles the exposure; it does not create it.
- **Too wide: the curated raises never leaked.** All four optional-dependency generators raise a hand-written message carrying an install hint (`pip install datasets`, `pip install "juniper-data[equities]"`). Those are exactly what D1 (I-5) added the 501 to surface. What leaks is an `ImportError` raised *beneath* the availability guard — a broken native extension, a partial install, a failed lazy import inside the third-party package — and those are the path-carrying ones.

**Fixed at the source, not at the amplifier.** Auditing every `detail=` in the module, exactly two interpolate exception text: the 400 `"Invalid parameters: {e}"` (a pydantic error describing the caller's own input — legitimate and unchanged) and this 501. With the 501 curated, batch-create copying `e.detail` is sound, so the `except HTTPException` branch is deliberately left alone; redacting there would have destroyed genuinely client-facing details and made batch-create diverge from single-create. The 501 now echoes the exception only when the generator's `is_available()` reports the dependency missing; otherwise it logs server-side and returns a correlation id.

---

### APD-CASCOR-005 — API-key comparison short-circuits on match in two of three copies

|                |                                                                                                                                                                 |
|----------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------|
| **Severity**   | Maintainability                                                                                                                                                 |
| **Status**     | OPEN                                                                                                                                                            |
| **Confidence** | **Low — see the assessment**                                                                                                                                    |
| **Source**     | `juniper-cascor/src/api/security.py:53`; `juniper-service-core/juniper_service_core/security.py:65`; contrast `juniper-data/juniper_data/api/security.py:80-84` |
| **Primer**     | I.5 — lines 1027-1029 (the analysis); Appendix A Q26 — line 9463 (the contradicting verdict)                                                                    |

**Problem.** `juniper-data` iterates the full key list without breaking, with a comment explaining why. cascor and service-core use `any(hmac.compare_digest(api_key, k) for k in self._api_keys)`, which short-circuits.

**Assessment — this is recorded, not escalated, and it is not a Security entry.** The short-circuit occurs only on a **successful** match. Every non-matching guess still costs all N comparisons, so there is no timing gradient for an attacker to climb; what leaks is the matched key's *index* in the server's list, to a caller who already holds that key. That is weak information disclosure, not a credential leak, and `compare_digest` keeps each individual comparison constant-time.

**The primer reaches the same conclusion and says so explicitly.** At line 1029: "So service-core's form is defensible and juniper-data's is belt-and-braces… A reviewer who flags `any()` here without that analysis is pattern-matching." Under §1's inclusion criterion, a design choice the primer explicitly endorses is out of scope as a security finding — which is why the severity is `M`, not `S`.

**What is actionable is the divergence, plus a contradiction inside the primer itself.** One repo carries a documented invariant that two copies silently dropped; unify the three and decide deliberately which behaviour is intended.
Separately, the primer's own appendix contradicts its body: Q26 at 9463 states that "iterating a list of keys and short-circuiting on the first match reintroduces the leak across the *set*, so the loop must not break early", and credits "the Juniper code" with getting this right — a description true of `juniper-data` and false of the other two.
Both passages cannot stand; that is a defect in the primer, noted here so a reader citing either one knows the other exists.

---

### APD-ECO-001 — No idempotency-key mechanism exists anywhere in the stack

|                |                                   |
|----------------|-----------------------------------|
| **Severity**   | Robustness                        |
| **Status**     | OPEN                              |
| **Source**     | Ecosystem-wide; no implementation |
| **Primer**     | I.7 — lines 1592, 1660            |
| **Confidence** | High                              |

**Problem.** `grep -rn "Idempotency-Key"` across all three clients, `juniper-data`, and `juniper-service-core` returns **zero** hits. Not one occurrence anywhere — no header constant, no handler, no test, no comment naming the header.
The only acknowledgements that the concept exists at all are two prose references in `juniper-data-client`: the explanatory comment at `juniper_data_client/constants.py:65` ("their own idempotency layer") and the module docstring at `juniper-data-client/tests/test_retry_policy.py:7` ("mutations must layer their own idempotency on top").

That is a stronger finding than a single stray hit would have been: the one package that *reasoned* about idempotency concluded the caller must supply it, and no caller does.

**Impact.** `juniper-data` gets idempotent dataset creation only incidentally, via content-addressed IDs. This was the enabling condition for `APD-CCLIENT-001`: without a key, retrying a mutation cannot be made safe.

> **Updated 2026-08-28 — the impact narrowed, and the entry is still open.** This read "Every
> non-idempotent `POST` in the stack duplicates on replay", which is **no longer true**: with
> `APD-CCLIENT-001` closed (cclient#143), all three clients now restrict auto-retry to idempotent
> methods, so no adapter replays a `POST` any more. What remains is the actual requirement `-001`
> could only work around, not satisfy: **a caller that needs a mutation retried still cannot make
> that safe.** The clients' answer is to refuse to retry, which is correct and is not the same as
> being able to. Note the direction of the dependency, because the round-27/28 lineage had it
> backwards and used it to park `APD-CCLIENT-001` as blocked on this entry. The prerequisite
> only ever ran one way: `APD-CCLIENT-001` is *restricting an unsafe retry*, which needs nothing from
> this entry; `APD-ECO-001` — this entry — is *enabling a safe one*, which is what needs the key.

---

## 4. Full register

`Sev` — **S**ecurity, **C**orrectness, **R**obustness, **M**aintainability, **E**rgonomics. `Conf` — confidence. All statuses OPEN unless stated. `Primer` gives the line in the primer asserting the claim; `Source` gives the current `file:line`. A `†` after an ID marks a **register-original** entry (see §1).

### 4.1 `juniper-data`

| ID             | Finding                                                                                                                                | Sev | Source                                                                   | Primer                | Conf |
|----------------|----------------------------------------------------------------------------------------------------------------------------------------|-----|--------------------------------------------------------------------------|-----------------------|------|
| APD-DATA-001 † | **FIXED (#266)** — 401 path unthrottled — the `#1082` fix never reached this copy                                                      | S   | `api/middleware.py:110-150`                                              | 1058 (mechanism)      | High |
| APD-DATA-002   | **FIXED (#261)** — Body limit bypassable by chunked request with no `Content-Length`                                                   | S   | `api/middleware.py:79-83`                                                | 757                   | High |
| APD-DATA-003   | **FIXED (#267)** — Blank API key enables auth that accepts an empty key                                                                | S   | `api/security.py:54`, `api/settings.py:155-160`                          | 1041                  | High |
| APD-DATA-004   | **FIXED (#275)** — Batch-create echoes raw `e.detail` beside a redacting branch                                                        | S   | `api/routes/datasets.py:423-431` vs `:433-447`                           | 4749                  | High |
| APD-DATA-005   | **FIXED (data#295)** — `APIKeyHeader` declared but never wired — no `securitySchemes` in OpenAPI | M   | `api/security.py:29`                                                     | 1062, 5199            | High |
| APD-DATA-006   | **FIXED (#263)** — `record_access` lock asymmetry — a `GET` can undo a tag edit                                                        | C   | `storage/base.py:217-222`                                                | 4197                  | High |
| APD-DATA-007   | **FIXED (data#282)** — Tag update is read-modify-write with no CAS or version check                                                                           | C   | `api/routes/datasets.py:785-794`, `storage/local_fs.py:262-298`          | 4171                  | High |
| APD-DATA-008   | Cache hit returns 201 — status line cannot signal "already existed"                                                                    | E   | `api/routes/datasets.py:71`, `:120-139`                                  | 3836                  | High |
| APD-DATA-009   | **FIXED (data#280)** — Batch-create returns 201 even when every item failed                                                                                   | C   | `api/routes/datasets.py:377`                                             | 4785                  | High |
| APD-DATA-010   | **FIXED (data#284)** — Batch export silently skips datasets deleted mid-stream                                                                                | C   | `api/routes/datasets.py:570-572`                                         | 4087                  | High |
| APD-DATA-011   | **FIXED (data#283)** — Offset pagination drifts — skips and duplicates under concurrent writes                                                                | C   | `storage/local_fs.py:253-255`, `storage/base.py:376-378`                 | 4412                  | High |
| APD-DATA-012   | **FIXED (data#283)** — `/filter` tie order is non-deterministic (unsorted glob feeds a stable sort)                                                           | C   | `storage/base.py:376`, `storage/local_fs.py:300-312`                     | 4470                  | High |
| APD-DATA-013   | **FIXED (data#281, ownership half — shape unification deferred to `-026`--`-033`)** — Two incompatible `detail` shapes — no `RequestValidationError` handler                                                                 | C   | `api/app.py:152-166`                                                     | 4728                  | High |
| APD-DATA-014   | **FIXED (data#281)** — 400 vs 422 split falls out of exception-subclass MRO, not design                                                                       | C   | `api/routes/datasets.py:106-112`                                         | 3910, 4742-4747       | High |
| APD-DATA-015   | **FIXED (data#288)** — Route ordering is load-bearing and unguarded (`/{dataset_id}` catch-all)                                        | R   | `api/routes/datasets.py:276`, `:338`, `:604`, `:628`, `:651`             | 3455                  | High |
| APD-DATA-016   | **FIXED ([data#313](https://github.com/pcalnon/juniper-data/pull/313))** — `download_artifact` is named streaming but fully materialises the body                                                                 | R   | `api/routes/datasets.py:693-704` (dead; the route is now `:837-878`)                                         | 3999                  | High |
| APD-DATA-017   | No `ETag`/`Cache-Control`/conditional requests despite a stored SHA-256                                                                | R   | `api/routes/datasets.py:700-704`; validator at `core/artifacts.py:50-63` | 3647                  | High |
| APD-DATA-018   | **FIXED (data#326 / data#354)** — No async job pattern — generation runs inside the request                                                                            | R   | `api/routes/datasets.py:150` (dead; generation is `:175`, via `to_thread`)                                             | 3853                  | High |
| APD-DATA-019   | Every page does full-population work; exact `total` recomputed per page                                                                | R   | `storage/base.py:462` (`filter_datasets`); `total = len(filtered)` is `:537`, cursor path `:545-548` — re-read 2026-09-03 | 4390                  | High |
| APD-DATA-020   | **FIXED (data#289)** — `/v1` is a repeated literal in five places; no `API_VERSION` constant                                           | M   | `api/app.py:140-142`, `api/routes/datasets.py:138`, `:253`               | 3278                  | High |
| APD-DATA-021   | **FIXED (data#290)** — `DatasetListFilter` declared and never used; route re-declares 12 params                                        | M   | `core/models.py:132-144` vs `api/routes/datasets.py:278-289`             | 4538                  | High |
| APD-DATA-022   | No `responses={}` anywhere — entire error surface absent from OpenAPI                                                                  | M   | all route decorators                                                     | 5189                  | High |
| APD-DATA-023   | **FIXED (data#292)** — No `operation_id=` — a handler rename renames every generated SDK method                                        | M   | all route decorators                                                     | 5157                  | High |
| APD-DATA-024   | **FIXED (data#295)** — Securing the service deletes `/openapi.json`, not just the explorers    | M   | `api/app.py:95`, `:101-103`                                              | 1065, 5207            | High |
| APD-DATA-025   | **FIXED (data#293)** — `application/octet-stream` on one binary route, `application/zip` on the other                                  | M   | `api/routes/datasets.py:702` vs `:584`                                   | 4017                  | Med  |
| APD-DATA-026   | Two list endpoints, same params, incompatible shapes                                                                                   | E   | `api/routes/datasets.py:257-273` vs `:276-335`                           | 4535                  | High |
| APD-DATA-027   | No `Link` header or pagination links anywhere                                                                                          | E   | `api/routes/datasets.py:276-335`                                         | 5002                  | High |
| APD-DATA-028   | `/versions` and `/latest` smuggle identity into a mandatory query param                                                                | E   | `api/routes/datasets.py:606`, `:630`                                     | 3408                  | Low  |
| APD-DATA-029   | Two URIs return the same representation with no `Content-Location`                                                                     | E   | `api/routes/datasets.py:628`, `:651`                                     | 3399                  | Low  |
| APD-DATA-030   | Error bodies carry no retryability signal                                                                                              | E   | `api/app.py:152-166`                                                     | 3316                  | Low  |
| APD-DATA-031   | No RFC 9457 problem details; three independent error sources                                                                           | E   | `api/app.py`, `api/middleware.py`, routes                                | 4718                  | Low  |
| APD-DATA-032   | Access counters live in the representation, blocking a strong metadata `ETag`                                                          | E   | `core/models.py:84-85`, `api/routes/datasets.py:672`                     | 4247                  | Low  |
| APD-DATA-033   | **FIXED ([juniper-data#297](https://github.com/pcalnon/juniper-data/pull/297))** — Rate-limit window is the one knob of three an operator cannot set | E   | `api/app.py:123-126`, `api/settings.py:164-165`                          | 1380                  | Low  |
| APD-DATA-034 † | **FIXED (#262)** — Blanket `ValueError` handler reports server faults as `400` — and no `coerce_native_scalars` equivalent exists here | C   | `api/app.py:152-158`                                                     | 9596 (cascor sibling) | High |
| APD-DATA-035 † | **FIXED (#273)** — CORS registered innermost → `SecurityMiddleware` 401s preflights; path-only `_is_exempt`                            | C   | `api/app.py:104-138`, `api/middleware.py:152-160`                        | 9592 (cascor sibling) | High |
| APD-DATA-036   | **FIXED (#261)** — Unguarded `int(content_length)` — `Content-Length: abc` is a 500, not a 400                                         | R   | `api/middleware.py:81`                                                   | 758                   | High |

**`APD-DATA-005` cross-reference** *(describes the pre-fix state; both rows are now FIXED — see the RESOLVED block below)*. `APD-DATA-024` nullified it further: because `openapi_url` **was** `None` whenever any API key was configured (`api/app.py:95` set `docs_enabled = not settings.api_keys`; `:101-103` gated `docs_url` / `redoc_url` / `openapi_url` on it together), a secured deployment served **no OpenAPI document at all** — so the missing `securitySchemes` block was unobservable exactly where it would matter. The severity is `M`, not `S`: there is no attacker and no gain, only an absent schema stanza. `api_key_header` was instantiated at `api/security.py:29` and referenced nowhere else in the repository.

> **Anchor note.** These `app.py` line numbers are **historical and no longer resolve** — they are kept because they locate the defect in the code as filed. data#295 renamed `docs_enabled` → `explorers_enabled` (now `app.py:106`), moved the three kwargs to `:112-114`, and **stopped gating `openapi_url` at all** — it is mounted unconditionally and protected by `SecurityMiddleware` instead. Live `:95` and `:101-103` are now comment lines inside the explanatory block. The anchors were refreshed on 2026-08-26 at 21:25Z from `app.py:91`/`:99`, and invalidated 44 minutes later by data#295 merging at 22:09Z — *the same session's own merge*. **Refresh anchors after the fix lands, not before**: a refresh performed while a fix PR is open is stale on arrival, and reads as current precisely because it is dated.

**RESOLVED 2026-08-26 — the owner chose (a), "serve it behind the key"; both rows closed in data#295.** The decision had three options, not the two it was originally framed with, and the difference is the whole point. `EXEMPT_PATHS` (`api/constants.py`) **already listed `/docs`, `/openapi.json` and `/redoc`**, and `SecurityMiddleware._is_exempt()` is a bare `path in EXEMPT_PATHS` evaluated regardless of whether a key was supplied — inert only because `openapi_url` was `None`. So:

| Option | What it means | Outcome |
|---|---|---|
| **(a) Serve behind the key** | Mount the document **and** remove the three paths from `EXEMPT_PATHS`, **and** wire `api_key_header` so `securitySchemes` + a `security` requirement appear | **CHOSEN** — closes `-005` and `-024` together |
| **(b) Mount but leave exempt** | Re-enable `openapi_url` alone | **The trap.** Serves the document to *unauthenticated* callers while looking exactly like (a) |
| **(c) Leave as built** | Keep deleting the document under auth | Not chosen; `-005` would have been a no-op |

**The transferable finding is that (a) and (b) are one keystroke apart and indistinguishable at a glance** — re-enabling `openapi_url` without touching `EXEMPT_PATHS` *is* (b). The fix therefore pins the exemption's absence, not just the document's presence, and the mutation matrix carries an arm (M1) that restores `/openapi.json` to the exempt set with everything else fixed: without a failing arm there, the pins would certify an open endpoint as secured.

Two decisions recorded with it: the interactive explorers (`/docs`, `/redoc`) stay **unmounted** whenever keys are configured, because Swagger UI and ReDoc fetch `/openapi.json` by XHR with no `X-API-Key` header and could only 401; and the scheme is declared **per protected router rather than app-wide**, so the exempt health probes are not documented as requiring a key they do not need.

**`APD-DATA-033` restated.** `DEFAULT_RATE_LIMIT_WINDOW_SECONDS` is **not** an unwired constant — it is load-bearing at `api/security.py:128` (constructor default), `:139` (`self._window`), `:147` (cache TTL) and `:163-165` (the `window` property).
The defect is narrower and real: `RateLimiter` is constructed at `api/app.py:123-126` passing only `requests_per_minute` and `enabled`, and `Settings` declares only `rate_limit_enabled` / `rate_limit_requests_per_minute` (`api/settings.py:164-165`). The window is the one knob of three with no operator-facing setting.

**`APD-DATA-036` mechanism.** `RequestBodyLimitMiddleware` is a `BaseHTTPMiddleware`, so a `ValueError` raised in its `dispatch` propagates *outside* `ExceptionMiddleware` and never reaches the app's `@app.exception_handler(ValueError)` at `api/app.py:152`; it lands on the `Exception` handler at `:160-166`, a 500. Both siblings return an explicit 400 instead — `juniper-cascor/src/api/middleware.py:96-97`, `juniper-service-core/juniper_service_core/middleware.py:107-110`.

**`APD-DATA-008` / `-017` / `-022` routing.** These three sit with the deferred `-026`–`-033` REST-shape group by owner routing, not by their own markers: a status line that cannot say "already existed" (`-008`), conditional requests over the stored SHA-256 (`-017`) and the `responses={}` error surface (`-022`) are each a wire-contract decision that belongs to the same REST-redesign call that `APD-DATA-013`'s marker defers the shape unification to. The grouping was carried by the defect-register handoff lineage alone (`prompts/thread-handoff_automated-prompts/HANDOFF_2026-08-25_defect-register-round-25-client-tier-sweep.md` and its successors) until it was recorded here on 2026-08-26. **Treat the remaining ten as one owner decision; do not action any of them unilaterally.**

> **`APD-DATA-033` was separated from this group on 2026-08-28, with the owner's explicit approval,
> and is now FIXED (juniper-data#297). It was in the group by ID adjacency, not by kind.** Every
> other member is a wire-contract or representation decision that belongs to the REST-redesign call:
> a status line that cannot say "already existed" (`-008`), conditional requests over the stored
> SHA-256 (`-017`), the `responses={}` error surface (`-022`), and the `-026`–`-032` shape rows.
> `-033` is an **operator knob** — a missing `Settings` field for a `RateLimiter` argument that has
> always existed. It changes no wire contract, no response shape and no error surface, so nothing
> about it needed the redesign decision. It sat here because its ID falls inside the `-026`–`-033`
> run.
>
> **This is the same failure the round-27 → round-28 handoff lineage recorded as its own method
> lesson and then repeated.** `APD-ECO-002` was parked in a seven-row "decision-shaped" bucket with
> no per-row rationale and turned out to be one kwarg per client; the lesson written down was *"a
> bucket label is not a rationale — when a group is parked, check each member was individually
> assessed."* `-033` was then carried in an eleven-row bucket whose note gives per-row reasons for
> `-008`, `-017` and `-022` and **none at all** for `-033`. A group boundary drawn by ID range is a
> label, not an assessment. **When parking a group, record why each member belongs in it; a member
> with no stated reason is unexamined, not deferred.** The other open `juniper-data` rows — `-016`, `-018` and `-019` — are design-shaped, but not part of that group. **`-016` is now FIXED (data#313)**; the two that remain were re-verified in source on 2026-09-02 and are restated below.

**`APD-DATA-018` / `-019` — re-verified 2026-09-02, and both remedies are larger than their one-line summaries suggest.**
Both findings hold exactly as filed; only their anchors had rotted (corrected in place above).
**`-018` is CLOSED as of 2026-09-04** (see the boxed note under it); the assessment below is kept as
written because it is what the closing decision was taken against — and because it was wrong in an
instructive way: the remedy it describes, a job store and polling endpoints, is precisely the one the
owner declined. Bounding the input turned out to be the smaller change and the better answer.

- **`-018` (no async job pattern).** Confirmed: zero `202`, zero job resource anywhere under `juniper_data/api/`; generation is
  still `await asyncio.to_thread(generator_class.generate, params)` (`api/routes/datasets.py:175`). The primer frames this as a
  **judgement call** — "202 the moment work outlives a sensible request timeout … That works until a generator outlives the
  client's socket timeout" — so the precondition worth measuring first is whether any generator actually does. The remedy is a job
  store, status transitions, retention, and polling endpoints: a wire contract every client must adopt, not a local change.

  > **Owner decision 2026-09-04. BOTH halves are now shipped and this row is CLOSED.**
  > The async-job pattern is **not** being built. The owner chose Option 6 of
  > [`notes/JUNIPER_2026-09-01_JUNIPER-DATA_ASYNC-JOB-PATTERN-DECISION-ANALYSIS.md`](JUNIPER_2026-09-01_JUNIPER-DATA_ASYNC-JOB-PATTERN-DECISION-ANALYSIS.md) —
  > bound the inputs — and settled that file's two open sub-questions for `csv_import`: the cap is in **bytes**, and exceeding it
  > **truncates** rather than rejecting outright, *provided* the caller is given a blocking notification and the dataset carries a
  > permanent truncated annotation.
  >
  > Shipped in **[data#326](https://github.com/pcalnon/juniper-data/pull/326)** (`cf387a82`): a 128 MiB cap derived from measured
  > throughput (median 14.4 MB/s over the whole `generate()` path — `util/ad-hoc/2026-09-04_measure_csv_import_throughput.py` in
  > juniper-data); a **422** refusal until the caller opts in via `allow_truncation`,
  > `JUNIPER_DATA_CSV_IMPORT_ALLOW_TRUNCATION`, or the matching `.env` entry; and a permanent `DatasetMeta.truncation` descriptor
  > carried over a reserved channel key popped before checksum + NPZ persist, mirroring `core/scaling.py`.
  >
  > **422 was chosen because it is already on the API's surface**, so the fix introduces no new status code — `APD-DATA-022`, which
  > would have to document one in `responses={}`, is parked, and a fix for one row must not force work inside a parked one.
  >
  > **Two review findings were confirmed and fixed before merge**, both of which made the cap decorative: the effective cap is now
  > `min(requested, deployment)` because an explicitly-supplied `max_bytes` previously won outright (and a generated client
  > serialising schema defaults would have raised a *lower* operator ceiling on every request); and `stat` is now only a cheap
  > pre-check, with the **read** enforcing the bound, because trusting `stat` let a FIFO (`st_size == 0`) or a file that grew
  > between the stat and the open be ingested without limit.
  >
  > **The `equities` half, closed the same day in [data#354](https://github.com/pcalnon/juniper-data/pull/354) (`ed099920`).**
  > `generators/equities/generator.py:264` was a bare slice that **truncated silently** with `EQUITIES_DEFAULT_MAX_SYMBOLS = None`,
  > so every request fanned out over all 503 bundled constituents — 18–34 minutes against a 30 s budget.
  >
  > **The two halves took different units, and that is the durable finding.** Measurement
  > ([`JUNIPER_2026-09-04_JUNIPER-DATA_EQUITIES-INGEST-SIZING-AND-FIELD-AVAILABILITY.md`](JUNIPER_2026-09-04_JUNIPER-DATA_EQUITIES-INGEST-SIZING-AND-FIELD-AVAILABILITY.md))
  > showed the equities cost is per **request**, not per byte: 163× the payload costs 1.16× the time. One symbol over 26 years is
  > 210 KB and ~2 s; the Russell 3000 over *one day* is 92 KB and 1.7–3.2 h. **A byte cap there would admit the expensive request
  > and reject the cheap one** — so `equities` bounds **symbols** (14 = 30 s ÷ 2.1 s/symbol, the owner's choice from the measured
  > range) while `csv_import` bounds **bytes**. *The unit belongs to the cost, not to the register.*
  >
  > Everything else is shared, because the failure mode is shared: 422 until opt-in, `min(requested, deployment)` so a request may
  > only lower the cap, and one `DatasetMeta.truncation` shape across both generators (`reason` / `unit` / `cap` / `requested` /
  > `imported` / `records_imported`). `equities_seq` reuses the same universe resolution and therefore inherits the bound **and**
  > the annotation.
- **`-019` (full-population work per page).** Confirmed: `total = len(filtered)` (`storage/base.py:537`) materialises the entire
  filtered set, then slices — **and it does so on the cursor path too** (`:545-548`), so `APD-DATA-011`'s keyset pagination did not
  address this. That is the useful relationship to record: `-011` fixed pagination's **correctness** half (drift under concurrent
  writes); `-019` is the untouched **performance** half. `LocalFSDatasetStore.list_datasets` still globs and sorts the whole
  directory per page. A real fix needs a storage index so filtering and ordering stop requiring full materialisation, plus making
  `total` estimated, cached or absent — the last of which is a response-shape change for existing clients.
>
> *(Corrected 2026-08-28: this sentence also listed `-005` + `-024` as open. Both have been `FIXED (data#295)` since 2026-08-26 — see their §4.1 rows and the §5.1 entry. The closure ran the §4 protocol's whole-file `grep -n 'APD-<ID>'` and this line still survived it, because the sentence names the IDs **inside a parenthetical about a different row's grouping** rather than as the subject of a status claim. **The fifth touch is necessary but not sufficient**: grep finds the mentions, but only reading each one decides whether a closure falsified it.)*

### 4.2 `juniper-service-core`

**Consumer reality — read this before triaging anything below.** The shared package is *partially* adopted, and which module an entry lives in decides whether it is live or latent. Verified by grepping every service's production tree for `juniper_service_core`:

- **`.middleware` and `.security` have exactly one production consumer: `juniper-recurrence`.** `juniper_recurrence/app.py:26-34` imports `RequestBodyLimitMiddleware`, `SecurityHeadersMiddleware`, `SecurityMiddleware`, `build_api_key_auth`, `build_rate_limiter` and `create_app`; the lazy map at `__init__.py:211-217` resolves those to `juniper_service_core.middleware` / `.security`.
  `APD-SVCCORE-004` and `-007` are therefore **live in production for recurrence**, and latent for everyone else. juniper-data, juniper-cascor and juniper-canopy each carry their own middleware and import none of it.
- **`.websocket.*` and `workers/` have no production consumer at all.** No service imports `control_stream`, `worker_stream`, `training_stream`, `control_security`, `commands` or `coordinator`. The nine entries rooted there — `-001`, `-003`, `-005`, `-010`, `-011`, `-012`, `-013`, `-015`, `-016` — are **latent library defects, not live exposure.** They become live if and when the wider service-core migration lands, which is exactly the loop §2.2 closes on. **All nine are now closed** — `-001`, `-003`, `-005`, `-010`, `-011`, `-012` and `-015` fixed; `-013` fixed as a docstring with the name kept by design; `-016` closed WON'T FIX as conformant behaviour. The §6 "triage before actioning" instruction is what settled the last two: both were `Low`, and reading the primer rather than the restatement changed the disposition of each.

  **"Latent" here means the *shared package* has no importer. It does not mean the defect appears nowhere.** `juniper-cascor` maintains its **own forks** of these modules (`src/api/websocket/{control_stream,control_security,worker_stream,training_stream,manager}.py`, `src/api/workers/coordinator.py`), mounted in production at `src/api/app.py:682-684`. Those forks are separate code objects with their own `APD-CASCOR-*` coverage — the register already tracks that relationship with `†`-marked sibling rows — so a latency call here is **not** a claim about cascor. Re-verified 2026-08-28 by re-deriving the consumer graph: every production import of `juniper_service_core` is package-root only (`enforce_auth_posture`, `enforce_dependency_floors`, `SettingsBase`, `get_secret`, `TrainingLifecycle`), and no service imports either subpackage. The rule stands as written.
  **But do not assume a fork is a copy.** Checked pairwise on 2026-08-28, the two trees have diverged **in both directions**: cascor is *stricter* on `-001` (a manifest-cardinality cap the shared package lacks — see that row), and the two carry *different* remediations of the same zero-`refill_rate` path in `-013` (cascor returns `0.0`, service-core `3600.0`). Sibling-check each row on its own evidence; "identical copy" has been wrong more often than right.
- **Package-root and boot-check entries are live for every consumer** — `-006`, `-008`, `-009`, `-014`, `-017` — because `enforce_auth_posture`, `enforce_dependency_floors`, `SettingsBase`, `get_secret` and `TrainingLifecycle` are imported by juniper-data, juniper-cascor, juniper-canopy and juniper-recurrence respectively.

| ID              | Finding                                                                                    | Sev | Source                                                                                       | Primer     | Conf |
|-----------------|--------------------------------------------------------------------------------------------|-----|----------------------------------------------------------------------------------------------|------------|------|
| APD-SVCCORE-001 | **FIXED (ml#1446)** — No cumulative cap across a worker-declared attachment list (`N ×` per-frame cap) | R | `websocket/worker_stream.py:323`, `:329`                                                     | 754        | High |
| APD-SVCCORE-003 | **FIXED (ml#1154)** — `_setting` silently defaults on a typo — including security controls | S   | `websocket/control_stream.py:66-69` (13 across both copies)                                  | 7950, 7964 | High |
| APD-SVCCORE-004 | **FIXED (ml#1300)** — `Retry-After` truncates toward zero → `Retry-After: 0` tight retry loop                    | R   | `security.py:205`                                                                            | 1409       | High |
| APD-SVCCORE-005 | **FIXED (ml#1447, disclosure + fix — see §5.1)** — Control-WS 64 KB cap checked after the frame is materialised | R | `websocket/control_stream.py:191-199`                                                        | 822        | High |
| APD-SVCCORE-006 | **FIXED (ml#1297, 4 of 5 — `UnknownTunableError` excluded by design)** — No package base exception; 3 of 6 derive from `RuntimeError`                               | R   | `auth_posture.py:51`, `dependency_floors.py:57`, `workers/registry.py:31`, +3                | 6918       | High |
| APD-SVCCORE-007 | **FIXED (ml#1303, disclosure -- the constraint itself stands by design)** -- Rate limiter is per-process — a documented constraint, not an oversight                    | R   | `security.py:99-104`, `:125-127`                                                             | 1275, 9448 | Low  |
| APD-SVCCORE-008 | **FIXED (ml#1237)** — No `py.typed` — all annotations and the 75-line `TYPE_CHECKING` block discarded            | M   | package root; `pyproject.toml`; `__init__.py:38-112`                                         | 7598, 7615 | High |
| APD-SVCCORE-009 | **FIXED (ml#1298)** — Three parallel name lists (`__all__`, `_LAZY_EXPORTS`, `TYPE_CHECKING`), no guard          | M   | `__init__.py:38-112`, `:114-192`, `:197-272`                                                 | 6398       | High |
| APD-SVCCORE-010 | **FIXED (ml#1154)** — `_setting` duplicated byte-identically in two modules                | M   | `websocket/control_stream.py:66-69`, `websocket/training_stream.py:44-47`                    | 7968       | High |
| APD-SVCCORE-011 | **FIXED (ml#1441)** — Two Protocols use incompatible member conventions                    | M   | `workers/coordinator.py:93-120` vs `websocket/commands.py:38-57`                             | 7944       | Med  |
| APD-SVCCORE-012 | **FIXED (ml#1443)** — `LifecycleCommandExecutor` subclassing neither supported nor forbidden | M   | `websocket/commands.py:60-68`                                                                | 8089       | Med  |
| APD-SVCCORE-013 | **FIXED (ml#1454, docstring — name kept by design)** — `LeakyBucket` is a token bucket   | M   | `websocket/control_security.py:53-79`                                                        | 1247       | Low  |
| APD-SVCCORE-014 | **FIXED (ml#1297)** — `DependencyFloorError` discards the structured `FloorViolation` list                       | E   | `dependency_floors.py:213-217`                                                               | 7005       | High |
| APD-SVCCORE-015 | **FIXED (ml#1441)** — `WorkerTaskProtocol` ships no default implementation                 | E   | `workers/coordinator.py:93-120`                                                              | 8058       | Med  |
| APD-SVCCORE-016 | **FIXED (ml#1454, won't fix — the behaviour is RFC-conformant and the "fix" is a regression; the ordering is now pinned instead)** — Pre-accept close codes are discarded | E | `websocket/control_stream.py:100`, `:107`, `:118`, `:234`, `:241`; `websocket/manager.py:73` | 849        | Low  |
| APD-SVCCORE-017 | **FIXED (ml#1298)** — `__dir__` returns exactly `__all__`, hiding module dunders                                 | E   | `__init__.py:290-291`                                                                        | 6400       | Low  |

**`APD-SVCCORE-016` restated — the previous wording was a false positive.** The entry formerly claimed the handshake gates "close after accepting rather than refusing the upgrade". The opposite is true: `control_stream_handler` calls `_check_handshake_gates` at `:234` and returns on failure at `:235`, six lines *before* `await websocket.accept()` at `:241`. All four rejection paths are correctly pre-accept.

The real defect at those lines is that the distinct close codes never reach the client. uvicorn converts a pre-accept close into a plain **HTTP 403** and discards both the code and the reason, so `1013` ("Control endpoint disabled", `:100`), `4029` ("Too many rejected handshakes", `:107`), `4003` ("Origin not allowed", `:118`) and `4001` ("Authentication required", `websocket/manager.py:73`) are all indistinguishable to the caller.
Four carefully chosen, individually meaningful codes collapse into one opaque status — a client cannot tell a kill switch from a cooldown from an Origin rejection from a bad key, and cannot decide whether to retry.

**`APD-SVCCORE-008` counts.** The register earlier cited a "62-name `TYPE_CHECKING` block". Against source today the block spans `__init__.py:38-112` — **75 lines, 62 imported names** — with a 62-entry `_LAZY_EXPORTS` (`:197-272`) and a 63-name `__all__`. The primer's figures ("73-line", 7615; "60 lazy names", 6343) describe an earlier revision; the source numbers above are the current ones.

### 4.3 `juniper-cascor`

| ID               | Finding                                                                                        | Sev | Source                                                        | Primer              | Conf |
|------------------|------------------------------------------------------------------------------------------------|-----|---------------------------------------------------------------|---------------------|------|
| APD-CASCOR-004 † | **FIXED (#524)** — 401 path unthrottled — the `#1082` fix never reached this copy              | S   | `src/api/middleware.py:147-186`                               | 1058 (mechanism)    | High |
| APD-CASCOR-006 † | **FIXED (#527)** — Blank API key enables auth that accepts an empty key — no `.strip()` filter | S   | `src/api/security.py:32-33`                                   | 1041 (data sibling) | High |
| APD-CASCOR-001a  | **FIXED (#540)** — Middleware order comment is wrong (omits `RequestBodyLimitMiddleware`)      | M   | `src/api/app.py:644-646`, `:630`                              | 9592                | High |
| APD-CASCOR-001b  | **FIXED (#540)** — CORS innermost → `SecurityMiddleware` answers preflights 401                | C   | `src/api/app.py:621`, `:641`; `src/api/middleware.py:189-198` | 9592                | High |
| APD-CASCOR-002   | **FIXED (#516)** — `ValueError` handler reclassifies serialisation faults as `400`             | C   | `src/api/app.py:678-684`                                      | 9596                | High |
| APD-CASCOR-003   | **FIXED — partial (cascor#593)** — 46 of 47 routes declare no `response_model`; **44 of the 46 done**, the two bare-dict health routes deferred to a cross-service wire decision | M   | `src/api/routes/` (only `health.py:130`)                      | 7761                | High |
| APD-CASCOR-005   | Key comparison short-circuits on match in 2 of 3 copies (see §3 assessment)                    | M   | `src/api/security.py:53`                                      | 1027-1029, 9463     | Low  |

**`APD-CASCOR-006` detail.** `self._api_keys: set[str] = set(api_keys) if api_keys else set()` (`:32`) followed by `self._enabled = len(self._api_keys) > 0` (`:33`). A configured `[""]` therefore enables authentication and `validate("")` succeeds via `hmac.compare_digest("", "")`. `juniper-service-core/juniper_service_core/security.py:44` carries the blank filter with a comment naming exactly this failure; neither cascor nor juniper-data received it.
Same reachability caveat as `APD-DATA-003`: the boot-time `enforce_auth_posture` check filters blanks, so triggering this needs auth-posture enforcement disabled. **FIXED** in [juniper-cascor#527](https://github.com/pcalnon/juniper-cascor/pull/527) — the `security.py` line is now byte-identical to the canonical service-core filter, and `_parse_api_keys`'s list branch filters too.

**`APD-CASCOR-005` routing.** This row is an **owner decision, not a task**. Its §3 assessment already
concludes that the short-circuit is defensible and that what is actionable is the *divergence* — "unify
the three and decide deliberately which behaviour is intended" — which is a choice between two working
postures, not a defect with a known fix; §6 lists it among the `Low`-confidence judgement calls to triage
before actioning. The primer contradicts itself across the two passages the row cites, so neither can be
used to settle it. **Do not unify the three copies unilaterally.** The routing was carried by the
defect-register handoff lineage alone until it was recorded here on 2026-08-26.

**`APD-CASCOR-003` carries an unfiled sibling.** All 47 cascor route decorators also declare no
`operation_id` — the `APD-DATA-023` gap, never filed as its own row here (it is recorded in that entry's
§5.1 row). Both are the same mechanical pass over the same decorators, so **do them together**; splitting
them rewrites every decorator twice. One caveat the `-023` close does not carry over: cascor wraps
responses in the `{"status","data","meta"}` envelope (`src/api/models/common.py`), so `response_model`
here means typing that envelope generically rather than writing a model per route.

**Measured 2026-08-26 — declaring the envelope does NOT change the wire.** The decisive fact is not
that `data` is `Any` (`:65`, which does mean pydantic neither validates nor filters the payload) but
that `success_response()` already **returns `ResponseEnvelope(...).model_dump()`** (`:97`). Every
enveloped body has therefore *already* round-tripped through the exact model a `response_model=` would
re-apply, so the second pass is idempotent by construction. `meta` is the field to watch — it is typed
`Meta`, not `Any` (`:66`, `Meta` = `timestamp` + `version`, `:46-50`) — but `success_response()` takes
no `meta` argument and always builds it from the default factory, so no route can put an extra key
there either.
**The safety is conditional on one property**: that a route's body comes from `success_response()`.
A handler that hand-builds a dict and returns it directly would be filtered against the envelope and
lose anything outside `status`/`data`/`meta`. That property holds today — the bare `return {...}` sites
(`workers.py:40`, `snapshots.py:116`, `:128`) are non-route helpers whose output is wrapped at the real
`@router` call sites, and `error_response()` is reached only from `app.py`'s exception handlers, which
return raw `JSONResponse`s that bypass `response_model` entirely. **Pin that property in the same PR**,
not the absence of extra keys: the failure mode is a future route that skips `success_response()`, and
nothing would fail. (`health.py:130`'s `response_model=ReadinessResponse` is not a counter-example; it
deliberately skips the envelope.)

**Outcome (cascor#593, 2026-08-26).** All 47 decorators gained an explicit `operation_id`, and the
44 `success_response()` routes gained `response_model=ResponseEnvelope`. The uniformity property
above is pinned directly, so the wire-safety argument cannot rot silently.
**Two routes are deliberately NOT closed, which is why the marker says *partial*.** `health_check`
and `liveness_probe` return bare dicts on the documented cross-service API-02
`{status, version, service}` base shared with `juniper-data` and `juniper-canopy`, and declaring a
model on them **is** a wire change — measured: an optional field absent from the 200 body reappears
as an explicit `"error": null`, because `response_model_exclude_none` defaults to `False`. Giving
those two purpose-built models is a cross-repo contract decision, not this defect.
One further design note the sibling `APD-DATA-023` settles the same way: the published `operation_id`
set is frozen by a test but **deliberately not asserted against `endpoint.__name__`**. The point of an
explicit id is to decouple the published SDK name from the Python handler name, so a handler rename
must change nothing — asserting the two match would re-couple them and reinstate the defect in a new
form. Both mutation matrices carry that rename as an expected-**survival** row.

**Register-original, unfiled: cascor and service-core carry `juniper-data`'s `APD-DATA-024`
exemption trap.** Found 2026-08-26 by asking the §6 sibling question after `APD-DATA-024` closed.
`juniper-cascor/src/api/middleware.py:16` and
`juniper-service-core/juniper_service_core/middleware.py:30` both list `/docs`, `/openapi.json` and
`/redoc` in `EXEMPT_PATHS`, and both resolve exemption with a bare `path in EXEMPT_PATHS` that never
consults the auth configuration — cascor's `app.py:616-618` then gates `docs_url` / `redoc_url` /
`openapi_url` on `docs_enabled = not settings.api_keys`, exactly the shape juniper-data had.
Not filed as new IDs because the register's 96 is a fixed identity; recorded here so the next reader
of either repo meets it.

**Register-original, unfiled: cascor's `/ws/control` carries `APD-SVCCORE-005`'s oversize-frame
defect, and there it is live.** Recorded 2026-08-28 alongside the ml#1447 fix, at the owner's
direction, as a note rather than a new ID — same disposition as the paragraph above, and it keeps
the 96 a fixed identity so counts stay comparable across the arc.
`juniper-cascor/src/api/websocket/control_stream.py:336` is the same loop, byte-for-byte in
behaviour: `if len(raw) > _MAX_MESSAGE_SIZE:` → send an error → **`continue`**, returning before
`_handle_command_message` (`:358`), which is the only place the `LeakyBucket` token is spent. Unlike
the `APD-SVCCORE-001` sibling — where cascor's fork was the *stricter* copy — here the two are
equivalent, so the shared package's fix does not reach cascor and cascor's copy is unmitigated.
**Reachability, checked rather than assumed**: `/ws/control` is enabled by default
(`disable_ws_control_endpoint: bool = False`, `src/api/settings.py:616`), mounted at
`src/api/app.py:683`, behind `ws_authenticate`; the Origin allowlist applies only when
`ws_control_allowed_origins` is non-empty. So the vector needs a valid API key — `R`, not `S`, by the
same reasoning as `APD-SVCCORE-001` — and each oversize frame is bounded at uvicorn's 16 MiB
`ws_max_size`, which `src/server.py:37-42` does not override. The port is ml#1447's two-line change:
close 1009 instead of `continue`. Its `pong` branch should be left alone for the reason recorded
there.

> **CORRECTION 2026-08-28 — this note said "latent in both". That was wrong for service-core, and
> the error was in the reasoning, not the observation.** It applied the §4.2 no-consumer rule to
> `middleware.py`. That rule covers `.websocket.*` and `workers/`; §4.2's own preamble classifies
> `.middleware` / `.security` as **LIVE**, because `juniper-recurrence` consumes them. Two facts the
> original pass did not check turned the latent claim over:
> `juniper_service_core/app.py`'s `create_app` passed **no** `docs_url` / `redoc_url` /
> `openapi_url` (zero occurrences package-wide), so FastAPI's defaults mounted all three
> **unconditionally** — service-core had no `docs_enabled` gate to be inert behind, unlike cascor and
> juniper-data. And `juniper-recurrence/juniper_recurrence/app.py:86-91` calls that factory, `:139`
> adds `SecurityMiddleware` with a real `api_key_auth`, and `juniper-deploy/docker-compose.yml:558`
> publishes a host port with `JUNIPER_RECURRENCE_REQUIRE_AUTH` defaulting to **true**. The full
> OpenAPI surface of a service configured fail-closed was therefore served to unauthenticated
> callers. A checked-in test, `juniper-recurrence/tests/test_app_smoke.py:89-94`
> (`test_docs_reachable_and_exempt`), asserted that as **correct behaviour** — the defect was pinned,
> which is why re-reading the sibling did not catch it and only re-deriving the consumer graph did.
> **cascor's half really is latent** — one `FastAPI(` at `src/api/app.py:611`, gated at `:610` /
> `:616-618`; canopy gates too (`src/main.py:403-405`). Recurrence was the only unguarded factory.
>
> **Fixed in ml#1434 (service-core) — the posture ported from data#295 option (a).** The three paths
> are removed from `EXEMPT_PATHS`, so `SecurityMiddleware` authenticates `/openapi.json` like any
> other route; `create_app` gains `explorers_enabled` (default `True`, preserving behaviour for
> unauthenticated deployments) to unmount `/docs` and `/redoc` under auth.
> `tests/test_security_middleware_exempt_paths.py` pins the paths' **absence** plus a two-arm
> behavioural check — unauthenticated `401` proves the exposure is closed, and *authenticated* `404`
> proves the explorers are genuinely unmounted, since `SecurityMiddleware` runs before routing and a
> 401-only assertion would pass with the pages still mounted. Service-core suite 540 passed.
> **Consumer change still owed**: `juniper-recurrence` must pass
> `explorers_enabled=not settings.api_keys` and invert `test_docs_reachable_and_exempt`; that lands
> after a service-core release, since recurrence resolves the package from PyPI
> (`juniper-service-core>=0.5.0,<0.6.0`).
>
> **New fork drift, deliberate and invisible to the gate.** Fixing service-core and not cascor means
> the canonical package no longer exempts the doc paths while cascor's fork still does.
> `tests/test_service_fork_drift.py` will **not** catch this: its six guards cover the body cap,
> `Content-Length` parsing, serialization-error narrowing, blank-key filtering, the pre-auth throttle
> and CORS ordering — `EXEMPT_PATHS` membership is not among them, so the gate stays green across a
> real divergence. Not waived (adding a `KNOWN_GAP` row would break the ledger's zero-waiver
> invariant) and not silently closed either: cascor's copy is latent behind its `docs_enabled` gate,
> and whether it becomes a filed ID is the open owner decision this note already carries. Recorded so
> the divergence is a decision on the record rather than an artefact of what the gate happens to
> measure.
>
> **Transferable finding — "latent" is a claim about the consumer graph, not about the code in front
> of you.** Both halves of this note read identically at the defect site. What separated them was one
> question asked of each: *does any consumer mount the document?* The §4.2 module rule was a proxy
> for that question and silently answered it wrong for the module that had a consumer.

### 4.4 `juniper-cascor-client`

| ID              | Finding                                                                                       | Sev | Source                                                                                             | Primer     | Conf |
|-----------------|-----------------------------------------------------------------------------------------------|-----|----------------------------------------------------------------------------------------------------|------------|------|
| APD-CCLIENT-001 | **FIXED ([cclient#143](https://github.com/pcalnon/juniper-cascor-client/pull/143))** — Retries `POST`/`DELETE`/`PATCH` with no idempotency key | C   | `constants.py:37`, `client.py:89-95`                                                               | 296        | High |
| APD-CCLIENT-002 | **FIXED (#124)** — `JuniperCascorServiceUnavailableError` is unreachable dead code            | C   | `exceptions.py:40-43`, `client.py:411-412` (dead branch), `:371` (interception), `constants.py:36` | 7041       | High |
| APD-CCLIENT-004 | **FIXED (#123)** — Exceptions carry no `status_code` / `response` / `detail` — so 400 and 422 are byte-identical | C   | `exceptions.py:4-49`; `client.py:404-414`                                                          | 6961, 6979 | High |
| APD-CCLIENT-005 | **FIXED (#129)** — No base-URL normalisation or validation at all                             | R   | `client.py:82-83`                                                                                  | 6691       | High |
| APD-CCLIENT-006 | **FIXED (cclient#135)** — `_dispatch` unguarded — one raising listener tears down the stream  | R   | `ws_client.py:453-457` vs `:459-471`                                                               | 7141       | High |
| APD-CCLIENT-007 | **FIXED (cclient#136)** — mypy targets 3.11 while the package requires ≥3.12                  | M   | `pyproject.toml:90` vs `:12`                                                                       | 7625       | High |
| APD-CCLIENT-008 | **FIXED ([cascor#610](https://github.com/pcalnon/juniper-cascor/pull/610) / [cclient#148](https://github.com/pcalnon/juniper-cascor-client/pull/148), root cause was the SERVER — cascor's API-09 migration never wrapped 422)** — Sniffs two error envelopes, encoding fleet drift rather than surfacing it | M   | `client.py:393-402` (dead; the extraction is now `:574-599`)                                        | 4773       | High |
| APD-CCLIENT-009 | **FIXED (cclient#136)** — `pool_connections` omitted where both siblings set it               | M   | `client.py:95`                                                                                     | 269        | High |
| APD-CCLIENT-010 | **FIXED (#126)** — Redundant `pass` after a docstring in all 8 exception classes              | M   | `exceptions.py`                                                                                    | 6888       | Med  |
| APD-CCLIENT-011 | **FIXED (cclient#138)** — `create_network(**kwargs: Any)` — 11 real parameters, none typed    | E   | `client.py:138-154`                                                                                | 6565       | High |
| APD-CCLIENT-012 | **FIXED (cclient#137)** — `auto_pong` is a positional boolean trap with no removal date — the trap closed here; the removal-date half closed with `APD-ECO-007` (cclient#151, removal in 0.9.0) | E   | `ws_client.py:244-250`                                                                             | 7407       | High |
| APD-CCLIENT-013 | **FIXED (cclient#136)** — `backoff_factor` hardcoded, not constructor-configurable            | E   | `client.py:91`                                                                                     | 1577       | High |

**Retired ID.** `APD-CCLIENT-003` ("status dropped on 4 of 5 branches — 400 and 422 are byte-identical") was **merged into `APD-CCLIENT-004`**. They are one defect in `_handle_response` (`client.py:404-414`) with one fix: 400 and 422 both raise `JuniperCascorValidationError(error_msg)` and are indistinguishable *because* the exception type carries no `status_code`. Give the exception hierarchy a `status_code` and both halves close together. **The number `APD-CCLIENT-003` is retired and must not be reused.** **Confirmed by the fix** ([#123](https://github.com/pcalnon/juniper-cascor-client/pull/123)): `status` was computed and then dropped on four of the five branches, and adding the field to the hierarchy closed both halves in one change, exactly as this note predicted.

**`APD-CCLIENT-002` detail.** `RETRYABLE_STATUS_CODES` includes 503 (`constants.py:36`), so urllib3's `Retry` exhausts and raises through `requests`, which surfaces as a `requests.RequestException` and is caught at `client.py:371` — converting it to `JuniperCascorClientError` before `_handle_response` is ever reached. The 503 branch at `client.py:411-412`, and therefore `JuniperCascorServiceUnavailableError` itself, is unreachable.

**FIXED** — [juniper-cascor-client#124](https://github.com/pcalnon/juniper-cascor-client/pull/124), with one line: `raise_on_status=False` on the `Retry`. urllib3 defaults it to `True`, which is what turns an exhausted retry into `MaxRetryError`; with it `False` the retrying is unchanged and only the give-up path differs, returning the final response so `_handle_response` classifies it.

**Why it hid, which is the transferable part.** Every pre-existing test of that arm mounts `HTTPAdapter(max_retries=0)` first — `TestErrorHandling.test_service_unavailable_503` and the `client` fixture in the dual-shape parser tests. The branch therefore had coverage, and the coverage passed, because it exercised the branch under a configuration production never uses. That is the **vacuous-pass class** applied to a *test fixture* rather than to a check's machinery: not "the test is broken" but "the test disables the thing that breaks it". When a defect is "unreachable in production", check whether its tests reach it by removing the very mechanism that makes it unreachable.

**Two alternatives were rejected**, and the reasons generalise. *Deleting* the dead branch and the exception type would be a breaking change to a published package — the type is exported in `__all__`, is used by the fake's worker scenarios, and is the correct type to raise. *Removing 503 from `RETRYABLE_STATUS_CODES`* would make the branch reachable by giving up the retry, which is the wrong trade: a 503 from a service still starting up is exactly what retrying is for.

**Sibling note (register-original).** `juniper-data-client` and `juniper-recurrence-client` share the same `raise_on_status` default, but neither has a *dead* branch — their generic `else` arm handles 5xx — so this is **not** the same defect there and no `†` entry is filed. It would still improve their `status_code` fidelity on an exhausted retry, where they currently report `None`. That is an unfiled improvement, not a defect. Anyone acting on it must also revisit `juniper-data-client`'s `test_not_found_and_generic_errors_also_carry_status`, which documents in a docstring that 503 never reaches the response branch and deliberately uses 409 instead.

**`APD-CCLIENT-005` restated.** The earlier wording claimed "a documented base URL yields `/v1/v1/...`"; no documented cascor base URL carries a `/v1` suffix, so that clause is dropped.
What reproduces is broader: `self.base_url = base_url.rstrip("/")` (`:82`) is the *entire* treatment. `grep -rn "urlparse\|urlsplit\|netloc"` across `juniper-cascor-client/` returns **zero** hits — no scheme defaulting, no path normalisation, no `/v1` suffix strip, and no `netloc` validation.
`juniper-data-client` normalises via `urlparse` (`client.py:180-201`) but never checks `netloc`; `juniper-recurrence-client` normalises *and* validates (`client.py:182-185`). cascor-client therefore also carries the hostless-URL gap that §2.3's sibling-drift table pairs it with — it is the only client with neither guard.

### 4.5 `juniper-data-client`

| ID              | Finding                                                                         | Sev | Source                                                                                                                                            | Primer | Conf |
|-----------------|---------------------------------------------------------------------------------|-----|---------------------------------------------------------------------------------------------------------------------------------------------------|--------|------|
| APD-DCLIENT-001 | **FIXED (#158)** — Exceptions carry no `status_code` / `response` / `detail`    | C   | `exceptions.py:4-37`, `client.py:312-320`                                                                                                         | 6961   | High |
| APD-DCLIENT-002 | **FIXED (#161)** — Public `validate_npz_contract` raises bare `ValueError`, escaping the hierarchy | C   | `contract.py:66`, exported `__init__.py:8`, `:15`                                                                                                 | 6931   | High |
| APD-DCLIENT-003 | **FIXED (#158)** — A 422 `detail` list is f-string-interpolated into an unparseable repr | C   | `client.py:306`, `:313-319`                                                                                                                       | 4771   | High |
| APD-DCLIENT-004 | **FIXED (#165)** — `_normalize_url` never validates `netloc` — hostless URL fails opaquely | R   | `client.py:180-201`                                                                                                                               | 6689   | Med  |
| APD-DCLIENT-005 | **FIXED (#164)** — Version literal drifted across six file headers (0.4.0 / 0.4.1 / 0.3.2)   | M   | `constants.py:12`, `contract.py:21`, `testing/__init__.py:7`, `testing/generators.py:7`, `testing/fake_client.py:7`, `tests/test_versioning.py:7` | 7332   | High |
| APD-DCLIENT-006 | **FIXED (#164)** — `validate_npz_contract -> str` where a `Literal` is available | M   | `contract.py:41`                                                                                                                                  | 6625   | High |
| APD-DCLIENT-007 | **FIXED (#162)** — Redundant `pass` after a docstring in all 6 exception classes | M   | `exceptions.py`                                                                                                                                   | 6888   | Med  |
| APD-DCLIENT-008 | **FIXED (#171)** — `create_dataset` — 9 positional-or-keyword params, 3rd a bare boolean | E   | `client.py:495-506` (pre-#171; the filed `:412-423` predates the 08-24 merges)                                                                    | 6592   | High |

**`APD-DCLIENT-005` count corrected.** The primer names **six** drifted file headers at 7332-7337, not four: the earlier entry omitted `testing/generators.py:7` and `testing/fake_client.py:7`. All six verified against source — `contract.py:21` reads `0.4.1`, `tests/test_versioning.py:7` reads `0.3.2`, and the remaining four read `0.4.0`, while `pyproject.toml` and `__init__.py` agree on the real version. Three distinct values across six decorative copies.

### 4.6 `juniper-recurrence-client`

| ID              | Finding                                                                          | Sev | Source                                                       | Primer | Conf |
|-----------------|----------------------------------------------------------------------------------|-----|--------------------------------------------------------------|--------|------|
| APD-RCLIENT-001 | **FIXED (recurrence#124)** — Exceptions carry no `status_code` / `response` / `detail` | C   | `client.py:260-271`, `exceptions.py:10-36`                   | 6961   | High |
| APD-RCLIENT-002 | **FIXED (recurrence#130)** — 30 s scalar timeout governs synchronous `train`/`crossval`; no per-call override | R   | `client.py:383`, `:473`, plumbing `:181`/`:262`; `constants.py:47` (pre-#130; the filed `:217`/`:311`/`:405` predate the 08-24 merges) | 1635   | High |
| APD-RCLIENT-003 | **FIXED (recurrence#127)** — Ships `py.typed` and the `Typing :: Typed` classifier with no mypy config | M   | `pyproject.toml:61-62`, `:25`                                | 7620   | High |
| APD-RCLIENT-004 | Client returns `dict[str, Any]` while the same-repo server declares models       | M   | `client.py:310`, `:404` vs `juniper_recurrence/routers/*.py` | 7768   | Med  |
| APD-RCLIENT-005 | **FIXED ([recurrence#143](https://github.com/pcalnon/juniper-recurrence/pull/143), documentation — `[project.urls]` is correct by design and is unchanged)** — Repository, distribution and import names are three different strings | M   | `pyproject.toml:49-52`                                       | 8188   | Low  |

**`APD-RCLIENT-004` cross-reference.** The shape question is `APD-ECO-004`'s, not this row's alone. The client's runtime dependencies are `requests` and `urllib3` only, and `client.py`'s `_parse_json` declares `dict[str, Any]` on purpose (its docstring: that annotation is what keeps the public return types real rather than laundered `Any` — the `APD-RCLIENT-003` close); importing the server's pydantic models from `juniper_recurrence/schemas.py` would make the client depend on the `juniper-recurrence` app distribution and on pydantic at import time. The shape that fits is client-side `TypedDict`s **held to the server models by a keys-equality drift test**, which the `pcalnon/juniper-recurrence` monorepo makes possible. But it is one decision across three clients and 45+ methods, so it is routed to `APD-ECO-004` as a proposal (recorded 2026-08-26; carried by the round-26 handoff before that) rather than actioned here. If it is ever actioned, two standing rules bind: an AST census over all seven repos before any signature boundary moves, and every fake mirrors its real signature under an `inspect.signature` parity pin.

**Owner decision 2026-08-26 — DEFERRED, recorded rather than actioned.** The proposal was put to the owner and deliberately not taken up: it is one decision spanning three clients and 45+ methods, for a `Maintainability` row, and `client.py`'s `_parse_json` documents `dict[str, Any]` as an intentional contract (that annotation is what keeps the public return types real rather than laundered `Any` — the `APD-RCLIENT-003` close). The blast radius is therefore large and the defect it removes is presentational. **This is a deferral, not a rejection**: `APD-RCLIENT-004` and `APD-ECO-004` both stay OPEN, and the shape above remains the one that fits if the fleet ever adopts typed client returns. Do not re-derive the proposal from a handoff — it is here.

### 4.7 `juniper-observability`

| ID          | Finding                                                                                           | Sev | Source                                              | Primer     | Conf |
|-------------|---------------------------------------------------------------------------------------------------|-----|-----------------------------------------------------|------------|------|
| APD-OBS-001 | **FIXED (ml#1156)** — `X-Request-ID` accepted unvalidated — contained by h11, not by this package | M   | `middleware/request_id.py:36`, `:40`                | 1109, 2101 | Med  |
| APD-OBS-002 | **FIXED (ml#1237)** — No `py.typed` — every consumer sees `[import-untyped]`                                            | M   | package root; `pyproject.toml`                      | 7598       | High |
| APD-OBS-003 | **FIXED (ml#1245)** — `register_info_or_update -> Any` beside three siblings returning `T`                              | M   | `prometheus_helpers.py:214-218`                     | 7669       | Med  |
| APD-OBS-004 | **FIXED (ml#1245)** — Two `__all__` lists must agree; nothing checks it                                                 | M   | `middleware/__init__.py:13-22`, `__init__.py:52-90` | 6250       | Med  |

### 4.8 Cross-client, ecosystem and meta

| ID          | Finding                                                                        | Sev | Source                                                                                                                                       | Primer    | Conf |
|-------------|--------------------------------------------------------------------------------|-----|----------------------------------------------------------------------------------------------------------------------------------------------|-----------|------|
| APD-ECO-001 | No `Idempotency-Key` mechanism anywhere in the stack — zero occurrences        | R   | ecosystem-wide                                                                                                                               | 1592      | High |
| APD-ECO-002 | **FIXED (data-client#175 / cascor-client#141 / recurrence#133)** — No retry jitter in any of the three clients — synchronised retry storms | R   | all three `client.py` `Retry(...)` sites                                                                                                     | 346       | High |
| APD-ECO-003 | One flat 30 s scalar timeout in all three clients; no per-call override        | R   | Scalar declared: data `client.py:178`, cascor `:127`, recurrence `:182`. Applied: data `:302`, cascor `:544`, recurrence `:265` — re-read 2026-09-03 | 378       | High |
| APD-ECO-004 | 45+ public methods return `Dict[str, Any]`; zero `TypedDict`, zero `@overload` | M   | all three `client.py`                                                                                                                        | 6709      | High |
| APD-ECO-005 | **FIXED ([dclient#177](https://github.com/pcalnon/juniper-data-client/pull/177) / [cclient#144](https://github.com/pcalnon/juniper-cascor-client/pull/144))** — No version-lockstep test in any client repo | M   | all three `tests/`                                                                                                                           | 7339      | High |
| APD-ECO-006 | **FIXED (dclient#178 / cclient#145 / rec#138)** — No client type-checks a consumer-shaped probe | M | `juniper-data-client/.pre-commit-config.yaml:167`, `juniper-cascor-client/.pre-commit-config.yaml:167`, `juniper-recurrence/.pre-commit-config.yaml:134-144` | 7676      | Low  |
| APD-ECO-007 | **FIXED ([cclient#151](https://github.com/pcalnon/juniper-cascor-client/pull/151) — the owned `auto_pong` removal date; the shared-package absence is DELIBERATE and now documented as policy)** — Neither shared package has any deprecation machinery | E   | `juniper_service_core/`, `juniper_observability/`                                                                                            | 7421      | Low  |
| APD-ML-001  | First-party pins inconsistently capped; the pattern is coherent but unstated   | M   | `juniper-ml/pyproject.toml:30-31`, `:34`, `:47-49`, `:52`, `:56`                                                                             | 7445-7446 | Low  |

**`APD-ECO-006` restated.** The earlier wording ("no `strict = true` in all three `pyproject.toml`") contradicted itself and is false: **two of three do** configure it — `juniper-data-client/pyproject.toml:88-93` and `juniper-cascor-client/pyproject.toml:89-94` both declare `[tool.mypy]` with `strict = true`.
The real gap is what those checkers are pointed at. Both pre-commit hooks scope mypy to `^juniper_<pkg>_client/(?!testing/).*\.py$` — library-internal source only, never a file that *imports* the package the way a consumer would.
So no client verifies that its own published type surface is usable from outside, which is the failure `APD-OBS-002` and `APD-SVCCORE-008` would have caught.

> **CORRECTION 2026-08-28 — the sentence about `juniper-recurrence-client` was false, and correcting it makes the row *larger*, not smaller.** The restatement said it "configures no mypy at all and has no `.pre-commit-config.yaml`". Both halves are wrong: `juniper-recurrence-client/pyproject.toml:90-96` declares `[tool.mypy]` with `strict = true` (added by `APD-RCLIENT-003`, whose comment says "something must actually check the surface it advertises"), and it **is** covered by a strict hook — `juniper-recurrence/.pre-commit-config.yaml:134-144`, `--config-file=juniper-recurrence-client/pyproject.toml`.
>
> **Why the original pass missed it: `juniper-recurrence-client` is not a standalone repository.** It is a sub-package of the `juniper-recurrence` monorepo, so its pre-commit config lives at the *repo root*, two levels up from the package. §3's anchor note already warns that this repo's paths are written bare; the same trap bit the triage here. Any per-repo sweep that looks for a sibling `.pre-commit-config.yaml` next to the `pyproject.toml` will conclude "no config" for every monorepo sub-package.
>
> **The consequence for the fix.** That hook's `files:` scope is `^juniper-recurrence-client/juniper_recurrence_client/.*\.py$` — library-internal source only, i.e. **exactly the same gap** as the other two. So this row is **uniform across all three clients, not drifted across two**, and the remedy is one identical mechanical change per client (a module that imports the package from outside it, plus widening the hook's `files:` regex to include it) rather than two fixes and one bootstrap. `reveal_type` / `assert_type` appear nowhere in any of the three.
>
> This is the `APD-ECO-002` shape again — a row parked as decision-shaped that is one small change per client — and the third time in this arc that a claim of the form "repo X has no Y" turned out to mean "Y was somewhere the sweep did not look."

**`APD-ECO-004` / `APD-ECO-007` cross-references.** `APD-ECO-004` also owns the `APD-RCLIENT-004` shape proposal (client-side `TypedDict`s held to the server models by a drift test — see the §4.6 note); `APD-ECO-007` owned the removal-date half of `APD-CCLIENT-012`; **both are now closed** — `auto_pong=False` warns and is removed in cascor-client 0.9.0 (cclient#151), so the "still lacks a removal date" statement that stood here is no longer true and the §4.4 markers have been updated to match. `APD-CCLIENT-012` itself is **FIXED** — the trap half closed in cclient#137 — and is *not* a retired ID; the only retired number in this register is `APD-CCLIENT-003` (see the §4.4 retired-ID note and [§1](#identifier-namespace)).

**`APD-ML-001` restated, and downgraded.** The earlier wording — "8 first-party pins uncapped, contradicting the file's own stated policy" — overstated the primer twice. The primer says at 7445-7446 that "the pattern is coherent even if **never stated as policy**": there is no stated policy to contradict. And it states no count of 8; its table has three uncapped rows across six packages. The count of 8 is this register's re-verification (register-derived), not an extraction.

Two further reasons this is Low confidence and probably not actionable: those exact eight pin strings are **byte-for-byte asserted by a passing lint** (`tests/test_pyproject_extras.py:106-125`), so "fixing" them means editing the contract test in the same PR;
and capping first-party pins on a meta-package fed by a **daily release train** would make `juniper-ml` a permanent release bottleneck — every sibling `0.y` bump would need a meta PR before it could be installed together. Record it; do not action it without deciding the release-train question first.

---

## 5. Fixed findings (before and since the primer)

Recorded so the register is not read as a list of live problems that includes resolved ones.

### 5.1 Fixed since this register was published (2026-08-14)

These carry their **original IDs** — they were counted in the 96, and are marked `FIXED` in place at their §4 table row and detail entry rather than renumbered or removed, so a reader following an existing reference still lands on the right entry and sees why it is closed. Working the §2.2 list is what produced the first of them, and it is now worked through: all four of its items are closed. The §2.3 copy-drift list is now worked through too.

| ID | Finding | Fixed by | Verification |
| --- | --- | --- | --- |
| APD-DATA-003 | Blank API key enabled auth that then accepted an empty `X-API-Key` | [juniper-data#267](https://github.com/pcalnon/juniper-data/pull/267) | `APIKeyAuth.__init__` filters blanks / whitespace-only / non-string entries before deriving `_enabled`; `_parse_api_keys`'s list branch filters too, closing the string-vs-list inconsistency that made the JSON form the reachable shape. Keeps the `list` container deliberately (`hmac.compare_digest` per key, SEC-01/JD-SEC-02) rather than adopting service-core's `set`. 13 arms added; one of them caught a real ordering bug in the first draft -- `dict.fromkeys` hashes every element, so filtering after it raises `TypeError` on an unhashable entry. Fixed at the point of use rather than relying on the boot-time `enforce_auth_posture` check, which can be disabled. |
| APD-DATA-001 † | 401 path unthrottled — the `#1082` fix never reached this copy; the register's #4 item | [juniper-data#266](https://github.com/pcalnon/juniper-data/pull/266) | `FailedAuthThrottle` ported into `api/security.py`; `check()` runs before authentication and `record_failure()` is gated on a 401. **Ported, not consolidated** — §2.2's loop warning about `APD-SVCCORE-003` still governs. Mirrors service-core's optional fourth constructor parameter defaulting to an *enabled* throttle, so `app.py` was unchanged and no settings field was added. 13 arms added; the half-port that matters is caught by mutation — deleting the single `record_failure` line fails 3 of them. Full suite 1173 passed. |
| APD-DATA-002 | Body cap bypassable by a chunked request with no `Content-Length` — the register's #1 item | [juniper-data#261](https://github.com/pcalnon/juniper-data/pull/261) | `POST`/`PUT`/`PATCH` always stream-read against a cumulative cap, aborting 413 mid-stream; `Content-Length` demoted to an early-reject hint. Seven tests added where there had been **no** body-limit coverage at all; the two bypass shapes are driven at the ASGI layer because httpx recomputes `Content-Length` and cannot express them. Ports the implementation already shipping in cascor and service-core. |
| APD-CASCOR-002 | Blanket `ValueError` handler reported serialisation faults as `400` | [juniper-cascor#516](https://github.com/pcalnon/juniper-cascor/pull/516) | `PydanticSerializationError` is classified 500 and logged at exception level; a plain `ValueError` still returns 400, so the existing `test_value_error_handler` passes unchanged — the handler was narrowed, not redefined. `coerce_native_scalars` remains as the cheap pre-emption for the numpy-scalar path. |
| APD-SVCCORE-003 | `_setting` silently defaulted a misspelled settings field — six of the eleven tunables are security controls | ml#1154 | `websocket/tunables.py` declares all eleven with defaults + a `security` flag; both handlers resolve through it. The decoupling is kept (no import of a service settings class); what changes is that the default is declared rather than repeated at 13 call sites, and a near-miss on the settings object is logged at WARNING naming both spellings — |
| | | | the `..._per_second` case now produces a log line instead of silence. `audit(settings)` reports defaulted security controls and suspected typos at boot; an undeclared name raises. Verified by mutation: deleting the near-match lookup fails the warning arm. |
| APD-OBS-001 | `X-Request-ID` accepted unvalidated into a process-wide ContextVar and echoed back | ml#1156 | 128-char cap + anchored allowlist `[A-Za-z0-9._:-]` on ingress; a failing value is **replaced** with a fresh UUID4, not sanitized — a stripped value would correlate to nothing on either side. Rejection is silent on purpose (logging it is the log-flood lever the guard bounds). UUID / ULID / W3C `traceparent` / `service:id` all still pass, pinned by tests. |
| | | | Moves the containment from h11's incidental defaults into this package. Mutation: a always-true validator fails 7 arms. |
| APD-SVCCORE-010 | `_setting` duplicated byte-identically in two modules | ml#1154 | Closed by the same change — one shared resolver, two thin delegating wrappers. A source-scan test asserts neither handler re-implements the `getattr(settings, name, default)` body, so the duplication cannot grow back. |
| APD-DATA-006 | `record_access` could write a pre-edit snapshot over a committed tag edit | [juniper-data#263](https://github.com/pcalnon/juniper-data/pull/263) | New `DatasetStore.update_tags` performs the whole read-modify-write inside the same `_version_lock`, in one thread hop. The test uses `LocalFSDatasetStore` deliberately — `InMemoryDatasetStore.get_meta` returns the very object it stores, so both writers mutate one instance and the lost write **cannot be expressed**; a test written against the convenient store would have passed on the broken code. |
| APD-DATA-036 | Unguarded `int(content_length)` surfaced a malformed client header as a 500 | [juniper-data#261](https://github.com/pcalnon/juniper-data/pull/261) — same patch, as this register predicted | Returns an explicit 400. Reproduced before fixing: the raw `ValueError` escapes to starlette's `ServerErrorMiddleware`, confirming it never reaches the app's own `ValueError` handler. |
| APD-CASCOR-006 † | The same gap in juniper-cascor's copy | [juniper-cascor#527](https://github.com/pcalnon/juniper-cascor/pull/527) | Same fix; this fork already used a `set`, so the `security.py` line is byte-identical to the canonical service-core filter. 11 arms added, complementing the request-side coverage this fork already had (`test_validate_empty_string_key_is_invalid`) with the configuration side -- a blank key must never become a valid credential in the first place. The settings half was caught here by its own new test after a first pass updated only the docstring. |
| APD-DATA-034 † | The same handler in juniper-data, which had **no** `coerce_native_scalars` equivalent | [juniper-data#262](https://github.com/pcalnon/juniper-data/pull/262) | Same narrowing. This was the worse of the two: with no helper standing in the way, every serialisation fault here was reported as a client error. Both fixes confirmed to bite by reverting them (`assert 400 == 500`). |
| APD-CASCOR-004 † | The same gap in juniper-cascor's copy | [juniper-cascor#524](https://github.com/pcalnon/juniper-cascor/pull/524) | Same port, same shape, same mutation check. One pre-existing test needed isolating rather than weakening: `test_failed_auth_does_not_increment_rate_limit` fires **exactly** the default budget of 10 failed attempts and then asserts a valid key passes, so it now builds a generous throttle and keeps measuring the *identity-keyed limiter's* counters. That interaction is correct behaviour, not a regression — once an IP burns its failed-attempt budget it is throttled at the door regardless of the next credential, because `check()` necessarily precedes knowing the key is good. |
| APD-DATA-035 † | CORS registered innermost, so `SecurityMiddleware` answered every browser preflight 401 | [juniper-data#273](https://github.com/pcalnon/juniper-data/pull/273) | Fixed by **reordering** the middleware — CORS registered last, so it executes outermost — not by an `OPTIONS` bypass in `_is_exempt`, which would have exempted every `OPTIONS` request from auth *and* rate limiting. CORS short-circuits only a genuine preflight (one carrying `Access-Control-Request-Method`), so a plain `OPTIONS` still authenticates; that distinction is pinned as its own test. Reproduced first: `OPTIONS /v1/generators` with a valid `Origin` returned 401 and no `Access-Control-Allow-Origin`. Negative control confirms the reorder did **not** widen the origin allowlist — a preflight from a disallowed origin still gets 400 with no ACAO. 5 arms added; mutation-tested, 4 of the 5 fail against the unfixed ordering (the fifth guards the OPTIONS-bypass alternative and holds either way, by design). |
| APD-CASCOR-001b | The same defect in juniper-cascor's copy | [juniper-cascor#540](https://github.com/pcalnon/juniper-cascor/pull/540) | Same reorder, same five arms, same 4-of-5 mutation result, verified on the real non-exempt route `/v1/network/stats`. Both forks also gained a **second** fix the original entries did not name: with CORS innermost an ordinary cross-origin request rejected by auth carried no CORS headers either, so a browser saw an opaque CORS failure rather than the real 401. |
| APD-CASCOR-001a | Middleware ordering comment omitted `RequestBodyLimitMiddleware` | [juniper-cascor#540](https://github.com/pcalnon/juniper-cascor/pull/540) | Folded into `001b` rather than shipped separately as the register anticipated: a reorder cannot land beside an ordering comment it has just made doubly wrong. The identical comment existed in `juniper-data` — an unrecorded sibling, since this ID was filed as cascor-only — and was corrected in data#273. Both repos' `AGENTS.md` middleware tables documented the old order and were corrected too. |
| APD-DATA-004 | 501 detail echoed a raw `ImportError`, which batch-create then copied verbatim | [juniper-data#275](https://github.com/pcalnon/juniper-data/pull/275) | **The last open `Security` row in the register.** Fixed at the source — the 501 construction — rather than at the batch-create branch the entry names: single-create returns the same detail directly, so the leak was never batch-specific, and the curated install hints D1 exists to surface never leaked at all. The interpolation is now gated on `is_available()`: a declared capability gap keeps its hint, anything else is logged server-side and answered with a correlation id. Mutation-tested in **both** directions, which is why there are three tests — reverting the fix fails the two leak arms, and forcing redact-everything fails the new batch hint arm *and* the pre-existing single-path hint test. A leak-only suite would have passed a fix that silently destroyed D1. |
| APD-DCLIENT-001 | Exceptions carried no `status_code` / `detail` / `response`, so a 400 and a 422 were byte-identical | [juniper-data-client#158](https://github.com/pcalnon/juniper-data-client/pull/158) | Keyword-only parameters on the base class, populated on every response-derived branch, so **no existing single-positional-message call site changes** and locally raised errors simply report `status_code=None`. `FakeDataClient` was updated in the same PR — it is documented as a drop-in replacement, so a double raising the right type with `status_code=None` would let a consumer's test pass against behaviour production does not have. Its statuses mirror the service and are not interchangeable: 400 unknown generator (explicit `HTTPException`), 422 `ttl_seconds` (pydantic `Field(ge=1)`), 404 missing resource. **This is the reference implementation for `APD-CCLIENT-004` and `APD-RCLIENT-001`** — see §2.3. |
| APD-DCLIENT-003 | A FastAPI 422 `detail` **list** was f-string-interpolated into an unparseable repr | [juniper-data-client#158](https://github.com/pcalnon/juniper-data-client/pull/158) | Closed together with `-001`, and inseparable from it: the repr lost nothing only because there was nowhere else for the structure to go. `detail` now holds the list exactly as decoded while a `_render_error_detail` helper renders `body.seed: Field required` for the message. Three tooling findings are recorded in the PR and worth carrying: flake8-bugbear **B042** (an exception whose `__init__` does not forward to `super()` loses its attributes across pickle/copy — fixed with `__reduce__`, not with B042's own suggestion, which is the defect being closed); **mypy --strict** (a shared `**kwargs` dict infers `dict[str, object]` and silently abandons type checking on exactly the new arguments); and **bandit B403/B301** on the pickle round-trip test, where a prose comment beginning with the suppression marker is itself parsed as a directive. Mutation-tested: all 10 new arms fail against the unfixed hierarchy. |
| APD-CCLIENT-004 | Exceptions carried no context, and `_handle_response` dropped the status on four of five branches | [juniper-cascor-client#123](https://github.com/pcalnon/juniper-cascor-client/pull/123) | Port of the data-client contract, closing the retired `APD-CCLIENT-003` with it exactly as the retired-ID note predicted. `FakeCascorClient` claims full API parity, so its 29 raises were updated too — statuses **verified against juniper-cascor rather than assumed**: 404 not-found, 409 conflict, and 422 for both validation sites, because `input_size` is a pydantic `Field(ge=1)` and decision-boundary `resolution` a `Query(ge=, le=)`, and FastAPI answers a constraint violation 422, not 400. Its one local-state error ("Client is closed") deliberately carries none. **A second, unrecorded defect was fixed with it**: this client passed the FastAPI 422 `detail` list straight to the exception, so `str(exc)` was an unparseable repr — the `APD-DCLIENT-003` shape, never filed here. `APD-CCLIENT-002` (the dead 503 branch) was deliberately left open here and closed immediately afterwards in [#124](https://github.com/pcalnon/juniper-cascor-client/pull/124); wiring the branch for context in this PR is what let that fix be a single line. Mutation-tested: all 15 new arms fail unfixed. |
| APD-RCLIENT-001 | Exceptions carried no `status_code` / `detail` / `response` | [juniper-recurrence#124](https://github.com/pcalnon/juniper-recurrence/pull/124) | The third port, completing the group. This handler is a **near-verbatim copy of juniper-data-client's pre-fix code**, so it carried the `APD-DCLIENT-003` f-string interpolation too — also unrecorded here. Both fixed together. Mutation-tested: all 8 new arms fail unfixed. Note for anyone following the §4.6 anchors: this package lives inside the `pcalnon/juniper-recurrence` monorepo, not a repository of its own. |
| APD-CCLIENT-002 | `JuniperCascorServiceUnavailableError` was unreachable — an exhausted retry never reached `_handle_response` | [juniper-cascor-client#124](https://github.com/pcalnon/juniper-cascor-client/pull/124) | One line: `raise_on_status=False`. Retrying is unchanged (pinned by an arm asserting a retrying client still makes `retries + 1` attempts); only the give-up path differs, so a 503 that outlives its retries now raises the typed error with `status_code=503`, and 429/502/504 carry their real status instead of `None`. Non-breaking — the typed error subclasses the base, so consumers catching the base still catch it. **The reason it hid is the transferable finding**: every pre-existing test of the arm mounted `HTTPAdapter(max_retries=0)` first, so the branch had passing coverage that exercised it under a configuration production never uses. Mutation-tested 3 of 4; the fourth (transport failures still raise `ConnectionError`) passes both ways by design, guarding the plausible misreading of `raise_on_status=False` as "stop retrying". Also corrected a doc regression introduced one commit earlier by #123, whose `AGENTS.md` `status_code` row listed "retry-exhausted" among the `None` cases. |
| APD-SVCCORE-008 | No `py.typed`, so every consumer's type checker discarded the whole annotated surface | [juniper-ml#1237](https://github.com/pcalnon/juniper-ml/pull/1237) | **The register filed two rows; the defect was six packages.** A consumer-shaped `mypy --strict` probe against the *installed* packages reported `[import-untyped]` for all six in-repo sub-packages — `juniper-ci-tools`, `juniper-config-tools`, `juniper-doc-tools` and `juniper-model-core` were unrecorded siblings, the `APD-DCLIENT-001` shape again. All six fixed together. **This was a deviation from an existing convention, not an ecosystem gap**: `juniper-data-client` and `juniper-cascor-client` already ship `py.typed` with exactly this declaration, so the fix was aligned to their form and all eight packages now read identically. **The marker file alone would not have shipped it** — these packages carry no `MANIFEST.in`, so setuptools omits an undeclared `py.typed`; a file-only change would have looked right in review and produced six wheels without it, which is why `pyproject.toml` gains both the `package-data` entry and the `Typing :: Typed` classifier and the gate asserts them independently. Verified by building the wheels and installing them into a clean venv: the probe goes from 2 errors to `Success`. **Mutation-tested in both directions** — the negative arm (marker absent) is the original defect, and a *positive control* proves the types are genuinely live rather than merely un-errored: `require_auth="yes"` where `bool` is declared, and `service_name=123` where `str` is, are now both flagged, having type-checked clean before. Consumers do **not** inherit these packages' own strict errors (service-core 71, observability 8) — mypy silences site-packages by default, confirmed in the venv rather than assumed. |
| APD-OBS-002 | The same gap in `juniper-observability` | [juniper-ml#1237](https://github.com/pcalnon/juniper-ml/pull/1237) | Closed by the same change. Recorded separately because it is a separate distribution: PEP 561 markers are per-package, so this needed its own file, its own `package-data` entry and its own classifier. **A near-miss worth carrying forward** surfaced in `juniper-model-core` while authoring this: inserting `[tool.setuptools.package-data]` immediately after `packages.find`'s `include` line silently re-homed that table's `exclude = ["tests*"]` into the new one, which would have packaged the test suite into the wheel. Both tables stay valid TOML and nothing failed — only parsing the result showed it, and it is now gated by `test_package_data_does_not_swallow_packages_find_keys`. `APD-OBS-003` / `-004` remain open; closing them takes this package to zero. |
| APD-OBS-003 | `register_info_or_update` returned `Any` while its three siblings returned `T` | [juniper-ml#1245](https://github.com/pcalnon/juniper-ml/pull/1245) | **The `Any` was not laziness — it was the optional extra.** `prometheus_client` is an extra (`juniper-observability[prometheus]`), so the collector classes cannot be imported at module scope and there was no name to annotate with; the three siblings escape this because they take the class as a `factory` parameter and infer `T` from it. Fixed with a `TYPE_CHECKING`-only import, which the module's existing `from __future__ import annotations` renders free at runtime. **Both halves are pinned, because fixing one breaks the other**: a test asserts the annotation resolves to `Info`, and a subprocess test asserts importing the module still does *not* pull in `prometheus_client` — hoisting the import to module scope would satisfy the annotation test and break every install without the extra. Verified by positive control, not by absence of errors: `reveal_type` reports `prometheus_client.metrics.Info`, and both `x.nonexistent_method()` and `bad: int = ...` are now flagged where `Any` accepted them silently. Mypy's error **set** (not count) was diffed against `origin/main` — identical modulo the line shift from the added import block. A live quirk worth knowing: `typing.get_type_hints()` raises `NameError` on a `TYPE_CHECKING`-only annotation, so the tests pass `localns={"Info": Info}`; that `NameError` is itself asserted as the guard that the import stayed lazy. |
| APD-OBS-004 | Two `__all__` lists had to agree and nothing checked it | [juniper-ml#1245](https://github.com/pcalnon/juniper-ml/pull/1245) | **Takes `juniper-observability` to zero open rows.** `middleware/__init__.py`'s `__all__` (8 names) and the `# Middleware` block of the package `__all__` are maintained by hand in two files, each internally consistent, so a name added to the submodule and forgotten in the package breaks `from juniper_observability import <name>` for every consumer with nothing failing. The pre-existing `test_no_unexpected_public_symbols` pins the package list against a hardcoded set but says nothing about the *relationship* between the two. Asserted as a **subset, not equality** — the package legitimately carries health / logging / prometheus / sentry names the submodule must not. A second arm asserts the re-exported objects are `is`-identical to the submodule's, which the name check cannot see: mutation-tested, a shadowing redefinition in the package passes the subset arm and fails only the identity arm. Both lists agree today; this closes the gap that let them drift. |
| APD-DATA-009 | Batch-create answered `201 Created` when every item in the batch had failed | [juniper-data#280](https://github.com/pcalnon/juniper-data/pull/280) | **201 when at least one dataset was created, 200 when none was.** Partial success stays `201` — resources genuinely were created, and the defect is specific to the all-failed case. `200` is not "success" either: it means the batch was processed and the per-item `results` are the authority, which is **the contract the three sibling batch routes already use** — `batch-delete` / `batch-tags` / `batch-export` declare no `status_code` at all. batch-create was the odd one out, and that framing is what made the fix narrow. Deliberately **not `207 Multi-Status`**: per-item statuses are a redesign of the endpoint's contract and a new semantic for every client to learn, which belongs with the deferred `-026`–`-033` group. Non-breaking for the official client, verified in source: `juniper-data-client`'s `_request` returns on `response.ok`, which `requests` defines as any status `< 400`. Scope was checked rather than assumed — only two routes declare `201` (single-create's is correct, it raises on failure) and **juniper-cascor has no fork**, so this is the rare entry with no unrecorded sibling. **The defect was pinned by a passing test, four times.** `test_counts_correct_on_all_failures` asserted `201` *beside* `total_created == 0`, and three tests in `test_api_routes.py` did the same — one of them also asserting `total_created == 0` on the next line. The contradiction was written down four times and never read as one, because the status assertion was incidental boilerplate copied from the success-path tests. All four updated, none weakened. **Mutation-tested in both directions**: reverting fails 3 arms (all-failed), and over-applying — downgrading whenever `total_failed > 0` — fails 3 *different* arms (partial success). An all-failed-only suite would have passed the wrong fix. Docs corrected with the code: `AGENTS.md` said "returns 201" unconditionally and the API reference documented no status code for this endpoint at all. |
| APD-DATA-013 | No `RequestValidationError` handler, so the 422 contract was FastAPI's default | [juniper-data#281](https://github.com/pcalnon/juniper-data/pull/281) | **Booked FIXED for the OWNERSHIP half only; shape unification is deliberately deferred** — read this before treating the row as fully closed. What is fixed: an explicit handler now owns the 422 response. It is **byte-identical** to FastAPI's default (verified against a payload baseline captured before the change), so nothing a caller sees changed — what changed is that a FastAPI upgrade reshaping its default can no longer silently break `juniper-data-client`, which parses that `detail` **list** per `APD-DCLIENT-003`. **The byte-compatibility is itself the trap, and it is what makes this entry instructive**: every payload assertion in the new tests passes just as happily with the handler unregistered, so ownership had to be asserted directly (`handler.__module__` is ours). Mutation-tested — unregistering the handler fails **that test and nothing else**. Without it the entire point of the entry would have been untested; this is the vacuous-pass class arriving by a new route, where the *correct* output is what hides the missing mechanism. What is NOT fixed: `detail` is still `list` on 422 and `str` elsewhere. Unifying them needs a response envelope (RFC 9457) — the deferred `-026`–`-033` group. **Flattening the list is not an interim fix**: it would destroy the per-field structure the client was just built to consume, so a test now pins the split and fails *here* rather than downstream (mutation: flattening fails 5 arms). The API reference documented `{"detail": "..."}` as **the** error format — a string, wrong for every 422 the service has ever returned — and now documents both shapes. |
| APD-DATA-014 | The 400-vs-422 split fell out of exception-subclass MRO rather than a decision | [juniper-data#281](https://github.com/pcalnon/juniper-data/pull/281) | **The statuses are unchanged, deliberately** (owner decision, 2026-08-23): unifying on 422 would break consumers checking 400 and would not even remove the split, since "unknown generator" stays 400. The defect was that the split was *accidental*, so the fix is that it is now a rule — written in the code and the API reference, and pinned by a parametrized table over all five rejection paths. The rule: **422** = violates the declared schema, rejected at the boundary before the handler runs; **400** = schema-valid but semantically wrong for the generator it names. `params` is typed `dict[str, Any]`, so the boundary cannot check it and only the resolved `params_class` can — that is the whole reason the two paths differ, and it reads as arbitrary until stated. `except (ValueError, ValidationError)` became `except ValueError`: **pydantic's `ValidationError` subclasses `ValueError`** (verified — MRO is `ValidationError → ValueError → Exception`), so naming both implied a distinction that does not exist, which is literally the finding. Mutation-tested: changing the 400 to 422 fails 2 arms. |
| APD-DATA-007 | The metadata read-modify-write was atomic within a process but not between processes | [juniper-data#282](https://github.com/pcalnon/juniper-data/pull/282) | **This is the caveat `APD-DATA-006`'s own fix wrote down.** That fix moved the tag read-modify-write into one hop under `_version_lock`, and its docstring said plainly that a `threading.Lock` orders threads of one interpreter and nothing more. Reproduced: twelve processes each adding one distinct tag left **two** tags on disk. **Both whole-document writers are fixed, not just the one this entry names** — `record_access` fires on every metadata read and every artifact download and also rewrites the entire document, so locking only `update_tags` would have relocated `-006` rather than closed it. Mechanism: an advisory `flock` on a per-dataset lock file, via a new `_meta_write_lock` hook whose default is a no-op (correct for `InMemoryDatasetStore`, whose state does not outlive the process). A **separate** lock file is required because the write path replaces the metadata file by rename — a lock on the old inode guards nothing after the first swap — and it is never unlinked, since deleting it lets another process lock a different inode of the same name and walk into the critical section. **A second, unrecorded defect surfaced during the reproduction**: the BUG-JD-03 atomic-write temp path was derived from the final path alone, so concurrent writers shared one temp file and the loser died with `FileNotFoundError` mid-write. Temp paths now carry a pid+uuid segment. **The first race test was VACUOUS and the mutation run is what caught it** — its barrier released workers as each subprocess was *spawned*, but `Popen` returns before the child imports anything, so the release file already existed at boot and start-up jitter serialised them; with the lock disabled both race tests still passed. The barrier is now two-phase (workers announce readiness after their store is up; the parent releases only once all have). Against unfixed code the tests report "7 of 8 tag writes were silently lost" and "expected exactly 8 accesses, counted 1". Mutation: disabling the lock fails 3 arms, restoring the deterministic temp path fails 1. **Reachability: LATENT today** — the entrypoint calls uvicorn with no `workers` argument and exposes no flag for it, and compose declares no replicas, so there is one process. It goes live the moment anyone runs multiple workers or replicas against one storage path, and the failure is silent data loss. CodeQL flagged the lock file world-readable on the first push; fixed in code (`0o600`) rather than dismissed, which cleared the thread automatically. |
| APD-DATA-012 | `/filter` tie order was non-deterministic — a stable sort fed by an unordered enumeration | [juniper-data#283](https://github.com/pcalnon/juniper-data/pull/283) | **`created_at` alone is not a total order**, and `list.sort` is stable, so rows sharing a timestamp came back in whatever sequence the enumeration produced. `Path.glob` specifies no ordering and — measured — does not deliver sorted order even on ext4. The demonstration that settles it: the same six datasets fed in two enumeration orders produced exactly reversed pages, so the output was a function of directory-entry order, not of the data. Fixed by sorting on `dataset_id` then stably by `created_at` descending, breaking ties by id **ascending** — reproducible across calls, processes, and *both* store implementations, whose enumeration orders otherwise disagree by construction (glob order vs dict insertion order); nothing had ever required them to agree. `list_all_metadata` also sorts its glob so the enumeration beneath is deterministic for any other consumer. Mutation: dropping the tiebreak fails 2 arms, unsorting the glob 1. The API reference documented this endpoint in **two** places, both listing only `limit`/`offset`; both now carry `cursor` and an ordering section. |
| APD-DATA-011 | Offset pagination drifted — an insert repeated a row across pages, a delete skipped one | [juniper-data#283](https://github.com/pcalnon/juniper-data/pull/283) | Closed together with `-012`, and **`-012`'s fix is what made this one possible**: keyset pagination needs a total order to name a position in. Reproduced — an insert between two fetches returned dataset `0003` on both pages. A cursor encodes one row's position in `(created_at DESC, dataset_id ASC)` and a page is everything strictly after it, so nothing inserted or deleted ahead of the cursor can shift it. **Purely additive** (owner decision, 2026-08-23): `offset` behaves exactly as before and is kept, not deprecated — it remains right for a one-shot page; `next_cursor` is returned in *both* modes so an offset caller can switch to a stable walk without a round trip; `DatasetListResponse` gains an optional field; and `juniper-data-client` needed no change at all, because it exposes no filter method. Two misuses are rejected rather than silently resolved: `cursor` with a non-zero `offset` (400 — passing both means one is misunderstood; `offset=0` is explicitly allowed since the guard must not fire on the parameter's own default), and a cursor the service did not issue (400, per the `-014` rule). Mutation: reversing the `created_at` comparison fails 2 arms, reversing the id comparison 1, removing the guard 1. |
| APD-DATA-010 | Batch export dropped requested datasets with no signal to the caller | [juniper-data#284](https://github.com/pcalnon/juniper-data/pull/284) | **Closes the last open `Correctness` row in `juniper-data`.** The entry names one path — a deletion racing the stream, whose branch carried the comment *"skip quietly"* — but reproduction found **two**, and the unnamed one is the likelier: an id that simply does not exist is filtered out at the existence pre-check, nowhere near a concurrent deletion. Requesting 4 ids returned 200 with 3 members and nothing identifying the fourth. Both are fixed. **The signal had to go inside the archive**: the response is streamed, so the status line and headers are on the wire before the first artifact is read and neither can report a dataset that disappears later. A header would have covered the pre-check path only, which is worse than nothing — one mechanism with partial coverage invites the caller to trust it. So the ZIP gains a `manifest.json` with `requested` / `exported` / `missing`, the last mapping each absent id to `not_found` or `vanished_during_export`; `requested` always equals `exported` plus the keys of `missing`. **Emitted only when something is missing**, so a complete export is byte-for-byte what the endpoint always produced and no consumer changes — the manifest's *presence* is the signal. Mutation-tested in that direction too: emitting it unconditionally fails 2 arms, restoring the silent mid-stream skip fails 1, dropping the pre-check accounting fails 3. The omission is logged at WARNING with **counts only** — `dataset_ids` is caller-controlled and this module's ERR-08 idiom keeps caller strings out of log records, the same discipline CodeQL's log-injection rule enforced on data#275. **Two pre-existing tests pinned the silence** (`len(names) == 1`, and a bare `namelist()` equality) and were updated rather than weakened — the third such instance in this arc. |
| APD-SVCCORE-006 | No package base exception — three exceptions subclassed `RuntimeError`, two `KeyError`, and nothing tied them together | [juniper-ml#1297](https://github.com/pcalnon/juniper-ml/pull/1297) | **Booked FIXED for 4 of the 5, with the fifth excluded BY DESIGN — the marker says so.** `JuniperServiceCoreError` is now a base of `AuthPostureError`, `DependencyFloorError`, `WorkerRegistryFullError` and `SnapshotNotFoundError`, **in addition to** each one's original base rather than instead of it: an existing `except RuntimeError` / `except KeyError` handler is untouched, and `SnapshotNotFoundError` is raised where a mapping lookup would be, so callers legitimately treat it as a `KeyError`. Exported **eagerly** from the package root beside `__version__`, because `except JuniperServiceCoreError` must not require the lazy PEP 562 machinery to import fastapi / pydantic-settings to resolve the name; `exceptions.py` is dependency-free and the blocked-import smoke test now proves the base is reachable with both blocked. **The exclusion**: `UnknownTunableError` keeps `KeyError` alone. `websocket/tunables.py` is pinned stdlib-only and standalone by two tests, one of which loads it **by file path bypassing the package `__init__`** — importing the package base there erases that property, in order to add a base to the one exception in the package with no production consumer. Attempted, observed the stdlib-only test fail, and backed out rather than widening its allowlist; a test now pins the exclusion so it reads as a decision. Mutation: a base deriving from `RuntimeError` fails 1 arm, dropping a legacy base fails 1, adding the base to the excluded exception fails 2. |
| APD-SVCCORE-014 | `DependencyFloorError` rendered the structured violations into prose and discarded them | [juniper-ml#1297](https://github.com/pcalnon/juniper-ml/pull/1297) | `check_dependency_floors` computes a `list[FloorViolation]` — distribution, floor, installed — and the raise site kept only the formatted message, so a caller wanting to report *which* distribution to upgrade had to parse the text back apart. The error now carries `violations`; the message is byte-identical and single-argument construction still works. **A draft carried a `__reduce__` override, and the mutation run is what exposed it as redundant**: removing it left every test passing, which cannot happen if it is load-bearing. CPython's `BaseException.__reduce__` returns `(cls, args, self.__dict__)` whenever the instance dict is non-empty, so the attribute is restored automatically and `args` here is just the message, which the signature takes positionally. The override was deleted and its rationale corrected rather than left as dead code justified by a false claim. **Carry this to `APD-DCLIENT-001`**, whose fix carries the same override with the same reasoning — harmless, since it reproduces the default, but the stated justification is wrong there too. |
| APD-SVCCORE-009 | The public surface is declared three times by hand and nothing checked the three agreed | [juniper-ml#1298](https://github.com/pcalnon/juniper-ml/pull/1298) | `__all__`, the `_LAZY_EXPORTS` name→module map that PEP 562 `__getattr__` resolves through, and a `TYPE_CHECKING` import block that exists so static analysis sees the same names. **They agreed already — which is precisely when the guard is worth adding**, because the failure mode is a name exported but unresolvable (`AttributeError` for a consumer, including through `import *`) or resolvable but invisible to every type checker, and neither surfaces until someone hits it. The lists are read from source with `ast`, not from the imported module: `TYPE_CHECKING` is `False` at run time, so its block is invisible to a live import and it is one of the three things under test. The invariant is `set(__all__) == set(_LAZY_EXPORTS) | EAGER_EXPORTS`, where the eager set (`__version__`, `JuniperServiceCoreError`) is itself asserted to be genuinely bound at import so the documented exception cannot quietly grow. **A behavioural arm resolves every exported name**, and mutation shows why it earns its place: pointing a lazy entry at a nonexistent module fails *only* that arm — every list comparison still passes. Adding a phantom name to `__all__` fails 2 arms; an empty parse fails the anti-vacuous guard. |
| APD-SVCCORE-017 | `__dir__` returned exactly `__all__`, so `dir()` was a strictly smaller view than the module | [juniper-ml#1298](https://github.com/pcalnon/juniper-ml/pull/1298) | Defining `__dir__` **replaces** the default rather than extending it, so `sorted(__all__)` hid `__name__`, `__file__`, `__doc__`, `__path__` and every eagerly bound name. That inverts the purpose: a `__dir__` on a PEP 562 module exists to *add* the lazily resolvable names — the ones `globals()` cannot know about — not to remove the ones already there. Now `sorted(set(__all__) | set(globals()))`; REPL completion and `inspect`-style tooling both read it. Mutation: reverting fails 1 arm. |
| APD-SVCCORE-004 | `Retry-After` truncated toward zero, so a rate-limited caller was told to retry immediately | [juniper-ml#1300](https://github.com/pcalnon/juniper-ml/pull/1300) | **The only live-in-production row in this package's `R` group** (juniper-recurrence is the sole `.security` consumer). `reset_in` was `int(window - elapsed)`; any sub-second remainder became `0`, and a client obeying `Retry-After: 0` retries at once into a limiter guaranteed to reject it again. **Not an edge case** -- measured on a 1-second window, the header read `0` at 0.30s, 0.60s, 0.90s *and* 0.99s in, i.e. at every rejection; on the 60-second default it is the final second of each window, which is exactly where a burst that just tripped the limit lands. Fixed by rounding **up** with a floor of 1. **The rounding direction is the fix, not the floor**, and the mutation run pins that: `max(1, int(...))` -- the obvious partial repair that satisfies "never zero" -- still under-reports every other remainder and fails 2 arms, while full truncation fails 7. Waiting a fraction too long costs the caller nothing; waking a fraction early reproduces the defect. Applied to the allowed path too, which feeds `X-RateLimit-Reset`: the two headers describe the same instant and carried the identical truncation, so they were free to disagree by a second. |
| APD-SVCCORE-007 | The rate limiter is per-process, and nothing said so where a consumer would look | [juniper-ml#1303](https://github.com/pcalnon/juniper-ml/pull/1303) | **Booked FIXED for DISCLOSURE only -- the constraint itself is deliberate and unchanged, and the marker says so.** The register is right that this is "not an oversight": fixed-window counters live in memory, and exact fleet-wide enforcement needs a shared store this package intentionally does not provide. So the question was whether a fix is owed at all, and the answer is that the *behaviour* is fine while the *disclosure* was not. `RateLimiter` said only "suitable for single-process deployments" -- the scope without the consequence -- and `build_rate_limiter`, the function a consuming service actually calls, said nothing at all, so a caller choosing `requests_per_minute` had no way to learn that four replicas admit four times the configured budget. **The standard already existed in the same file**: `FailedAuthThrottle` documents the replica multiplication and the shared-store requirement explicitly. One control explaining the limitation while its sibling stayed quiet is how a reader concludes the quiet one does not have it; the two now agree and a test asserts they keep agreeing. Reachability checked, not assumed: recurrence's entrypoint exposes no `workers` setting and compose declares no replicas, so this is not currently violated -- the same one-config-change-away shape as `APD-DATA-007`. Pinning prose follows this repo's own `NetGuaranteeDocTest` precedent: when the guarantee a reader relies on *is* the prose, the prose is what has to be pinned. Mutation: removing the factory's disclosure fails 1 arm. |
| APD-DCLIENT-002 | Public `validate_npz_contract` raised bare `ValueError`, escaping the hierarchy `-001` established | [juniper-data-client#161](https://github.com/pcalnon/juniper-data-client/pull/161) | **The register row anchors one raise site; the defect was nine** — every `raise` in `contract.py`, all escaping through the single public entry point (the `APD-SVCCORE-008` scope-shape again). The new `JuniperDataContractError` subclasses **both** `JuniperDataClientError` and `ValueError`, deliberately: the validator has documented `Raises: ValueError` since it shipped, and the sole real consumer pins that contract — juniper-recurrence's routers catch `(JuniperDataClientError, ValueError)` and its `routers/_common.py` dispatches on `isinstance(exc, (JuniperDataValidationError, ValueError))` — so plain single inheritance from the base would have been a breaking change this entry does not license. Deliberately **not** a subclass of `JuniperDataValidationError`, whose docstring binds it to HTTP 400/422 semantics; a contract violation is local (post-download), so `status_code` / `detail` / `response` stay `None` per the base-class convention for locally raised errors. Messages are byte-identical. The existing `pytest.raises(ValueError)` tests are deliberately unchanged — they *are* the back-compat pin — and a new arm per raise site asserts the concrete type; mutation-verified: reverting a single site to bare `ValueError` fails exactly its arm. The `dt`-shape site (`contract.py:100`) had no coverage at all before this; it does now. The export test resolves the module via `importlib.import_module` rather than a second module-level import, to stay clear of CodeQL's `py/import-and-import-from` merge-blocker. |
| APD-DCLIENT-007 | Redundant `pass` after a docstring in the exception classes | [juniper-data-client#162](https://github.com/pcalnon/juniper-data-client/pull/162) | The register filed six; `APD-DCLIENT-001` had already given the base class a real body, so **five** remained — a docstring is a complete class body, and the `pass` lines were dead weight (`APD-DCLIENT-002`'s new class was authored without one). **A second, unrecorded item was fixed with it**: the `__reduce__` override shipped by `APD-DCLIENT-001`, whose removal `APD-SVCCORE-014`'s verification note explicitly ordered carried here. It reproduced CPython's default byte-for-byte — `BaseException.__reduce__` returns `(cls, args, self.__dict__)` whenever the instance dict is non-empty — while its docstring claimed the default rebuilds from `args` alone; removing it leaves both pickle/copy round-trip suites green, which cannot happen if it is load-bearing. **The mutation run corrected the fix's own prose before it shipped**: the first draft claimed the pickle pin catches dropping the message from `super().__init__` — measured false, because that mutation empties `args` on *both* sides and every equality arm still holds; what the pin actually catches is B042's own remedy — forwarding the kw-only extras into `super()` puts them in `args` and the rebuild's `cls(*args)` raises `TypeError` — and that mutation demonstrably fails the test. The docstrings now say exactly that and no more. Deleting the method tripped the sequence-safety symbol screen as `LOST`, correctly; waived with the `Allow-Symbol-Loss: method:JuniperDataClientError.__reduce__` trailer in the branch's single commit, proven `WAIVED` locally before pushing per the waiver discipline. |
| APD-CCLIENT-010 | Redundant `pass` after a docstring in the exception classes | [juniper-cascor-client#126](https://github.com/pcalnon/juniper-cascor-client/pull/126) | Port of `APD-DCLIENT-007`'s close (data-client#162) to this client's copy of the shared contract. The register filed eight; `APD-CCLIENT-004` had given the base a real body, so **seven** remained. **The same second, unrecorded item travelled with the port and was fixed with it**: cclient#123 copied juniper-data-client's `__reduce__` override *together with its false docstring rationale* — the §2.3 sibling-alignment convention faithfully propagating a defect in its reference implementation, which is the drift-group shape inverted. Removal proven by deletion-under-test (full suite green without it); the pickle pin re-documented to what it actually catches (extras forwarded into `args` → `cls(*args)` raises `TypeError`, mutation-verified). Symbol screen waived with `Allow-Symbol-Loss: method:JuniperCascorClientError.__reduce__` in the single commit, `WAIVED` proven locally **before** the first push this time. `juniper-recurrence-client` carried the third copy of the same override and rationale — no register row names it, so this sentence is its record: fixed immediately after in [juniper-recurrence#128](https://github.com/pcalnon/juniper-recurrence/pull/128), completing the sweep across all four carriers (that port had no redundant `pass` statements — its subclasses were already docstring-only). |
| APD-RCLIENT-003 | Ships `py.typed` and the `Typing :: Typed` classifier with no mypy config | [juniper-recurrence#127](https://github.com/pcalnon/juniper-recurrence/pull/127) | The row understates: no mypy configuration existed anywhere in the **monorepo** and no lane ran mypy at all — configuration absent *and* execution absent. Fixed with both halves: `[tool.mypy]` (strict) in the package's own `pyproject.toml` — landing exactly at the row's anchor — plus a pre-commit mypy hook scoped to the package (`types-requests` / `urllib3` as hook deps, a per-module override for the guarded optional `juniper-observability` import), enforced by the existing `ci-pre-commit` lane. **Strict surfaced one real looseness, and only one**: `_parse_json` returned `Any`, silently laundering every public method's declared `dict[str, Any]` — all nine strict findings funnelled through that single function. It now validates the body is a JSON object and raises the typed client error on a syntactically valid non-object body, which previously surfaced as a downstream `AttributeError` in the caller; a new test arm pins the non-object case. Vacuous-pass discipline applied twice, because a mis-scoped hook succeeds silently: a planted type error fails the identical invocation (the config bites), and the hook's `files` regex is proven to match exactly the package files and nothing else. **Deliberately not done**: the app and model packages *also* ship `py.typed` with nothing checking them — unrecorded siblings of this row's class, measured at 0 and 2 hook-flag findings respectively and named here rather than silently skipped. |
| APD-DCLIENT-005 | Version literal drifted across six file headers | [juniper-data-client#164](https://github.com/pcalnon/juniper-data-client/pull/164) | **Scope re-derived past the register's own correction: nine files carried the header, in four distinct values** — `tests/conftest.py`, `tests/test_fake_client.py` and `tests/test_fake_client_batch.py` sat at `0.3.1`, below the register's floor of three values across six copies. All nine aligned to the real version, and `tests/test_file_header_versions.py` now scans every `*.py` under the package and `tests/`, failing with the lagging files named, behind an anti-vacuous floor (≥9 matches) so a regex or path typo cannot pass by matching nothing. Mutation: de-aligning one header fails the test naming exactly that file. The deliberate cost is stated in the CHANGELOG — a release version bump now touches the headers loudly instead of drifting silently. |
| APD-DCLIENT-006 | `validate_npz_contract -> str` where a `Literal` is available | [juniper-data-client#164](https://github.com/pcalnon/juniper-data-client/pull/164) | Returns `ContractKind = Literal["tabular", "sequence"]`; the constants become `Final[ContractKind]`, so a constant's value and the Literal's members cannot drift apart — mutation-verified: editing a value to `"tabularX"` fails type checking. Exported from the package root for consumer annotations; source-compatible, and local strict mypy *improves* 18 → 16 pre-existing findings with no new files. **The merge surfaced an adjacent unfiled defect**: `constants.py` declares an `__all__` covering 28 of ~142 module-level constants, and CodeQL's `py/unused-global-variable` blocked the green PR on the two touched names via unresolved review threads — fixed by exporting the three names this PR introduces (never by resolving threads by hand). **The remaining ~114 unexported constants are an unfiled finding recorded here**: complete `__all__` in one pass with a drift test asserting every public module assignment is exported, rather than piecemeal as CodeQL trips over whatever lines a future PR happens to touch. **Completed** ([juniper-data-client#172](https://github.com/pcalnon/juniper-data-client/pull/172)): all 141 public constants exported (AST-measured 141/141/0 missing/0 phantom), regenerated in file order grouped by the module's section headers, with `tests/test_constants_all_drift.py` pinning set-equality both directions, no duplicates, and runtime resolvability — mutation-verified in both directions. |
| APD-DCLIENT-004 | `_normalize_url` never validates `netloc` — hostless URL fails opaquely | [juniper-data-client#165](https://github.com/pcalnon/juniper-data-client/pull/165) | The guard is `juniper-recurrence-client`'s, verbatim in spirit: an empty `netloc` after scheme-defaulting raises `JuniperDataConfigurationError` naming the offending value, at construction — where the misconfiguration is, instead of deep inside `requests` on the first call. Six hostless shapes parametrized (empty, whitespace, bare `http://`/`https://`, path-only bare and scheme-qualified) plus a catchable-as-base arm; the seven pre-existing normalization tests pin that every valid form is untouched. Mutation: deleting the guard fails exactly the 7 new arms. Closes the data-client half of §2.3's base-URL `netloc` drift row. Subsequently hardened by [#166](https://github.com/pcalnon/juniper-data-client/pull/166) — case-insensitive scheme matching, `hostname` over `netloc` — see the `APD-CCLIENT-005` row for the finding's origin. |
| APD-CCLIENT-005 | No base-URL normalisation or validation at all | [juniper-cascor-client#129](https://github.com/pcalnon/juniper-cascor-client/pull/129) | The register's restatement held: zero `urlparse`/`netloc` hits in the package, `rstrip("/")` the entire treatment — the only client with neither guard. Full port of the sibling normalisation (whitespace, scheme default, netloc guard, trailing slash, trailing `/v1` strip — the last also repairing the silent double-`/v1` `api_url` a suffixed base produced, pinned by its own arm). Ships the **new `JuniperCascorConfigurationError`**, completing the sibling exception alignment; subclasses the base so existing catch-alls are unaffected. Mutation: deleting the guard fails exactly the 7 hostless arms; suite 398 green. **A Cursor Automation review thread then blocked the green PR with a confirmed finding — and it was right**: the ported scheme check was case-sensitive, so `HTTPS://host` was re-prefixed into `http://HTTPS://host` — a silent TLS downgrade sending `X-API-Key` over HTTP to hostname `https` — and a `netloc` guard passes userinfo-only authorities (`http://user:secret@`) that `hostname` rejects. Fixed in the same PR (case-insensitive matching, `parsed.hostname`; mutation-verified — reverting the `.lower()` fails both uppercase arms), and since the flaw was copied *from* the reference implementation, swept back into both siblings: [data-client#166](https://github.com/pcalnon/juniper-data-client/pull/166) and [recurrence#129](https://github.com/pcalnon/juniper-recurrence/pull/129). The reference implementation propagating its own flaw outward is the §2.3 drift class operating in reverse. **Deliberately not touched, recorded**: the WS stream classes (`CascorTrainingStream` / `CascorControlStream`) keep their separate `rstrip`-only treatment — the `ws://` scheme family needs its own defaulting rules, and no register row names them. Both §2.3 base-URL drift rows are now struck through in place; the remaining sibling-drift row is the idempotency one, blocked on `APD-ECO-001`'s owner decision. |
| APD-RCLIENT-002 | 30 s scalar timeout governs synchronous `train`/`crossval`; no per-call override | [juniper-recurrence#130](https://github.com/pcalnon/juniper-recurrence/pull/130) | Both methods gain keyword-only `timeout: Optional[float] = None`, forwarded to `_request` only when set so `_request`'s `setdefault("timeout", self.timeout)` keeps governing the absent case; an explicit `None` falls back to the client-wide value and **never reaches `requests`**, where it means wait-forever — pinned by its own arm. The row named exactly the right scope: `train` fits in-request and `crossval` multiplies that by `n_folds`; the other public calls stay on the client-wide scalar. Also fixed en route: the timeout error message interpolated `self.timeout` unconditionally, which with an override present would misreport what governed the failed request — it now reports the effective value. New tests assert at the transport layer (`responses.calls[0].request.req_kwargs["timeout"]`), not on client state. Mutation-verified both ways: dropping the `train` forwarding fails exactly the train-override + effective-message arms (crossval arm unaffected); reverting the message to `self.timeout` fails exactly the message arm. Suite 72 green at 98.29% against the 90% CI gate; `mypy --strict` + full pre-commit clean. The row's filed `client.py:217`/`:311`/`:405` anchors were dead (the 08-24 merges moved the file); refreshed in place. **Advances but does not close `APD-ECO-003`** — its recurrence arm is done; the data-client / cascor-client arms remain, and which of their methods warrant the kwarg is part of that row's decision. |
| APD-CCLIENT-006 | `_dispatch` unguarded — one raising listener tears down the stream | [juniper-cascor-client#135](https://github.com/pcalnon/juniper-cascor-client/pull/135) | The disconnect path's guard ported verbatim into `_dispatch`: per-callback try/except, `logger.exception` at ERROR naming the message type. The asymmetry was load-bearing, not cosmetic — `_dispatch` runs inside `stream()` *before* the yield, so one faulty listener killed the whole iterator (and `listen()`), skipped every later listener for that message, and surfaced as a stream failure. Scope verified as exactly `CascorTrainingStream._dispatch`: `CascorControlStream` has no listener dispatch (command_id-correlated futures), and the two register anchors (`:453-457` vs `:459-471`) still matched the file at fix time. Two arms added — a unit arm (second listener still runs, ERROR log names the type) and a stream-level arm (both messages still reach the consumer *and* the surviving listener), the latter pinning the teardown class itself. Mutation: restoring the pre-fix bare loop fails exactly the two new arms, both on the leaked `RuntimeError`. Suite 403 green at 97.47%; full pre-commit clean. The §2.3 same-file table row is struck through in place — both rows of that table are now closed. |
| APD-DCLIENT-008 | `create_dataset` — 9 positional-or-keyword params, 3rd a bare boolean | [juniper-data-client#171](https://github.com/pcalnon/juniper-data-client/pull/171) | The row's own caution — "kw-only enforcement is breaking only if someone calls positionally" — was discharged **before** the edit: an AST census across all 7 consuming repos found 223 `create_dataset` calls, **zero** passing a third positional argument (14/56/153 at 0/1/2 positionals; the same-named non-client receivers are h5py's `Group.create_dataset` in cascor snapshot code). `*` inserted after `params` on **both** `JuniperDataClient` and `FakeDataClient` — the fake mirrors the boundary so a consumer test calling it positionally fails exactly as production would. A new signature-pin suite holds the convention: keyword-only kinds on both classes, real↔fake signature equality, third-positional `TypeError`, keyword form green. Mutation-verified per side: removing the real client's `*` fails exactly the real-kind pin + parity; removing the fake's fails exactly the fake-kind pin + parity + the DID-NOT-RAISE arm. Suite 289 green; full pre-commit clean. The filed `client.py:412-423` anchor was dead (the 08-24 merges moved the file); refreshed in place. |
| APD-CCLIENT-013 | `backoff_factor` hardcoded, not constructor-configurable | [juniper-cascor-client#136](https://github.com/pcalnon/juniper-cascor-client/pull/136) | Constructor parameter in the sibling position (before `api_key`) — safe because an ecosystem AST census of `JuniperCascorClient(...)` construction found 72 calls, none passing more than one positional argument. Stored on the instance and asserted where it takes effect: the mounted adapter's `Retry.backoff_factor`. Mutation: re-hardcoding the `Retry` argument fails exactly the constructor arm while the default arm stays green. |
| APD-CCLIENT-009 | `pool_connections` omitted where both siblings set it | [juniper-cascor-client#136](https://github.com/pcalnon/juniper-cascor-client/pull/136) | New `DEFAULT_POOL_CONNECTIONS: int = 10` (both siblings run 10/10); both knobs passed to `HTTPAdapter` explicitly. **The first draft's runtime test was provably vacuous for this defect** — requests' own `pool_connections` default is also 10, so the mutation run (drop the kwarg) passed it; the defect is the silent dependence on urllib3's default, not a wrong value. The shipped gate therefore pins the **call site** (AST: every `HTTPAdapter(...)` in `client.py` passes both knobs by keyword) alongside the runtime values, and the same mutation now fails exactly that arm. The §5.4 lesson operating as designed: the mutation ran before the claim was written. |
| APD-CCLIENT-007 | mypy targets 3.11 while the package requires ≥3.12 | [juniper-cascor-client#136](https://github.com/pcalnon/juniper-cascor-client/pull/136) | `[tool.mypy] python_version` 3.11 → 3.12; the strict gate had been type-checking a Python the package refuses to install on (`requires-python = ">=3.12"`). Full pre-commit re-run green **at the new target** — the fix surfaced zero new findings. New `tests/test_tooling_python_floor.py` parses `pyproject.toml` and pins the mypy target to the `requires-python` floor so the two cannot drift apart again; mutation: reverting to 3.11 fails exactly that gate. |
| APD-CCLIENT-012 | `auto_pong` is a positional boolean trap with no removal date | [juniper-cascor-client#137](https://github.com/pcalnon/juniper-cascor-client/pull/137) | The **trap half** is closed: `*` inserted before `auto_pong` on all three constructors — `CascorTrainingStream`, `CascorControlStream`, and `FakeCascorTrainingStream` (the fake must fail a positional call exactly as production would). Census: 49/32/35 constructions, max 1 positional anywhere, every `auto_pong` use already by keyword — the boundary broke no caller. `tests/test_ws_signature.py` pins the keyword-only kind on all three plus a positional-`TypeError` arm; mutation on the training stream fails exactly its own two arms, leaving control/fake green. The **removal-date half** — deprecating the legacy `auto_pong=False` ping-yield posture — is the deprecation-machinery question `APD-ECO-007` already tracks, deliberately not decided here; the §4 marker says so in place. |
| APD-CCLIENT-011 | `create_network(**kwargs: Any)` — 11 real parameters, none typed | [juniper-cascor-client#138](https://github.com/pcalnon/juniper-cascor-client/pull/138) | Grounded in the server's `NetworkCreateRequest` as contract of record, which refuted three docstring claims en route: every field is server-defaulted (the "(required)" trio was false — a bare call legally creates a default network, and canopy relies on it), `epochs_max` left the create surface and bodies sending it are **silently ignored** by pydantic's extra-handling, and three registry-backed fields were undocumented. The 14 current fields become keyword-only `Optional` params (only set values sent; server defaults authoritative; registry names deliberately `str`-not-Literal so newer servers stay callable). `**extra` kept as the forward-compat channel — canopy's dict-splat adapter catches only `JuniperCascorClientError`, so a strict signature would have turned unknown keys into an uncaught TypeError crash path — but it now **WARNs naming the keys**, converting the silent-drop class (typos, retired knobs) into a visible signal. Fake mirrors the signature (parity-pinned); the fake's stricter-than-server validation (requires the server-defaulted trio; defaults retired `epochs_max` into its config) is an **observed fake-vs-server divergence deliberately left unchanged and recorded here**. Full suite passed unmodified against the typed surface — empirical zero-breakage. Mutations: dropping one field's forwarding and silencing the warning each fail exactly their own arm. |
| APD-DATA-015 | Route ordering is load-bearing and unguarded (`/{dataset_id}` catch-all) | [juniper-data#288](https://github.com/pcalnon/juniper-data/pull/288) | Test-only pin, `juniper_data/tests/unit/test_route_order_guard.py`, inside the CI-collected `unit and not slow` scope. It reads `APIRouter.routes` — the public declaration table FastAPI's matcher walks (0.137 stopped flattening included routers into `app.routes`, which now holds opaque `_IncludedRouter` nodes) — and never request outcomes, because a request-level probe passes as long as *something* answers. Four pins so it cannot go vacuous: the generic no-parametrised-route-shadows-a-later-literal invariant per router (regex from Starlette's own `compile_path`); the named datasets pin that `/filter`, `/stats`, `/versions`, `/latest` **exist** and each precede `GET /{dataset_id}`; the disjoint-literal-first-segment pin that makes per-router order app-complete; and the `/v1` mount pin via `app.openapi()`. Mutations run before the claims were written: hoisting the catch-all above `/versions` fails the generic and the named pin (live probe: `GET /v1/datasets/versions?name=x` → `404 Dataset 'versions' not found`); deleting `/versions` fails only the named pin — which is why it exists. Unit lane 1026/1026 green. The §4 row's anchors predate the 08-25 merges — the catch-all now sits at `datasets.py:808`, the four literals at `:332`, `:429`, `:761`, `:785`. |
| APD-DATA-020 | `/v1` is a repeated literal in five places; no `API_VERSION` constant | [juniper-data#289](https://github.com/pcalnon/juniper-data/pull/289) | The live census found **eight** sites, not five: the register's five (three `include_router` calls in `api/app.py`, two self-referential `artifact_url` f-strings in `api/routes/datasets.py`) plus the three health entries of `EXEMPT_PATHS` in `api/constants.py`, which post-date the primer. `API_VERSION = "v1"` / `API_PREFIX = f"/{API_VERSION}"` now live in `api/constants.py` and every site derives from them; behaviour is byte-identical. `tests/unit/test_api_prefix.py` pins three things: the **published value** (`/v1` — changing it is a breaking change for every client and must fail a test, not slip through a constant edit), the **call sites** (an AST walk fails on any string constant or f-string in the non-test package that *starts with* `/v1`, because a value assertion cannot see a literal creeping back — prose that merely mentions `/v1/...` mid-docstring is deliberately not matched), and the **derived surfaces** (every OpenAPI path under the prefix, the exempt health probes, a created dataset's `artifact_url`). The `-015` guard drops its local `/v1` and imports the constant, so the wire value is pinned in exactly one test. Mutations: restoring `prefix="/v1"` on one router fails only the call-site pin; `API_VERSION = "v2"` fails only the published-value pin. Unit lane 1031/1031. Side-finding worth carrying: a same-second cp/sed/mv mutation restore left a *validating* stale `.pyc` compiled from the mutated source — 121 spurious failures until the cache was purged; mutation passes now run with `PYTHONDONTWRITEBYTECODE=1`. |
| APD-DATA-021 | `DatasetListFilter` declared and never used; route re-declares 12 params | [juniper-data#290](https://github.com/pcalnon/juniper-data/pull/290) | Measured before deciding, as the row asked: wiring the model in (`Depends(DatasetListFilter)`) **changes the wire contract** — `tags` disappears from the query parameters altogether (the route takes a comma-separated `str`; the model declared `list[str]`) and every shared parameter loses its description; `limit`/`offset`/`cursor` would stay on the route regardless, so the duplication would not even go away (probe retained as `juniper-ml/util/ad-hoc/apd_data_021_openapi_probe.py`). The route is therefore the contract of record: the dead model — exported nowhere, referenced by no repo — is deleted (waived on the sequence-safety screen with an `Allow-Symbol-Loss: class:DatasetListFilter` trailer in the one commit), the route now consumes the `TAGS_MATCH_DEFAULT` / `TAGS_MATCH_PATTERN` constants it used to duplicate inline, and `tests/unit/test_filter_contract.py` pins the contract: the exact 13-name parameter set, `tags` as a nullable string (never an array — the specific reshaping wiring would cause), `tags_match` bound to the constants **at the call site** (AST — a value comparison cannot see an inline literal equal to the constant) as well as in the published schema, and an anti-resurrection pin on the model name. Behaviour byte-identical. Mutations (`juniper-ml/util/ad-hoc/apd_data_021_mutation_check.py`): an inline pattern literal fails only the call-site pin; a resurrected bare class fails only the anti-resurrection pin. Unit lane 1035/1035. |
| APD-DATA-023 | No `operation_id=` — a handler rename renames every generated SDK method | [juniper-data#292](https://github.com/pcalnon/juniper-data/pull/292) | None of the 21 decorators (datasets 16, generators 2, health 3) declared one, so every published `operationId` was FastAPI's generated `<handler>_<path>_<method>` (`create_dataset_v1_datasets_post`) — coupled to the handler name, the router prefix and the version prefix at once. Every decorator now carries `operation_id="<handler name>"` (rewrite retained as `juniper-ml/util/ad-hoc/apd_data_023_add_operation_ids.py`, idempotent); behaviour unchanged, and the published ids change **once**, from the generated shape to the explicit one — no generated SDK exists in the ecosystem (`juniper-data-client` is hand-written; no generator marker in any client repo), so nothing consumed the old ids. `tests/unit/test_operation_ids.py` pins three ways: at the call site (AST over `api/routes/` — a route registered without the keyword inherits the generated id silently), on the registered routes (`APIRouter.routes`, never `app.routes`; the served `unique_id` is the declared one and never `generate_unique_id(route)`), and as the published contract (no duplicate `operationId`; the set equals a frozen 21-name set, so an id rename is a conscious contract edit while a handler rename fails nothing). Mutations (`juniper-ml/util/ad-hoc/apd_data_023_mutation_check.py`, bytecode off, copy-restore): deleting one `operation_id=` fails four pins; renaming a handler fails nothing — the decoupling the row asks for; changing one id fails only the contract pin; duplicating an id fails uniqueness and contract. `AGENTS.md` left alone on purpose (at its P5 ceiling since data#291); the convention lives in the test docstring and the CHANGELOG. Unit lane 1080/1080 (CI, and locally once the T6 hold lifted); API-facing subset 127/127 while the host was held. **Sibling gap, unfiled (2026-08-26):** `juniper-cascor`'s 47 route decorators (`src/api/routes/`, ten modules) carry the same absence — no `operation_id=` anywhere — so its generated ids are equally coupled to handler name, path and prefix; the same rewrite + three-pin shape applies, and `APD-CASCOR-003`'s `response_model` pass touches every one of those decorators anyway, so do both in one pass. |
| APD-DATA-025 | `application/octet-stream` on one binary route, `application/zip` on the other | [juniper-data#293](https://github.com/pcalnon/juniper-data/pull/293) | The artifact route said `application/octet-stream` and the export `application/zip` as two unrelated inline literals. Both payloads are ZIP containers (an NPZ artifact is numpy's zip of `.npy` members; the export is a zip of NPZ files) and octet-stream is only the RFC 9110 §8.3 fallback a recipient may assume when no `Content-Type` is present at all, so the direction the primer itself recommended was taken — `application/zip` on both — with the alternatives (octet-stream on both; keep the split and pin it) and the one-constant reversal cost recorded in the PR. `BINARY_MEDIA_TYPE` in `api/constants.py` is the one spelling; both routes derive; the `Content-Disposition` filenames (`<id>.npz` / `datasets.zip`) are unchanged. Blast radius measured before choosing: wire-visible only in the artifact response's `Content-Type`; `juniper-data-client` returns `response.content` without reading the header and no canopy / cascor / worker code reads it either; the two integration assertions that spelled octet-stream follow. `tests/unit/test_binary_media_types.py` pins the published value, every `media_type=` call site (AST — a value assertion cannot see an inline literal equal to the constant) and the wire: both routes answer the published value, keep their filenames, and the bytes really are a zip (`np.load` reads the artifact; the export holds `.npz` members). Mutations (`juniper-ml/util/ad-hoc/apd_data_025_mutation_check.py`, bytecode off, copy-restore): an inline `application/zip` literal fails only the call-site pin; the artifact route restored to octet-stream fails the call-site and artifact wire pins; `BINARY_MEDIA_TYPE = octet-stream` fails the published-value pin and both wire pins. Unit lane 1084/1084. |
| APD-ECO-002 | No retry jitter in any of the three clients — a shared transient outage was retried by every client instance on an identical schedule | [juniper-data-client#175](https://github.com/pcalnon/juniper-data-client/pull/175) / [juniper-cascor-client#141](https://github.com/pcalnon/juniper-cascor-client/pull/141) / [juniper-recurrence#133](https://github.com/pcalnon/juniper-recurrence/pull/133) | One kwarg per client — `backoff_jitter=DEFAULT_BACKOFF_JITTER` on the `Retry` — plus the constant and a pin. **Measured before the value was chosen**: urllib3 applies jitter as an ABSOLUTE additive term (`backoff_value += random.random() * backoff_jitter`), not a proportional one, so it is matched to each repo's `DEFAULT_BACKOFF_FACTOR` (0.5 in all three) — a full window of spread across the 1.0 s first retry, the step that carries the most callers. **No dependency floor moved**: `backoff_jitter` arrived in urllib3 2.0.0 (upstream #2952, 2023-04-26) and all three already pinned `urllib3>=2.0.0` — checked against the upstream changelog rather than assumed. Deliberately a module constant, **not** a constructor parameter: the filed defect is "no jitter at all", and widening three public surfaces is a separate decision. Retry counts, allowed methods, status forcelists, cascor-client's `APD-CCLIENT-002` `raise_on_status=False` posture and recurrence-client's HEAD/GET-only allow-list are all untouched, so retry *behaviour* is unchanged and only its timing is decorrelated. **The decisive pin is behavioural, not structural** — 200 sampled backoffs must actually differ and fall inside urllib3's documented bounds, because a stored constant proves nothing if urllib3 ignores it; the positivity arm exists because a `0.0` silently restores the herd while leaving the call site looking correct, so the *value* is pinned, not the kwarg's presence. The mutation matrix carries an **expected-survival** row: dropping the kwarg fails 2 arms and setting the constant to `0.0` fails 2 arms in every repo, while **retuning it to 0.25 fails nothing** — the pins track the property the entry exists for, not the tuning value. `juniper-recurrence-client` had no retry-policy suite at all, so one was created, bringing it to parity with both siblings. Local full suites: data-client 336 passed / 10 skipped, cascor-client 461 passed, recurrence-client 75 passed. |
| APD-DATA-005 / APD-DATA-024 | `APIKeyHeader` declared but never wired, **and** securing the service deleted the whole OpenAPI document — so the missing `securitySchemes` was unobservable exactly where it mattered | [juniper-data#295](https://github.com/pcalnon/juniper-data/pull/295) | Closed together because neither is fixable alone: adding a scheme to a document nobody can fetch changes nothing, and publishing the document without the scheme ships one that misdescribes its own auth. **The decision had three options, not two, and two of them are one keystroke apart.** `EXEMPT_PATHS` already listed `/docs`, `/openapi.json` and `/redoc`, and `_is_exempt()` is a bare membership test evaluated *regardless of whether a key was supplied* — so re-enabling `openapi_url` alone would have served the document to **everyone** while looking exactly like "behind the key". All three paths were removed from the exempt set; the document is now authenticated like any other route (and so also subject to the rate limiter and the failed-auth throttle, the correct posture for a protected endpoint). Two sub-decisions recorded in place: the explorers stay **unmounted** under auth, because Swagger UI / ReDoc fetch `/openapi.json` by XHR with no `X-API-Key` header and could only 401; and the scheme is declared **per protected router rather than app-wide**, so the exempt health probes are not documented as requiring a key. `auto_error=False` keeps enforcement in `SecurityMiddleware` — the declaration only describes it. `test_middleware.py`'s `assert "/docs" in EXEMPT_PATHS` asserted the old behaviour and was inverted, with the reason recorded at the assertion. **Mutation matrix, five arms including an expected-survival row**: restoring `/openapi.json` to `EXEMPT_PATHS` fails 2 (the trap arm — without it these pins would certify an *open* endpoint as secured); re-gating `openapi_url` fails 1 (the original `-024`); dropping the router security dependency fails 2 (the original `-005`); declaring the exempt health router as protected fails 1 (the document would overstate the policy); and **renaming the generated scheme fails nothing**, as it must — the pins resolve the scheme by its `type`/`in`/`name` shape, not by FastAPI's default class-derived key. Unit lane 1094/1094 (1084 before; 10 new pins). |
| APD-CASCOR-003 (partial) | 46 of 47 routes declared no `response_model` — and the SAME 47 decorators declared no `operation_id` (the unfiled `APD-DATA-023` sibling) | [juniper-cascor#593](https://github.com/pcalnon/juniper-cascor/pull/593) | Two gaps over one set of decorators, done in one pass because splitting them rewrites all 47 twice. **44 of the 46** gained `response_model=ResponseEnvelope`; all 47 gained an explicit `operation_id`. **Wire-neutrality was measured, not assumed**: `success_response()` already returns `ResponseEnvelope(...).model_dump()`, so an enveloped body has already round-tripped through the exact model `response_model=` re-applies — idempotent by construction, a stronger guarantee than "`data` is `Any`". That holds only while every enveloped route goes through the helper, so `TestEnvelopeUniformity` pins **that property** rather than the absence of extra keys: the failure mode is a future route that skips the helper, and nothing would fail. **Why partial**: `health_check` and `liveness_probe` return bare dicts on the cross-service API-02 `{status, version, service}` base shared with juniper-data and juniper-canopy, and declaring a model there **is** a wire change — measured, an optional field absent from the 200 body reappears as `"error": null` because `response_model_exclude_none` defaults to `False`. Purpose-built models for those two are a cross-repo contract decision, not this defect; the exclusion is pinned so it reads as a decision rather than an oversight. The published `operation_id` set is frozen by a test but **deliberately not asserted against `endpoint.__name__`** — asserting they match would re-couple the SDK name to the Python name and reinstate the defect in a new form. **Mutation matrix, five arms**: dropping an id fails 2; changing a published id fails the frozen-set arm; dropping a `response_model` fails 2; declaring the envelope on a bare-dict health route fails 3; and **renaming a handler fails nothing**, the expected-survival row that is the evidence the decoupling works. Routes read from each `APIRouter`, never `app.routes` (FastAPI ≥0.137 `_IncludedRouter` carries no `path`/`methods`, so that walk silently sees nothing). **The mutation harness was itself found vacuous and hardened**: matching only `^FAILED` reported a mutation that broke *import* as a clean survival — it now matches `ERROR` and falls back to the process exit status. `src/tests/unit/api` 2203 passed; full `src/tests/unit` green. **Sibling gap recorded, not filed** (§4.3 note): cascor and service-core both carry juniper-data's `APD-DATA-024` `EXEMPT_PATHS` trap — recorded here as "latent in both", which was **wrong for service-core** and corrected 2026-08-28; it was live in `juniper-recurrence` and is now fixed. See the §4.3 CORRECTION block. |

| APD-CCLIENT-001 | Retries `POST`/`DELETE`/`PATCH` with no idempotency key | [juniper-cascor-client#143](https://github.com/pcalnon/juniper-cascor-client/pull/143) | `RETRY_ALLOWED_METHODS` is now `["HEAD", "GET"]`, matching `juniper-recurrence-client` — the sibling with the closest semantics, since both wrap a stateful lifecycle service. **This was the last open row of the §2.3 sibling-package-drift table**, four months after both siblings landed XREPO-11. Each exclusion is justified **at the constant**, not left to the reader: `POST`/`PATCH` are non-idempotent by RFC 9110 §9.2.2; `DELETE` is RFC-idempotent *in end state* but its one call site destroys a trained network, so a replay landing after another actor recreated it deletes the new one (juniper-data-client dropped it for the same reason); `PUT` is never issued and was dropped rather than carried as dead configuration implying a capability the client lacks. The entry's own impact analysis held up: `save_snapshot` was the genuinely duplicating site, and the lifecycle POSTs were **accidentally** safe via cascor's 409 FSM guard, which is a property of the server's state machine rather than an idempotency contract. **The decisive pin is not the constant.** Structural arms pass if the list is right but never reaches urllib3 — a refactor building `Retry()` without `allowed_methods` silently restores urllib3's default, which *does* include DELETE and PUT — so the pin asks urllib3's own `is_retry` on the mounted adapter across every retryable status. A negative control keeps GET retrying, since dropping it re-creates the outage XREPO-02 exists to prevent. Mutation: restoring the old list fails 9 tests including all four `is_retry` arms. Full suite 476 passed, 7 subtests. |
| APD-DATA-033 | Rate-limit window is the one knob of three an operator cannot set | [juniper-data#297](https://github.com/pcalnon/juniper-data/pull/297) | `Settings.rate_limit_window_seconds` (env `JUNIPER_DATA_RATE_LIMIT_WINDOW_SECONDS`), defaulted from `api.constants.DEFAULT_RATE_LIMIT_WINDOW_SECONDS` — **the same object** the `RateLimiter` constructor defaults to, not a second literal that happens to agree, so the two cannot drift; `create_app` passes it through. The register's restatement was right that this was never an unwired constant: it is load-bearing at three sites inside `RateLimiter`, and the gap was only the absent operator-facing setting. **The two structural arms are deliberately not the proof** — a `Settings` field can exist, parse and validate while `create_app` never reads it, which *is* the defect, so both pass unchanged against the broken code. The decisive arm builds the app and reads the window back off the `RateLimiter` the app actually constructed. Mutation: dropping the `window_seconds=` kwarg fails **only that arm, 1 of 3** — which is the evidence the other two are insufficient rather than an accident of coverage. Unit lane 1220 passed, 7 subtests. **Separated from the §4.1 eleven-row REST group with the owner's approval** — see the note there, and the method lesson it records about ID-adjacency group boundaries. |
| APD-ECO-005 | No version-lockstep test in any client repo | [dclient#177](https://github.com/pcalnon/juniper-data-client/pull/177) / [cclient#144](https://github.com/pcalnon/juniper-cascor-client/pull/144) | `[project].version` and `__version__` are two hand-maintained literals; the silent direction is the static release path bumping only `pyproject.toml`, shipping a wheel whose dunder lies. Added to **two** repos, not three: `juniper-recurrence-client` is `dynamic = ["version"]`, where the dunder *is* the source, and is exempt by the same rule `tests/test_release_train_registry.py` already states. Added deliberately **while both literals agree** — the same reasoning as the `APD-SVCCORE-009` public-surface guard. Four arms, and three exist to stop the gate reading nothing: `pyproject.toml` is resolved from `__file__` rather than the process CWD (pytest can run from anywhere, and a relative read silently picks up a *different* project's file from a parent directory), the parsed project name and the imported package's location are asserted, and an **exemption guard** fails if the package ever moves to `dynamic` — at which point the lockstep assertion becomes *tautological rather than false* and would keep passing while guarding nothing. Mutation: bumping pyproject alone fails the lockstep arm and nothing else. Suites 340 passed / 480 passed. This closes the guard `APD-CCLIENT-F01` explicitly left open ("The *guard* is still absent — see `APD-ECO-005`"). |
| APD-SVCCORE-011 / APD-SVCCORE-015 | `WorkerTaskProtocol` used the package's *other* Protocol convention (`pass` bodies, not `@abstractmethod`), **and** shipped no default implementation — so every consumer wrote all three methods from scratch | [juniper-ml#1441](https://github.com/pcalnon/juniper-ml/pull/1441) | One object, two primer-stated remedies, closed together and fixed separately. **`-011`**: the three members become `@abstractmethod` + `raise NotImplementedError`, matching `websocket/commands.py`'s `CommandExecutor` — the primer's instruction was "both work, only one can be usefully inherited from, so **pick one per package**" (7944), and this is that convention applied. Because `Protocol`'s metaclass derives from `ABCMeta`, a partial implementation now fails at **construction** rather than returning `None` into `get_next_assignment`'s dispatch path. **`-015`**: adds `JsonTaskProtocol`, the `LifecycleCommandExecutor` analogue for the worker seam — structural, not inherited, exactly as that class is. **Derived, not invented**: the package's own tests already wrote this envelope from scratch **three times** (`_CoverageProtocol`, `_RecordingProtocol`, `_TrivialProtocol`), which is the entry's "every consumer writes all three methods" reproducing *inside the package*, and `PendingTask`'s docstring already named the shape ("a JSON-only worker packs a plain dict"). It declares **no** attachments deliberately — `worker_stream.py` reads one frame per declared attachment with no cumulative cap (`APD-SVCCORE-001` — open at the time of this fix, **closed shortly after by ml#1446**), so a default echoing worker-declared names back would have handed that unbounded read to every adopter. **A checked-in test asserted the defect as correct**: `test_worker_task_protocol_default_bodies_return_none` pinned the `pass` bodies as returning `None`, so every re-reading of the source found a test agreeing with it — the same shape as `data#295`'s `assert "/docs" in EXEMPT_PATHS`. Inverted here with the reason recorded at the assertion, not deleted. **The structural arms are deliberately not the proof**: `isinstance(JsonTaskProtocol(), WorkerTaskProtocol)` passes against a default whose envelope omits `task_id`, which would make every result unattributable in `submit_result` — so the decisive arm drives a real dispatch → result → collect cycle through `WorkerCoordinator` and reads the outcome back off it. **Mutation, two arms**: dropping `task_id` from the envelope fails **1 of 6** — only the round-trip arm, the structural arm *surviving* being the evidence it is insufficient rather than an accident of coverage; reverting `@abstractmethod` / `raise` to `pass` fails **exactly the two `-011` arms** and nothing else. Adding a public name touches the three parallel lists `APD-SVCCORE-009`'s guard pins (`test_public_surface.py` 9/9). Suite **545 passed**; `coordinator.py` **98%** statement coverage, 0 missed. |
| APD-SVCCORE-012 | `LifecycleCommandExecutor` — the concrete default published beside `CommandExecutor` — said nothing about whether subclassing it was supported | [juniper-ml#1443](https://github.com/pcalnon/juniper-ml/pull/1443) | The primer states the risk precisely (8089): publishing a concrete default beside a Protocol "invites the inheritance you were avoiding … **its docstring does not say whether that is supported, so the answer is whatever the first consumer assumes**." Answered explicitly — **not supported, and the refusal is enforced at runtime**. Each docstring now names the supported variation points instead of leaving them implied: **injection** (the `start` callback is the designed one), **implementing** the structural Protocol directly, or **wrapping** an instance. **Applied to both defaults, not just the filed one**: `APD-SVCCORE-015` (ml#1441) had shipped a *second* concrete default with exactly this shape — `JsonTaskProtocol` beside `WorkerTaskProtocol` — hours earlier, so closing the row for `LifecycleCommandExecutor` alone would have left the identical ambiguity in freshly-merged code. **`@final` alone would have been a vacuous guard, and that is the entry's real lesson.** Nothing type-checks this package: the repo's mypy hook is scoped to `^(scripts\|tests)/.*\.py$` and the six sub-packages are governed by a Ruff hook whose selected rules (`E,F,W,B,I,N`) do not enforce `@final`; `ci-service-core.yml` runs pytest + the coverage gate + build, with no type-checker at all. The decorator would have read as a protection it does not provide — **the same shape this register already names about `APD-SVCCORE-001`'s unreachable `_MAX_BINARY_SIZE`**. So `@final` is kept for a later type-checker adoption and `__init_subclass__` is what actually holds; the arm asserting `__final__` is labelled "not the proof" at the assertion. Safe to enforce: **zero subclassers of either class anywhere in the ecosystem** (checked cascor, data, canopy, recurrence, cascor-worker), both pre-1.0. **Mutation**: making both `__init_subclass__` bodies inert *while leaving `@final` in place* fails **3** arms — both enforcement arms plus the `JsonTaskProtocol` one — and `test_final_decorator_is_declared` **survives**, which is the direct demonstration that the structural arm would certify the broken code as fixed. The negative controls (ordinary construction and dispatch) survive too, as they must: without them a guard that broke construction outright would still pass the enforcement arms. Suite **551 passed**; `commands.py` **100%**, `coordinator.py` **98%**. |
| APD-SVCCORE-001 | Per-frame binary cap with no cumulative bound — one result submission permitted `len(attachment_names) × _MAX_BINARY_SIZE`, every frame retained in memory | [juniper-ml#1446](https://github.com/pcalnon/juniper-ml/pull/1446) | **A bound on each item is not a bound on the sum.** Two independent bounds, because the defect has two halves. `_MAX_ATTACHMENTS = 32` — cardinality, **checked before the first `receive()`**, which is the substantive half: a late check still rejects the submission *and still lets a hostile worker hold the handler through 32 frames first*. **Not an invented number** — it ports juniper-cascor's `_MAX_TENSOR_MANIFEST_ENTRIES` (`src/api/workers/protocol.py:64`, enforced `src/api/websocket/worker_stream.py:391`), whose own comment names this exact failure mode. `_MAX_TOTAL_BINARY_SIZE` — aggregate bytes per submission, which cascor does **not** have; set equal to the per-frame cap by a stated principle rather than an invented magnitude (*one submission may not deliver more bytes than a single frame was already permitted to carry*), and kept as an **independent policy** from the per-frame cap so raising one cannot silently raise the other, pinned by a structural arm. The §3 severity caveat is unchanged and still governs — uvicorn's 16 MiB `ws_max_size` capped the reachable ceiling at `32 × 16 MiB` — so what closes here is the unbounded-`N` half, the part that was genuinely unbounded. **Mutation matrix, three rows, one of them an expected survival.** (B) disabling the cumulative check fails **1**, the cumulative arm only. (C′) moving the cardinality check *after* the first `receive()` fails **1**, and specifically on the **un-consumed-queue** assertion — the error frame still fires, so an error-only assertion would have passed; that is what validates the arm's design. (C) moving the same check to the **top of the loop body** **survives, correctly** — that position still runs before the first `receive()`, so it is equivalent code rather than a regression. **C was written first as the "decisive" mutation and its survival was a bad mutation, not a weak test**; recorded because distinguishing the two is the entire point of running them, and a surviving mutant is evidence of a coverage gap only once it has been shown to change behaviour. The cardinality arm uses the **real** `_MAX_ATTACHMENTS` so it pins the shipped value; only the byte budget is monkeypatched down. Stale cross-references chased by the whole-file grep: this closure falsified "`APD-SVCCORE-001`, still open" in the `-011`/`-015` row above **and** two live docstrings in the package. Suite **555 passed**; `worker_stream.py` **97%**. |
| APD-SVCCORE-005 | Control-WS 64 KB cap checked after materialisation — **and, unstated in the entry, the oversize path spent no rate-limit token, so one connection could repeat the allocation for free** | [juniper-ml#1447](https://github.com/pcalnon/juniper-ml/pull/1447) | **Closed in two parts, because the filed complaint and the actionable defect are not the same thing.** *Disclosure* for the filed half: `receive_text()` has already built the string before any length check can run, and the real ceiling is uvicorn's `ws_max_size` (16 MiB default, unset here) — there is nothing to fix at this layer, and the constraint is now documented at the site, the same disposition `APD-SVCCORE-007` took for the per-process rate limiter. *Fix* for the half found by adversarial review rather than by the entry: the oversize branch did `continue`, and it returns **before** `_handle_command_message`, the only place a token is spent — so every oversize frame was free. **Closing (1009, RFC 6455 "Message Too Big", unused by any other gate here) rather than charging the bucket, deliberately**: by the time the bucket could be charged the allocation has already happened, so charging would be *accounting, not protection*; closing is the only change that alters an abusive client's cost, and it is already what this loop does for the two adjacent protocol violations (malformed JSON and non-object JSON both close 1003). **The `pong` path also reaches `continue` without spending a token and is left that way deliberately**, now pinned so it reads as a decision: the bucket is the *command* budget (`ws_control_rate_limit_per_sec`, "control-command rate limit"), and charging keepalive would let a burst of legitimate commands exhaust it and rate-limit the client's pong — which the heartbeat loop reads as a dead peer and closes 1011, a self-inflicted disconnect on a healthy connection. **A checked-in test asserted the defect as correct, the third instance in this arc**: `test_recv_loop_rejects_oversized_message` wrapped the call in `pytest.raises(WebSocketDisconnect)`, asserting the loop *kept running* past the oversize frame and ended only when the scripted input ran out. Inverted with the reason recorded at the test, after `test_docs_reachable_and_exempt` and `test_worker_task_protocol_default_bodies_return_none`. **Mutation**: reverting the close to `continue` fails both oversize arms and leaves the pong arm passing. **The decisive arm is not the close code** — asserting only that would pass against an implementation that closed *after* draining the queue — it is that a second oversize frame is **never read** (exactly one rejection for three queued frames). Suite **557 passed**; `control_stream.py` **98%**. |
| APD-SVCCORE-013 / APD-SVCCORE-016 | `LeakyBucket` is a token bucket, name and docstring both wrong; and the four distinct pre-accept close codes all reach the caller as an opaque 403 | [juniper-ml#1454](https://github.com/pcalnon/juniper-ml/pull/1454) | **The §6 triage instruction settled both, and reading the primer rather than the register's restatement changed the disposition of each.** These were the last two open `juniper-service-core` rows, both `Low`, both on §6's "triage before actioning" list. **`-013` — docstring corrected, name kept by design.** The primer's own treatment concludes the two algorithms "are implemented identically and named interchangeably — **read the code, not the class name**", and calls this class "misnamed, correctly implemented". Renaming a name exported from `juniper_service_core.websocket` would break a published surface to settle an ambiguity the reference material calls conventional. What was genuinely missing was the correction *in the code*: the docstring asserted "leaky-bucket" with nothing anywhere to weigh it against. It now states which algorithm this is, why the name stays, and the consequence a caller must not get wrong — **bursts up to `capacity` are admitted immediately**, which is pinned by a new arm, since a real leaky bucket would admit one and refuse the rest. **`-016` — WON'T FIX, and the obvious fix is a regression.** A handshake failure is still HTTP, and RFC 6455 §10.2 *recommends* `403` for an unacceptable Origin, so uvicorn collapsing the codes is conformant rather than defective. Making them observable requires `accept()`-ing the socket **first** and then closing — the pattern the primer criticises as "harder for the client to distinguish from a network failure", and which would complete a handshake for a caller the kill switch, the cooldown or the Origin allowlist has already refused. **Note the primer is itself wrong about Juniper here** (849, point 1): it says Juniper "accept[s] the socket and then close[s] it", which the 2026-08-28 restatement had already disproved — all four gates are pre-accept. So the row is closed by *protecting* the current ordering, not by changing it. **The decisive arm is new and the four existing gate tests are not it**: `test_handshake_gate_rejects_*` assert the return value and the close code, so **all four survive an accept-then-close rewrite**; the added arm asserts `accepted is False` on every rejection path. Mutation: inserting `await websocket.accept()` at the top of `_check_handshake_gates` fails **only** that arm. Suite **559 passed**. |
| APD-ECO-006 | No client type-checked a consumer-shaped probe — every mypy hook was scoped to library-internal source, so no client verified that its own published type surface was usable from outside | [dclient#178](https://github.com/pcalnon/juniper-data-client/pull/178) / [cclient#145](https://github.com/pcalnon/juniper-cascor-client/pull/145) / [rec#138](https://github.com/pcalnon/juniper-recurrence/pull/138) | One identical mechanical change per client: a `typing_probe/consumer_probe.py` **outside** the package (inside it, it would be internal source again — the defect) plus a widened hook `files:` regex. **Uniform across all three, not drifted across two** — see the ml#1449 correction above. **The probe is not ceremonial, and there are two independent pieces of evidence.** It caught a real error in its own first draft: `validate_npz_contract(path)` was written on the assumption it took a filename, when it takes the loaded `Dict[str, np.ndarray]` and returns `ContractKind` — precisely the mistake a consumer makes and the library's own internal type-check cannot see. And each was **mutation-checked** against the real package: `List[str]`→`List[int]` on data-client reports the library's actual `list[str]`; `Dict[str, Any]`→`list[str]` on cascor-client's `get_network()` fails 2 arms; calling recurrence-client's `predict()` positionally reports "Too many positional arguments" off its real keyword-only signature. Each probe therefore reads the published annotations rather than asserting a tautology. `recurrence`'s hook is **strict**, so its probe satisfies `strict = true` — the client the register had recorded as configuring "no mypy at all" is in fact the most strictly checked of the three. Coverage per probe: every `__all__` name resolves for an importer; the public methods return what they declare, with a value read back off each result so a return widened to `Any` cannot pass unnoticed; and every published error is statically catchable through the package base, because a consumer writes `except <Pkg>ClientError` and a subclass that stops deriving from it breaks that consumer with **no runtime signal** until the exception escapes. **Deliberately NOT closed by this row, and stated in all three PRs**: mypy resolves each package from the **source tree**, not an installed distribution, so `py.typed` is bypassed. All three ship `py.typed` and **nothing asserts it is packaged** — a wheel that dropped it would hand every consumer an untyped package while these probes stayed green. That is the `APD-SVCCORE-008` / `APD-OBS-002` class, needs a check against the built artifact, and is recorded as a separate gap rather than folded in to make this row look more completely closed than it is. |
| APD-RCLIENT-005 | Repository, distribution and import names are three different strings | [juniper-recurrence#143](https://github.com/pcalnon/juniper-recurrence/pull/143) | **Closed as a DOCUMENTATION fix — and both dispositions the handoff lineage proposed for it were wrong.** The row was carried three rounds as either a metadata defect (repoint `[project.urls]`) or a false positive to close WON'T FIX. Reading the **source primer** settles it: 8188-8195 calls this "a documentation problem, and why `[project.urls]` (`juniper-recurrence-client/pyproject.toml:49-52`) pointing at the right repository **matters**". So the correct URLs are the *mitigation*, not the defect — repointing them at a `pcalnon/juniper-recurrence-client` that does not exist would make the metadata wrong — and the same passage calls the hyphen-to-underscore import transform "conventional, not required". Neither reading of the row as a naming defect survives its own source. **What was actually missing was the disclosure.** `README.md` is what PyPI renders and the only page a consumer of the published wheel ever sees, and it carried **zero** mentions of the monorepo layout while linking its two sibling clients to their own standalone repositories — actively reinforcing the expectation that this one has a standalone repo too. The root README documents the layout, but a PyPI reader never reaches it. Fixed with an Install-section note giving the distribution name, the import name and the repository path, so a `ModuleNotFoundError` on the import name leads to the right `pip install`; `[project.urls]` deliberately unchanged. Verified: `juniper-check-doc-links` **PASSED** on both changed files, no trailing whitespace, documentation only. **Anti-vacuous note, recorded rather than papered over**: this close is prose and *nothing pins it* — no test asserts the README says any of this, so a future rewrite could silently undo it and no gate would fire. The register's §6 freshness rule (the source is authoritative) is the only guard. That is a weaker close than the `APD-SVCCORE-007` disclosure precedent, which landed its disclosure in source next to the constraint it describes. |
| APD-CCLIENT-008 | Sniffs two error envelopes, encoding fleet drift rather than surfacing it | [juniper-cascor#610](https://github.com/pcalnon/juniper-cascor/pull/610) / [juniper-cascor-client#148](https://github.com/pcalnon/juniper-cascor-client/pull/148) | **The row was filed against the CLIENT; the defect was on the SERVER, and both the filed anchor and the filed premise were wrong.** The anchor `client.py:393-402` is dead (the extraction is `:574-599`), and the primer's stated cause — "different services answer with different envelopes" (4773-4777) — cannot apply to a client that only talks to cascor. The drift was **intra-cascor**: API-09 registered handlers for `ValueError`, `HTTPException` and `Exception` but never `RequestValidationError`, which is *not* a `ValueError` subclass (MRO `ValidationException -> Exception`) and for which FastAPI **actively installs** its own default — so this was never passive fallthrough the `Exception` handler might have covered. Every Pydantic field-validation 422 returned raw `{"detail": [...]}` while every other error returned the envelope. **The evidence was checked into cascor the whole time**: the `HTTPException` handler's docstring asserted "migration complete after PR 3" and that clients "no longer have to parse two error formats", while `test_candidate_pool_invariants.py` branched on both shapes with a comment saying 422 "is untouched by API-09". Both corrected. **Closed by completing the migration, not by disclosing the sniff** — a disclosure close would have been near-vacuous, since `_handle_response:592-596` already documented the two shapes. cascor now wraps 422; the client reads the structured list from `error.detail`. **The per-field list is preserved, not flattened** (`APD-DATA-013`'s recorded constraint: flattening "would destroy the per-field structure the client was just built to consume"), and **no top-level `detail` alias** was reinstated — PR 3 retired it deliberately, and it would not help the only known consumer anyway, which tests `body["error"]` first and so degrades gracefully to prose. Mutations: unregistering the server handler fails **6** arms; reverting the client to `error.message` fails **3** and correctly leaves the 2 regression guards (`error.detail` null on every `HTTPException` route; a *string* `detail` is prose) passing. Suites: cascor **2225**, client **502**. The broad cascor run earned its keep — it caught a real consumer of the old shape, `test_start_training_rejects_unknown_params`, now reading `error.detail` with its SEC-07 substance intact. **A 3.12/ubuntu CI failure was adjudicated a flake, not accepted as one**: 3.12/macOS, 3.13 and 3.14 passed the same commit, the assertion was async lifecycle timing (`training_active`) unrelated to response bodies, and a re-run went 26/26 green. **Not re-filed as a new `APD-CASCOR-*` id**, per the owner-directed fixed-96 identity rule (register:729/:735). |
| APD-ECO-007 | Neither shared package has any deprecation machinery | [juniper-cascor-client#151](https://github.com/pcalnon/juniper-cascor-client/pull/151) | **Closed in two parts, because the row bundles a policy claim with a concrete debt, and the primer answers them differently.** *Policy half — the absence is DELIBERATE, and documented rather than removed.* The headline verifies (zero `DeprecationWarning` / `PendingDeprecationWarning` / legacy aliases in either package today), but the primer calls it "**a coherent position** while the consumer set is a known handful of first-party repositories, and not one the moment a third party depends on you" — a *conditional* risk, not a present defect. Two facts settle it: **nothing in either package is currently deprecated**, so machinery would ship with zero call sites and tests that could only exercise it synthetically (the vacuous-pass shape); and the position has been **exercised twice** — both changes service-core marked "potentially breaking" (`CHANGELOG:110`, `:122`) were cleared by *censusing consumers* and verifying zero affected, which a deprecation cycle would only have slowed. So both READMEs now state the policy, the trigger that would change it (a third-party dependant, or 1.0), and — the part that prevents re-derivation — **which existing implementation to reuse**: `juniper_config_tools.env_with_legacy_alias` for env vars, the data-client alias pattern for API renames. The primer records that three services independently re-implemented the env lookup before it was consolidated; naming the reference is what stops a fourth. *Concrete half — the `auto_pong` removal date, which this row OWNED from `APD-CCLIENT-012`.* Dated: warns now, removed in cascor-client 0.9.0. The primer's objection was never that the flag is wrong — "both are defensible" — but that "**nothing tells you who still sets it**". A fleet census answers it: **zero** production users, all eleven occurrences being the client's own tests. The escape hatch is not taken away silently: the warning invites a relay that genuinely needs to observe pings to say so before 0.9.0. **The fake warns too**, at `stacklevel=3` against production's `4` (it sets `_auto_pong` directly rather than through `_init_liveness`) — the #91 fake-parity rule. **A mutation exposed a defect in the new tests themselves**: routing constructions through a helper-plus-lambda put both candidate frames inside the test file, so `filename == __file__` held for a right *and* a wrong `stacklevel` and the fake mutation PASSED. Restructured to construct inline; each mutation now fails exactly its own arm. Suite **515**. *Not closed*: no deprecation machinery was added to either shared package — that is the documented decision, not an omission. |
| APD-DATA-016 | `download_artifact` is named streaming but fully materialises the body | [juniper-data#313](https://github.com/pcalnon/juniper-data/pull/313) | **The name was the misleading part, and the fix had to widen a seven-backend interface without a flag day.** `io.BytesIO(get_artifact_bytes(...))` inside a `StreamingResponse` bounds the *socket buffer*, not process memory: the whole NPZ existed in RAM before the response object did, once per concurrent download. New `DatasetStore.open_artifact_stream` yields chunks and is **deliberately not abstract** — the base implementation falls back to `get_artifact_bytes`, so InMemory / Cached / Redis / HuggingFace / Kaggle / Postgres are untouched and a new backend does not get harder to write; only `LocalFSDatasetStore` overrides it. So the memory bound is a property of the **store**, and the route docstring says that rather than implying the endpoint is universally safe. **The decisive test arm is not a byte round-trip** — a whole read round-trips too — it is that a small `chunk_size` yields **more than one** chunk. **The subtle arm is eager absence**: a generator body does not run until iterated, so an existence check moved inside one would defer the 404 until after the route had committed to a 200 and sent headers, answering *200 with an empty body* for a missing dataset; a test asserts the call returns `None`, not a generator. Mutations: reverting the override to a whole read fails the two chunking arms, moving the check inside fails the eager-absence arm. Suite **1386** (unit 1227), bytes verified identical to the materialised read. **Two halves of the same primer passage were deliberately not touched**: the `ETag` / conditional-GET half is `APD-DATA-017`'s, inside the owner-routed REST group; and the `application/octet-stream` half was **already fixed** before this work — `BINARY_MEDIA_TYPE` is `application/zip`, owns every call site, and has its own pinning test. Checked, not assumed. |
| APD-DATA-018 | No async job pattern — generation runs inside the request | [juniper-data#326](https://github.com/pcalnon/juniper-data/pull/326) / [juniper-data#354](https://github.com/pcalnon/juniper-data/pull/354) | **Closed by bounding the inputs, not by building the async pattern** — owner decision 2026-09-04, Option 6 of [`JUNIPER_2026-09-01_JUNIPER-DATA_ASYNC-JOB-PATTERN-DECISION-ANALYSIS.md`](JUNIPER_2026-09-01_JUNIPER-DATA_ASYNC-JOB-PATTERN-DECISION-ANALYSIS.md). **The two halves took different units, and that is the finding.** `csv_import` bounds **bytes** (128 MiB, from a measured 14.4 MB/s over the whole `generate()` path); `equities` bounds **symbols** (14, from 30 s ÷ 2.1 s per symbol). **A cap's unit must be something the server can measure BEFORE doing the work.** `csv_import` has an input in hand, so bytes are a `stat`; `equities` has no input — the request is a ticker list and a date range, and its byte count does not exist until the fetches the cap exists to prevent have already been made, so a byte cap there could only ever be a *prediction* derived from (symbols × horizon). The symbol count is known with zero network calls, which is where `_resolve_symbols` raises. *(**Corrected 2026-09-05.** This row originally argued the unit choice from a correlation — "163× the payload costs 1.16× the time … the Russell 3000 over one day is 92 KB and 1.7–3.2 h, so a byte cap would admit the expensive request and reject the cheap one" — and had the direction **backwards**. That 92 KB came from a purely proportional model, `2,923 × 32.1 B/day`, with no per-request intercept; Russell 3000 × 1 day is 2,923 separate HTTP calls, and fitting an intercept to the source document's own two smallest rows gives **2.07 MB** — 22× larger, and larger than the 210 KB single-symbol request. Bytes are **positively** correlated with cost here. The decision stands; the argument for it was replaced with one that does not depend on the correlation at all. Same correction applied to [`JUNIPER_2026-09-04_JUNIPER-DATA_EQUITIES-INGEST-SIZING-AND-FIELD-AVAILABILITY.md`](JUNIPER_2026-09-04_JUNIPER-DATA_EQUITIES-INGEST-SIZING-AND-FIELD-AVAILABILITY.md) §1 and `juniper-data/CHANGELOG.md`.)* Sizing and the full field-availability matrix: [`JUNIPER_2026-09-04_JUNIPER-DATA_EQUITIES-INGEST-SIZING-AND-FIELD-AVAILABILITY.md`](JUNIPER_2026-09-04_JUNIPER-DATA_EQUITIES-INGEST-SIZING-AND-FIELD-AVAILABILITY.md). Both halves share one contract: **422 refusal until the caller opts in**, a request may only *lower* the cap (`min(requested, deployment)`), and an authorised cut writes a **permanent** `DatasetMeta.truncation` descriptor over a reserved channel key popped before checksum + NPZ persist. `equities`' old cut was a bare slice that truncated **silently** with `max_symbols=None` — every request fanning out over all 503 constituents at 18–34 min against a 30 s budget. `equities_seq` inherits the bound *and* the annotation. Review caught two ways the byte cap was defeatable (caller could raise it; `stat` was trusted over the read) and the mutation matrix caught two ways the tests were not: a record-boundary arm that passed against the guard's deletion, and an equities annotation that no test required to reach `generate()`. Matrix is 12/12 with two expected-survival rows. |

The §2.3 copy-drift recommendation was built alongside them: `tests/test_service_fork_drift.py` ([juniper-ml#1103](https://github.com/pcalnon/juniper-ml/pull/1103)) now holds **all six** copy-drift guards as `ENFORCED` so they cannot silently regress — every row in §2.3's table.
No `KNOWN_GAP` rows remain -- the ledger's self-maintaining half fired twice (throttle, then blank-key filter) and both times did exactly what it was built to do: fail on the fix and demand promotion.
Promoting the `pre-auth-throttle` row exercised that mechanism end to end and exposed a limit worth carrying forward: a single name marker would have gone green on a bare `import`, so the promoted row asserts two markers — see the caveat under §2.3.
The final row, `cors-outside-auth`, could not be expressed as markers at all. Its regression shape is two `add_middleware` calls **swapping places**, so both markers are present either way and any presence-only check would report SUCCESS on the exact defect it exists to catch — the vacuous-pass class. It is encoded instead as an **ordered** site: `RequestIdMiddleware` must be registered before `CORSMiddleware`, which is precisely "CORS is registered last, so it runs outermost". The ordering matcher carries its own negative controls, and both of them are killed by disabling the order check — deliberately placed in the always-on structural class, so they still run in `ci.yml` where the cross-repo arms skip.

### 5.2 Fixed before this register

Two were fixed between the primer's publication on 2026-08-13 and this register; the third was fixed earlier, before the primer was written, and appears here only because the primer discusses it.

| ID | Finding | Fixed by | Verification |
| --- | --- | --- | --- |
| APD-SVCCORE-F01 | WebSocket heartbeat closed with the reserved code `1006`, which RFC 6455 §7.4.1 forbids an endpoint from sending — so the `websockets` server refused to serialise it and no close frame reached the peer | [juniper-ml#1081](https://github.com/pcalnon/juniper-ml/pull/1081) — after the primer | Both sites now close `1011` with the timeout in the reason: `websocket/control_stream.py:180`, `websocket/training_stream.py:118`. An AST anti-resurrection guard prevents recurrence. |
| APD-SVCCORE-F02 | The 401 path consumed no rate-limit budget | [juniper-ml#1082](https://github.com/pcalnon/juniper-ml/pull/1082) — after the primer | `FailedAuthThrottle` is checked **before** authentication (`middleware.py:205-213`) and `record_failure` is gated on a 401 (`:226-227`); `:222-225` is the `except HTTPException` line and its explanatory comment. Fixed in the shared package first and, on 2026-08-16, ported into both forks — see `APD-DATA-001` † and `APD-CASCOR-004` †. |
| APD-CCLIENT-F01 | `__version__` left at `0.4.0` while the package shipped `0.5.x`/`0.6.x` | **Before** the primer — the exact commit is unidentified | All three literals now read `0.7.0`; a tombstone comment records the incident at `__init__.py:11-13`. Attempts to attribute this to a specific PR did not resolve, so no provenance is claimed rather than an unverifiable one being recorded. ~~The *guard* is still absent — see `APD-ECO-005`.~~ **The guard landed 2026-08-28** in cclient#144 (and dclient#177 for the sibling): `tests/test_version_lockstep.py` now pins `[project].version` to `__version__`, so this exact drift fails a test instead of shipping. |

---

## 6. Method, and its limits

**How this was produced.** Four independent extraction passes ran over disjoint ranges of the primer (front matter + overview, Part I, Part II, Part III + appendices). Each was required to re-read the cited source before assigning a status, and explicitly forbidden from marking anything `OPEN` without having done so. Their outputs were then merged, deduplicated, and re-verified.
A fifth pass audited every primer line anchor against the primer text itself, which produced the `†` provenance markers, three restatements, and the removal of one entry the primer never asserted about Juniper code.

**What this register is not.** It is not an audit of the Juniper codebase. It is an extraction of what one document happened to assert while teaching something else. The primer visited the code where it made a good example, so coverage is uneven by construction:

- `juniper-data`, `juniper-service-core`, and the client libraries are heavily represented because they were the primer's running examples. juniper-data leads; `juniper-service-core` is second, and carries sixteen entries here.
- `juniper-canopy`, `juniper-cascor-worker`, and most of `juniper-cascor` were barely visited at all. **Their absence here is not evidence of health.**
- Severity reflects the primer's framing plus verification, not a risk assessment against deployment reality. An operator may reasonably re-rank.

**Confidence.** `Low` entries are included deliberately under the over-inclusion rule and should be triaged before being actioned — several are matters of judgement the primer itself flags as defensible (`APD-DATA-028`, ~~`APD-DATA-033`~~ (fixed, data#297), `APD-SVCCORE-007`, `APD-SVCCORE-013`, `APD-CASCOR-005`, `APD-ML-001`). `APD-DATA-033` is worth noting as a case where the `Low` confidence was **about the framing, not the finding**: the original wording implied an unwired constant, which was false, but the narrower restatement — a missing operator-facing setting — was real and cost two lines to close.

**Freshness.** Statuses were verified on 2026-08-14. `file:line` anchors drift with every commit; the primer line anchors are stable because that document is not being edited. Where the two disagree, the source is authoritative and this register is stale.
