# Backup design — consensus round 1, verbatim validator reports

**Project**: Juniper (workstation backup infrastructure, host `yamaguchi`)
**Author**: Paul Calnon
**Date**: 2026-09-21
**Status**: RECORD — round 1 complete; reconciliation into the design is PENDING (see the handoff named below)
**Subject**: `notes/JUNIPER_2026-09-21_JUNIPER-ECOSYSTEM_BACKUP-INFRASTRUCTURE-INTEGRATED-DESIGN.md`
**Procedure**: `notes/JUNIPER_2026-08-30_JUNIPER-ECOSYSTEM_INDEPENDENT-AGENT-CONSENSUS-PROCEDURE.md` (§2 lanes, §4 iteration, §7 minimum record)
**Handoff that carries the remaining work**: `prompts/thread-handoff_automated-prompts/HANDOFF_2026-09-21_backup-redesign-round-1-validated-reconciliation-pending.md`

## Why this file exists

The six validator reports below were delivered as subagent results inside one Claude Code session. Subagent output lives in the session transcript only (`~/.claude/projects/…/subagents/agent-<id>.jsonl`), which is local to one machine and not versioned. The reconciliation of these reports into the design was paused by the owner on 2026-09-21 before it completed, so the reports are archived here verbatim — they are the input the next thread reconciles from, and the record the consensus procedure's §7 requires.

**What has and has not been applied to the design (as of this archive):**

| Lane | Delivered | Applied to the design? |
| --- | --- | --- |
| A2 | 15 corrections | **Yes** — applied 2026-09-21 before the archive (see the design's §12) |
| A3 | 12 corrections (+ 2 defects in `2026-09-21_lint_design_snippets.py`, fixed) | **Yes** — applied 2026-09-21 before the archive |
| A1 | 13 DISAGREE items, 8 unrecorded facts, 7 UNTRACEABLE items | **No** — pending |
| B1 | 17 findings (3 REFUTED claims that change P0) | **No** — pending |
| B2 | 10 amputations, 22 actionability defects | **No** — pending |
| B3 | 20 security findings | **No** — pending |

Six agents ran in parallel from one launch message; all six were killed within a minute of each other by a
session usage limit (`HTTP 429 … session limit`) part-way through, and were resumed in place with their context
intact after the reset. None saw another's output. The reports are reproduced exactly as delivered (formatting
included); each one's own scope statement says what it read and what it did not. **No secret value appears in
any report** (each lane worked under the rule "count, compare, or describe by length and character class; never
print"), and the archive was scanned for secret-shaped content before being committed.

Each report is preceded by a `<!-- markdownlint-disable -->` marker because the reports are verbatim external
text (table rows far beyond the 512-character line limit, nested headings that restart at H1). Nothing in a
report was re-flowed, corrected or trimmed; the only difference from the delivered text is that trailing
whitespace at line ends was stripped (the repository's pre-commit hook does this to every file).


---

## Report A1 — Lane A — measurement re-creation; entry point: the LIVE HOST only (systemd, journal, filesystems, database copies)

Sections of the design assigned: §1, §4, §5, §6, Appendix A. Subagent id `ae965c8772bb167bd` (41,842 characters).

<!-- markdownlint-disable -->

Nothing remains to measure — every checklist item has been re-derived from the host. Final report follows.

# Lane A1 validation report — `JUNIPER_2026-09-21_JUNIPER-ECOSYSTEM_BACKUP-INFRASTRUCTURE-INTEGRATED-DESIGN.md` (§1, §4, §5, §6, Appendix A)

Entry point: the live host `yamaguchi` only, as `pcalnon` (uid 1000; process groups do **not** carry gid 139, so group-only files were read through `sg duplicati`; `adm` membership gave system-journal access). Measurements were taken 2026-09-21 03:47–03:55 CDT and, after the session interruption, 13:40–13:55 CDT; where the two windows differ the report says so. Read-only throughout; nothing written outside `…/scratchpad/valA1/` (scripts `bc.sh`, `d_home_tree.sh`, `e_usr_lib.sh`, `f_snapshot_timer.sh`, `g_watchdog.sh`, `h_dropbox.sh`, `i_destination.sh`, `j_env_shape.sh`, `k_journal_counts.sh`, `m_forensics_run1.sh`, `n_inode_times.sh`, `o_journal_timeline.sh` + `o2_summarize.py`, `p_wrapper_paths.sh`, `q_misc.sh`, `k2_followups.sh`, filter `redact.py`; DB copies under `dbcopies/run1`, `dbcopies/run2`). No secret value was printed; secret-bearing journal lines were counted through `awk` and dropped by the filter (316 dropped from the timeline dump).

## 1. Claim table

| claim (§, short quote) | measured value / evidence (command + literal output excerpt) | verdict |
| --- | --- | --- |
| §1 "down since 2026-09-18 14:12 UTC … last successful run … `duplicati-20260918T140000Z.dlist.zip.aes`, 877 files / 202.8 GiB" | newest destination file `2026-09-18 09:12:00 duplicati-20260918T140000Z.dlist.zip.aes` (CDT = 14:12:00Z); root snapshot `LastBackupFinished = '20260918T141256Z'`; `find -type f \| wc -l` → `877`; `du -sb` → `217760949689 bytes = 202.8 GiB` | AGREE |
| §1 / §4.1 "9 dlists, newest `20260918T140000Z`" | `find … -name '*.dlist.*'` → 9: `20260825T102739Z, 20260901T140000Z, 20260908T140000Z, 20260912T140000Z, 20260915T085649Z, 20260915T204850Z, 20260916T183346Z, 20260917T221344Z, 20260918T140000Z` | AGREE |
| §1 "scheduled runs of 09-19 and 09-20 did not happen" | journal: no server process alive at 09:00 CDT either day (unit failed from 09-18 21:23 to 09-19 19:14; the 09-19 21:57:41 server stopped `2026-09-20T04:06:07` "Deactivated"; next start 13:32); destination newest mtime `2026-09-18 09:12:00`; watchdog `ALERT UNREACHABLE … Connection refused` at `2026-09-19T12:00:39` and `2026-09-20T12:00:39` | AGREE |
| §1 "watchdog … `UNREACHABLE` 09-19/09-20 and `login failed (401)` on 09-21" | `~/.local/state/duplicati/server-watchdog.log`: `2026-09-21T02:52:01-0500 ALERT UNREACHABLE backup=2 login failed: FATAL: login failed (401)`; also a further `2026-09-21T12:00:39 … (401)` after the document was written | AGREE |
| §1 "server now listening on `localhost:8300` runs as the new `duplicati` user against a database created … 18:19:43 CDT … zero backup definitions" | `ss -ltnp` → `127.0.0.1:8300`, `[::1]:8300`; `/proc/1397393/status` `Uid: 133 … Gid: 139 … Groups: 139`; WAL birth `2026-09-20 18:19:43.917937740`; forensics `[live] … Version table: [(1, 12)] … Backup: 0 row(s)` | AGREE |
| §1 Leg A "pcalnon profile … orphaned since 08-25 … `Duplicati-server.backup` (02:19:47) lists only `Ubuntu` and `Ubuntu-fresh`, `Schedule` empty" | profile `.sqlite` mtime `2026-08-25 02:19:48`, `-wal` `02:20:46`; forensics `[pcalnon_backup] … Backup: 2 row(s) … ID=2 Name='Ubuntu' … ID=3 Name='Ubuntu-fresh' … Schedule: 0 row(s)`; snapshot journal `source : /usr/lib/duplicati/data/Duplicati-server.sqlite` daily | AGREE (but the profile was touched on 09-20: `installation.txt` mtime `18:10:32`, dir mtime `18:10:41`) |
| §1 Leg B "4 KB, one-page file … 112 MB WAL … per-job schema version 19 … single aborted `Recreate` … 02:19:48" | forensics `header: … 'db_size_pages': 1, 'schema_cookie': 0`; `wal: {'size': 112587272 … 'frames': 27327}`; `tables (19)`; `Version table: [(1, 19)]`; `Operation … [(1, 'Recreate', 1787642388)]` (= 02:19:48 CDT) | AGREE |
| §1 Leg B "2.3.0.4 refused it ('… is 11'), 2.4.0.0 refused it ('… is 12')" | journal `2026-09-19T20:48:12 … version 19 but the largest supported version is 11`; `… is 12` at 09-19 21:43/21:52/21:55 and 09-20 18:02/18:03/18:12 | AGREE (the 09-19 "is 12" refusals are not in the document — see DISAGREE 4) |
| §1 Leg B "operator moved the WAL aside at 18:19:26; server started 16 s later" | `temp/` mtime `18:19:26.342`, WAL ctime `18:19:26.342`; `ActiveEnterTimestamp=Sun 2026-09-20 18:19:42 CDT` | AGREE |
| §1 Leg B "Four attempts to import … two JSON parse errors on a file beginning with `S` … two AES wrong-password" | forensics `ErrorLog: count=4 … 'Failed to import backup'` at 1789947578/585 (`JsonReaderException: Unexpected character … value: S`) and 1789947759/767 (`SharpAESCrypt.WrongPasswordException`) | AGREE |
| §1 Leg C "21:03:27 … first opened with a key … Mismatch (21:08), Missing (21:15 onwards, after systemd rejected the `Environment=` line … `%`)" | `2026-09-18T21:03:30 Server has started … port 8300` with **no** "No database encryption key" line that minute (present at 20:22:12 and 20:42:38); `KEY_MISMATCH first 21:08:06`; specifier rejection `21:15:40 duplicati.service:11:`; `SettingsEncryptionKeyMissingException first 21:16:18` | AGREE |
| §1 "consistent copy … 2026-09-20 08:45 CDT … `~/.local/state/duplicati-server-db/Duplicati-server.sqlite` (240 KiB, schema 11) … holds `Yamaguchi`: 2 sources, 45 filters, 10 job options, daily 14:00 UTC, per-job index path" | forensics `[root_snapshot] … Version [(1, 11)] … ID=2 Name='Yamaguchi' … DBPath='/usr/lib/duplicati/data/BMXWPAOGLP.sqlite'`, `Schedule … Time='1789740000', Repeat='1D'`, `Source: 2 row(s)`, `Filter: 45 row(s)`, `backup 2: 10 options`; size `245760` B | AGREE on content; **the file has been replaced since** — birth/mtime now `2026-09-21 08:45:00.97` (timer fired again: `source … (240.0 KiB) … integrity : ok (16 tables)`) |
| §1 "settings key … byte-identical to the backup passphrase" | env-shape instrument: line 22 `key='SETTINGS_ENCRYPTION_KEY' … EQUALS_COMMENTED_PASSPHRASE=True` | AGREE |
| §1 "debug mode wrote every `.env` line … 10 times over on 2026-09-20" | per-minute `LINE: "` counts on 09-20: `110 16:41, 110 16:44, 22 16:50, 22 17:30` = 264 lines = 12 passes; `Settings Encryption Key:` prints `5, 5, 1, 1` = 12 | DISAGREE — 12 passes (10 in the 16:41/16:44 crash loops + 1 at 16:50 + 1 at 17:30) |
| §1 "earlier settings key … committed in a comment of `scripts/duplicati-wrapper.bash` and printed 23 times in the journal by systemd's own specifier error" | env-shape: `committed literal … len=36 metachars='$*@%'`; journal `grep -c 'Invalid slot'` → `23` = 22 `Failed to resolve specifiers` + 1 `Failed to resolve unit specifiers in … $DAEMON_OPTS': Invalid slot` | AGREE on 23 (see DISAGREE 3 for their dates) |
| §4.1 unit row: vendor unit `/usr/lib/systemd/system/duplicati.service` edited in place (mtime 09-21 01:44), no drop-in | `FragmentPath=/usr/lib/systemd/system/duplicati.service`, `DropInPaths=` (empty), no `*.d` dirs; `stat` → `2026-09-21 01:44:57 … root:root 644`; `dpkg -V` → `??5?????? /lib/systemd/system/duplicati.service` (not a conffile) | AGREE |
| §4.1 unit row: "**`NeedDaemonReload`** — on-disk `ExecStart` differs from the loaded one" | `systemctl show` → `NeedDaemonReload=no` (03:49 and 13:48); loaded `argv[]=/home/duplicati/bin/duplicati-wrapper.bash --daemon-opts="${DAEMON_OPTS}"` = on-disk line 15 `ExecStart=… '--daemon-opts="${DAEMON_OPTS}"'` | DISAGREE — a reload has happened; what is true is that the **running** process (started 18:19:42, argv[1] `'--webservice-port=8300 '`) predates the edit, so the next restart executes a different command line |
| §4.1 unit row: `User=duplicati Group=duplicati`; active since 18:19:42; PID 1397393; argv `duplicati-server "--webservice-port=8300 "` (one argument, trailing space) | `User=duplicati Group=duplicati`; `ActiveEnterTimestamp=Sun 2026-09-20 18:19:42 CDT`; `MainPID=1397393`; `/proc/1397393/cmdline` → `argc = 2`, `1 '--webservice-port=8300 ' len 23` | AGREE |
| §4.1 data-folder row: `/home/duplicati/.config/Duplicati/` mode 0777, "files 0777"; DB created 18:19:43, schema 12, 0 backups, 4 import errors (18:39, 18:42); `control_dir_v2/lock_v2` mtime 18:19:45 | dir `duplicati:duplicati 777`; files: `Duplicati-server.{sqlite,sqlite-wal,sqlite-shm,backup}` and crashlog `777`, but `.env 660`, `installation.txt 664`, `machineid.txt 664`, subdirs `775`; `lock_v2` `18:19:45.109`; import errors 18:39:38/18:39:45/18:42:39/18:42:47 | AGREE, except "files 0777" is only the four copied DB files + crashlog |
| §4.1 "Abandoned data folder `/usr/lib/duplicati/data/` (0700, now owned by `duplicati`; whole `/usr/lib/duplicati` chowned to `duplicati`)" | `stat` → `duplicati:duplicati 700 /usr/lib/duplicati/data`; `/usr/lib/duplicati` dir `duplicati:duplicati 755`; `find -user duplicati -type f \| wc -l` → `0`; `-type d` → `97`; `find -user root -type f` → `1443` (incl. `-rwxr-xr-x root root … duplicati-server`) | DISAGREE — only the directory tree was chowned; every regular file, binaries included, is still `root:root` |
| §4.1 snapshot row: system timer 13:45 UTC, root, "still firing daily; last 2026-09-20 08:45 CDT, `integrity ok (16 tables)`, 240 KiB" | `OnCalendar=*-*-* 13:45:00 UTC`; `User=` (empty → root); journal 09-14…09-21 every day `integrity : ok (16 tables)`, `240.0 KiB` since 09-19; `LastTriggerUSec=Sun 2026-09-20 08:45:01` at 03:50, and a new run `2026-09-21T08:45:00` | AGREE (superseded by today's run) |
| §4.1 watchdog row: user timer 12:00 local, `Linger=yes`, `UNREACHABLE` 09-19/09-20 (connection refused), `401` 09-21 02:52 | `Linger=yes`; `OnCalendar=*-*-* 12:00:00`; user journal `2026-09-19T12:00:39 … ALERT UNREACHABLE … Connection refused`, same 09-20; `2026-09-21T02:52:01 … login failed (401)` | AGREE |
| §4.1 disabled CLI lane: `~/.config/systemd/user/duplicati-backup.*` disabled; deployed runner lacks the 08-29 DB-holder fix; a copy run **as `duplicati`** 09-20 19:07 refused (`PASSPHRASE is unset`) | `is-enabled duplicati-backup.timer` → `disabled`, service `static`, `inactive`; `~/.local/bin/duplicati-scheduled-backup.bash` 13,340 B mtime `2026-08-24 16:16:41` vs repo `util/duplicati_scheduled_backup.bash` 14,390 B, 32 changed lines, last commit `ed82c1f6 2026-08-30T03:48:44Z perf(backup): one find, not a readlink fork per fd`; `/home/duplicati/.local/state/duplicati/backup-20260920-190738.log`: `FATAL: PASSPHRASE is unset or empty; refusing to run`, `last-run.status` `result=FAILED when=2026-09-20T19:07:38-05:00` | AGREE |
| §4.1 destination row: `sda1` 3.6 T / 400 G used; 877 files / 203 G, all `pcalnon:duplicati 0770`, no ACL entries, no setgid; no stray names | `findmnt` → `/dev/sda1 ext4 3.6T 399.8G`; census `877 pcalnon:duplicati:770`; `du -sh` → `203G`; `getfacl` (unfiltered) → only `user::rwx group::rwx other::---`; `find -perm -g+s` → none; stray-name grep → none | AGREE (the `+` that `ls` shows is uutils coreutils 0.8.0 flagging Dropbox's `user.com.dropbox.attrs` xattr, not an ACL) |
| §4.1 Dropbox row: root `/mnt/Backups/Ubuntu/Dropbox` (Pro), daemon PID 2948130, pcalnon **without gid 139**, "Up to date", 9 folders excluded, root also holds personal files | `info.json` → `{'path': '/mnt/Backups/Ubuntu/Dropbox', 'subscription_type': 'Pro'}`; `dropbox status` → `Up to date`; `exclude list` → 9 entries (`Apps, Camera Uploads, Data, Mobile Uploads, Operations, Photos, Public, Screenshots, Sent files`); pid 2948130 `Uid: 1000 … Groups: 4 24 27 30 46 100 114 128 984 993 1000 1003` (started Sat Sep 19 17:36:35); root holds 13 files (9 jpg, 2 pdf, 1 lnk, `.dropbox`) | AGREE |
| §4.1 wrapper row: `/home/duplicati/bin/duplicati-wrapper.bash` → symlink to the primary checkout's `scripts/duplicati-wrapper.bash`; `bin/` 0777; target now `main` `d721fc78` (#1969) | `lrwxrwxrwx … -> /home/pcalnon/Development/python/Juniper/juniper-ml/scripts/duplicati-wrapper.bash` (created `16:53:58`); `bin` `777`; target sha256 `07e230fd…` == `git show main:scripts/duplicati-wrapper.bash` == worktree copy; primary `HEAD` = `main` `d721fc78 2026-09-21T08:07:47Z … (#1969)`; target mtime `03:13:57` | AGREE (the "three changes between 02:15 and 03:14" are not observable, only the final mtime) |
| §4.1 `/etc/default/duplicati`: `DAEMON_OPTS="--webservice-port=8300"`, owned `duplicati:duplicati` 0644 | `stat` → `duplicati:duplicati 644`, 404 B, mtime `2026-09-20 16:39:58`; line 10 `DAEMON_OPTS="--webservice-port=8300"`, line 9 `# DAEMON_OPTS="--webservice-port=8300 --portable-mode"`; `dpkg -V` → `??5?????? c /etc/default/duplicati` | AGREE |
| §4.1 `.env`: 0660 duplicati:duplicati, 1,599 B; one active line (`SETTINGS_ENCRYPTION_KEY`, 32-char, single-quoted, contains `$ @ & #`), five commented lines (`PASSPHRASE_OLD`, `PASSPHRASE`, three spellings of the key); active key equals `PASSPHRASE` | `stat` → `1599 … duplicati:duplicati 660`; instrument: `22: key='SETTINGS_ENCRYPTION_KEY' export=True val_len=34 quoted=True … metachars="$@&'#" SAME_AS_COMMITTED_LITERAL=False EQUALS_COMMENTED_PASSPHRASE=True` (34 = 32 + the two quotes; commented `PASSPHRASE=<32 chars redacted>`); lines 17–21 `# export PASSPHRASE_OLD`, `PASSPHRASE`, `SETTINGS_ENCRYPTION_KEY` ×3 (35 chars each); awk `lines 22 commented_assignments 5 active_assignments 1` | AGREE |
| §4.1 package: `duplicati 2.4.0.0`, upgraded from 2.3.0.4 at 09-19 21:29, reinstalled 21:35 | `dpkg -s` → `Version: 2.4.0.0`; `dpkg.log`: `2026-09-19 21:29:28 upgrade duplicati:amd64 2.3.0.4 2.4.0.0`, `21:35:03 upgrade duplicati:amd64 2.4.0.0 2.4.0.0` | AGREE |
| §4.1 tar lane: no timer/path/cron/udev; both drives absent from `/media/pcalnon/` | `list-unit-files`, `crontab -l`, `/etc/cron.d`, `grep -rl 'EBC5-F0A3\|DFF3-2782\|juniper-backup'` → nothing; `lsblk` shows no such label/UUID; `/media/pcalnon` holds only the empty mount dirs `random_drive`, `temp_backups` | AGREE |
| §4.1 pcalnon profile: 0700; 4 KB stub + 112 MB WAL; `Duplicati-server.backup` = last real profile DB; 57 GB of job-DB copies under `backups/` | `pcalnon:pcalnon 700`; `4096` / `112587272`; `.backup` 479,232 B `2026-08-25 02:19:47`; `du -sh backups` → `56G` (57,783,156 KiB ≈ 55 GiB / 59 GB, 14 files) | AGREE (56 GiB) |
| §4.2 09-12→09-15: 09-13 and 09-14 14:00Z fail `Found 861 files that are missing`; ad-hoc 09-15 08:56Z succeeds | root-snapshot ErrorLog `exception@1789308059` (09-13 14:00:59Z) and `@1789394458` (09-14 14:00:58Z): `Found 861 files that are missing from the remote storage, please run repair`; watchdog `09-13 … ParsedResult=Fatal`, `09-14 … Fatal`, `09-15 … Success`; dlist `20260915T085649Z` | AGREE |
| §4.2 09-15: `.dropbox.cache`/`.dropbox` created 15:27–15:38; 20:46Z `865 files missing`; two `TaskCanceled`; ad-hoc 20:48Z succeeds | `.dropbox.cache` birth `15:38:06` mtime `15:27:36`; `.dropbox` birth `15:38:14`; ErrorLog `@1789505212` (20:46:52Z) `Found 865 files…`, `@1789505315`/`@1789505815` `TaskCanceledException`; dlist `20260915T204850Z` | AGREE |
| §4.2 09-16 14:01Z `Found 6 remote files that are not recorded…`; ad-hoc 18:33Z succeeds; no stray file today | `LastErrorDate = '20260916T140106Z'`, `exception@1789567266: … Found 6 remote files that are not recorded in local storage…`; dlist `20260916T183346Z`; stray grep empty | AGREE |
| §4.2 09-17 01:31 `No database encryption key was found…`; runs 09-17 22:13Z and 09-18 14:00Z succeed | `2026-09-17T01:31:01 … No database encryption key was found. The database will be stored unencrypted.`; dlists `20260917T221344Z`, `20260918T140000Z` | AGREE |
| §4.2 09-18 19:57 crash loop `installation.txt' is denied`; repeats 20:02 | `first 2026-09-18T19:57:47 … Access to the path '/usr/lib/duplicati/data/installation.txt' is denied.` (5 starts, `code=killed, status=6/ABRT`); again `20:02:25` | AGREE |
| §4.2 09-18 20:22 starts OK, still unencrypted; 20:26 `/mnt/Backups' is denied` | `20:22:12 No database encryption key…`, `20:22:13 Server has started … port 8300`; `20:26:54 … Failed to process the path: Access to the path '/mnt/Backups' is denied.` | AGREE |
| §4.2 09-18 21:03:27 start **with** a key, no warning; data mtime 21:03:34 | `21:03:30 Server has started` and no `NOKEY_WARNING` that minute; `/usr/lib/duplicati/data` mtime `2026-09-18 21:03:34.67` | AGREE |
| §4.2 09-18 21:08→21:11 three crash loops `SettingsEncryptionKeyMismatchException` | bursts `21:08:06` (5 starts), `21:11:26` (10 starts, two "Start request repeated" at 21:11:30/21:11:43) | AGREE |
| §4.2 09-18 21:15→21:29 `Environment=…%6%x…` rejected (`Failed to resolve specifiers … Invalid slot`, 23 occurrences) → `SettingsEncryptionKeyMissingException`; one unit revision with the key inside `ExecStart` fatal to load | specifier lines: `21:15:40 :11:`, `21:16:10 :11:`, `21:17:54 :10:`, `21:29:51 :10:` (4 on 09-18), then 18 on 09-19 (`05:53:43–51` ×4, `17:46:13–23` ×3, `18:06:53–18:07:32` ×11); `KEY_MISSING first 21:16:18`; `2026-09-18T21:23:25` ×2 lines: `Failed to resolve unit specifiers in … $DAEMON_OPTS': Invalid slot` + `duplicati.service: Unit configuration has fatal error, unit will not be started.` | AGREE on the mechanism and the fatal revision (21:23:25); DISAGREE on "23 occurrences" in that window (see list) |
| §4.2 09-19 19:14 same Missing loop; data chowned (ctime 19:15:00) | `19:14:58`, `19:15:00 … SettingsEncryptionKeyMissingException`; `/usr/lib/duplicati/data` ctime `2026-09-19 19:15:00.938` | AGREE |
| §4.2 09-19 19:19–20:44 "20 starts die with `wrapper.bash: line 30: --webservice-port=8300: command not found`" | `/home/duplicati/bin/wrapper.bash: line 30: --webservice-port=8300: command not found` ×5 at 19:19:55, ×10 at 20:39:31, ×5 at 20:44:36 (`status=127`) = 20 | AGREE |
| §4.2 09-19 20:48, 21:41–21:55 "wrapper echoes `--webservice-port=8300 --portable-mode`; server fails 25 times with `…Duplicati-server.sqlite' is denied`; one attempt at 20:48 … `version 19 … is 11`" | `Input Parameters: --webservice-port=8300 --portable-mode` at 20:48:11, 21:20:37, 21:41:03, 21:43:06, 21:52:21, 21:55:37, 21:57:35; failed starts (`Main process exited`): 20:48 ×5 all `version 19 … is 11`; 21:41 ×5 `Access to the path '/home/duplicati/.config/Duplicati/Duplicati-server.sqlite' is denied`; 21:43 ×4, 21:52 ×5, 21:55 ×5 `version 19 … is 12` | DISAGREE — 24 failed starts: 5 "denied", 5 "is 11", 14 "is 12" |
| §4.2 09-19 21:20, 21:57 two starts succeed **on port 8200** | `21:20:41 Server has started … port 8200`, `21:57:41 … port 8200` (each with a `signin.html?token=` URL) | AGREE |
| §4.2 09-19 21:20 pcalnon TrayIcon crashes with the same "version 19" message | `~/.config/Duplicati/Duplicati.GUI.TrayIcon-crashlog.txt` mtime `2026-09-19 21:20:56`: `The database has version 19 but the largest supported version is 11` (×4), `Duplicati.GUI.TrayIcon.Net10.Program`; profile `-shm` mtime `21:20:56.08` | AGREE |
| §4.2 09-20 04:15–04:43 "pcalnon profile files copied into `/home/duplicati/.config/Duplicati/`" | births: `backups/` `09-19 18:40:36`, `control_dir_v2` `18:42:18`, crashlog `18:42:18`, `machineid.txt` `18:42:22`, `Duplicati-server.sqlite` stub `09-19 21:57:36`, `installation.txt` `09-20 04:15:42`, `.backup` `04:41:07`, `-shm` `04:43:02` | DISAGREE — three waves (09-19 18:40–18:42, 09-19 21:57:36, 09-20 04:15–04:43) |
| §4.2 09-20 13:32–13:37, 16:50, 17:30 four successful starts; first (13:32:46) echoes `--portable-mode`, listens on 8200; from 13:36:37 no `--portable-mode`, 8300 | `13:32:46 Input Parameters: --webservice-port=8300 --portable-mode` → `13:32:50 … port 8200`; `13:36:37 Input Parameters: --webservice-port=8300` → `13:36:40 … port 8300`; `16:50:15` and `17:30:17 … port 8300` | AGREE (but five `KeyMissing` failures at `13:32:34`, identifier `wrapper.bash`, precede the success — see list) |
| §4.2 09-20 16:39–16:53 `.env` created; `/etc/default/duplicati` rewritten; symlink created; 16:41 `encrypted, but no key`, 16:44 `Mismatch`; debug echoes 220 lines, 50 commented assignments | `.env` birth `16:39:43`; `/etc/default/duplicati` `16:39:58`; symlink `16:53:58`; `16:41:16 … appears to be encrypted, but no key` + `KeyMissing`; `16:44:29 … Mismatch`; window 16:40–16:46: `LINE_echoes 220`, `LINE_commented_assignments 50`, `Exporting_Environment 10`, `Settings_Encryption_Key_prints 10` | AGREE (window figures exact) |
| §4.2 09-20 17:57:56 WAL copied next to the stub; 18:02 and 18:12 `version 19 … is 12` | `temp/Duplicati-server.sqlite-wal` birth `17:57:56.305`; `18:02:55` ×4 + `18:03:00` ×1, `18:12:26` ×5 `… largest supported version is 12` | AGREE |
| §4.2 09-20 18:19:26 WAL moved into root-owned `temp/`; 18:19:42 start; 18:19:43 fresh WAL; 18:19:46 `Server has started` (key supplied); 18:39/18:42 four import failures | `temp/` `root:root 755` mtime `18:19:26.342`; `ActiveEnterTimestamp 18:19:42`; WAL birth `18:19:43.917`; `18:19:46 Server has started … port 8300`, no "no key" warning; ErrorLog 18:39:38/45, 18:42:39/47 | AGREE |
| §4.2 09-20 22:26Z, 22:59Z PRs #1967/#1968 merge | worktree git: `6708cb28 2026-09-20T22:26:27Z … (#1967)`, `52571621 2026-09-20T22:59:22Z … (#1968)` | AGREE (via commit dates; GitHub itself is outside this lane) |
| §4.2 09-21 02:52 watchdog `login failed (401)` | user journal `2026-09-21T02:52:01 … login failed (401)` | AGREE |
| §4.3.1 `exec "${DUPLICATI_SERVER}" "${DUPLICATI_OPTS}"` (line 199 at #1968); one argv element with trailing space | `git show 52571621:…` → `199:exec "${DUPLICATI_SERVER}" "${DUPLICATI_OPTS}"`; live argv[1] `'--webservice-port=8300 '`; journal `Executing Duplicati Server: "/usr/lib/duplicati/duplicati-server --webservice-port=8300 "` | AGREE |
| §4.3.2 `DUPLICATI_INPUT_PARAMS=("${*}")`, `DUPLICATI_ENV_VARS_GLOBAL=("${DAEMON_OPTS}")` (lines 102, 105) | `102:    DUPLICATI_INPUT_PARAMS=("${*}");`, `105:    DUPLICATI_ENV_VARS_GLOBAL=("${DAEMON_OPTS}");` | AGREE (behavioural claims are lane 2b, not re-run here) |
| §4.3.3 `.env` parser lines 107–148 runs `eval`; "already defined" is `env \| grep KEY` | `135: eval "${EXPORT_CMD}"`; `131: DEFINED_ENV_VAR="$(env \| grep "${ENV_FILE_VAR_KEY}")"`; at `main`: `154: eval …` | AGREE |
| §4.3.4 `DEBUG_MODE` (line 196) prints the key; line 197 echoes the option string unconditionally | 2 lines dropped by the filter (they carry `Settings Encryption Key:`) at #1968; `197:echo "Executing Duplicati Server: \"${DUPLICATI_SERVER} ${DUPLICATI_OPTS}\""` (no `DEBUG_MODE` guard); at `main` the same echo is line 216; `68:DEBUG_MODE=${FALSE}` | AGREE |
| §4.3.5 no `set -euo pipefail`; `DUPLICATI_ENV_GLOBAL` defined and never read | `grep '^\s*set -'` → nothing (both revisions); `DUPLICATI_ENV_GLOBAL` occurs once in each | AGREE |
| §4.3.7 header comment lines 38–57 carries a credential-shaped 36-char literal; in `main` since #1967 | line 49 token len 72 (= `Environment=SETTINGS_ENCRYPTION_KEY=` + 36); env-shape `committed literal … len=36`; same 72-char token present at `6708cb28` (#1967) | AGREE (`gh repo view` visibility not checked — GitHub) |
| §4.3.8 symlink "points into a developer checkout on a feature branch"; `bin` and `.config/Duplicati` world-writable | primary checkout is on **`main`** (`git rev-parse --abbrev-ref HEAD` → `main`, `d721fc78`, clean for the file); `bin` `777`, `.config/Duplicati` `777` | DISAGREE on "feature branch" (today it is `main`); AGREE on 0777 |
| §4.3.9 on-disk unit `ExecStart=… '--daemon-opts="${DAEMON_OPTS}"'` | line 15 exactly that; lines 13–14 are the two earlier forms, commented | AGREE |
| §4.3.10 vendor unit edited in place (`dpkg` md5 mismatch; not a conffile) | `dpkg -V duplicati` → `??5??????   /lib/systemd/system/duplicati.service`; conffiles list holds only `/etc/default/duplicati` | AGREE |
| §4.4 all five dirs `drwxrwx--- pcalnon:duplicati`, no default ACLs, no setgid; all 877 volumes `-rwxrwx--- pcalnon:duplicati` | `stat` ×5 → `pcalnon:duplicati 770`; `getfacl` base entries only; census `877 pcalnon:duplicati:770`; ctimes: 874 files `2026-09-19 17:37`, dirs `/mnt/Backups` `09-18 20:36:53`, `/mnt/Backups/Ubuntu` `20:37:41` (dates the unrecorded chgrp) | AGREE |
| §4.4 Dropbox runs as `pcalnon` without the `duplicati` group (daemon predates the membership) | `Groups: 4 24 27 30 46 100 114 128 984 993 1000 1003`; daemon start `Sat Sep 19 17:36:35 2026`; `getent group duplicati` → `duplicati:x:139:pcalnon,duplicati` | AGREE |
| §4.4 Dropbox root is the whole dir; nine folders excluded; personal files sync; `Backups/` holds frozen set (811), ten old dlists, `_yamaguchi_keys/`, `_yamaguchi_records/`; escrow copy synced | exclude list = 9; root: 9 jpg + 2 pdf + 1 lnk; `_yamaguchi_frozen_20260826` → `811` files; `find Backups -maxdepth 1 -name '*.dlist.*'` → `10` (`.gpg`); `_yamaguchi_keys/{README.md,env}`; `dropbox filestatus …/_yamaguchi_keys` → `up to date` | AGREE (also present, unrecorded: `Backups/lost+found` dated 2023-05-16 and `/mnt/Backups/Ubuntu/.dropbox-dist` owned `duplicati:duplicati`, 09-14 20:49) |
| §5.1 profile and copy `Duplicati-server.sqlite` identical SHA-256 | `352a9bb23c32…` for both `/home/pcalnon/.config/Duplicati/…` and `/home/duplicati/.config/Duplicati/…` | AGREE |
| §5.2 snapshot journal `source : /usr/lib/duplicati/data/Duplicati-server.sqlite (240.0 KiB)` daily through 09-20 08:45; `/etc/default/duplicati` still carries the commented `--portable-mode` variant | journal 09-19, 09-20 (and 09-21) `(240.0 KiB)`; earlier `200.0/208.0/228.0/232.0 KiB`; line 9 of the defaults file | AGREE |
| §5.2 snapshot content (`Backup` ID 2 `Yamaguchi`, `DBPath`, `Schedule Time=1789740000 Repeat=1D`, 2 `Source`, 45 `Filter`, 10 `Option`, `LastBackupDate=20260918T140000Z`, `BackupListCount=9`, `TargetFilesCount=877`) | all literally present in `[root_snapshot]`; additionally `TargetFilesSize = '217760949689'` (byte-identical to `du -sb`) | AGREE |
| §5.2 profile `.sqlite`/WAL unmodified since 2026-08-25 02:20:46 | mtimes `02:19:48.014` / `02:20:46.941` (the `-shm` was touched `09-19 21:20:56` by the TrayIcon; `.sqlite` ctime `09-19 21:20:55`) | AGREE |
| §5.2 `.backup`: `Ubuntu` (id 2, `SJTCQIIZSJ.sqlite`), `Ubuntu-fresh` (id 3, `DQRVQNDIFX.sqlite`), empty `Schedule`, notification `Error while running Ubuntu: A task was canceled.` 02:19:46 | `DBPath='/home/pcalnon/.config/Duplicati/SJTCQIIZSJ.sqlite'` / `…DQRVQNDIFX.sqlite`; `Schedule: 0 row(s)`; `Notification … Title='Error while running Ubuntu', Message='A task was canceled.' … Timestamp='1787642386'` | AGREE |
| §5.3 header page size 4096, WAL mode, 1 page, schema cookie 0; 45 pages after replay; 19 tables (list); `Version 19`; `Operation (1,'Recreate',1787642388)`; `Configuration` = `repair-in-progress`; `Remotevolume`/`Fileset` empty | `'page_size': 4096, 'write_version': 2, 'read_version': 2 … 'db_size_pages': 1, 'schema_cookie': 0`; `page_count (after WAL replay): 45`; table list identical; `Configuration keys … ['repair-in-progress']`; `Remotevolume by Type/State: []`; `Fileset count … [(0, None, None)]` | AGREE |
| §5.3 server schema 16 tables, v11 (2.3.0.4) / v12 (2.4.0.0) | `[pcalnon_backup]`, `[root_snapshot]` → `tables (16) … Version [(1, 11)]`; `[live]` → `(16) … [(1, 12)]`, `Backup` gains `OperationType` | AGREE |
| §5.3 mechanism: `.backup` and `backups/backup Duplicati-server 20260825021947.sqlite` both 02:19:47, cancelled `Repair`/`Verify` at 02:19:19–02:19:46 | `.backup` `02:19:47.579`; `backups/backup Duplicati-server 20260825021947.sqlite` 479,232 B `02:19:47.579`; ErrorLog: `Failed while executing Repair` at `1787642270` (02:17:50), `1787642317` (02:18:37), `1787642364` (02:19:24); `Verify` at `1787642386` (02:19:46) | AGREE on files; DISAGREE on "02:19:19" (span is 02:17:50–02:19:46) |
| §5.3 current server DB: WAL born 18:19:43.917, 90 frames, salts `0cd60a99/6d70864b`; schema 12; `Backup` 0; `ErrorLog` 4 | `wal: {… 'salt1': '0cd60a99', 'salt2': '6d70864b', 'frames': 90}`; identical in run 2 | AGREE |
| §5.4 journal sequence: unencrypted through 09-18 20:42 → keyed 21:03:27 → Mismatch 21:08 → Missing 21:15 → same 09-19 19:14 | `20:42:38 No database encryption key…`; `21:03:30` no warning; `21:08:06 Mismatch`; `21:16:18 Missing`; `09-19 19:14:58 Missing` | AGREE |
| §5.4 "later successful starts … do not identify the key or the folder"; `Mismatch` at 09-20 16:44 | 8200 starts 09-19 21:20/21:57, 09-20 13:32; 8300 starts 13:36/16:50/17:30 all print only `Server has started` (+ sign-in URL); `16:44:29 Mismatch` | AGREE |
| §5.5 `--portable-mode` "still present in the 09-20 13:32 wrapper echo, gone at 13:36" | `13:32:46 … --webservice-port=8300 --portable-mode`; `13:36:37 … --webservice-port=8300` | AGREE |
| §5.5 "chmod'ed the folder to 0777 *after* the 18:19 start" | `/home/duplicati/.config/Duplicati` ctime `2026-09-20 18:33:45`; stub ctime `18:25:47` | AGREE |
| §5.5 "`/usr/lib/duplicati` (binaries included) and `data` chowned; destination chgrp'd; home dirs opened to 0777" | binaries **not** chowned (0 duplicati-owned files); destination chgrp'd 09-18 20:36–20:37 (dirs) / 09-19 17:37 (files); `.config/Duplicati` and `bin` 0777, `/home/duplicati` itself 755 | DISAGREE on "binaries included" only |
| §5.6 row C "shows exactly such lines at 09-20 13:32" | `13:32:50 Server has started … port 8200` without Mismatch/Missing — true; but `13:32:34` (16 s earlier, identifier `wrapper.bash`) has 5 × `SettingsEncryptionKeyMissingException` | AGREE with the qualification |
| §6 S-1 old key: wrapper comment lines 38–57, in `main` since #1967; 23 journal lines | see §4.3.7 row; `Invalid slot` count 23 | AGREE |
| §6 S-2 `.env` line 22, 0660; journal 10 `Exporting Environment` + 10 `Settings Encryption Key:` on 09-20 16:41–16:44 | line 22 active; window counts `10`/`10` (`5+5` per minute) | AGREE for the window (day total 12/12) |
| §6 S-3 `.env` lines 17–18 commented; 50 `LINE: "# export …"` echoes on 09-20 | lines 17 `# export PASSPHRASE_OLD=`, 18 `# export PASSPHRASE=`; commented-assignment echoes: 50 in the window, **60** over the day | AGREE for the window (day total 60) |
| §6 S-4 same passphrases in `…/Backups/_yamaguchi_keys/env`, now inside the Dropbox-synced tree | `_yamaguchi_keys/env` 388 B `-rwxrwx---+`; `dropbox filestatus` → `up to date` (contents not read) | AGREE |
| §6 S-5 primary checkout `.env` (`DUPLICATI_WEB_CREDENTIAL`), mode 0664 | `pcalnon:pcalnon 664 1024 …/juniper-ml/.env`; names include `export DUPLICATI_WEB_CREDENTIAL=` (plus 12 others, several Slack tokens/secrets) | AGREE (and broader than stated) |
| §6 S-6 sign-in token URL in journal 09-20 18:19:46 | `18:19:46 … Use the following link to sign in: http://localhost:8300/signin.html?token=<REDACTED len=249>`; also at 09-19 21:20:41, 21:57:41, 09-20 13:32:50, 13:36:40 | AGREE (five such URLs in total) |
| A.1 every line | matched to the second: 01:31:01, 19:57:47, 20:22:13, 20:26:54, 21:03:30, 21:08:06, 21:15:40 (`:11:`), 21:16:18, 20:48:12, 13:32:50, 16:41:15 (`LINE:` — counted, not read), 16:44:29, 18:02:55, 18:19:46 | AGREE (nit: the 20:48:12 line's identifier is `wrapper.bash`, not `duplicati-server`) |
| A.2 row 1 (profile + WAL) | 19 local tables, `Version 19`, `Recreate`, `repair-in-progress`, no volumes/filesets | AGREE |
| A.2 row 2 (`.backup`) | 16 tables, 11, `Ubuntu` id 2 / `Ubuntu-fresh` id 3, `Schedule` 0, notification 02:19:46 | AGREE |
| A.2 row 3 (root snapshot) | 16 tables, 11, `Yamaguchi` id 2, `DBPath=/usr/lib/duplicati/data/BMXWPAOGLP.sqlite`, `LastBackupDate=20260918T140000Z`, 9 filesets, 877 files, 45 filters, `TargetURL=enc-v1://…` | AGREE |
| A.2 row 4 (live) | 16 tables, 12, 0 backups, 4 × `Failed to import backup` (JSON `S` ×2, AES ×2) | AGREE |
| A.3 all seven rows | `temp/` `18:18:49 / 18:19:26 / 18:19:26`; 112 MB WAL `17:57:56 / 2026-08-25 02:20:46 / 18:19:26`; live WAL `18:19:43.917 / 18:42:47 / 18:42:47`; `lock_v2` `18:19:45` ×3; `.env` `16:39:43` ×3; symlink `16:53:58`; `/usr/lib/duplicati/data/` `2026-08-25 02:38:43 / 2026-09-18 21:03:34 / 2026-09-19 19:15:00` | AGREE (every row) |
| checklist: boot list | `journalctl --list-boots` → current boot `799ca02e…` since `Mon 2026-09-07 23:12:25 CDT`; earlier boots 09-05 19:29, 09-07 15:13, 09-07 15:15 | measured (the document makes no boot claim; consistent with "reboot survival 09-08") |
| checklist: `/home/pcalnon` path modes to the wrapper target | `namei -l`: `/home/pcalnon 755`, `Development/python/Juniper/juniper-ml/scripts` all `775 pcalnon:pcalnon`, file `-rwxrwxr-x` | measured — traversable by `duplicati` |
| checklist: existence of `insecure-permissions.txt`, `/mnt/Backups/Ubuntu/README.md`, `/mnt/Backups/Ubuntu/Yamaguchi`, snapshot sqlite | first three: `No such file or directory`; snapshot `-rw------- pcalnon 245760 2026-09-21 08:45:00.97` (mode 0600) | AGREE with §3.5/§4.4 (absent), snapshot present |

## 2. DISAGREE — what the document should say instead

1. **§4.1 unit row, "`NeedDaemonReload` — on-disk `ExecStart` differs from the loaded one".** Measured `NeedDaemonReload=no` at 03:49 and 13:48; the loaded `ExecStart` equals on-disk line 15. Say instead: "the unit was edited at 01:44:57 and has been reloaded; the **running** process (started 18:19:42) was launched by the earlier `"${DAEMON_OPTS}"` form — its argv is `--webservice-port=8300 ` — so the next restart executes `--daemon-opts="${DAEMON_OPTS}"` for the first time." (The §4.3.9 consequence stands; its trigger is a restart, not a reload.) Also note: the unit file is open in a live `vim` process (pid 2318515) and two `su - duplicati` sessions are running.
2. **§4.1 abandoned-folder row and §5.5, "whole `/usr/lib/duplicati` chowned to `duplicati` … (binaries included)".** Only the directory and its 96 subdirectories are `duplicati:duplicati`; all 1,443 regular files, `duplicati-server` included, remain `root:root`. Say: "`/usr/lib/duplicati` and every directory beneath it were chowned to `duplicati`; no file was."
3. **§4.2 09-18 21:15→21:29 row and §6 S-1, "23 occurrences".** 23 is the count of `Invalid slot` lines, but they are 22 `Failed to resolve specifiers` (unit line 11 at 21:15:40 and 21:16:10, line 10 from 21:17:54) plus one `Failed to resolve unit specifiers in … $DAEMON_OPTS'` at 21:23:25 (the ExecStart revision, with `Unit configuration has fatal error, unit will not be started`). Only 4 fall in 21:15–21:29 on 09-18; 18 are on 09-19 (05:53 ×4, 17:46 ×3, 18:06–18:07 ×11), i.e. the rejected `Environment=` line survived in the unit until at least 09-19 18:07:32.
4. **§4.2 09-19 20:48 / 21:41–21:55 row and §5.3.** Not "25 failures with `…sqlite' is denied` and one `version 19` attempt at 20:48". Measured 24 failed starts: 5 at 20:48 all `version 19 … largest supported version is 11`; 5 at 21:41 `Access to the path '/home/duplicati/.config/Duplicati/Duplicati-server.sqlite' is denied`; 14 at 21:43/21:52/21:55 `version 19 … is 12` (post-upgrade). §5.3's list of "is 12" refusals should include 09-19 21:43–21:55, not only 09-20 18:02/18:12.
5. **§1 and §4.2 16:39–16:53 row, "10 times over".** 12 passes on 09-20: `LINE:` echoes 110/110/22/22 at 16:41/16:44/16:50/17:30 (264 lines, 60 commented assignments), `Exporting Environment` 12, `Settings Encryption Key:` 12 (5/5/1/1). The two successful starts at 16:50 and 17:30 also echoed the file and printed the key once each. S-2/S-3's window figures (10/10/50 in 16:41–16:44) are exact.
6. **§4.3.8, "a developer checkout on a feature branch".** The primary checkout is on `main` at `d721fc78`, clean for that file, and the symlink target is byte-identical to `main:scripts/duplicati-wrapper.bash`. Keep the risk statement, drop the present-tense "feature branch".
7. **§1 / §4.1 snapshot row, "last 2026-09-20 08:45 CDT … the recovery source".** The timer fired again at 2026-09-21 08:45:00 and replaced the file (new inode; `integrity : ok (16 tables)`, same source, 240 KiB). Content is still the Yamaguchi job, but P0 should say "copy the current file aside first — it is overwritten every day at 13:45 UTC".
8. **§4.2 09-20 13:32 row / §5.6 row C.** Add: at 13:32:34, five starts of the older `wrapper.bash` died with `SettingsEncryptionKeyMissingException` (`appears to be encrypted, but no key`); the 13:32:50 success came from `duplicati-wrapper.bash` sixteen seconds later — two wrapper files were in play.
9. **§4.2 09-20 04:15–04:43 row, "pcalnon profile files copied".** Three waves by inode birth: 09-19 18:40–18:42 (`backups/`, `control_dir_v2/`, crashlog, `machineid.txt`), 09-19 21:57:36 (the 4 KB `Duplicati-server.sqlite` stub — so the "pcalnon-owned copy" denied at 21:41 was an earlier inode), 09-20 04:15:42–04:43:02 (`installation.txt`, `.backup`, `-shm`).
10. **§5.3, "cancelled `Repair`/`Verify` at 02:19:19–02:19:46".** The `.backup` ErrorLog has `Repair` failures at 02:17:50, 02:18:37, 02:19:24 and the `Verify` failure at 02:19:46; nothing at 02:19:19.
11. **§4.1 data-folder row, "files 0777".** Only `Duplicati-server.{sqlite,sqlite-wal,sqlite-shm,backup}` and the crashlog are 0777; `.env` is 0660, `installation.txt`/`machineid.txt` 0664, subdirectories 0775 (`temp/` root 0755).
12. **§4.2 omission.** The unit's last journal lines are `2026-09-20T18:47:44` and `18:51:33`: `Failed to process the path: Access to the path '/media/pcalnon/temp_backups' is denied` (×4 each) — the live server was pointed at the old `temp_backups` path after the import failures.
13. **A.1 nit.** The 20:48:12 line was emitted under identifier `wrapper.bash` (the first wrapper's name), not `duplicati-server`.

Unrecorded facts worth adding (not contradictions): the destination chgrp/chmod is datable — dirs `/mnt/Backups{,/Ubuntu}` ctime 09-18 20:36:53/20:37:41, 874 of 877 volumes ctime 09-19 17:37; `Backups/lost+found` (2023-05-16) shows that directory was once a filesystem root; `/mnt/Backups/Ubuntu/.dropbox-dist` is a Dropbox distribution owned `duplicati:duplicati` (09-14 20:49); `/home/duplicati` was created 09-18 19:31:40; the pcalnon profile's `installation.txt` was rewritten 09-20 18:10:32; today's watchdog run (12:00:39) alerted `401` again; `/opt/miniforge3/envs` is 62G (§3.4 says 47 GB — outside my sections).

## 3. UNTRACEABLE

- §4.1 wrapper row "target changed three times between 02:15 and 03:14" — only the final state is observable (mtime 03:13:57, == `main`); intermediate contents left no host trace.
- §4.3.7 "repository is public (`gh repo view`)", §4.3 shellcheck/40 synthetic cases, §4.3.11 PR body/diff claims — GitHub/lane-2b facts, not host state.
- §5.4 "what the key at 21:03 was" — root's shell history and `/home/duplicati/.bash_history` (0600) are unreadable; the only host evidence is the absence of the "no key" warning at 21:03:30.
- Which data folder the live server has open — `/proc/1397393/{fd,cwd,environ}` are `Permission denied`; the inference (`lock_v2` written 18:19:45 in `/home/duplicati/.config/Duplicati`, live WAL born 18:19:43.917, argv carrying no `--portable-mode`/`--server-datafolder`) is strong but indirect.
- Whether `temp/Duplicati-server.sqlite-wal` is byte-identical to the profile WAL — it is `0600 duplicati` (`Permission denied`); same size (112,587,272) and mtime (02:20:46.941), birth 17:57:56.
- Which of the 23 `Invalid slot` lines carry the literal — by rule, counted and never read; the 22 specifier lines are `Environment=` values and the 23rd is the `ExecStart` text.
- Appendix C.3 "snapshot copies before 09-18 are cleartext" — those copies no longer exist on disk (one file, overwritten daily). Note that the 08-25 pcalnon `.backup` also shows `TargetURL=enc-v1://…`, so encrypted fields are not unique to the post-09-18 root DB; whether the pcalnon profile was keyed cannot be decided from the host.
- `_yamaguchi_keys/env` "same passphrases" — file exists and is synced; its contents were deliberately not read.

## 4. Instrument adequacy

**(a) "the copied `Duplicati-server.sqlite` is a per-job schema at version 19 with one `Recreate` row — could the instrument have shown a server schema if one were there?"** Yes. The instrument copies main + `-wal` + `-shm`, opens the copy `mode=ro`, replays the WAL (reported `page_count (after WAL replay): 45` from a 1-page main file and 27,327 frames, `quick_check: ['ok']`) and lists `sqlite_master` verbatim. In the same run, the same code printed the 16-table server schema, `Version 11/12` and `Backup` rows for the three other databases, and it flagged the profile explicitly (`** LOCAL (per-job) database schema detected, not a server schema **`). Had the WAL not been replayed, the result would have been 0 tables (schema cookie 0), not 19 — so the 19 tables demonstrably come from the WAL, where a `Backup` table would have appeared the same way. The one residual risk (a second, later WAL generation) is excluded by the WAL header (`checkpoint_seq 0`, single salt pair) and the 08-25 mtimes.

**(b) "the current server DB has 0 backups — could the copy have been torn?"** No. Run 1 (13:40:27) and run 2 (13:49:19) produced byte-identical copies (sha256 `352a9bb2…` main, `38ffbce8…` WAL, `194105bf…` shm) with `quick_check ok`, 90 frames, salts `0cd60a99/6d70864b`; the WAL/shm mtimes (2026-09-20 18:42:47 / 18:43:41) are more than 19 h older than either copy, so no write was in flight. A torn copy would have shown a broken WAL frame chain (fewer valid frames) or a `quick_check` error; neither occurred.

## 5. What I could not verify without root

- Contents of `/usr/lib/duplicati/data/` (0700): existence/mtime of `BMXWPAOGLP.sqlite`, the real server DB's `enc-v1:` fields and mtime; `sg duplicati` does not help (owner-only mode).
- `/proc/1397393/{environ,fd,cwd,exe}` — whether `SETTINGS_ENCRYPTION_KEY` is in the live process environment and which database file it holds open.
- `/home/duplicati/{.bash_history,.viminfo}` (0600) and root's history — the 09-18 21:03 key and the exact commands of the migration.
- `temp/Duplicati-server.sqlite-wal` (0600) — byte identity with the profile WAL.
- `systemctl cat`/`show` sufficed for the unit; no credential store (`LoadCredential=` is empty) needed inspection.

<!-- markdownlint-enable -->

---

## Report A2 — Lane A — measurement re-creation; entry point: the REPOSITORY and the documents of record only

Sections of the design assigned: §2, §3, §4.3, §7 code blocks, Appendix B/C. Subagent id `af1ecce24c3f8cbeb` (24,246 characters).

<!-- markdownlint-disable -->

## Lane A2 — Lane A (measurement re-creation) report on `notes/JUNIPER_2026-09-21_JUNIPER-ECOSYSTEM_BACKUP-INFRASTRUCTURE-INTEGRATED-DESIGN.md`

Entry point: the worktree `/home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/atomic-sauteeing-truffle` (HEAD `d721fc78` + the five staged files), the cited `notes/` documents, the three handoffs, `git show` at `6708cb28`/`52571621`/`d721fc78`/`aab07eba`, `gh pr view`/`gh api` for #1967–#1969, and the scratch dir `/tmp/claude-1000/…/scratchpad/valA2/` (linter workdir under `snippets/`, an exec-neutered wrapper harness under `synth/`). Nothing on the live host was read; no other agent's output was read; nothing was written into the repository. Line 49 of the wrapper is referred to by line number and length only.

### 1. Verification table

| design location | claim | source checked | verdict | correction if any |
| --- | --- | --- | --- | --- |
| §0, §11 | "thirteen session handoffs", "7 notes documents" | `ls prompts/thread-handoff_automated-prompts/`, `ls notes/` | AGREE | 13 duplicati/backup handoffs 08-21 → 09-07; 7 notes files match DUPLICATI/BACKUP/CONSENSUS (incl. the 08-22 RUNBOOK and 08-24 FRESH-SET-CERTIFICATION, which the design does not name) |
| §0 | three instruments kept as provenance | staged files | AGREE (note) | a fourth staged instrument, `util/ad-hoc/2026-09-21_wrap_long_markdown_lines.py`, is not mentioned |
| §2 header | owner prompt archived at `prompts/manual/prompt132_2026-09-21.md`, branch `feat/backup-updates-and-redesign-work`, commit `aab07eba` | `git cat-file -t aab07eba`, `git branch -a --contains`, `git show aab07eba:prompts/manual/prompt132_2026-09-21.md` | AGREE (incomplete) | the same file is also on `main` since `d721fc78` (#1969, merged 08:07Z) and present in this worktree |
| §2 R-1…R-12 | every owner bullet represented; truncated sentence handled | prompt132 lines 15–42 vs table | AGREE | all 27 bullets map to R-1…R-12 (or §4.3/§5); the truncated "the additional backup should" is R-12 + D-5. R-4's *precedence* order is the wrapper header's (52571621 lines 26–30), the prompt states only a parse order |
| §2 R-13 | "a backup that silently stops must be indistinguishable from none" ← P §6 item 3 | PLAN lines 205–206 | DISAGREE (wording) | PLAN: "A backup that silently stops is indistinguishable from one that works." — the design's requirement sentence inverts the intent |
| §2 R-14 | ← P §6 item 4, Y §8.19 | PLAN 207–209; YAM §8.19.3 | AGREE | |
| §2 R-15 | ← Y §8.18 | YAM 1644–1728 | AGREE | |
| §2 R-16 | AES module, blocksize, retention/no-auto-compact pairing ← Y §3, PLAN §4 | YAM 84–91; PLAN 118–124 | AGREE (partial) | PLAN §4 says encryption **gpg** and retention "none initially"; AES and the pairing come from YAM §3/§4 only |
| §2 R-17 | mount verified, fstab-managed ← Y §8.10, DMG | YAM 611–663; `grep -iE 'mount|fstab'` DMG = empty | DISAGREE (citation) | DMG has no mount rule; the "verify the mount before any destination operation" rule is `HANDOFF_2026-08-22_duplicati-dbpath-and-recovery.md` §3 |
| §3.1 | PLAN §3 three-tier posture | PLAN 65–72 | AGREE | |
| §3.1 | cloud only as report upload (YAM §8.18.4) and password-manager escrow (YAM §8.24.1) | YAM 1719–1728, 2311–2332 | AGREE | |
| §3.1 | `Ubuntu` destroyed by interrupted compact 2026-07-13 (DMG §2) | DMG 43–66 | AGREE | |
| §3.1 | `Ubuntu-fresh` "0-for-3, superseded; YAM §3" | YAM line 59 (in §2, lines 31–67) | DISAGREE (section) | YAM §2 |
| §3.1 | Yamaguchi on the root server, portable mode (YAM §3–§8) | YAM line 44 (§2), §8.19.4 | AGREE (minor) | §2–§8 |
| §3.1 | "As certified" settings list | YAM §3 (68–98), §8.6-3, §8.10.1, §8.13.1 | AGREE (note) | YAM §3 proper certifies 46 filters, 13:00 schedule, 11 settings; the 44 filters / 14:00Z / 2 sources / 10 settings are §8.6-3, §8.10.1, §8.13.1 |
| §3.1 | destination after 08-26 migration; tempdir since 08-28 | YAM §8.13.4, §8.14.4 | AGREE | |
| §3.2 | six PLAN §7 criteria CLOSED in YAM (§8.7, §5.3, §8.4, §8.27, §8.13) | YAM 422–450, 113–154, 261–298, 2604–2810 | AGREE | §8.7 itself still shows criterion 5 unticked; closure is §8.27.1/§8.27.6 |
| §3.2 | PLAN boxes unticked; "Destination CHOSEN (temp_backups)" stale | PLAN 6, 219–230 | AGREE | |
| §3.3 rule 1 | Linger=no GNOME scope, 42-day outage ← DMG §5 | DMG 347–350 | DISAGREE (section) | DMG §9 "Structural findings" (§5 is "Surviving restore points") |
| §3.3 rule 2 | unmounted destination reads as "everything missing" (YAM §8.10); fstab (YAM §8.10.2) | YAM 636–642, 611–663 | AGREE | wording "Verify the mount before any destination operation" is HANDOFF_2026-08-22 §3 |
| §3.3 rule 3 | never stage on tmpfs ← GPG §9; runner guard 3b | GPG 295–297; `util/duplicati_scheduled_backup.bash:102–109` | AGREE | GPG §9 item 5 calls tmpfs staging "the biggest self-inflicted source"; DMG §9 and YAM §3 also record it |
| §3.3 rule 4 | retention only with `--no-auto-compact=true` ← YAM §3, DMG §2 | YAM 86–89; DMG 43–66 | AGREE | |
| §3.3 rule 5 | blocksize irreversible ← PLAN §4 | PLAN 118 | AGREE | |
| §3.3 rule 6 | drill rules ← DMG §4, YAM §5.3 | DMG 153–239 (§4a); YAM §5 item 3 | DISAGREE (section) | DMG §4a (not §4): `--no-local-blocks=true` mandatory, SHA-256 + length never exit code, `--time=` selection |
| §3.3 rule 7 | portable-mode trap ← YAM §8.19.4 | YAM 1822–1831 | AGREE | resolution order / `DataFolderManager` = NO ARTIFACT (product) |
| §3.3 rule 8 | DB is key material; cleartext without a key ← YAM §8.19.3 | YAM 1799–1803 correction block | AGREE | primary statement is §8.20.2 |
| §3.3 rule 9 | escrow off the source disk ← YAM §8.18 | YAM 1651–1717 | AGREE | |
| §3.3 rule 10 | hand-written `dbconfig.json` "silently maps a destination to the wrong local database" ← YAM §8.6 | YAM 377–385, 892–902 | DISAGREE (characterisation) | YAM §8.6-5: the CLI "would fail to parse the file — or … create a fresh random DB — rather than open the wrong one. Misleading rather than dangerous." |
| §3.3 rule 11 | never Repair / never kill -9 ← 08-22 handoff §3 | handoff lines 70–77 | AGREE | verbatim |
| §3.3 rule 12 | Paused + non-empty queue is a fault ← YAM §8.22 | YAM 2187–2190 | AGREE | |
| §3.3 rule 13 | read `TargetURL`; hardcoded path = confident wrong answer (YAM §8.14); zero = refusal (YAM §8.21) | YAM 978–983 (§8.13.2), 1151–1188 (§8.14.2), 2039–2057 (§8.21.2) | AGREE (pointer) | the pattern sentence is §8.13.2; §8.14.2 supplies three more instances |
| §3.3 rule 14 | primary-checkout ExecStart ← 09-07 handoff §1.6; copies-not-symlinks ← install_duplicati_timer header | handoff 171–176; `util/install_duplicati_timer.bash:19–24` | AGREE | |
| §3.3 rule 15 | "every handoff §method" | 08-21 §9, 08-22 §10; 09-07 has no method section | AGREE (over-general) | 2 of the 3 cited handoffs carry it |
| §3.4 | docker/conda gaps, sizes, O-2, O-5 ← 08-21 handoff §2.1, §3 | handoff 88–92, 181–195, 243, 251 | AGREE | |
| §3.4 | tar lane "covers ten repos but not `juniper-legacy` or parent-level dirs" | `util/juniper-backup.bash:102` (10 entries); 08-21 handoff §3 rows 1, 12 mark both tar ✅ | AGREE on fact, DISAGREE on attribution | the coverage fact comes from `APPLICATION_REPOS`, not from the cited handoff, whose table says the opposite as of 08-21 |
| §3.4 | open owner items from YAM §8.25–§8.27 | YAM 2524–2536, 2589–2603, 2789–2810; handoff §1.5/§1.6/§1.8 | AGREE | cloud reporting and the ProgramState check are enumerated via the 09-07 handoff that §8.27.6 points to |
| §3.5 b1, App C1 | YAM §8.20.4 / handoff §1.8 "schema-19 profile server DB … orphaned" | YAM 1974; handoff 226–228 | AGREE | quote exact |
| §3.5 b2, §7.3.5, D-1 | "YAM §8.19.3 (2026-08-30) records the owner's decision to reject SETTINGS_ENCRYPTION_KEY ('adds a third key needing escrow')" | YAM 1940–1941 (§8.20.2); §8.19.3 is dated 08-29 and holds only the correction block | DISAGREE (section) | YAM §8.20.2 |
| App C2 | "YAM §3 line 'a 2.3.0.4 run upgraded the profile server DB schema 11→19'" | YAM line 39 (§2) | DISAGREE (section + paraphrase) | see replacement text below; also repeated at §7 line 194 and §8.20.4 |
| App C3 | YAM §8.19.3 "the passphrase is stored in cleartext" | YAM 1799–1803; §8.20.2 heading 1919 | AGREE (pointer) | the sentence is the §8.20.2 heading; §8.19.3 says "It is cleartext" |
| App C4, §7.7 | snapshot script lines 20, 36; unit line 5 say "encrypted passphrase" | `yamaguchi_server_db_snapshot.py:20,36`; `util/systemd/yamaguchi-server-db-snapshot.service:5` | AGREE (minor) | line 36 reads "passphrase inside this DB is encrypted" (paraphrase); line 20 and unit line 5 carry the phrase verbatim |
| App C5 | record's paths `/mnt/Backups/Ubuntu/Yamaguchi`, `/mnt/Backups/Ubuntu/README.md` | YAM §8.13.4, §8.26.1; handoff §1.2, §2 | AGREE (record half) | the move is host-side |
| App C6 | "criterion 5 NEVER exercised" superseded by YAM §8.27 (09-08) | handoff 49–53; YAM 2612–2640, 2789–2795 | AGREE | |
| §4.1 | snapshot timer 13:45 UTC; SRC/DEST | `yamaguchi-server-db-snapshot.timer:15`; script lines 52–54 | AGREE | |
| §4.1, §7.6, AC-5 | watchdog 12:00 local, 26 h rule, `--backup-id 999` → JOB_MISSING | `yamaguchi-watchdog.timer:10`; `yamaguchi_watchdog.py:42,100,160–161` | AGREE | |
| §4.1 | deployed CLI runner lacks the 08-29 DB-holder fix | handoff §1.8; YAM §8.25.6; runner `db_holder_pids` (~lines 163–180) | AGREE | |
| §4.1, §7.2, §7.8 | tar lane drives, `/media/pcalnon/`, no timer/path/cron/udev in repo | `juniper-backup.bash:121–124, 303–305, 329`; grep of `.github util/systemd scripts util` = none | AGREE | |
| §4.2 | PR #1967/#1968 merged 09-20 22:26Z/22:59Z "(empty PR templates)" | `gh pr view`: 22:26:28Z, 22:59:23Z | AGREE (minor) | #1968 body is the unfilled template only; #1967 also carries an auto-generated description sentence |
| §4.1, §4.3 | #1969 = `d721fc78`, merged 08:07 UTC; adds `--daemon-opts` block lines 109–123; drops `DAEMON_OPTS` env branch | `gh pr view 1969`; `git diff 52571621 d721fc78` | AGREE | block is lines 110–123 (109 is the pre-existing assignment) |
| §4.3 intro | shellcheck 0.10.0 + 0.11.0 clean on both revisions | `shellcheck -S style` 0.11.0 on both → rc 0 | AGREE (0.11.0 only) | 0.10.0 not available on this host |
| §4.3 item 1 | line 199 `exec … "${DUPLICATI_OPTS}"`; two options glued | 52571621:199; harness T1 → `ARGC=1 ARGV[1]=<--webservice-port=8300 --webservice-interface=loopback >` | AGREE | reproduced exactly |
| §4.3 item 2 | lines 102, 105; substring dedup | 52571621:102,105,157–160; harness T3, T3c | AGREE | `--webservice-port-x=1` suppresses the default port; `--log-file` first suppresses a later `--log` |
| §4.3 item 3 | lines 107–148 eval parser and its consequences | 52571621:107–148; harness T2, T4, T5d, T7, T8c/T8d, T9, T13 | AGREE except one word | INJECTED printed; space truncation (`a b`→`a`); second-`=` cut (`x=y`→`x`); `DUPLICATI_SERVER=` redirects exec; `DEBUG_MODE=0` turns secret printing on; `PASSPHRASE_FILE` first suppresses `PASSPHRASE` (control: alone it exports); last unterminated line dropped; CR kept. "unbalanced quote aborts the line **silently**" — eval prints `unexpected EOF while looking for matching` to stderr; the line is dropped and rc stays 0 |
| §4.3 item 4 | line 196 key print, 112/133 echoes, 197 unconditional; #1967 key print unconditional | 52571621:112,133,196,197; `6708cb28:207` | AGREE | |
| §4.3 item 5 | no `set -euo pipefail`; exits 0; `DUPLICATI_ENV_GLOBAL` never read | harness T11 rc=0; line 76 only occurrence | AGREE | |
| §4.3 item 7, §6 S-1 | header lines 38–57 carry a 36-char credential-shaped literal; repo PUBLIC; in main since #1967 | 52571621:38–57 (line 49, 36 chars); `gh repo view` PUBLIC; `6708cb28:49` | AGREE | "gitleaks passed both PRs" not re-derived |
| §4.3 item 9 | 52571621 passes `--daemon-opts="…"` through and suppresses the default port; d721fc78 extracts `"--webservice-port` | code reading (line 187 substring); harness T6 → `ARGV[1]=< "--webservice-port >` | AGREE | |
| §4.3 item 11 | #1969 body quote; "no such script is in its diff"; notes naming convention | `gh api pulls/1969/files`; `git show d721fc78 --name-status -M`; naming-convention doc line 17, §3 | DISAGREE (second clause) | quote verbatim; but the diff contains `notes/backup_tests_pre-and-post_reboot.md` as an R100 rename (+0/−0) of repo-root `____TODO__DO-ME-AFTER-REBOOT____` (added `ea83bad7`, #1494, 2026-08-30): 14 lines of bash that `lpr` and `shred -u` the escrow sheet, then run the census and `yamaguchi_reboot_verify.bash`. Naming-convention violation: AGREE |
| §5.2, §5.3 | epochs 1789740000 = 2026-09-18T14:00Z; 1787642388 = 2026-08-25 02:19:48 CDT; 112,587,272 B / 27,327 frames; 90 frames ≈ 370 KB | `date`, arithmetic 32 + n×4120 | AGREE | 32+27,327×4120 = 112,587,272; 32+90×4120 = 370,832 |
| §5.3 | consistent with the 08-22 handoff's `DBPath` finding | handoff §1 lines 22–36 | AGREE | |
| §5.3 | server schema has 16 tables | YAM §8.20.1, §8.20.2 | AGREE | |
| §6 S-5 | `yamaguchi_server_api.py` reads primary-checkout `.env` key `DUPLICATI_WEB_CREDENTIAL`, port 8300; `duplicati_api.py` bare-secret fallback | `yamaguchi_server_api.py:40–42`; `duplicati_api.py:56–84` | AGREE | side-finding: `duplicati_api.py`'s candidate keys omit `DUPLICATI_WEB_CREDENTIAL`, so a file holding only that key already falls to the whole-file fallback |
| §7.6 | watchdog currently reads the primary checkout's `.env`; reporter writes durable record first | `yamaguchi_watchdog.py:58–59,89` (imports `yamaguchi_server_api`); `duplicati_backup_failure.bash:19–22,49,52` | AGREE | |
| §7.7 | snapshot script SRC/DEST/OWNER; unit runs as root | script 52–54; unit line 15 | AGREE | |
| §7.8 | install pattern; `Juniper-8.0.0.python`; "135 G / 67 G free as of the record" | `install_duplicati_timer.bash:11–24`; `juniper-backup.bash:124`; `HANDOFF_2026-08-27_backup-per-repo-review-and-arc-tail.md:371` "~135 GiB / ~67 GiB" | AGREE | the record is unnamed in the design |
| §7.9 | `--repos`, `--label full`, every `MEDIA_NAMES` entry forced under `/media/pcalnon/`, exit codes | `juniper-backup.bash:34–57,188–190,203–204,303–305,329` | AGREE | label regex `[A-Za-z0-9._-]+` admits `full` |
| §7.5 | job settings from the record | YAM §3, §8.10.1, §8.13.1 | AGREE | |
| §8 P0-6 | `yamaguchi_build_job.py` reads passphrase in-process, parses excludes from the runner, refuses duplicates | lines 39, 57–63, 66–74, 32–37, 96–97, 165–169 | AGREE | |
| §8 P2/P3, §9 | tools named exist; `reboot_verify pre|post`; `census --runs` | `ls`; `yamaguchi_reboot_verify.bash:67,87`; `yamaguchi_census.py:79` | AGREE | |
| §8 P4-4 | removal PR "owed since 2026-08-30" | YAM §8.19.8-7 (08-29), §8.20.5-7 | AGREE (minor) | already listed 08-29 |
| §0, App B | forensics instrument: copy-then-read-only, masks | `2026-09-21_duplicati_server_db_forensics.py:109–123,174,70–78,222–230` | AGREE | copies main/-wal/-shm, opens `mode=ro`, masks URL query, prints Option names only |
| §0, App B | env-shape instrument never prints a value | `2026-09-21_env_file_shape.py:76–114` | AGREE | comments redacted after `=`; equality by SHA-256 |
| App B rows 1, 5 | `etc/default/duplicati` linted by "KEY=VALUE grammar"; `.env` by "grammar" | linter output "[PASS] no linter" for both; `os.path.splitext` → `''` for both names | DISAGREE (vacuous pass) | neither block is checked; the grammar branch (`ext in (".env", ".default")`) is unreachable for these names |
| App B run | `python3 util/ad-hoc/2026-09-21_lint_design_snippets.py --doc … --workdir …` | run twice in `valA2/snippets/` | DISAGREE (first-run result) | 12 tagged blocks found; run 1: exit 1, 2 FAIL (`duplicati.service`, `juniper-backup.service`: Exec target "not executable: No such file or directory", no rewrite) because each block is linted before later blocks are extracted; run 2 on the populated workdir: exit 0, 12/12 PASS with both Exec paths rewritten. All 8 bash blocks pass `bash -n` + shellcheck 0.11.0; the three T2 units and the service unit pass `systemd-analyze verify` once targets resolve |
| whole doc | repeated numbers agree (877; 202.8 GiB/203 G; 9 dlists; 45 filters; 2 sources; 10 options; schema 11/12/19; 27,327 and 90 frames; 220/50/10/10/23; 18:19:42–46; 21:03:27/30/34; 08:45 CDT = 13:45 UTC; PR times) | every occurrence cross-read | AGREE | one soft spot: §1 "last successful run … 2026-09-18 14:12 UTC" vs §4.2 "09-18 14:00Z succeed" and fileset `20260918T140000Z` — reconcilable as start vs completion, but the design never says so |

### 2. DISAGREEs with exact replacement text

1. **§2 R-13** — replace "Alerting: a backup that silently stops must be indistinguishable from none" with "Alerting: a backup that silently stops must not be indistinguishable from one that works (PLAN §6 item 3: 'A backup that silently stops is indistinguishable from one that works')".
2. **§2 R-17 source** — replace "Y §8.10, DMG" with "Y §8.10.2, HANDOFF_2026-08-22_duplicati-dbpath-and-recovery.md §3". DMG contains no mount or fstab text.
3. **§3.1** — replace "(0-for-3, superseded; YAM §3)" with "(0-for-3, superseded; YAM §2)". The sentence is YAM line 59, inside §2 (lines 31–67).
4. **§3.3 rule 1** — replace "(DMG §5)" with "(DMG §9)". DMG lines 347–350: "`app-gnome-duplicati-2525453.scope` … `Linger=no`. The process therefore dies at logout … the mechanical root cause of the 42-day silent outage".
5. **§3.3 rule 6** — replace "(DMG §4, YAM §5.3)" with "(DMG §4a, YAM §5 item 3)". DMG §4a: "`--no-local-blocks=true` is mandatory", "judged by SHA-256 + byte length … never by exit code", "both scripts now select by `--time=<fileset timestamp>`".
6. **§3.3 rule 10** — replace "a hand-written `dbconfig.json` silently maps a destination to the wrong local database (YAM §8.6)" with "a hand-written `dbconfig.json` is a stale locator the CLI would fail to parse or ignore, creating a fresh random database rather than opening the intended one (YAM §8.6-5, §8.12.4: 'Misleading rather than dangerous … Always pass `--dbpath` regardless')".
7. **§3.5 bullet 2, §7.3.5, D-1** — replace "YAM §8.19.3 (2026-08-30) records the owner's decision to **reject** `SETTINGS_ENCRYPTION_KEY`" with "YAM §8.20.2 (2026-08-30) records the owner's decision to **reject** `SETTINGS_ENCRYPTION_KEY`". YAM lines 1940–1941: "**Owner decision (2026-08-30): accept as-is and document.** `SETTINGS_ENCRYPTION_KEY` was rejected — it adds a third key needing escrow and makes the DB unreadable if lost." §8.19.3 is dated 2026-08-29 and holds only the cleartext correction block.
8. **Appendix C item 2** — replace "YAM §3 line 'a 2.3.0.4 run upgraded the profile server DB schema 11→19'" with "YAM §2 (line 39): 'the TrayIcon crash of 02:30 was that same era's binary refusing the profile server DB after a 2.3.0.4 run upgraded it (schema 11→19; pre-upgrade copy preserved as `Duplicati-server.backup`)'; repeated as 'schema 19' at YAM §7 (line 194) and §8.20.4 (line 1974)".
9. **§3.4 attribution** — after "the tar lane covers ten repos but not `juniper-legacy` … or the parent-level `notes/ prompts/ util/ backups/`" add "(per `util/juniper-backup.bash` `APPLICATION_REPOS`, line 102; the 08-21 handoff's §3 table marked both as tar-covered at the time)".
10. **§4.3 item 3** — replace "an unbalanced quote aborts the line silently" with "an unbalanced quote makes `eval` print `unexpected EOF while looking for matching` to stderr (the journal), drop the line, and continue with exit status 0".
11. **§4.3 item 11** — replace "no such script is in its diff, and its `notes/backup_tests_pre-and-post_reboot.md` does not follow the notes naming convention" with "the only 'script' in its diff is `notes/backup_tests_pre-and-post_reboot.md`, a 100 %-similarity rename (+0/−0) of the repo-root file `____TODO__DO-ME-AFTER-REBOOT____` (added in `ea83bad7`, #1494, 2026-08-30): a 14-line bash file that prints (`lpr`) and `shred -u`s the escrow sheet, then runs the census and `yamaguchi_reboot_verify.bash`. It carries a `.md` extension, lives in `notes/`, and does not follow the notes naming convention".
12. **Appendix B rows 1 and 5** — replace "KEY=VALUE grammar" and "grammar" with "not linted (extracted only): the instrument's grammar branch keys on `os.path.splitext`, which returns no extension for `etc/default/duplicati` and for a dot-file named `.env`". (Or fix the instrument to key on basename/lang.)
13. **Appendix B run line / §0** — add: "Run it twice, or extract all blocks before linting: the instrument lints each block as it is written, so units whose `ExecStart=` names a later block fail on the first pass (`duplicati.service`, `juniper-backup.service`) and pass on the second."
14. **§2 R-16 source** — replace "Y §3, PLAN §4" with "Y §3/§4 (AES, pairing), PLAN §4 (`--blocksize`; PLAN §4 itself specifies gpg and no initial retention)".
15. **§3.3 rule 15** — replace "(every handoff §method)" with "(`HANDOFF_2026-08-21…` §9, `HANDOFF_2026-08-22…` §10)".

Minor, no replacement required: §4.3 intro "lines 109–123" is 110–123; §3.1 "YAM §3–§8" for the root/portable facts is §2–§8; §7.7 line 36 reads "passphrase inside this DB is encrypted" rather than "the encrypted passphrase"; §2 header should note the prompt also landed on `main` via `d721fc78`; §4.2 "empty PR templates" — #1967's body is not empty; §7.8 "as of the record" should name `HANDOFF_2026-08-27_backup-per-repo-review-and-arc-tail.md` §7 (~135 GiB / ~67 GiB).

### 3. NO ARTIFACT (cited, not present in the repository or documents of record)

- Product sources and help output cited in §3.3 rule 7, §5.4, §5.5, §7.3.5, §7.5, App C7–C8: `Duplicati/Library/AutoUpdater/DataFolderManager.cs` (`GetDataFolder`, `PrepareSecureDataFolder`), `Duplicati/Server/Program.cs` `GetDatabaseConnection`, `ReWriteAllFieldsIfEncryptionChanged`, `BackendManager.RenameFileAfterError`, `RunScript.cs`, `/usr/lib/duplicati/run-script-example.sh`, `duplicati-cli help no-local-blocks`, `duplicati-database-tool wipe-encryption` (2.3.0.108), the 2.0.9.105/106 key history, `duplicati-server-util` defaults, `Duplicati.GUI.TrayIcon.Net10`.
- `man systemd.exec` claims (`EnvironmentFile=` not specifier-expanded; `ReadWritePaths=` nesting).
- The step-2b report, its "40 synthetic-input cases" and its shellcheck 0.10.0 run (other agents' output; 0.10.0 absent on this host).
- "gitleaks passed both PRs" — GitHub check runs were not queried.
- Design-proposed files that exist only as tagged blocks: `util/install_duplicati_service.bash`, `util/systemd/duplicati.service`, `util/systemd/duplicati.default`, `usr/local/lib/duplicati/*`, `util/juniper-backup-scheduled.bash`, `util/ad-hoc/2026-09-21_backup_destination_permissions.bash`, `util/ad-hoc/2026-09-21_probe_settings_key_on_copy.bash`, the three `juniper-backup.*` user units (expected — §7.3.4 says the installer refuses without them).

### 4. What this lane cannot verify

Everything that is a statement about the live host rather than the repository or the record: §1 and §4.1's host columns (unit state, PIDs, argv, modes 0777/0700, ownership, `NeedDaemonReload`, dpkg md5, Dropbox daemon groups and exclude list), the §4.2 journal/inode/dpkg timeline, §4.4 modes/ACLs/stray files, the §5 forensics results on the database copies (877/45/2/10/9, `DBPath`, `TargetURL`, `Version`, `Operation`, WAL salts), the `.env` shape and the key/passphrase equalities (§6 S-2/S-3), the journal counts (220/50/10/10/23), the 09-18 key candidates, the symlink-target changes, `/etc/default/duplicati` content, `_yamaguchi_keys/` placement under the Dropbox root, Appendix A.1–A.3, and all Duplicati 2.4.0.0 behavioural claims (lane 2c). The repository-facing and record-facing claims above were re-derived from the artifacts named in the table; where a verdict says AGREE it was reached from the artifact, not from the design's prose.

Instruments used and whether they could have said otherwise: `grep`/`sed` over the record (would have shown any other section), `git show`/`git diff`/`gh pr view`/`gh api` (any other commit, time, file status or rename source), the scratch harness on an exec-neutered copy of the wrapper (controls T8d and the reverse-order T3c produced the opposite result when the mechanism was absent), and two runs of the design's own linter (the second run differing from the first is what exposed the ordering defect).

<!-- markdownlint-enable -->

---

## Report A3 — Lane A — measurement re-creation; entry point: PRODUCT BEHAVIOUR and CODE EXECUTION (Duplicati source at the 2.4.0.0 tag, systemd man pages, running the design's code blocks against synthetic inputs)

Sections of the design assigned: §5.5, §7.3, §7.5, §7.8, §7.9, Appendix B. Subagent id `a7b00f283c3a2bc9b` (43,355 characters).

<!-- markdownlint-disable -->

Nothing further is needed — the last excerpt settles (d): on Linux the default-provider selector tries **only** `LibSecretLinuxProvider` and then `return null;` — there is no `pass` fallback as a *default* provider (the `pass` module exists only as an explicit `--secret-provider`). All evidence is in; here is the final report.

# Lane A3 — measurement re-creation: FINAL REPORT

**Scratch (all artifacts, harnesses, fetched sources, literal outputs):** `/tmp/claude-1000/-home-pcalnon-Development-python-Juniper-juniper-ml/227cf645-dd93-4802-91f7-23dcaa92c971/scratchpad/valA3/` — `help_*.txt` (sandboxed Duplicati help), `man_*.txt`, `gh/v2.4.0.0_stable_2026-09-03/*` (verbatim GitHub sources), `result_{k,l,m,g}.txt`, `security_*.txt`, `lint_run.txt`, `snippets/` (extracted blocks), `verify2/`.
**Sandbox discipline held:** every Duplicati binary ran as `env -i HOME=<scratch>/fakehome XDG_CONFIG_HOME=… timeout 60 <bin> help|system-info`; the only files created outside scratch were the sandbox's own `fakehome/.config/Duplicati/{machineid.txt,installation.txt,dbconfig.json}` (inside scratch) and one `/tmp/valA3-backupdir-<pid>` directory required by test (m), removed afterwards. `duplicati-server --version` was never run; no `--server-datafolder`/`--dbpath` pointed anywhere real. Host: systemd 259 (259.5-0ubuntu3.4), Duplicati 2.4.0.0 (`2.4.0.0_stable_2026-09-03`), Ubuntu 26.04.1, udisks2 2.10.91-1ubuntu2.1.
**Nothing was written into the repository.**

**Incident to disclose (my instrument, now fixed):** the first run of the wrapper harness printed `env | grep '^TEST_'`, which also captured two *inherited session* variables, `TEST_TWINE_USERNAME`/`TEST_TWINE_PASSWORD` (a TestPyPI token present in the shell environment), into `result_k.txt` and into that tool's output. I allow-listed the exact names the harness sets, regenerated the file, and swept scratch (`grep -rl` finds nothing). The value is not reproduced anywhere in this report; the owner may want to rotate that TestPyPI token as a precaution since it transited a tool result.

---

## Part 1 — product claims

| claim | source | quote (verbatim) | verdict |
|---|---|---|---|
| **(a)** `--no-local-blocks` deprecated in 2.4.0.0; not using local blocks is the default; opt-in `--restore-with-local-blocks` | sandboxed `duplicati-cli help no-local-blocks` (`help_cli_nolocalblocks.txt`); `Duplicati/Library/Main/Options.cs:555-556,1548`; `changelog.txt:3511` (section 2.0.9.101_canary_2024-06-27) | "`--no-local-blocks (Boolean): Do not use local data [DEPRECATED]: The default is now to not use local blocks for restore. To opt-in for using local blocks, set the option --restore-with-local-blocks.`" / `UseLocalBlocks => GetBool("restore-with-local-blocks")` / "Changed restore to not use local blocks by default" | **AGREE** (default flipped in canary 2.0.9.101, 2024-06-27; deprecation text present in 2.4.0.0). See DISAGREE #2: §7.5 rule 5 still passes `--no-local-blocks=true`. |
| **(b)** precedence `--server-datafolder` > `--portable-mode` > `DUPLICATI_HOME` > `~/.config/Duplicati`; no automatic portable mode | `DataFolderManager.cs:104-141` (`GetDataFolder`), `:84`, `:348-353`; `duplicati-server help` | "`if (!string.IsNullOrWhiteSpace(datafolderArg)) {…} // Portable mode is prefered over the environment variable else if (PORTABLE_MODE) { dataFolder = …Path.Combine(UpdaterManager.INSTALLATIONDIR, "data") } // Use the legacy environment variable, if set else if (!string.IsNullOrWhiteSpace(envOverride)) {…} // Use the default location else {… DataFolderLocator.GetDefaultStorageFolderInternal(…)}`"; `DATAFOLDER_ENV_NAME = $"{APPNAME}_HOME"`; `ParseBoolSlim`: "`// In debug builds, we default to portable mode if (value == null) #if DEBUG return true; #else return false;`"; help: "`--server-datafolder: … This option can also be set with the environment variable DUPLICATI_HOME.`" | **AGREE** (release builds: no auto-portable; only DEBUG builds default to portable). |
| (b) sandboxed `system-info` | `help_cli_systeminfo.txt` | `Default data folder: <scratch>/fakehome/.config/Duplicati/` · `Install folder: /usr/lib/duplicati/` · `Version name: "2.4.0.0_stable_2026-09-03" (2.4.0.0)` · `Tempdir: /tmp/` · `SQLite: 3.53.4` | reported as asked (HOME-derived default even though the install folder is `/usr/lib/duplicati/`). |
| **(c)** 0700 gate: pre-existing folder verified at every start, never modified; overrides `--allow-insecure-datafolder` / `insecure-permissions.txt` | `DataFolderManager.cs:169-172,186-222`; `Server/Program.cs:1136-1141`; `Common/IO/Util.cs:43,50,55,68-90`; `SystemIOLinux.cs:479-530`; `changelog.txt:1-23,296-316,539-581` | "`A pre-existing folder is never modified. Its permissions are verified as-is: if they are already in the canonical locked-down form the folder is accepted; otherwise it is rejected, because Duplicati does not adopt or "heal" a folder it did not create`"; Program.cs: "`DataFolderManager.PrepareSecureDataFolder(System.IO.Path.GetDirectoryName(databasePath), createIfMissing: true);`" (runs before `OpenDatabaseAsync` on every start); Util.cs: `AllowInsecureDatafolderOption = "allow-insecure-datafolder"`, `AllowInsecureDatafolderEnvVar = "DUPLICATI__ALLOW_INSECURE_DATAFOLDER"`, `InsecurePermissionsMarkerFile = "insecure-permissions.txt"`; changelog 2.4.0.0: "`To opt out … pass --allow-insecure-datafolder, set the environment variable DUPLICATI__ALLOW_INSECURE_DATAFOLDER=true, or place a file named insecure-permissions.txt in the installation folder.`"; Linux canonical: "`if ((info.Permissions & expectedMode) != expectedMode)` [0700] … `// No group/other access bits should be set` … `// The owner must be the current user or root.`" | **AGREE** on mechanism and on "0700, no group/other bits, owner current user or root". **Release: DISAGREE (precision)** — introduced in **2.3.0.107_canary_2026-07-13** ("Locked down data folder permissions and require exact permissions, with opt-out via `--allow-insecure-datafolder`"), first beta 2.3.1.0 (2026-07-28), first stable 2.4.0.0; `insecure-permissions.txt` (install folder) re-introduced in 2.3.0.108. Third override the design omits: the env var. |
| **(d)** `SETTINGS_ENCRYPTION_KEY` / `--settings-encryption-key` / `--disable-db-encryption` / `--require-db-encryption-key` exist | sandboxed `duplicati-server help` | "`--disable-db-encryption: Use this option to disable database encryption of sensitive fields`" · "`--require-db-encryption-key: Use this option to require a custom provided key for database encryption of sensitive fields and not rely on the serial number.`" · "`--settings-encryption-key: … This option can also be set with the environment variable SETTINGS_ENCRYPTION_KEY.`" · also `--disable-default-secret-provider` | **AGREE** (note the stale "serial number" wording in the product's own help). |
| (d) what is encrypted | `RestAPI/Database/Connection.cs:66-89,862,1794`; `Encryption/EncryptedFieldHelper.cs:82,109-139` | `_encryptedFields` = every `SupportedCommands` option of `ArgumentType.Password` across Backend/Encryption/Compression/Generic/Web/SourceProvider/RestoreDestination loaders **and** `new Options(…).SupportedCommands` (so `passphrase`), plus `JWT_CONFIG, PBKDF_CONFIG, REMOTE_CONTROL_CONFIG, SERVER_SSL_CERTIFICATE, SERVER_SSL_CERTIFICATEPASSWORD, SERVER_CA_CERTIFICATE_KEY, SERVER_CA_CERTIFICATE_PASSWORD, REMOTE_CONTROL_STORAGE_API_KEY, CLIENT_LICENSE_KEY`; TargetURL: "`m_encryptSensitiveFields ? EncryptedFieldHelper.Encrypt(n.TargetURL, m_key) : n.TargetURL`"; `HEADER_PREFIX = "enc-v1:"`; "`if (keyHash != key.Hash) throw new SettingsEncryptionKeyMismatchException();`" / null key → `SettingsEncryptionKeyMissingException` | **AGREE** |
| (d) `ReWriteAllFieldsIfEncryptionChanged` two-start re-key | `Connection.cs:131-152`; `Program.cs:324,1275`; `changelog.txt` (2.0.9.106 section) | "`if (this.ApplicationSettings.EncryptedFields != m_encryptSensitiveFields) { … AddOrUpdateBackup(b, false, null); … this.SetSettings(…); this.ApplicationSettings.EncryptedFields = m_encryptSensitiveFields; … VACUUM`"; called unconditionally at start (`connection.ReWriteAllFieldsIfEncryptionChanged();`); the connection is built as `new Connection(con, disableDbEncryption, encKey, …)` so the old key still decrypts on the `--disable-db-encryption` start; vendor procedure: "`If you need to change the key, you can temporarily decrypt the database by starting the server with --disable-db-encryption. After starting, stop the instance again, change SETTINGS_ENCRYPTION_KEY to the new key and start again without the argument, to have it re-encrypted.`" | **AGREE** |
| (d) `wipe-encryption` exists + help | sandboxed `duplicati-database-tool help` / `help wipe-encryption`; `WipeEncryption.cs:44-52`; `changelog.txt:473-507` | "`wipe-encryption <databases>   Removes or clears any encrypted strings from the server database so it can be used without the original encryption key. A backup of the database is created first unless --no-backups is given. Only applies to the server database; other databases are skipped.`" Options: `--server-datafolder`, `--no-backups`, `--include-untracked-databases`, `--dry-run`, `--allow-insecure-datafolder`. Added in **2.3.0.108_canary_2026-07-20**. | **AGREE** |
| (d) exception names | `Interface/CustomExceptions.cs:211,223`; `Interface/Strings.cs:51-52` | `class SettingsEncryptionKeyMismatchException : UserInformationException` ("Encryption key used to encrypt target settings does not match current key."), `class SettingsEncryptionKeyMissingException` ("Encryption key is missing.") — identical to the journal lines in Appendix A.1 | **AGREE** |
| (d) default secret-provider behaviour with no key | `Program.cs:1195-1265`; `Main/SecretProviderHelper.cs:94-116`; `DynamicLoader/SecretProviderLoader.cs:178-210` | "`if (!hasValidEncryptionKey && defaultSecretProvider != null) { … ResolveSecretAsync(DATABASE_SECRET_VALUE_NAME…) … if (!disableDbEncryption && string.IsNullOrWhiteSpace(encryptionKey) && defaultSecretProvider.IsSetSupported && !hasEncryptedFields) { var tmpkey = Convert.ToBase64String(RandomNumberGenerator.GetBytes(32)); … SetSecretAsync(…)`" then "`if (!hasValidEncryptionKey && !disableDbEncryption) { disableDbEncryption = true; … Strings.Program.NoEncryptionKeySpecified(…)`"; Linux selector: "`if (OperatingSystem.IsLinux()) { var res = new LibSecretLinuxProvider(); if (await res.IsSupported(…)) { … if (await res.DoesCollectionExist(…)) return res; } } return null;`" | **AGREE on the mechanism** (mint a random key via the OS store, else fall back to unencrypted with the warning). **DISAGREE (precision):** the *default* provider on Linux is **libsecret only**; `pass` (`UnixPassProvider`, key `"pass"`) is registered but is never tried as a default — it must be named with `--secret-provider`. |
| (d) machine-derived key history | `changelog.txt:3329-3388` (2.0.9.105_canary_2024-08-29), `:3275-3327` (2.0.9.106_canary_2024-09-03); `DataFolderManager.cs:147-157`; sandbox observation | 105: "`Encrypting data in Duplicati-server.sqlite with machine serial number` … `The key used to encrypt is derived from the machine serial number, so the database cannot be decrypted on another machine`"; 106: "`Note for users of 2.0.9.105: The method for extracting an encryption key from the machine seriail number did not produce secure results. This feature has now been removed`" / "`Removed DeviceId as the source for settings encryption keys`" / "`Added blacklisting of some settings encryption keys`" (residual: `IsKeyBlacklisted → DeviceIDHelper.EMPTY_DEVICE_ID_HASHES`); `installation.txt`/`machineid.txt` are written by `GetDataFolder` from `AutoUpdateSettings.Update{Install,Machine}FileText` and were created by a mere `duplicati-cli system-info` in the sandbox | **AGREE** |
| **(e)** `--run-script-before-required`: any non-zero or timeout aborts; plain `--run-script-before`: 0/2/4 run, 1/3/5 stop | `Modules/Builtin/RunScript.cs:385-395,407-448`; `duplicati-cli help run-script-before-required` | required: "`if (requiredScript) { if (!p.HasExited) throw new UserInformationException(Strings.RunScript.ScriptTimeoutError(scriptpath), "RunScriptTimeout"); else if (p.ExitCode != 0) throw new UserInformationException(Strings.RunScript.InvalidExitCodeError(scriptpath, p.ExitCode), "RunScriptInvalidExitCode"); }`"; plain: "`if (eventname == "BEFORE") { switch (p.ExitCode) { case 0: // OK, run operation case 2: // Warning, run operation case 4: // Error, run operation break; case 1: … OperationAbortReason.Normal … case 3: … Warning … default: … Error`" (2/3 → `WriteWarningMessage`, 4/5/other → `WriteErrorMessage`); help: "`If the script returns a non-zero error code or times out, the operation will be aborted.`" | **AGREE with the source and CLI help.** **DISAGREE on the citation:** `/usr/lib/duplicati/run-script-example.sh` says the *opposite* ("`--run-script-before-required … The backup will only be run if the script completes with an allowed exit code (0, 2, or 4)`" and for plain "`Any other exit code than 0 will be logged as a warning`") — the example is wrong against the source; do not cite it as agreeing. |
| (e) env names; timeout default | `RunScript.cs:366-379,75,129-132`; `duplicati-cli help run-script-timeout`; example script | `DUPLICATI__EVENTNAME, DUPLICATI__OPERATIONNAME, DUPLICATI__REMOTEURL, DUPLICATI__PARSED_RESULT, DUPLICATI__LOCALPATH, DUPLICATI__RESULTFILE`; `DEFAULT_TIMEOUT = "60s"`; help "`* default value: 60s`" | **AGREE** |
| (e) file backend writes in place under the final name; retry renames to a new name | `Backend/File/FileBackend.cs:389-398,429-448`; `Main/Backend/BackendManager.PutOperation.cs:243-247,419-431`; `BackendManager.Handler.cs:443-520` | "`string targetFilePath = GetRemoteName(targetFilename); … using (var targetStream = systemIO.FileCreate(targetFilePath)) … CopyStreamAsync(sourceStream, timeoutStream…)`" (file-path variant: `FileMove`/`FileCopy` to the same final name); "`// If we have previously attempted to upload, we need to rename the file if (operationState == OperationState.Uploading && TrackedInDb) { RenameFileAfterError(size); …`"; "`private void RenameFileAfterError(long Size) { … var newname = VolumeBase.GenerateFilename(p.FileType, p.Prefix, guid, time, …); … "Renaming \"{0}\" to \"{1}\"" … Context.Database.LogRemoteVolumeRenamed(oldname, newname); remoteFilename = newname;`" — no delete of the old remote name in this path | **AGREE.** Citation precision: the symbol lives in `BackendManager.PutOperation.cs` (partial class file), not `BackendManager.cs`. |
| **(f)** server-util verbs; `--hosturl` default; `--webservice-password-init`; `loopback`; allowed-hostnames | sandboxed `duplicati-server-util help`; `duplicati-server help` | Commands: `pause <duration>, resume, delete, list-backups, run, login, change-password <new-password>, logout, import <file> <passphrase>, export <backups>, health, issue-forever-token, status`; "`--hosturl <hosturl> The host URL to use [default: http://127.0.0.1:8200/]`"; "`--webservice-password-init: Access password feature for automated installation. Sets the password only if not already set, and then exits.`"; "`--webservice-interface: … The special value "loopback" means the loopback adapter.`"; "`--webservice-allowed-hostnames: The hostnames that are accepted, separated with semicolons. If any of the hostnames are "*", all hostnames are allowed…`" | **AGREE** |
| **(g)** `LoadCredential=` / `$CREDENTIALS_DIRECTORY` | `man systemd.exec` (259) ll. 3665-3678, 4035-4041; experiment E4/E7 | "`The data is only accessible to the user associated with the unit, via the User=/DynamicUser= settings (as well as the superuser). When available, the location of credentials is exported as the $CREDENTIALS_DIRECTORY environment variable to the unit's processes.`"; E4 (transient user unit): `CREDENTIALS_DIRECTORY=[/run/user/1000/credentials/run-p…service]`, `settings-key bytes=40` | **AGREE** (runtime-verified in user scope) |
| (g) `RequiresMountsFor=` | `man systemd.unit` ll. 794-801 | "`Automatically adds dependencies of type Requires= and After= for all mount units required to access the specified path. Mount points marked with noauto … will be pulled in by this unit.`" | **AGREE** |
| (g) `ProtectHome=read-only` ≈ `ReadOnlyPaths=`; nest `ReadWritePaths=` | `man systemd.exec` ll. 1375-1378, 1624-1627 | "`Similarly, "read-only" is mostly equivalent to ReadOnlyPaths=`"; "`Nest ReadWritePaths= inside of ReadOnlyPaths= in order to provide writable subdirectories within read-only directories. Use ReadWritePaths= in order to allow-list specific paths for write access if ProtectSystem=strict is used.`" | **AGREE (man page).** Runtime: UNVERIFIABLE here (E5/E6 non-discriminating — user-scope namespacing silently skipped; `kernel.apparmor_restrict_unprivileged_userns = 1`). |
| (g) `AmbientCapabilities=` with `User=` | `man systemd.exec` ll. 850-855 | "`Ambient capability sets are useful if you want to execute a process as a non-privileged user but still want to give it some capabilities. Note that, in this case, option keep-caps is automatically added to SecureBits=`" | **AGREE** |
| (g) `StartLimitIntervalSec`/`Burst` in `[Unit]` | `man systemd.unit` ll. 983-1013; `systemd-analyze verify` of the extracted unit | "`Configure unit start rate limiting. Units which are started more than burst times within an interval time span are not permitted to start any more.`" (documented in systemd.unit → `[Unit]`); verify: rc=0, no warning | **AGREE** |
| (g) `PrivateTmp=` private `/tmp`, "still tmpfs" | `man systemd.exec` ll. 1739-1753; `findmnt /tmp` | "`/tmp/ and /var/tmp/ directories inside it are not shared with processes outside of the namespace … If "true", the backing storage of the private temporary directories will remain on the host's /tmp/ and /var/tmp/ directories.`"; host: `tmpfs tmpfs /tmp` | **AGREE** (tmpfs because the host's `/tmp` is tmpfs; "disconnected" would be tmpfs regardless) |
| (g) `UMask=` | `man systemd.exec` ll. 1125-1128; E9 | "`Controls the file mode creation mask. Takes an access mode in octal notation. … Defaults to 0022 for system units.`"; E9 (`UMask=0007`): `umask` → `0007`, created file `660` | **AGREE** (runtime-verified) |
| (g) `$FOO` vs `${FOO}` | `man systemd.service` ll. 1291-1299; E1/E1b with the design's own `etc/default/duplicati` | "`Use "${FOO}" … always resulting in exactly a single argument. Use "$FOO" as a separate word … replaced by the value of the environment variable split at whitespace, resulting in zero or more arguments. For this type of expansion, quotes are respected when splitting into words, and afterwards removed.`"; E1: `ARGV[0]=<--webservice-port=8300> ARGV[1]=<--webservice-interface=loopback> ARGV[2]=<--server-datafolder=/home/duplicati/.config/Duplicati>`; E1b: one argument | **AGREE** (runtime-verified) |
| (g) `EnvironmentFile=` parsing; no specifier expansion | `man systemd.exec` ll. 3107-3142 (EnvironmentFile), 3070-3074 (Environment); E2; E3-file | "`an unquoted value after the "=" is parsed with the same backslash-escape rules as POSIX shell unquoted text, but unlike in a shell, interior whitespace is preserved and quotes after the first non-whitespace character are preserved … A backslash "\" followed by any character other than newline will preserve the following character`"; `'`-quoted: "`No backslash-escape sequences are recognized`"; `"`-quoted: "`Backslash ("\") followed by any of ""\`$" will preserve that character`"; Environment= (contrast): "`Variable expansion is not performed inside the strings and the "$" character has no special meaning. Specifier expansion is performed`"; E2 `EnvironmentFile` value `K1=a%6%xb%%c` delivered verbatim `[a%6%xb%%c]`; E3-file: `systemd-analyze verify` on a unit with `Environment=SETTINGS_ENCRYPTION_KEY=abc%6%xdef` → "`Failed to resolve specifiers in SETTINGS_ENCRYPTION_KEY=abc%6%xdef, ignoring: Invalid slot`" (the exact journal line of §5.4) | **AGREE** (both halves reproduced; the man page states the specifier rule only for `Environment=`, and the experiment confirms `EnvironmentFile=` passes `%` through) |
| **(h)** `PathExists=` fires immediately if the path exists at activation | `man systemd.path` ll. 94-98 | "`If a path already exists (in case of PathExists= and PathExistsGlob=) … at the time the path unit is activated, then the configured unit is immediately activated as well.`" | **AGREE** |
| (h) user units `WantedBy=default.target`; `Persistent=true` on user timers | `man systemd.special` ll. 971-976; `man systemd.timer` ll. 334-343; verify `--user` rc=0 | "`default.target: This is the main target of the user service manager, started by default when the service manager is invoked.`"; "`Persistent= … the time when the service unit was last triggered is stored on disk … triggered immediately if it would have been triggered at least once during the time when the timer was inactive … only has an effect on timers configured with OnCalendar=`" (no scope restriction) | **AGREE** |
| (h) udisks automounts under `/media/<user>/` only with a session | `man udisks` ACCESS CONTROL; `/usr/share/polkit-1/actions/org.freedesktop.UDisks2.policy`; `/usr/share/doc/udisks2/changelog.Debian.gz` (2.10.91-1, 2025-09-03); `strings /usr/libexec/udisks2/udisksd`; `ls /media/pcalnon /run/media/pcalnon` | "`By default, logged-in users in active log-in sessions are permitted to perform operations (for example, mounting…) on devices attached to the seat their session is on.`"; `filesystem-mount` defaults: `allow_any=auth_admin, allow_inactive=auth_admin, allow_active=yes`; **"`Use /run/media instead of /media as mountpoint for removable media.`"** (installed 2.10.91-1ubuntu2.1 includes it); binary contains only `/run/media/%s`; `/run/media/pcalnon/` exists (root-owned, ACL, mtime 2026-09-16) while `/media/pcalnon/` holds only hand-made `random_drive`, `temp_backups` | Session half: **AGREE**. Path half: **DISAGREE** — on this host udisks mounts under **`/run/media/pcalnon/`**, not `/media/pcalnon/`. The automount *trigger* (`/usr/libexec/gvfs-udisks2-volume-monitor`, package gvfs-daemons) is a desktop-session component: UNVERIFIABLE from man pages, consistent with the claim. |
| **(i)** server schema 11 (2.3.0.4) / 12 (2.4.0.0); local 19 for both; exact error string | GitHub contents API at `v2.3.0.4_stable_2026-07-09` and `v2.4.0.0_stable_2026-09-03`; `SQLiteHelper/Strings.cs:26-27` | `RestAPI/Database/Database schema/`: 2.3.0.4 → `1. … 11. Add BackupTargetUrl table.sql`; 2.4.0.0 → adds `12. Add OperationType to Backup.sql`; local: `1. … 19. Add metadata content column.sql` at both tags; string: "`The database has version {0} but the largest supported version is {1}.`" | **AGREE.** Path precision: at 2.3.0.4 the local schema is `Duplicati/Library/Main/Database/Database schema/` (no `Local/`; `…/Database/Local/` does not exist at that tag). |
| **(j)** vendor unit has no `User=`, is not a conffile, lives at `ReleaseBuilder/Resources/debian/systemd/duplicati.service`; `/etc/default/duplicati` is a conffile | GitHub raw at both tags (identical, 268 B); `dpkg -L duplicati`; `/var/lib/dpkg/info/duplicati.{conffiles,md5sums}`; `dpkg-query --control-show duplicati conffiles`; `dpkg -V duplicati`; `deb-conffiles(5)` | Unit (whole file): `[Unit] Description=Duplicati web-server / After=network.target / [Service] Nice=19 / IOSchedulingClass=idle / IOSchedulingPriority=7 / EnvironmentFile=-/etc/default/duplicati / ExecStart=/usr/bin/duplicati-server $DAEMON_OPTS / Restart=always / [Install] WantedBy=multi-user.target`; installed path `/lib/systemd/system/duplicati.service` (md5 `e989e07f…`), conffiles file contains exactly `/etc/default/duplicati`; `dpkg -V`: `??5?????? c /etc/default/duplicati` and `??5??????   /lib/systemd/system/duplicati.service` (both modified; only the first is a conffile) | **AGREE** (a non-conffile is replaced silently on upgrade, as §4.3 item 10 says; note vendor `Restart=always`). |

---

## Part 2 — executing the design's code

### Lint run (`2026-09-21_lint_design_snippets.py`, 12 tagged blocks; `lint_run.txt`)

| block | doc line | result |
|---|---|---|
| `etc/default/duplicati` | 330 | `[PASS] no linter` — **not actually linted** (no file extension → no grammar check); content is one `DAEMON_OPTS="…"` line; E1 proves it splits into 3 argv words |
| `etc/systemd/system/duplicati.service` | 342 | `[FAIL] systemd-analyze verify: Command /usr/local/lib/duplicati/duplicati-wrapper.bash is not executable: No such file or directory` — **instrument ordering defect**: the unit is verified before the wrapper (doc line 399) has been extracted; re-verified with the extracted wrapper → **rc=0** |
| `usr/local/lib/duplicati/duplicati-wrapper.bash` | 399 | `bash -n` PASS, `shellcheck -x -S style` PASS |
| `util/install_duplicati_service.bash` | 555 | PASS / PASS |
| `home/duplicati/.config/Duplicati/.env` | 618 | `[PASS] no linter` — **not linted** (`splitext('.env')` yields no extension); content `--webservice-port=8300` passes the intended grammar by inspection |
| `util/ad-hoc/2026-09-21_backup_destination_permissions.bash` | 659 | PASS / PASS |
| `usr/local/lib/duplicati/yamaguchi-pre-backup-guard.bash` | 705 | PASS / PASS |
| `juniper-backup.timer` / `.path` | 768 / 783 | `systemd-analyze verify --user` PASS (spice-vdagent noise is host-side) |
| `juniper-backup.service` | 797 | FAIL for the same ordering reason (scheduler at line 812); re-verified → **rc=0** |
| `util/juniper-backup-scheduled.bash` | 812 | PASS / PASS |
| `util/ad-hoc/2026-09-21_probe_settings_key_on_copy.bash` | 948 | PASS / PASS |

### (k) Wrapper v2 — exec-neutered copy (`k/wrapper-neutered.bash`, diff = the single `exec --` line replaced by an argv/env printer; `result_k.txt`)

Environment: `DUPLICATI_SERVER=/bin/true`, `DUPLICATI_DATA_FOLDER=<scratch>/datafolder` (0700), `DUPLICATI_REQUIRE_MOUNT=/tmp`, `DUPLICATI_ENV_FILE=<scratch>/synthetic.env` (contains `--webservice-port=8301`, `--log-level=Information`, the eight `TEST_*` lines incl. a CRLF line and a final line without newline, `export TEST_EXP=1`, `TEST_PRESET=file`, `SETTINGS_ENCRYPTION_KEY=envfilekey-16ch` [15 chars]).

| case | literal result | vs document |
|---|---|---|
| C1 no env file, no argv | `ARGV[0]=<--webservice-interface=loopback> ARGV[1]=<--webservice-port=8300> ARGV[2]=<--server-datafolder=<scratch>/datafolder>`; stderr `no env file at … (skipping)`, `settings encryption key: NOT SET (…)`; rc=0 | PASS |
| C2 env file, inherited `TEST_PRESET=env` | port **8301** (from `.env`) in position 1, `--log-level` appended 4th; `TEST_EQ=a=b==`, `TEST_DOLLAR=ab$HOME$1cd` (no expansion), `TEST_SEMI=a;echo INJECTED` (literal; **`INJECTED present? 0`**), `TEST_SPACE=hello world`, `TEST_QUOTED=x y`, `TEST_EXP=1`, `TEST_CRLF=crlf$` (CR stripped, `cat -A`), `TEST_LAST=last` (no-newline line read), `TEST_PRESET=env` + stderr `synthetic.env:12: TEST_PRESET already set in the environment; file value ignored`; `SETTINGS_ENCRYPTION_KEY length=15`, stderr `settings encryption key: environment/.env (15 chars)` | PASS (all eight grammar cases + env-wins-and-logged) |
| C3 argv `--webservice-port=8302` | `ARGV[1]=<--webservice-port=8302>` (default < .env < argv) | PASS |
| C4 argv `…=8302 …=8303` | `ARGV[1]=<--webservice-port=8303>` | PASS (later wins) |
| C5 argv `--log-file=/x --webservice-port=8302` | 5 words: interface, port 8302, datafolder, log-level, `--log-file=/x` (first-seen order kept) | PASS (accumulate, stable order) |
| C6 malformed line 3 | rc=**78**, `FATAL: …/k/bad.env:3: unparseable line (allowed: KEY=VALUE, export KEY=VALUE, --option[=value], # comment)` | PASS |
| C7 `--print-command` with `--webservice-password=x --settings-encryption-key=abc` | `would exec: /bin/true --webservice-interface=loopback --webservice-port=8302 --server-datafolder=… --log-level=Information --webservice-password=\<redacted\> --settings-encryption-key=\<redacted\>`; rc=0 | PASS (cosmetic: `%q` escapes the angle brackets) |
| C8 / C8b credential (40 chars, without / with trailing newline) + `.env` key present | stderr `settings encryption key: systemd credential settings-key (40 chars)`; `SETTINGS_ENCRYPTION_KEY length=40` (credential overrides the .env value; trailing newline stripped) | PASS |
| C9 empty credential | rc=78 `FATAL: systemd credential settings-key is empty` | PASS |
| C10 missing data folder | rc=78 `FATAL: data folder missing: …/no-such-folder` | PASS |
| C11 data folder `/tmp` (root-owned, writable) | rc=78 `FATAL: data folder /tmp is owned by root, not pcalnon` | PASS — this exercises the owner check without root; a folder owned by a *different non-root* user cannot be created here (see UNVERIFIABLE) |
| C12 data folder 0500 | rc=78 `FATAL: data folder not writable by pcalnon: …/k/ro-df` | PASS |
| C13a `DUPLICATI_REQUIRE_MOUNT=` | rc=0, no mountpoint message | PASS (check skipped) |
| C13b `REQUIRE_MOUNT=<scratch>/k` | rc=78 `FATAL: …/k is not a mountpoint; refusing to start a server whose destination lives there` | PASS |
| C14 argv `foo` | rc=78 `FATAL: argv: not a Duplicati option: 'foo'` | PASS |
| C15 `.env` `DUPLICATI_SERVER=/bin/false` | stderr `redirect.env:1: DUPLICATI_SERVER already set in the environment; file value ignored`; exec target unchanged | PASS (works because the readonly default is set before the file is read) |
| C16 `.env` `LD_PRELOAD=/nonexistent/evil.so` | exported: printer `LD_PRELOAD=/nonexistent/evil.so`; six `ERROR: ld.so: object '/nonexistent/evil.so' from LD_PRELOAD cannot be preloaded` lines from the wrapper's own child commands | **Observation** (not a documented behaviour): any variable name is exportable from `.env` |
| C17 env file mode 000 | rc=78 `FATAL: env file … exists but is not readable by pcalnon` | PASS |
| C18 argv `--daemon-opts="--webservice-port=8300"` | accepted as option `--daemon-opts`, passed through verbatim as `ARGV[4]` | Observation: the legacy word would reach the server |
| C19 `.env` line `  --webservice-port=8305` | rc=78 unparseable | Observation: leading whitespace is tolerated for `KEY=VALUE` but fatal for `--option` lines |
| C20 `.env` `--tempdir="/a b"` / `TEST_Q2="dq"` | `ARGV[3]=<--tempdir="/a b">` (quotes **kept** on option lines); `TEST_Q2=dq` (stripped on KEY lines) | Observation: §7.3.5 "matching surrounding quotes are removed" holds only for `KEY=VALUE` |
| C21 `.env` `SETTINGS_ENCRYPTION_KEY=` | `SETTINGS_ENCRYPTION_KEY unset` → "NOT SET" | PASS |
| C22 `DUPLICATI_SERVER=/nonexistent/server` | rc=78 `FATAL: server binary not executable` | PASS |
| **E7** end-to-end under `systemd-run --user` with `EnvironmentFile=<extracted etc/default/duplicati>` + `LoadCredential=settings-key:…` + `'$DAEMON_OPTS'` | `settings encryption key: systemd credential settings-key (40 chars)`; `ARGV[0]=<--webservice-interface=loopback> ARGV[1]=<--webservice-port=8300> ARGV[2]=<--server-datafolder=/home/duplicati/.config/Duplicati> ARGV[3]=<--log-level=Information>`; rc=0 | PASS for the unit mechanics. **Finding:** the server would receive `--server-datafolder=/home/duplicati/.config/Duplicati` (from `DAEMON_OPTS`) while the preflight validated `DUPLICATI_DATA_FOLDER=<scratch>/datafolder` — preflight checks the wrapper's default, not the effective option (DISAGREE #8). |

### (l) Pre-backup guard (`result_l.txt`; `YAMAGUCHI_DEST_MOUNT=/tmp`, `YAMAGUCHI_DEST_DIR=<scratch>/dest` with `duplicati-b0123…dblock.zip.aes`, `duplicati-i0123…dindex.zip.aes`, `duplicati-20260918T140000Z.dlist.zip.aes`)

| case | literal result | vs document |
|---|---|---|
| L1 clean, no REMOTEURL | rc=0, no output | PASS |
| L2 `stray.txt`, `DUPLICATI__OPERATIONNAME=Backup` | rc=5 `GUARD(Backup): foreign entry in destination: …/dest/stray.txt` | PASS |
| L3 `DUPLICATI__REMOTEURL=file://<other>` | rc=5 `GUARD(unknown): job TargetURL 'file:///…/dest-other' is not the guarded destination file:///…/dest` | PASS |
| L4 / L4b `file://<dest>/` and `file://<dest>` | rc=0 | PASS |
| L4c `file://<dest>?auth-username=x` | rc=5 (refused) | Observation: any query string on the job URL fails closed |
| L5 dest 0500 | rc=5 `… is not writable by pcalnon` | PASS |
| L6 `DEST_MOUNT=<scratch>` | rc=5 `… is not a mountpoint` | PASS |
| L7 dest 0300 (writable, unlistable → `find` fails) | rc=**5**, only `find: '…/dest': Permission denied` (no GUARD line) | PASS — the ERR trap converts the internal failure to 5 (L11 probe: `set -euo pipefail; trap "exit 5" ERR; x="$(false)"` → rc=5) |
| L8 dest missing | rc=5 `… does not exist` | PASS |
| L9 stray directory / L10 `….dlist.zip.aes.tmp` | rc=5 `foreign entry …` | PASS |

### (m) Scheduler (`result_m.txt`; `MEDIA_ROOT=/`, `DEVICES=tmp` → `//tmp is a mountpoint`; `BACKUP_DIR=/tmp/valA3-backupdir-<pid>`; `STATE_DIR=<scratch>/state`)

| case | literal result | status file | vs document |
|---|---|---|---|
| M1 first run | `running …/fake-runner-ok.bash: mounted=tmp due=tmp` / `fake-runner-ok invoked with 0 arg(s):` / `OK (stamped tmp)`; rc=0; `last-success.tmp` created | `result=OK / when=2026-09-21T13:37:48-05:00 / reason=devices tmp` | PASS |
| M2 second run | `SKIPPED: mounted (tmp) but none due within 7 days`; rc=0 | `result=SKIPPED …` | PASS |
| M3 `PERIOD_DAYS=0` | runs again, `OK (stamped tmp)` | `result=OK` | PASS |
| M4 runner exits 4 | `FAILED rc=4 (no stamp written)`; rc=**4**; stamp mtime `before=1790015868 after=1790015868 UNCHANGED` | `result=FAILED / reason=runner rc=4` | PASS |
| M5 `DEVICES=nonexistent`, fresh stamp | `SKIPPED: no configured drive mounted (nonexistent)`; rc=0 | SKIPPED | PASS |
| M6 same + `STALE_DAYS=0`, stamp age 0 d | SKIPPED, rc=0 (`age > STALE` is `0 > 0`) | SKIPPED | boundary as coded (strictly greater); consistent with "after STALE_DAYS" |
| M7 stamp aged 2 d + `STALE_DAYS=0` | `FAILED: no configured drive mounted (nonexistent); no successful run in 2 days (limit 0)`; rc=**1** | `result=FAILED / reason=…; stale 2d` | PASS |
| M8 / M8b no stamp, `STALE_DAYS=0` / default 21 | `… no successful run in 100000 days (limit 0|21)`; rc=1 | FAILED, `stale 100000d` | PASS |
| M9 lock held by another process | `SKIPPED: another run holds …/state/run.lock`; rc=0 | SKIPPED | PASS (observation: the skipped invocation overwrites `last-run.status` while the holder is still running) |
| M10 runner missing | `FATAL: runner missing: …`; rc=1 | `result=FAILED / reason=runner missing` | PASS |
| M11 / M12 `BACKUP_DIR` missing / read-only | `WARN: //tmp mounted but valA3-backupdir-… missing or read-only` then `SKIPPED: no configured drive mounted (tmp)`; rc=0 | SKIPPED | PASS (reason text slightly misleading; the WARN line carries the cause) |

### (n) Installer and permissions scripts (static)

- `util/install_duplicati_service.bash`: `bash -n` PASS, shellcheck PASS. Logic: `cmp -s src dst` after `install` is valid (`install` copies content unchanged; mode differences do not affect `cmp`); `stat -c '%U:%a'` → `root:600` comparison is correct (`%a` has no leading zero). Latent: the `for pair in "src:dst"` split (`${pair%%:*}` / `${pair##*:}`) breaks if any path contains `:`, and `REPO_DIR` derives from `BASH_SOURCE` so a checkout path with a colon would misparse. `systemd-analyze verify` after `daemon-reload` under `set -e` aborts the script on a verify warning-free failure — acceptable. Sources named as `scripts/duplicati-wrapper.bash`, `util/systemd/duplicati.service`, `util/systemd/duplicati.default` (the doc says these must exist; the tagged block names differ, which the doc acknowledges).
- `util/ad-hoc/2026-09-21_backup_destination_permissions.bash`: `bash -n` PASS, shellcheck PASS. `setfacl -R -m g:duplicati:rwX -m d:g:duplicati:rwX <dir>` is **valid syntax**: on a scratch tree rc=0, directories gained `group:duplicati:rwx` + `default:group:duplicati:rwx`, the file gained `group:duplicati:rw-` (`X` correctly withheld), and the `d:` entry is silently skipped for files under `-R` (a direct `setfacl -m d:… file` says `Only directories can have default ACLs`, rc=1; under `-R` rc=0). Note: the later `chmod 0700 _yamaguchi_keys` leaves the named ACL entries in place with an empty mask (effective `---`), and the default ACL on that directory survives — harmless because the design moves the directory anyway.

### (o) Units

- `systemd-analyze verify` (Exec targets pointed at the extracted scripts): `duplicati.service` rc=0; `juniper-backup.{service,timer,path}` `--user` rc=0.
- `systemd-analyze security --offline=true duplicati.service`: **`Overall exposure level for duplicati.service: 5.2 MEDIUM`**. The tool never labels a *row* "UNSAFE" (`grep -c UNSAFE` = 0; the word appears only in the overall verdict). Rows marked ✗ that are caused by directives the design sets: `AmbientCapabilities=` (0.1, "Service process receives ambient capabilities"), `CapabilityBoundingSet=~CAP_(DAC_*|FOWNER|IPC_OWNER)` (0.2, "Service may override UNIX file/IPC permission checks"), `ProtectHome=` (0.1, "read-only access to home directories"), `UMask=` (0.1, "Files created by service are group-writable by default"). All ✓: `User=`, `NoNewPrivileges=`, `ProtectSystem=strict`, `PrivateTmp=`, `ProtectKernel*`, `ProtectControlGroups=`, `RestrictSUIDSGID=`, `LockPersonality=`, `RestrictRealtime=`, `SystemCallArchitectures=native`.
- `juniper-backup.service` (user): 9.6 UNSAFE — it sets no hardening directives and the design makes no claim about it.

---

## DISAGREE list — corrections the document needs

1. **§7.8 / §7.9 / §4.1 / scheduler default `MEDIA_ROOT=/media/${USER}` / `util/juniper-backup.bash` `MOUNT_NAME="media"`: udisks on this host mounts removable media under `/run/media/pcalnon/`, not `/media/pcalnon/`.** Evidence: udisks2 2.10.91-1 changelog "Use /run/media instead of /media as mountpoint for removable media (Closes: #1074568)", installed 2.10.91-1ubuntu2.1; `udisksd` contains only `/run/media/%s`; `/run/media/pcalnon/` exists (born 2026-09-11, mtime 09-16) while `/media/pcalnon/` holds only two hand-made directories. As written, `PathExists=/media/pcalnon/EBC5-F0A3/Juniper-8.0.0.python` never fires and every T2 run reads SKIPPED (then FAILED after 21 days). Correction: either use `/run/media/pcalnon/<LABEL>` or — better, and already the design's own T3 approach — fstab lines keyed by UUID with `noauto,nofail,x-systemd.automount` under stable paths, which also removes the graphical-session dependency. `util/juniper-backup.bash` needs the same change (§7.9's "absolute mount root" item becomes mandatory for T2).
2. **§7.5 rule 5** restore command passes `--no-local-blocks=true`, which §3.3 rule 6 and AC-4 call deprecated; in 2.4.0.0 it only emits a deprecation warning. Drop it; the default already excludes local blocks.
3. **§5.5 "Since 2.3.1.0"** → the gate shipped in **2.3.0.107_canary (2026-07-13)**, first beta 2.3.1.0 (2026-07-28), first stable 2.4.0.0 (2026-09-03); on this host (2.3.0.4 stable → 2.4.0.0) it first applied with the 09-19 upgrade. Also add the third override, `DUPLICATI__ALLOW_INSECURE_DATAFOLDER=true`.
4. **§7.5 contract "verified against `/usr/lib/duplicati/run-script-example.sh`"** — that file states the opposite mapping (required: "allowed exit code (0, 2, or 4)"; plain: "Any other exit code than 0 will be logged as a warning"). The design's statement is right *per `RunScript.cs:390-395,433-448` and `duplicati-cli help`*; cite those and note the vendor example is wrong.
5. **§7.5 rule 2 `BackendManager.RenameFileAfterError`** → `Duplicati/Library/Main/Backend/BackendManager.PutOperation.cs:419` (partial-class file); the behaviour claim (new name, partial file left) is confirmed.
6. **§7.3.5 "first tries the OS secret store (libsecret or `pass`)"** → the Linux default provider is **libsecret only** (`SecretProviderLoader.cs:195-208`); `pass` is available solely via `--secret-provider=pass://…`.
7. **§5.3 / (i) path**: at 2.3.0.4 the local schema directory is `Duplicati/Library/Main/Database/Database schema/` (`Local/` appears only at 2.4.0.0); versions 19/19 stand.
8. **§7.3.3 preflight** validates `DATA_FOLDER` (env/default), not the `--server-datafolder` word the server actually receives; E7 shows the two diverge as soon as `DAEMON_OPTS` names a different path. Derive the preflight target from `OPT_VALUE[--server-datafolder]` after merging.
9. **§7.3.2 "files 0600" vs §7.3.5 ".env Mode 0640"** — inconsistent; group read is moot under a 0700 folder, so 0600.
10. **AC-12 wording**: `systemd-analyze security` marks rows ✗/✓ with an exposure number; "UNSAFE" is only the overall label. Reword to an overall-exposure threshold (offline baseline 5.2 MEDIUM) and list the four expected ✗ rows above.
11. **Appendix B**: `etc/default/duplicati` ("KEY=VALUE grammar") and `.env` ("grammar") are **not** linted by the instrument (no extension / `.env` has no `splitext` extension → "no linter"), and the two unit FAILs are an ordering artifact (units verified before their Exec targets are extracted). Fix `2026-09-21_lint_design_snippets.py` (basename-based grammar dispatch; extract all blocks first, then lint) so the appendix's claims are true.
12. **§7.3.3 `.env` contract (observation → recommended change)**: `KEY=VALUE` lines may export *any* name — `LD_PRELOAD`, `DOTNET_*`, `PATH`-adjacent variables — into a process that holds `CAP_DAC_READ_SEARCH` (C16). Same trust domain as the file owner, so low severity, but an allow-list of names (or `--option` lines only, now that the key is a credential) closes it.

Minor observations (no correction required): C18 legacy `--daemon-opts="…"` word passes through to the server; C19 leading whitespace before an option line is fatal; C20 quotes on option lines are kept; M9 a lock-skipped invocation overwrites `last-run.status`; M11/M12 reason text says "no configured drive mounted" when the drive is mounted but the directory is missing/read-only; L4c a job URL with a query string is refused.

---

## UNVERIFIABLE (by this lane)

- Runtime effect of `ProtectHome=read-only` + nested `ReadWritePaths=` and of `PrivateTmp=` for a *system* unit: needs root; user-scope reproductions (E5/E6/E8) were non-discriminating because the user manager silently skipped mount namespacing (`kernel.apparmor_restrict_unprivileged_userns = 1`). Man-page text supports the design.
- Wrapper preflight against a data folder owned by another **non-root** user (needs root to create); the root-owned case (`/tmp`) is refused as designed. Duplicati's own acceptance of a root-owned 0700 folder is verified from source only.
- The udisks automount *trigger* requiring a graphical session (`gvfs-udisks2-volume-monitor`): not documented in systemd/udisks man pages; polkit `allow_active=yes` vs `auth_admin` supports the session requirement.
- The literal string "No database encryption key was found. The database will be stored unencrypted.": mechanism verified in `Program.cs:1255-1265` (`Strings.Program.NoEncryptionKeySpecified`); the Server strings file was not located at the tag (`Duplicati/Server/Strings.cs` 404). The design's Appendix A.1 journal line is the runtime string.
- Whether `/usr/lib/duplicati/data` exists (not probed by rule); the "no auto-detection" claim rests on `DataFolderManager.GetDataFolder` alone.
- `docs.duplicati.com` pages were not fetched; every product claim was settled from local `help` output, the vendor example script, and tagged GitHub source.

<!-- markdownlint-enable -->

---

## Report B1 — Lane B — adversarial analysis review; lens: refute the ROOT CAUSE and the RECOVERY procedures

Sections of the design assigned: §1, §5, §6, §8 P0/P1, Appendices A and C. Subagent id `a014dbd07f35b4267` (22,405 characters).

<!-- markdownlint-disable -->

# Lane B1 validation — findings on `notes/JUNIPER_2026-09-21_JUNIPER-ECOSYSTEM_BACKUP-INFRASTRUCTURE-INTEGRATED-DESIGN.md` (worktree `/home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/atomic-sauteeing-truffle`)

Scope: §1, §5, §6, §8 P0/P1, Appendices A and C. Sources re-read: Duplicati tag `v2.4.0.0_stable_2026-09-03` (raw files saved under my scratch dir `…/scratchpad/valB1/`), `man systemd.exec` (systemd 259.5), the host read-only, DB **copies** only (via the design's own `2026-09-21_duplicati_server_db_forensics.py`), and two scratch instruments that print booleans/lengths only: `valB1/keyhash_probe.py`, `valB1/env_equality.py`. No secret value was printed or read into this transcript; the journal was always filtered as instructed. Nothing was written into the repository.

## Findings

**1. §5.4 candidate order for the 09-18 key — REFUTED (offline, on the design's own copy).**
Claim (§5.4): *"The candidate order for Procedure A is therefore: (1) the 36-character literal from the wrapper's comment block, verbatim; (2) the `PASSPHRASE` value (today's `.env` key); (3) any other value the operator typed…"*; §1: *"…one that re-uses the whole data folder if the 09-18 key can be identified"*.
Attack: an `enc-v1:` field embeds the SHA-256 of its key, so candidates can be tested without a server. `EncryptedFieldHelper.cs` 129–139: `contentHash = value.Substring(0, hashSizeInBytes); keyHash = value.Substring(hashSizeInBytes, hashSizeInBytes); … if (keyHash != key.Hash) throw new SettingsEncryptionKeyMismatchException();` and `KeyInstance.CreateKey`: `new KeyInstance(key, key.ComputeHashToHex(hasher /*SHA256*/), …)`.
Evidence: `keyhash_probe.py` run under `sg duplicati` on the copies of the 09-20 root snapshot and the pcalnon `.backup`, with candidates L (the committed literal, raw plus 11 shell-mangling variants: quote-strip, backslash-unescape, `$*`/`$@` removal, bash `"$…"` expansion, `%%`, CR/LF), PASSPHRASE, PASSPHRASE_OLD, the `.env` active key and its three commented `SETTINGS_ENCRYPTION_KEY` lines (33/33/35 chars after quote-strip): `[snapshot0920] encrypted blobs=4 content-hash-verified=4 distinct-key-hashes=1 … key-hash match: NONE of the candidates/variants`; `[pcalnon_backup] encrypted blobs=6 content-hash-verified=6 distinct-key-hashes=1 … NONE`. Content hashes verify 4/4 and 6/6, so the layout parse is right and the test is discriminating, not vacuous.
Verdict: REFUTED. Candidates (1) and (2) are excluded before any probe; the 09-18 key is unrecorded anywhere the design looked. The 21:08 evidence already pointed here: 15 `Mismatch` restarts (21:08:06–21:11:43) with **no** specifier error (first at 21:15:40) mean a `%`-free or file-delivered key ≠ K1 was in play *before* the `%`-literal was ever loaded — the design's "the operator was demonstrably trying to deliver L, therefore L is (1)" ignores that the Mismatch is equally evidence that L ≠ K1.
Change: replace the ordering with the offline SHA-256 test as step 3 (it needs only the snapshot copy, no root, no server); record that both named candidates fail; leave only root's shell history / editor backups of the unit as candidate sources; route P0 to Procedure A0 (finding 3) or A2. Also: the Linux default secret provider is `LibSecretLinuxProvider` (`SecretProviderLoader.cs` 195–205) and the server auto-generates a key when a provider is reachable (`Program.cs` 1212–1231) — that is how the **pcalnon** profile DB came to be `enc-v1:` under a keyring-held key in August without anyone typing one; a root system unit has no session bus, so it does not explain 09-18.

**2. §8 P0 step 3 probe script — REFUTED as a discriminator.**
Claim: *"if grep -q 'Server has started' … KEY ACCEPTED"* on a copy made by `cp -p "${SRC}" "${WORK}/Duplicati-server.sqlite"`.
Attack: (a) the script copies the main file only, never `-wal`/`-shm`; (b) `Program.cs` 324 runs `connection.ReWriteAllFieldsIfEncryptionChanged()` on every start, so a copy whose `encrypted-fields` flag is False starts under **any** key, gets encrypted under that key, and prints `Server has started` — a vacuous pass that also poisons the copy; (c) `/usr/lib/duplicati/data` mtime is 21:03:34 and never moved again (`stat`: `mtime=2026-09-18 21:03:34.67`, ctime 09-19 19:15:00). A directory mtime changes only on entry create/delete/rename, so the `-wal`/`-shm` created when the 21:03:27 instance opened the DB were never removed by a clean close — the encryption rewrite plausibly lives only in that WAL and the main file alone may still be the pre-21:03 (cleartext) state. No positive or negative control exists in the script.
Verdict: REFUTED (as written it can say ACCEPTED for a wrong key).
Change: use the offline hash test; if a server probe is kept, copy the WAL/SHM siblings (or `sqlite3 .backup`), assert on the copy `Option(-2,'encrypted-fields')='True'` and `TargetURL LIKE 'enc-v1:%'` first, and add a negative control (a random key must produce `Mismatch`). Settling observation: `sudo ls -la /usr/lib/duplicati/data/` and, on a copy of the main file alone, the two SELECTs above — if the main file is cleartext, that is a second key-free recovery source.

**3. §1 / §8 "two recovery procedures … neither needs a Recreate" — REFUTED (the cheapest route is missing).**
Claim (§1): *"§8 gives two recovery procedures — one that re-uses the whole data folder if the 09-18 key can be identified, one that rebuilds the job through the API from the snapshot"*; C.3: *"Copies taken by the snapshot lane before 09-18 are cleartext"* — stated and then dropped.
Attack: those cleartext copies are inside the backup set. YAM §8.19.3: the snapshot destination `~/.local/state/duplicati-server-db/` *"was verified against all 44 filters … neither of which covers `.local/state/`"*; timer 13:45Z, backup 14:00Z (`journalctl -u yamaguchi-server-db-snapshot`: 09-17 and 09-18 `wrote … (228/232 KiB) … this snapshot rides along in the next Yamaguchi backup`). The root DB was unencrypted through 09-18 20:42:38 (journal "No database encryption key…" at 09-17 01:31:01, 09-18 20:22:12, 20:42:38), so the 09-17 and 09-18 14:00Z filesets each hold a **cleartext schema-11 server DB** with `TargetURL`, `passphrase`, `DBPath`, schedule, 45 filters, options. Restoring `/home/pcalnon/.local/state/duplicati-server-db/Duplicati-server.sqlite` at `--time=2026-09-18T14:00:00Z` with the escrowed passphrase (the design's own §7.5 item 5 gives the command shape; YAM §8.4/§8.13 drills prove the path) needs no key, no wipe, no re-entry; only `LastBackupDate` is one day stale. The on-disk copy is overwritten daily (only the 09-20 file exists), so the filesets are the sole holders.
Verdict: REFUTED. Change: add **Procedure A0** ahead of A; use a fresh `--dbpath` for the restore so the live index is untouched; then apply finding 4.

**4. Procedure A / A2 step 4 "The job appears with its existing index — no Recreate" — REFUTED (DBPath stays absolute into a folder the new unit makes read-only and P4 deletes).**
Evidence: snapshot `Backup: ID=2 Name='Yamaguchi' … DBPath='/usr/lib/duplicati/data/BMXWPAOGLP.sqlite'`; `RestConnection.cs` 766–771 `ResolveDbPath`: `if (string.IsNullOrWhiteSpace(dbPath) || Path.IsPathRooted(dbPath)) return dbPath` — a rooted path is never relocated; the 11→12 script is one line: `ALTER TABLE "Backup" ADD COLUMN "OperationType" TEXT NOT NULL DEFAULT 'Backup';`. The §7.3.2 unit has `ProtectSystem=strict` with `ReadWritePaths=/home/duplicati /mnt/Backups/Ubuntu/Dropbox/Backups`, so `/usr/lib/duplicati/data/BMXWPAOGLP.sqlite` is read-only at the first run; §7.3.1/P4 then removes that folder. A2 ("the index path … survive the wipe") inherits the same defect; `WipeEncryption.cs` never touches `DBPath`.
Verdict: REFUTED. Change: before the first run, move `BMXWPAOGLP.sqlite` into the new data folder and update `Backup.DBPath` (UI Database → Placement → Save, or `PUT /api/v1/backup/2`); 2.4.0.0 then stores it relative (`GetRelativeDbPath`, lines 779–790). Same step for A0.

**5. §5.3 mechanism and attribution of the 02:19:47 event — mechanism SURVIVES (sharpened), wording WEAKENED, attribution REFUTED.**
Claim: *"one second after Duplicati saved the profile server DB (`Duplicati-server.backup` and `backups/backup Duplicati-server 20260825021947.sqlite`, both 02:19:47), a Recreate wrote a new local database into the server-database path … consistent with the 2026-08-22 handoff's finding that the `Ubuntu` job's `DBPath` had been silently changed to the server DB path"*.
Evidence: neither file is a "save". `Duplicati-server.backup` is the **original inode** (`stat`: birth 2023-05-17 00:43:03, ctime 02:19:47.788 = rename; the stub's birth is the same instant 02:19:47.788). Producer: `RepairHandler.cs` 87–104: on `knownRemotes <= 0` after `LocalRepairDatabase.CreateRepairDatabaseAsync` throws, `baseName = Path.ChangeExtension(m_options.Dbpath, "backup"); … File.Move(m_options.Dbpath, baseName); await RunRepairLocalAsync(…)` — then `RecreateDatabaseHandler` writes the stub + WAL (born 02:19:48.142, `Operation='Recreate'`). `backups/backup Duplicati-server 20260825021947.sqlite` (birth 02:19:47.693) is `DatabaseUpgrader.cs` 266–280's pre-upgrade copy (`BackupFilenamePrefix + " " + name + " " + yyyyMMddHHmmss + ".sqlite"`), which only fires when `Version < MAX` — i.e., the **local-DB** upgrader (max 19) opened the server file as a job DB. The database-tool's backups are named `<name>-<ts>.bak` (`Helper.cs` 30), so it is not that. Mechanism nailed harder than the design states. But the actor: the design's own reading of `.backup` shows `ID=2 Name='Ubuntu' … DBPath='/home/pcalnon/.config/Duplicati/SJTCQIIZSJ.sqlite'` and no `dbpath` among its 7 job options — the row had already been corrected, so the server's `Ubuntu` job could not have supplied the server path. Something ran a Repair with an explicit `--dbpath` naming the server file; `~/.bash_history` has 0 such lines and the user-lane logs show only `dbpath …/DQRVQNDIFX.sqlite`. Whether the pcalnon server was even running at 02:19:47 is doubtful (a new WAL inode at 02:19:48.142 means no WAL existed, i.e., the old DB had been cleanly closed).
Change: rewrite the mechanism sentence (rename by RepairHandler, pre-upgrade copy by the local upgrader); drop the "consistent with the 08-22 DBPath finding"; state the actor is unidentified. Appendix C.1 stands.

**6. §1 "Every later start supplied a different key or none" vs §5.4/§5.6 "do not identify the key or the folder" — §1 SURVIVES only with an argument the design lacks; §5.4/§5.6 WEAKENED (too weak).**
Evidence: `WebServerLoader.cs` 221–229: `ports = from n in portstring.Split(',') where int.TryParse(n, out _) …; if (ports == null || !ports.Any()) ports = [DEFAULT_OPTION_PORT /*8200*/]`; the stored `last-webserver-port` is only *reordered* within the parsed list (316–319). So a `Server has started … port 8200` proves the option word was unusable, hence the `--portable-mode` glued into it was inert, hence `GetDataFolder` fell to `$HOME/.config/Duplicati` (`DataFolderManager.cs` 105–140; the loaded unit has `Environment=` empty and `/etc/default/duplicati` carries only `DAEMON_OPTS`). Journal starts: 09-19 21:20:41 (8200), 21:57:41 (8200), 09-20 13:32:50 (8200), 13:36:40, 16:50:15, 17:30:17, 18:19:46 (8300). The three 8200 starts were on the duplicati home folder; the four 8300 starts had a working option word that, per the design's own wrapper-echo reading, no longer carried `--portable-mode`. The root folder was last opened at 09-19 19:15:01 (`Missing`). The 13:32:34–38 `Missing` ×5 and 16:41 `Missing`/16:44 `Mismatch` were the home folder's own fresh DBs (stub born 09-19 21:57:36, five seconds before the 21:57:41 start). §5.6's Leg C row currently says the refuting observation *exists* ("it shows exactly such lines at 09-20 13:32") and then explains it away — as written it is not a refutation test.
Change: add the port discriminator to §5.4; rewrite the §5.6 row so the refuter is "a `Server has started` on a start whose folder is the root folder" and record that none exists after 09-18 21:03:30.

**7. Data-folder mtime as the encryption marker — WEAKENED (right conclusion, wrong evidence).**
Claim (§4.2, §5.4, A.3, C.3): *"fields of the root DB encrypted; `/usr/lib/duplicati/data` mtime 21:03:34"*, *"cleartext … until 2026-09-18 21:03:34"*.
Attack: re-encrypting fields rewrites the DB file/WAL, not the directory; a directory mtime records an entry create/delete/rename — here most plausibly `-wal`/`-shm` creation seven seconds after the 21:03:27 launch. Encryption is established by the absent warning at 21:03 plus `Mismatch` at 21:08:06, i.e., "between 21:03:27 and 21:08:06". The same mtime also implies the WAL was never deleted (finding 2).
Change: relabel the timestamp and the mechanism in §4.2/A.3/C.3.

**8. §6 journal scrub and "who can read it" — REFUTED as complete.**
Claim: *"the journal cannot delete individual lines. `journalctl --rotate` followed by `journalctl --vacuum-time=1s` removes every archived file…"*; exposure columns name the journal only.
Evidence: `systemctl is-active rsyslog` → `active`; `/var/log/syslog-20260921` (161,776,730 B, `syslog:adm` 0640): `grep -c 'LINE: "'` → **264**, `Settings Encryption Key:` → **12**, `Exporting Environment Variable` → **12** — identical to the journal counts; `zgrep -c 'Failed to resolve specifiers' /var/log/syslog-20260919.gz /var/log/syslog-20260920.gz` → **4** and **18** (the 36-char literal). `pcalnon` is in `adm` (`id -nG`). A journal scrub leaves all of these; logrotate keeps day files for weeks.
Verdict: REFUTED (scrub incomplete; readership row correct). Change: add `/var/log/syslog*` (current, dated, `.gz`) to S-1/S-2/S-3 "where exposed" and to D-8. Counts in §4.2/§6 are under-reported: journal echo lines 264 (commented-out assignments 60 = 12 `PASSPHRASE` + 12 `PASSPHRASE_OLD` + 36 `SETTINGS_ENCRYPTION_KEY`), 12 and 12 — not 220/50/10/10; specifier lines 21 in the journal, 22 in syslog — not 23. Each echoed commented line does carry text after `=` (verified by redacted shape), so the qualitative claim holds.

**9. S-2 "the active key equals PASSPHRASE" — SURVIVES (strengthened).**
The design's instrument compares only to a commented line. `env_equality.py` (booleans only): `env commented PASSPHRASE == pcalnon live PASSPHRASE: True`; `env ACTIVE settings key == pcalnon live PASSPHRASE: True`; `PASSPHRASE_OLD` likewise True; the three commented keys are not identical to each other (33/33/35) and equal neither passphrase. One wording trap: the `.env` comment says `PASSPHRASE -> the fresh set (Ubuntu-fresh)`, a stale label — say explicitly that the value is the live Yamaguchi passphrase.

**10. S-1/S-4/S-5 — SURVIVE.** `gh repo view pcalnon/juniper-ml --json visibility` → `{"isPrivate":false,"visibility":"PUBLIC"}`; `dropbox filestatus /mnt/Backups/Ubuntu/Dropbox/Backups/_yamaguchi_keys/env` → `up to date`, and `dropbox exclude list` names nine root folders, none under `Backups/`; primary `.env` is `-rw-rw-r--` with one `DUPLICATI_WEB_CREDENTIAL` line. (The escrow file is also group-readable by `duplicati` — `-rwxrwx--- pcalnon duplicati` — worth a line.)

**11. 0700 data-folder gate (§5.5, §7.3.2) — SURVIVES with three corrections.**
`DataFolderManager.cs` 143–145: `if (mode == AccessMode.ReadWritePermissionSet) PrepareSecureDataFolder(dataFolder, createIfMissing: true);` → `VerifyDataFolderSecurity(dataFolder, folderPreExisted)` throws `InsecureDataFolderPermissions` for a pre-existing non-canonical folder at every start. Linux predicate (`SystemIOLinux.cs` 500–530): mode includes 0700, no 077 bits, owner current user **or root**. Overrides: `--allow-insecure-datafolder`, env `DUPLICATI__ALLOW_INSECURE_DATAFOLDER`, marker `insecure-permissions.txt` (`Util.cs` 50–55) — the design omits the env var; no marker exists in `/usr/lib/duplicati/`. Reconciliation: the 18:19:42 start passed the gate, so the folder was canonical then; the loosening is dated by ctimes (three files 18:25:47.326, directory 18:33:45.057; `.env`, `backups/`, `installation.txt` untouched — so not a recursive chmod). "The next start **may** refuse it" → **will** refuse it; §4.3 item 9's "on 8200, or not at all" collapses to "not at all" independent of the argv bug.

**12. §8 P0 step 7 `duplicati-server-util … pause` — WEAKENED.** `ServerUtil/Connection.cs` 225–315: auth is a saved refresh token, else a signin token minted from a readable **and decryptable** server DB (`jwt-config` is in `_encryptedFields`, so this needs the settings key too), else `--password`/prompt. The recovered DB carries the root-era `server-passphrase`, so the old UI password (S-5) works until step 9 changes it; the bare command as written prompts or fails. Also "the job's `startup-delay`" is a server setting (`Option` BackupID −2, present in the snapshot); harmless.

**13. Procedure A2 wipe claims — SURVIVE with precision.** `WipeEncryption.cs`: clears `enc-v1:` values in `Backup.TargetURL`, `Source.Path`, `ConnectionString.BaseUrl`, `BackupTargetUrl.TargetURL` and password-typed `Option.Value` (`passphrase`, `jwt-config`, `pbkdf-config`, `remote-control-config`, ssl cert fields, `client-license-key`), sets `encrypted-fields='False'`, makes a `-<ts>.bak`, refuses non-server DBs. Snapshot `Source` rows are cleartext (`/home/pcalnon/`, the VDI), so "sources survive" holds; `server-passphrase`/`-salt` are hashes, not in the list, so the UI password survives — "set the UI password afresh" is optional, not required. Upgrade 11→12 reads no encrypted field. Finding 4 still applies.

**14. Leg A — SURVIVES.** `.backup` (forensics copy): jobs `Ubuntu`/`Ubuntu-fresh`, `Schedule: 0 row(s)`, notification `Error while running Ubuntu … 1787642386`; the stub+WAL is a local schema; the root file **grew** 228→232→240 KiB across the 09-17/18/19 snapshots and the 09-20 copy's `LastBackupDate='20260918T140000Z'` matches the newest dlist — the root DB recorded the last backup, so it was the live folder, not merely a file that existed. "Created in the profile and moved" is excluded by the `.backup` content. One correction: the pcalnon `.backup` is itself `enc-v1:` (six blobs, one key) — YAM/C.3's cleartext statements are true of the root DB only.

**15. Timeline internal contradictions — WEAKENED.** §4.2 dates the profile copy to "09-20 04:15–04:43 (inode birth times)" while the same table cites the 09-19 20:48 "version 19" error on that copy. Births: `backups/` 09-19 18:40:36, `machineid.txt`/crashlog 09-19 18:42, stub 09-19 21:57:36, `installation.txt` 09-20 04:15:42, `.backup` 04:41:07, `-shm` 04:43:02 — two rounds. §4.1 `NeedDaemonReload` is now `no` (`systemctl show`), and the loaded `ExecStart` is already the `--daemon-opts="${DAEMON_OPTS}"` form, so the §4.3.9 failure is armed for the next restart (and finding 11 fires first).

**16. Universals checked.** (a) §8 preamble *"Nothing under `/mnt/Backups/Ubuntu/` is deleted or moved by any step below"* vs P1.4 *"Move `_yamaguchi_keys/` out of the Dropbox root"* — that folder is `/mnt/Backups/Ubuntu/Dropbox/Backups/_yamaguchi_keys` — counter-example inside the document; (b) "the only overrides are…" — env var omitted (finding 11); (c) "no ACL entries / no default ACLs" — `getfacl` shows base entries only on the whole chain and on a volume; `ls` shows `+` because of the `user.com.dropbox.attrs` xattr — SURVIVES; (d) "no timer, path unit, cron or udev rule anywhere" for the tar lane — SURVIVES (`crontab -l`: clamtk only; timers: none matching); (e) rule 7 "no automatic portable mode" — SURVIVES (`GetDataFolder`: `--server-datafolder` > `--portable-mode` > `DUPLICATI_HOME` > default, no probing of `<install>/data`); (f) "Rotation does not re-encrypt existing volumes" — SURVIVES; (g) "the pcalnon profile never held the Yamaguchi job" — SURVIVES.

**17. §5.4 `EnvironmentFile=` reasoning — SURVIVES but moot.** `man systemd.exec` (259): unquoted values *"parsed with the same backslash-escape rules as POSIX shell unquoted text … interior whitespace is preserved"*, no specifier expansion; the literal's metacharacters are `$*@%` with no backslash, so it would pass verbatim. Since L ≠ K1 (finding 1), the paragraph proves nothing about the 21:03 key.

Not re-verified: `LocalDatabase.VerifyParameters` for Procedure B's index re-attach; the Dropbox daemon's gid; anything in §4.3.

## Three most consequential

1. **Finding 3** — a cleartext, key-free copy of the job definition sits in the 09-17 and 09-18 filesets; P0 should start there, not with a key hunt or a wipe.
2. **Findings 1 + 2** — Procedure A's two named candidates are excluded by an offline hash test on the design's own snapshot copy, and the probe script would report ACCEPTED for a wrong key if the copied main file is not itself encrypted.
3. **Finding 4** — every recovery procedure that keeps the snapshot's `Backup` row leaves `DBPath` absolute under `/usr/lib/duplicati/data/`, which the new unit makes read-only and P4 deletes; the first backup fails and the index is then lost. (On the secrets side, finding 8: rsyslog holds a complete second copy of every exposure that D-8 does not scrub.)

## Observations that would settle what is left open

- `sudo ls -la /usr/lib/duplicati/data/` (is there a `-wal`/`-shm` dated 09-18 21:03?) and, on a copy of the main file **without** its WAL, `SELECT Value FROM Option WHERE BackupID=-2 AND Name='encrypted-fields'` and `SELECT substr(TargetURL,1,7) FROM Backup` — decides whether the main file is a cleartext recovery source and whether the probe would pass vacuously.
- Root's `~/.bash_history` and any editor backups (`/usr/lib/systemd/system/duplicati.service~`, `*.swp`, a since-deleted `duplicati.service.d/` drop-in) for 09-18 20:42–21:03 — the only remaining places the 21:03 key could be recorded; a candidate found there is tested with `keyhash_probe.py` in seconds.
- `duplicati-cli list file:///mnt/Backups/Ubuntu/Dropbox/Backups/Yamaguchi --time=2026-09-18T14:00:00Z --dbpath=<fresh temp db> '*duplicati-server-db*'` (passphrase from escrow) — confirms the fileset carries the snapshot file before Procedure A0 is written up.
- Root's shell history around 2026-08-25 02:19 for a `--dbpath=/home/pcalnon/.config/Duplicati/Duplicati-server.sqlite` invocation — identifies the actor of the rename+Recreate.
- `sudo ls -la /var/log/syslog-2026092*` after any scrub — confirms the second copy is gone.

<!-- markdownlint-enable -->

---

## Report B2 — Lane B — adversarial analysis review; lenses: AMPUTATION (what the inputs said that the design dropped) and ACTIONABILITY (what an operator following it would actually do)

Sections of the design assigned: whole document against its inputs. Subagent id `ac64b4dfb81ff2ca6` (21,966 characters).

<!-- markdownlint-disable -->

# Lane B2 report — AMPUTATION + ACTIONABILITY

**Artifact reviewed**: `notes/JUNIPER_2026-09-21_JUNIPER-ECOSYSTEM_BACKUP-INFRASTRUCTURE-INTEGRATED-DESIGN.md`, working-tree copy at mtime 2026-09-21 13:53:12 CDT (1187 lines). The file was edited twice while I reviewed it (staged version 1158 lines → +§4.5 udisks finding, `.env` allow-list, AC-12 rewrite). Line numbers are from the 13:53 version unless marked *staged*. Nothing was written into the repository; scratch evidence is under `scratchpad/valB2/` (`forensics.out`, `lintwork/`, `snippets*/`).

Instruments I ran (all read-only): the design's own `2026-09-21_lint_design_snippets.py` (12/12 blocks pass — but see D1: `systemd-analyze verify` cannot see the defect that matters), the current wrapper block against synthetic `.env` files and a fake server, `2026-09-21_duplicati_server_db_forensics.py` on a scratch copy of today's snapshot, `systemd-analyze security --offline`, `setfacl` on a scratch tree, `man systemd.path/exec`, `duplicati-server|server-util|database-tool help`, Duplicati sources (`DataFolderManager.cs`, `Program.cs`, `RunScript.cs`, `FileBackend.cs`, master), markdownlint-cli 0.42.0, and the host's non-privileged state.

---

## 1. AMPUTATIONS (item dropped | where it lives in the inputs | consequence | proposed fix)

1. **The cleartext passphrase file inside the backup source is missing from the secret inventory.** | YAM §8.18.2, HANDOFF 09-07 §1.8 (the §8.8 reconciliation, with sha256[:16] fingerprints); host: `juniper-ml/.claude/worktrees/curious-plotting-hummingbird/.env` still exists, 114 B, **mode 0664**, Aug 23. | §6 (line 301 ff.) presents "three secret exposures" as the complete list and rules D-2 on that basis; the one copy of both passphrases that is world-readable *and* inside the archive is absent, so the D-2 "accept + scrub" ruling is made without it. | Add S-7; disposition per HANDOFF §1.8: reconcile fingerprints, then remove (escrow now exists per YAM §8.24.1/§8.27.5); carry the `git worktree remove` deletes-it-silently trap.

2. **HANDOFF §1.7 — the unexplained 2026-08-30 stuck pause — is dropped from P0.** | HANDOFF §1.7 ("untested candidate: first start after a `DAEMON_OPTS` change"; "read `paused-until` BEFORE resuming"; "capture the baseline during the restart window"); YAM §8.22/§8.23. | P0 step 7 (line 1043) performs exactly the untested sequence (fresh start under changed `DAEMON_OPTS`, then `pause`/`resume`) with no `paused-until`/`SchedulerQueueIds` sample; if the pause recurs the backup silently stops again — the 42.6 h class. | P0 step 7: `GET /api/v1/serverstate` before `pause` and after `resume`, record `ProgramState`, `paused-until`, `SchedulerQueueIds`; step 8 waits for the run to actually begin.

3. **"Never move anything under `/mnt/Backups/Ubuntu/`" is carried and then contradicted.** | HANDOFF §2 trap 1; the design's own §8 preamble line 988 ("Nothing under `/mnt/Backups/Ubuntu/` is deleted or moved by any step below"). | §7.4 table ("moved out of the Dropbox root") and P1 step 4 ("Move `_yamaguchi_keys/` out") move an escrow copy that Dropbox will then propagate as a cloud deletion; the operator holds two contradictory rules. Also unrecorded: the escrow file is now `-rwxrwx---+ pcalnon:duplicati` (was 0600 in a 0700 dir, YAM §8.19.2), so S-4's "who can read it" now includes group `duplicati`. | State the exception: `cp -a` to `/mnt/Backups/Ubuntu/_yamaguchi_keys/` (sibling of `Dropbox/`), verify sha256 both sides, `dropbox exclude add` or delete only on owner sign-off; amend the preamble; chmod the file 0600.

4. **PLAN §7 criterion 3 (second drill from a different restore point after ≥1 incremental) collapses into one drill.** | PLAN §7 bullet 3; YAM §8.7. | AC-4 (line 1087) drills "the new fileset" only. The re-attached index (Procedure A/B) serving the *old* filesets is exactly what one drill cannot see. | AC-4 = two drills: one from `20260918T140000Z` (pre-recovery), one from the first post-recovery fileset.

5. **The report-URL bearer JWT is not treated as a secret while the DB is copied five ways.** | YAM §8.18.4, §8.19.8 item 6; HANDOFF §1.8 ("it is a credential, not merely a URL"). | §7.3.6 "stay as the owner left them"; Procedures A/A2/B, the daily snapshot into `~/.local/state`, and P4's tar all carry `additional-report-url`; §6 never lists it. | Add to §6 (carried; rotate if any DB copy leaves the machine); Procedure B says whether `build_job.py` re-creates it.

6. **Open items from the record are dropped without a stated disposition.** | HANDOFF §1.4 (drift-guard tests in 7 repos), §1.8 (`yamaguchi_run_script_after.bash` alerting candidate A "never revoked"; `duplicati_first_backup.bash` hardcoded gate; §8.21.5 residuals); YAM §8.27.6. | D-12 (line 1114) says "inherited items … unchanged from the record" but names five; the rest dissolve — the failure shape HANDOFF §6.1 recorded. | D-12 enumerates every open item from HANDOFF §1/§8.27.6 with carried / out-of-scope / closed-by-§.

7. **CON §7 instrument adequacy is supplied only for §5.** | CON §2 Lane A ("could this instrument have produced a different answer?"), §7. | §11 (line 1122) points to §5.6, which covers the three legs; the §4 observations the plan acts on (877 files, "no ACL entries", "Up to date", gid check) have no adequacy statement — and one is already stale: `ls` shows `+` (an ACL xattr) on every `Backups/` entry while `getfacl` prints only base entries; the udisks change (§4.5) was found by a validation lane, not the census. | §11 lists instrument + adequacy per §4.1 row, or marks §4 as a Lane A subject with its own record.

8. **Owner-prompt bullet "Dropbox syncs only the contents of `Backups/`" is corrected but not resolved.** | prompt132 line 35; design §4.4. | R-9 "Kept" with personal files still syncing beside the ciphertext; no decision item asks the owner whether the root should be exclude-everything-but-`Backups/`. | Add D-13 (Dropbox root policy).

9. Minor: §2 provenance says the prompt lives on `feat/backup-updates-and-redesign-work` at `aab07eba`; it is on `main` via #1969 (`d721fc78`, files: `notes/backup_tests_pre-and-post_reboot.md`, `prompts/manual/prompt132_2026-09-21.md`, `scripts/duplicati-wrapper.bash`). The 629-byte #1969 note (`lpr` the escrow sheet, `shred -u`, run the census/reboot-verify) carries nothing the design lacks; §4.3 item 11 characterises it correctly.

10. Live-state omission: `/mnt/Backups/Ubuntu/.dropbox-dist` is a full Dropbox client (270.4.3312) owned `duplicati:duplicati`, created 2026-09-14 20:49 — a second install by/for the service user, not in §4.1/§7.11. The running daemons are pcalnon's (`/home/pcalnon/.dropbox-dist/…`, uid 1000).

---

## 2. ACTIONABILITY DEFECTS (location | what an operator would do | what happens | fix)

1. **§7.8 `.path` unit (lines 835–836, `PathExists=…`) + `Type=oneshot` service.** | Install and enable as written; plug a drive on a day nothing is due. | `systemd.path(5)` on this host (systemd 259): *"When a service unit triggered by a path unit terminates (regardless whether it exited successfully or failed), monitored paths are checked immediately again, and the service accordingly restarted instantly"*; the SKIPPED exit-0 loops until the service start-rate limit (5/10 s) trips, and *"the error condition … is propagated … to the path unit and causes the path unit to fail as well, thus losing the monitoring"*. The mount-detection lane is dead seconds after the first plug-in, and again after every login with a drive attached. Appendix B's `systemd-analyze verify` passes it. | `PathChanged=/run/media/pcalnon` (man page: re-check-on-termination "does not apply to PathChanged="), or a udev `ENV{SYSTEMD_USER_WANTS}` on the two UUIDs, or a trigger file the scheduler consumes. Test: plug a freshly stamped drive and watch `systemctl --user status juniper-backup.path` for 30 s.

2. **§7.8 service line 847 `OnFailure=duplicati-backup-failure.service`.** | Reuse the existing reporter. | Its `ExecStart` is `duplicati-backup-failure.bash duplicati-backup.service` (`~/.config/systemd/user/duplicati-backup-failure.service:7`; `util/duplicati_backup_failure.bash:31,46`) — it journal-tails the *disabled CLI lane* and writes a `failures.log` record naming the wrong unit. | A `juniper-backup-failure.service` passing `juniper-backup.service`, or a template `…-failure@.service` with `OnFailure=…@%n.service`.

3. **P3 step 1 vs §4.5/§7.9 (lines 1068–1069).** | Land/enable the scheduler and expect "one OK (plugged) run" before step 2 fixes `util/juniper-backup.bash`. | The runner still forces `/media/pcalnon/<name>` (`util/juniper-backup.bash:122–124,329`) and, per the design's own §4.5, has skipped both drives since 09-07; the scheduler (`MEDIA_ROOT=/run/media/${USER}`, line 881) reports mounted+due, calls the runner, and every plugged run ends `FAILED runner rc=1`. No OK run is possible in step 1. | Reorder (runner fix first) and have scheduler and runner read one mount-root setting.

4. **§7.5 guard + §7.3.4 installer + P0 step 7.** | Add `--run-script-before-required=/usr/local/lib/duplicati/yamaguchi-pre-backup-guard.bash` and `resume`. | The installer installs wrapper/unit/defaults only; the guard has no repository source path and no install step. `RunScript.cs`: for the required form any failure (including a missing file) aborts — the first post-recovery backup, which the schedule fires immediately on `resume`, aborts. | Name the source (`util/…/yamaguchi-pre-backup-guard.bash`), install it 0755 root:root in `install_duplicati_service.bash`, and have step 7 dry-run it as `duplicati` with `DUPLICATI__REMOTEURL` set before `resume`.

5. **P0 step 7/5/9 and §7.3.6: `duplicati-server-util pause|resume|change-password <new>`.** | Type the commands. | `duplicati-server-util help` (2.4.0.0): authentication is `--password <password>` on argv (or `login`, which also takes `--password`); `change-password <new-password>` puts the *new* secret on argv; no prompt/stdin path is documented. R-18 ("no secret on a command line") is violated by the plan's own commands, into root's history and `ps`. Also `--webservice-password-init` only acts while `AutogeneratedPassphrase` is true and exits with code 102 (Program.cs) — under `Restart=on-failure` it must never sit in `DAEMON_OPTS`, and the design's "on a single start" doesn't say "service stopped, by hand". | Add `pause`/`resume`/`change-password` to the in-process client (`util/ad-hoc/yamaguchi_server_api.py` reads the credential from a file), or one `login` under `HISTCONTROL=ignorespace` then `--settings-file`; set the first password in the web UI.

6. **P0 step 3 probe (lines 1011–1019): `cp -p "${SRC}"` copies the main file only.** | Run the probe on the abandoned root DB. | The folder's last writers died on `SettingsEncryptionKeyMismatch/Missing` exceptions (09-18 21:08 → 09-19 19:14); a WAL-mode SQLite left by process death keeps its frames in `-wal`. If the 21:03:34 re-encryption lives there, the copy opens an older or torn state and every candidate reads accepted-for-the-wrong-reason or rejected. The orchestrator's own forensics tool copies "main file plus its -wal and -shm siblings" for exactly this reason. | Copy `Duplicati-server.sqlite{,-wal,-shm}` (or `cp -a` the folder); assert the probe's open leaves no `-wal`; add `timeout -k 10 45`.

7. **P0 step 3 text (line 1035): "`printf '%s' '<value>' > file`".** | Type the candidate key on a root command line. | Root's bash history and scrollback hold the key — the same channel the design just closed in §6. | `read -rs` or `install -m 0600 /dev/stdin file` after `set +o history`; say so.

8. **P0 step 4 (line 1036): "write the accepted key … for the first start only".** | Remove the credential after the first start. | Next restart → `SettingsEncryptionKeyMissingException` → 5 restarts → start-limit → down; the 09-18 failure re-run. | "Until P1 step 1 replaces it."

9. **P0 step 5 (line 1038), Procedure A2.** | Run `duplicati-database-tool wipe-encryption … --server-datafolder <copy>`. | The tool has no `--server-datafolder` option (its options: `--allow-insecure-datafolder`, `--version`; `wipe-encryption <databases>` takes DB paths and writes a backup beside it) → unknown-option error. The re-entry of `TargetURL`/passphrase "through the UI or `PUT /api/v1/backup/2`" names no tool; a curl `PUT` carries the passphrase on argv. | Exact command on the DB path, run as the copy's owner in a 0700 dir; re-enter via the UI or `yamaguchi_build_job.py`'s in-process read.

10. **P0 step 6 (line 1040), Procedure B: the new job will not be id 2.** | Rebuild on a fresh DB. | `sqlite_sequence` starts at 1; the deployed watchdog runs with the default `--backup-id 2` (`util/ad-hoc/yamaguchi_watchdog.py:160`; unit has no override), `yamaguchi_census.py --backup-id` default 2, AC-2 `export 2`, AC-5 and P0 step 9 all assume 2 → `JOB_MISSING` alerts forever, AC-2/AC-3 commands hit the wrong job. | Record the assigned id; every later command/AC uses `<id>`; the watchdog unit gets `--backup-id <id>` on redeploy.

11. **AC-2/AC-3/AC-9 instruments after the UI password rotates (P0 step 9).** | Run `yamaguchi_server_api.py status`, `yamaguchi_census.py`, `yamaguchi_reboot_verify.bash post`. | `yamaguchi_server_api.py:41` hard-codes `CRED_FILE = …/juniper-ml/.env`; `yamaguchi_reboot_verify.bash:73` calls it; the census goes through `duplicati_api.py` (`PW_FILE` default `.env`). S-5 re-points only `duplicati_api.py`. After rotation, AC-2 (which P0 step 8 runs *before* step 9 creates the credential file) and AC-9 fail 401 — exactly today's watchdog symptom. | One credential path (`~/.config/duplicati-backup/web-credential`) in both clients, same PR; order P0 so the file exists before AC-2.

12. **§7.3.1/§7.10/AC-3 — the capability proof.** | Trust "AC-3 measures this". | From the snapshot's 45 filters, the large 0700 trees `Dropbox-OLD/` (327k entries) and `snap/` (256k) are *excluded*; the unfiltered 0700 trees (`.config/Slack` 54k, `.config/Code` 39.5k, `.mozilla` 23k, `.local/state` 22.6k, `.config/Cursor` 12k, `.amp`, `.claude/projects`, `.ssh` 31, `.gnupg` 98) sum to ≈15 % of 1,024,167 — so a ±2 % count sees the gross loss but cannot see `~/.ssh`/`~/.gnupg`, the trees §7.10 names. No direct test that ambient `CAP_DAC_READ_SEARCH` survives into the .NET process exists. The baseline numbers are real (snapshot Metadata `SourceFilesCount=1024167`, `SourceSizeString=290.610 GiB`) but appear nowhere in the record — provenance must be cited. | AC-3 adds `Warnings=0`, `NotProcessedFiles=0` (the §8.27.1 pattern) and a `duplicati-cli find` of one `~/.ssh` and one `~/.gnupg` path; a pre-flight `systemd-run -p User=duplicati -p AmbientCapabilities=CAP_DAC_READ_SEARCH --wait cat ~/.ssh/known_hosts` (owner action).

13. **§7.7 "group read is enough"; run the snapshot unit as `duplicati`.** | Make `~/.local/state/duplicati-server-db/` group-writable. | The design's own data-folder gate is 0700 (no group bits), and `/home/pcalnon/.local/state` is **0700** — the `duplicati` user cannot traverse to the target however it is chmod'ed → EACCES. | Keep the unit root on the new path, or use a root-created 2770 staging dir inside the source; say which.

14. **§7.4 permission script.** | Run it on the Dropbox-synced tree. | `chmod 0660` on ~1,700 volumes strips the executable bit Dropbox syncs → ~1,700 metadata updates (bandwidth, not correctness; not stated); `find -type f -exec chmod 0660` leaves `_yamaguchi_keys/env` at 0660 (should be 0600); recursive `setfacl -m d:g:…` silently skips files (tested rc=0 — harmless, as designed). | chmod the escrow file 0600 explicitly; run only when `dropbox status` reads up to date; note the churn.

15. **§7.3.5 vs P1 step 2**: the `.env` contract now says mode 0600 (line 660); P1 step 2 says `chmod 0640` and R-7 promises group access. Pick one (the gate checks the directory only — `PrepareSecureDataFolder`/`VerifyDataFolderSecurity`, master).

16. **P4 step 1**: tars `/usr/lib/duplicati/data/` (server DB encrypted under the *burned, public* S-1 key, plus the per-job index with the full path list) into `~/.local/state/…` — a backed-up, group-shared, cleartext tar; violates the design's own rule 8. Wipe/re-key the copy first or encrypt the tar.

17. **P0 step 3 probe relies on `startup-delay`**: the option exists among the 46 server settings, but its value is unverifiable here; if cleared, an accepted-key probe server owns the real `DBPath` and destination for 45 s and can start the overdue run, then be SIGTERMed (rule 11). Run the probe under `systemd-run --wait -p InaccessiblePaths=/mnt/Backups -p InaccessiblePaths=/usr/lib/duplicati/data`.

18. **§10 vs P0**: P0 silently applies D-1 (credstore + `--require-db-encryption-key`), D-4 (`AmbientCapabilities` in the unit), D-6 (installed copy), D-9 (loopback) before they are ruled; the preamble's "nothing in a later phase is a precondition" is true of phases, false of decisions. Mark D-1/4/6/9 "rule before P0" or state P0 applies the recommendations provisionally.

19. **§7.5 guard scope**: the job's 10 options don't include `--upload-verification-file`, so `duplicati-verification.json` is not written today; enabling it later makes the guard refuse every run — say so in the guard. The exact `file://<dir>` match fails closed on a query string or trailing-slash variant (acceptable; state it).

20. **`Restart=on-failure`** (vendor `always`): a clean exit 0 is not restarted — intended? Not stated. `After/Wants=network-online.target` is unnecessary for a loopback server.

21. **§7.8 install sentence (line 956)**: `install_duplicati_timer.bash` installs the *old* lane's five files by name; no installer exists for the three new units and two scripts.

22. **Naming/format**: markdownlint-cli 0.42.0 (the pinned hook) **fails**: line 1095 (AC-12 row) MD038 + MD056 — the `|` inside `` `CapabilityBoundingSet=~CAP_(DAC_*|FOWNER|IPC_OWNER)` `` splits the table cell, and the hook's `--fix` will mangle rather than repair it. JR-DEP-SEC-005 (line 315) exists (`notes/requirements/by-area/SEC.md:2017`, gitleaks allowlisting) — fine; no `## Requirements` section is planned for the PR, and §12 lists no files the PR adds (`util/ad-hoc/2026-09-21_*.py` ×4 incl. `wrap_long_markdown_lines.py`, which §0/Appendix B never mention). 42 alias-only section references (`PLAN §…`, `YAM §…`) against a convention that wants filenames on every reference once ≥2 documents are cited. Secrets hygiene of the text: clean (no `enc-v1` value, no token, no 32–40-char mixed string).

Verified as claimed (so the reconciler need not re-check): wrapper v2 passes separate argv words, dies exit 78 on a malformed line and on a non-allow-listed name, keeps `$HOME`/`$(…)` literal, keeps `a=b=c` values, `${!key+x}` is correct under `set -u`, shellcheck clean; `/etc/credstore` is 0700 root (tmpfiles `credstore.conf:10`); `ProtectHome=read-only` + nested `ReadWritePaths=` is the documented pattern; the file backend writes in place under the final name (`FileBackend.cs`); `DUPLICATI__REMOTEURL` etc. exist; the 0777 chmod (ctime 18:33:45) postdates the 18:19:42 start, so "the next start may refuse it" stands; offline `systemd-analyze security` gives 5.2 MEDIUM with no `UNSAFE` row, matching the rewritten AC-12; `Yamaguchi/` holds 877 entries with no stray name and the `.gpg` dlists/README/`lost+found` are siblings under `Backups/`, as §4.4 says.

---

## 3. Five most consequential

1. **D1** — `PathExists=` re-triggers on termination; the T2 detection lane fails within seconds of the first plug-in and stays failed. The one instrument the design applies (`systemd-analyze verify`) cannot see it.
2. **D5 + D7** — the plan's own commands put the UI password, the new password and the candidate settings key on command lines, in the section that exists to end that exposure (R-18 self-violation, three places).
3. **D10 + D11** — after Procedure B or the password rotation, the watchdog, census, reboot-verify and AC-2/3/5/9 all point at the wrong job id or credential; the alerting lane stays red exactly as it is today, and AC-2 is scheduled before the credential it needs exists.
4. **D6** — the key-identification probe copies the DB without its WAL; the leg-C test can misreport, and every downstream branch (A / A2 / B) is chosen on its verdict.
5. **A1** — the world-readable cleartext passphrase file inside the source (YAM §8.18.2) is missing from the inventory that D-2 is ruled on. Close behind: **D4 + D3** (the guard has no source or installer, so the first post-recovery run aborts; P3 cannot produce its own "OK" run).

---

## 4. What I could not determine

- Whether `/usr/lib/duplicati/data/` currently holds a `-wal` (0700 `duplicati`, no sudo) — i.e. whether D6 is live or latent. `sudo ls -la /usr/lib/duplicati/data/` settles it.
- Whether ambient `CAP_DAC_READ_SEARCH` reaches the .NET server process (no privilege to test); the amended AC-3 or the `systemd-run` pre-flight settles it.
- The current value of `startup-delay` (the forensics tool never prints option values, by design) — decides D17's severity.
- What Dropbox does with a setgid/0770 root and ~1,700 mode-bit changes (not testable read-only); and ext4 behaviour of the default-ACL creation mode (my scratch test ran on tmpfs and showed anomalous setuid/sticky bits on a new subdirectory).
- Duplicati semantics at the 2.4.0.0 tag: I read `master` for the data-folder gate and RunScript contract; the two-start re-key (`ReWriteAllFieldsIfEncryptionChanged`) and the `insecure-permissions.txt` marker were not in the excerpts I obtained.
- Whether `duplicati-server` ignores an unknown `--print-command` forwarded from a `.env` `--option` line (the current wrapper forwards it; the validator class was not fetched).
- Whether the version reviewed is the one the pool will reconcile: the document changed twice during the review; every line number above should be re-anchored by heading text, not number.

<!-- markdownlint-enable -->

---

## Report B3 — Lane B — adversarial analysis review; lens: SECURITY

Sections of the design assigned: §6, §7.1, §7.3, §7.4, §7.5, §8 P0/P1/P2. Subagent id `ac918ed6dc31f2f0f` (27,332 characters).

<!-- markdownlint-disable -->

# Lane B3 (security lens) — adversarial review of `notes/JUNIPER_2026-09-21_JUNIPER-ECOSYSTEM_BACKUP-INFRASTRUCTURE-INTEGRATED-DESIGN.md`

Scope: §6, §7.1, §7.3, §7.4, §7.5, §8 P0/P1/P2. Read-only; no secret value printed (the one literal I refer to is named by length and location only); no live SQLite opened; secret files inspected by metadata only.

**Host fact the design never states, and that reframes every "local readers only" sentence in §6**: `pcalnon` is in `duplicati(139)`, `adm`, `sudo`, `docker` (`/var/run/docker.sock` is `srw-rw---- root docker`, daemon active), `libvirt`, `kvm`. A hostile pcalnon process is already root-equivalent on this host. The boundaries the design can still buy are: confined service vs host, cloud account vs local disk, public repo vs local, and accidental/commodity damage vs deliberate.

## Findings (threat/actor | claim in the design | attack or gap | evidence | severity | fix)

**F-1** | (c) compromised server, or anyone who gets code into the unit | §7.3.1: the capability lets it "read and traverse any file … but cannot write outside its own permissions"; D-4 chooses "capability" with no trade-off | `CAP_DAC_READ_SEARCH` bypasses every read/search check: `/etc/shadow`, `/etc/ssh/ssh_host_*_key`, `/etc/credstore/*` (every service's secrets), `/root`, `/var/lib/docker`, the journal and `/var/log/syslog*` (which hold the leaked passphrases), other users' homes. `ProtectSystem=strict` / `ProtectHome=read-only` stop *writes* (mount flags, not DAC — correctly not defeated by the capability) but hide nothing. The unit keeps the host's network (`IPAddressDeny`, `RestrictAddressFamilies`, `PrivateNetwork` unset), so everything read can leave; Duplicati needs no egress here (destination is `file://`, Dropbox is a separate daemon). No `SystemCallFilter`, `ProtectProc`, `PrivateDevices`, `ProtectClock`, `ProtectHostname`, `ProtectKernelLogs`, `RestrictNamespaces`. | `man capabilities`; offline `systemd-analyze security --root=<scratch>` of the design's unit: **5.2 MEDIUM**, ✗ on CapabilityBoundingSet=~CAP_DAC_*, IPAddressDeny, RestrictAddressFamilies (all), PrivateNetwork, ProtectProc, ProcSubset, SystemCallFilter (every group), PrivateDevices, ProtectClock, ProtectHostname, ProtectKernelLogs, RestrictNamespaces, MemoryDenyWriteExecute, UMask | HIGH (design) | D-4 must state the real trade: ACLs bound the blast radius to `/home/pcalnon` but are fragile (`chmod 600` / `mkdir -m 700` / gpg's permission checks reset the ACL mask → silent omission, exactly what AC-3 fears); the capability bounds it to the whole host. If the capability stays: `IPAddressDeny=any` + `IPAddressAllow=localhost`, `RestrictAddressFamilies=AF_UNIX AF_INET AF_INET6`, `SystemCallFilter=@system-service` (F-2), `ProtectProc=invisible`, `ProcSubset=pid`, `PrivateDevices=yes`, `ProtectClock=yes`, `ProtectHostname=yes`, `ProtectKernelLogs=yes`, `RestrictNamespaces=yes`, `InaccessiblePaths=-/etc/shadow -/etc/gshadow -/etc/ssh -/etc/credstore -/etc/credstore.encrypted -/var/lib/docker -/var/log -/root` (or `ProtectHome=tmpfs` + `BindReadOnlyPaths=/home/pcalnon` + `BindPaths=/home/duplicati`); disable update checks / usage reporting (the only legitimate egress). Record the residual: "duplicati uid inside the unit == root-read".

**F-2** | same | (implicit) path masking would bound the capability | `open_by_handle_at(2)` is explicitly granted by `CAP_DAC_READ_SEARCH` and opens any inode of a mounted filesystem by handle, bypassing `InaccessiblePaths=` / `TemporaryFileSystem=` (the "shocker" technique). It lives only in `@privileged`; the unit sets no `SystemCallFilter=`. | `systemd-analyze syscall-filter @privileged` contains it; `@system-service`, `@file-system`, `@default`, `@mount` → 0 | MEDIUM (design) | `SystemCallFilter=@system-service` (excludes `@privileged`) or at least `~open_by_handle_at @mount @module @reboot @swap`; AC-12 must include a run under it (O-3).

**F-3** | (b) any pcalnon process; (a) any local user who reads a credential file; sign-in-token holders | §7.3.6/§7.6 treat the UI password as a watchdog convenience; S-5 "stale", S-6 "none needed" | Any authenticated API principal sets `--run-script-before=/any/path` on the job, or creates a job with Source `/` → code runs as `duplicati` *inside* the unit *with* `CAP_DAC_READ_SEARCH`. The UI credential is therefore a host-wide read credential: today read from `/home/pcalnon/Development/python/Juniper/juniper-ml/.env` (`-rw-rw-r--` 0664, world-readable; `util/ad-hoc/yamaguchi_server_api.py:41`), tomorrow from `~/.config/duplicati-backup/web-credential` (every pcalnon process, every Claude session). A same-uid process can also read the 0600 DB plus the key from `/proc/<pid>/environ` and mint a JWT. | RunScript.cs (fetched): scripts run via `Process.Start` inheriting the environment; run-script paths are plain job options with no allow-list; `ss`: `127.0.0.1:8300` and `[::1]:8300` | HIGH (design) | State "UI credential = root-read" in §7.3.6 and §10; keep it 0600 outside any checkout, rotate it, `--webservice-disable-signin-tokens` once a password exists, `--webservice-allowed-hostnames=localhost`; make the acceptance check pin the job's run-script option to the design's path.

**F-4** | (c), and anyone who replaces the guard | §7.1 p.2 "secrets never pass through a shell parser or a log"; §7.3.2 "never in the environment block"; §7.5 names only the `DUPLICATI__EVENTNAME`-class variables | The wrapper `export`s `SETTINGS_ENCRYPTION_KEY` then `exec`s: the key sits in the server's environ for its lifetime and is inherited by *every* child. RunScript.cs exports **all job options unfiltered** (`foreach (kv in options) psi.EnvironmentVariables["DUPLICATI__"+…] = kv.Value`) → the guard (bash) and its children (`mountpoint`, `find`, `id`) receive `DUPLICATI__passphrase` *and* the settings key; `/usr/lib/duplicati/run-script-example.sh:75` documents "all Duplicati options are exposed as environment variables". The guard's **stdout is parsed by Duplicati as option overrides** (example.sh:85); the design documents exit codes only. A `set -x`, a debug echo (the 09-20 class), or a swapped guard prints both secrets. | RunScript.cs; example.sh on host; Program.cs: `ApplySecretProviderAsync` runs on the server's *own* options before startup, so `--settings-encryption-key=$settings-key --secret-provider=file://…` resolves in-process and never enters the environment; `duplicati-server-util` also takes `--secret-provider` | MEDIUM (design) | List the secret-provider channel as the preferred delivery (argv shows only the placeholder; no environ, no inheritance). Until then the guard's first statement must `unset DUPLICATI__passphrase SETTINGS_ENCRYPTION_KEY` and it must never write to stdout; state the stdout contract in §7.5; extend AC-8 to run-script output in the job log.

**F-5** | (c) plain `duplicati` uid outside the unit; today any local user | §7.3.3 "No secret value is ever printed"; `.env` is the tunable surface | (i) `die "${src}: not a Duplicati option: '${word}'"` echoes the whole malformed word — a mistyped `--webservice-password=…` line in `.env` lands verbatim in the journal *through the fail-closed path*. (ii) `.env` `KEY=VALUE` export has no name allow-list: `LD_PRELOAD`, `DOTNET_STARTUP_HOOKS`, `DOTNET_ROOT`, `PATH`, `BASH_ENV` inject code into the capability-bearing process from a file the plain duplicati uid can write (design: 0640 in a 0700 dir; today 0660 in a 0777 dir = anyone). | wrapper text §7.3.3; `ls -la /home/duplicati/.config/Duplicati` | MEDIUM | Print `${word%%=*}` plus a length; refuse `^(LD_|DOTNET_|PATH$|BASH_ENV$|ENV$|CREDENTIALS_DIRECTORY$)`; make `.env` `0640 root:duplicati` (R-5 fixes only its location, not its owner).

**F-6** | (b) every pcalnon process incl. the Dropbox daemon; (d) the Dropbox account via propagation | §7.1 p.5; §7.4 `2770` + default ACL `g:duplicati:rwx`, files `0660`; R-8 asks only "readable+executable by pcalnon so Dropbox can sync" | The design grants **write/delete** on all 877 volumes to every pcalnon process (Claude sessions, browser, pip hooks) and — by its own prerequisite AC-7 — to the Dropbox daemon, so a cloud-side deletion (compromised account, linked phone, another PC) or local ransomware / a stray `rm -rf` propagates into the local master and on to T1c. §7.4 states no trade-off. Today pcalnon *owns* the directories (`pcalnon:duplicati 0770`), so owner bits grant write regardless of the group. | `getent group duplicati`; `ls -ld` on the destination; Dropbox `/proc/<pid>/status` Groups lacks 139 | HIGH (design; conditional on the owner's reading of R-7) | Directories `duplicati:duplicati 2750`, files `0640`, `UMask=0027`, existing volumes `chown duplicati`; pcalnon/Dropbox read via the group (or a dedicated readers group). Dropbox remote→local writes then fail loudly instead of silently deleting the master; hand maintenance stays `sudo -u duplicati` (already accepted for the data folder). Say that pcalnon is root-equivalent via `docker` anyway, so this buys accident/commodity protection, not adversary protection.

**F-7** | (c) any duplicati-uid process; (a) any local user | §8 "nothing in a later phase is a precondition of an earlier one"; hardening in P2 | `/usr/lib/duplicati` **directory** is `duplicati:duplicati 0755` (files root-owned; 19 subdirs duplicati-owned incl. `webroot/`, `lvm-scripts/`, `utility-scripts/`), no sticky bit → the duplicati uid can `mv` any entry aside and drop its own `duplicati-cli` / `duplicati-server-util` / `duplicati-database-tool` / `duplicati-server`; `/usr/bin/duplicati-*` are symlinks into it; root and pcalnon run those tools during P0. `webroot/` writable → JavaScript injected into the UI pcalnon's browser loads. `/home/duplicati/bin` 0777 (live symlink) and `/home/duplicati/.config/Duplicati` 0777 with no sticky bit (`protected_regular=2` does not apply) → any local user can rename-replace `.env` or the server DB until P0 step 4. Procedure B never creates the data folder 0700 (the empty one moved aside in step 2 is 0777, and 2.4.0.0 refuses it — confirmed: `duplicati-server help` aborts in `VerifyDataFolderSecurity` on a 0755 folder). | `find /usr/lib/duplicati -maxdepth 1 ! -user root`; `ls -la`; sandboxed run | HIGH (ordering) | Before P0 step 3: `chown -R root:root /usr/lib/duplicati` except `data/`; delete `bin/duplicati-wrapper.bash` and `bin/` (not "chmod 0755 or remove" in P2 — D-6's "fallback" symlink should not exist); `chmod 0700` the data folder; add `install -d -m 0700 -o duplicati -g duplicati` to Procedure B.

**F-8** | (b) every pcalnon process (agents switching branches in the primary checkout) | §7.7 / P2.3 "re-point … redeploy the system timer"; rule 14 | `/etc/systemd/system/yamaguchi-server-db-snapshot.service` runs as **root** (`User=` empty): `ExecStart=/usr/bin/python3 /home/pcalnon/…/util/ad-hoc/yamaguchi_server_db_snapshot.py`, daily (next fire 2026-09-21 08:45 CDT); `sys.path[0]` is that pcalnon-writable directory. The design describes the unit and defers it to P2 without naming it as the pcalnon→root path it is. | `systemctl cat`, `systemctl list-timers` | HIGH (live), MEDIUM (design gap) | Before the next fire: root-owned installed copy under `/usr/local/lib/duplicati/`, `User=duplicati` (the new data folder is duplicati-owned; §7.7 already says root is unnecessary), `ProtectSystem=strict`; same treatment for the watchdog (integrity only).

**F-9** | (b) every pcalnon process (adm) | §6: journal readers are "adm/systemd-journal members and root"; D-8 scrub = `journalctl --rotate` + `--vacuum-time=1s` | The design does not know rsyslog exists. `/usr/lib/systemd/journald.conf.d/syslog.conf` sets `ForwardToSyslog=yes`; rsyslog active+enabled. Counts (never printed): `syslog-20260919.gz` key-bearing lines 5 / specifier-error 4; `syslog-20260920.gz` 18 / 18 / sign-in token 2; `syslog-20260921` (161 MB, `-rw-r----- syslog adm`) key 76, `PASSPHRASE=` 12, `Exporting Environment` 12, token 3 — **99 key-bearing lines, equal to the journal's 99**; logrotate `rotate 10` daily keeps them until ~2026-10-01. `/var/log/journal` ACL `group:adm:r-x`, files `group:adm:r--`; `systemd-journal` has no members; pcalnon is in adm. User journal: 0. | `ls -la /var/log/syslog*`, count-only greps, `getfacl` | HIGH (exposure) / MEDIUM (design) | Scrub syslog too (edit the plain files as root, regenerate the `.gz`), then rotate+vacuum the journal — in P0's session, not "after P1"; rewrite "local readers only" as "every process running as pcalnon" and re-argue D-2 against that reader set.

**F-10** | sinks in root/duplicati/pcalnon space | §6 lists journal, `.env`, repo, escrow, repo `.env`, token | Not inventoried: (a) two live `su - duplicati` shells (PIDs 3065639, 3117158; started 09-19 18:24:50 and 18:52:00) — `/home/duplicati/.bash_history` (0600, 310 B) was last written 09-19 18:51, so every command typed as duplicati during the 09-20 migration (`.env` created 16:39 09-20) is still in memory and **will be appended on exit**; (b) `/home/duplicati/.viminfo` 14.7 KB, mtime 09-20 17:59 (vim used as duplicati after `.env` was written; viminfo keeps registers and command history); (c) root's history/viminfo — the design *relies* on root's history as "the only record" of the 09-18 key and schedules no cleanup of it; (d) `/usr/lib/systemd/system/.duplicati.service.swp` — root vim open for hours, swap `-rw-r--r--` world-readable (key-line count today 0; the same editor carried the key into that unit on 09-18); (e) CUPS spool of the printed escrow sheet (USB HP OfficeJet; 32 completed jobs; job-file retention unknown); (f) Claude Code transcripts under `~/.claude/projects/...` capture every `journalctl` output and are cloud-linked; (g) world-readable server-DB copies (`Duplicati-server.backup` 0777, `backups/*pre-dbpath-fix.sqlite` 0644) are `enc-v1:` (count 5 each) under the pcalnon-profile (libsecret) key — encrypted, but wrongly exposed. | `ps -o lstart`, `ls -la`, count-only greps | MEDIUM | Add a sink checklist to §6; `history -c; unset HISTFILE` in the live shells before they exit; wipe `.viminfo`; shred the swap when the editor closes; count-grep the transcripts (O-4) and purge; 0600 the DB copies now.

**F-11** | (e) the public repository | S-1 "treat as burned … a history rewrite is not worth its cost" | Agree on the rewrite (GitHub keeps `refs/pull/1967/head`; `forks_count: 0`). Gaps: (a) the burned key becomes the **live** settings key from P0 step 4 until P1.1 "same week" — in that window the daily snapshot (pcalnon 0600 in `~/.local/state`) and the recovery copy are decryptable with a public key; (b) recurrence controls: `secret_scanning_non_provider_patterns: disabled` (push protection covers provider tokens only); gitleaks' generic rule cannot match a value containing `* @ $ %` (value class `[a-z0-9\-_.=]`) — which is why both PRs passed; no gitleaks in `.pre-commit-config.yaml` (only `detect-private-key`); CI runs gitleaks after publication; `.gitleaks.toml` is allowlist-only. | `scripts/duplicati-wrapper.bash:49` (36-char literal inside the "Old Unit file" comment); `git log -S` → 6708cb28 (#1967, 2026-09-20); `gh api … security_and_analysis` | MEDIUM | Fold P1.1 into P0 (it is two restarts); enable non-provider patterns; add a gitleaks rule for `(SETTINGS_ENCRYPTION_KEY|PASSPHRASE(_OLD)?|DUPLICATI_WEB_CREDENTIAL|webservice-password|passphrase)=` with a non-placeholder value plus a pre-commit gitleaks hook; make that rule AC-8's grep. Checked and clean: `prompts/manual/prompt132_2026-09-21.md` at aab07eba has 0 credential-shaped lines; the 5 `PASSPHRASE=` literals in `tests/test_duplicati_scheduled_backup.py` and `util/ad-hoc/2026-08-03_yubikey_curve448_keytocard_e2e.bash` are 19–33-char placeholders.

**F-12** | (d) the Dropbox account, Dropbox itself, any linked device/app | S-4 "Move `_yamaguchi_keys/` out of the Dropbox root, **or exclude it with `dropbox exclude add`**"; D-2 "accept + scrub; rotate at the next rebuild" | `dropbox filestatus …/_yamaguchi_keys/env` → `up to date` (synced). Dropbox help (fetched): selective-sync exclusion means files "are removed from your hard drive, and are only stored on dropbox.com" — **the `exclude` branch removes nothing from the cloud**. Moving the folder out puts it in deleted files, retained 30 days (Basic/Plus/Family) / 180 days (Professional) with 180-day version history on Professional; "Delete forever" is a separate action. The escrow env holds PASSPHRASE and PASSPHRASE_OLD (README, fetched), so the account has held the ciphertext of the whole home *and* its key since ~09-15, plus the key to the ten synced `.gpg` dlists (every path on the machine 2024–2026). D-2 was argued for a *local* exposure and never connected to S-4. Dropbox is not end-to-end encrypted. | `dropbox filestatus`, README, help pages | HIGH | Delete the `exclude` alternative; after moving, "Delete forever" on dropbox.com; audit account security (2FA, linked devices, third-party apps); treat PASSPHRASE as exposed to a third party → D-2 should lean to a new set under a new passphrase, or record an explicit owner acceptance naming Dropbox; exclude `_yamaguchi_records/` if it lists paths.

**F-13** | (d), and workstation ransomware | §7.5 covers local→cloud only; R-16 "AES module … unchanged" | Missing: cloud→local propagation (F-6); Dropbox version history / Rewind as the *only* recovery from a mass delete of T1+T1c — and its corollary that deleted ciphertext stays restorable server-side for the retention window (a "new set" under D-2 must also delete-forever the old one); AES format: the 877 volumes are AES Crypt **v2** (newest dlist header bytes `41455302`), whose KDF is the legacy iterated SHA-256 (8192 iterations per the v2 spec — not re-fetched; aescrypt.com: v3's KDF "is much stronger than previous versions"); 2.4.0.0 defaults `--aes-version=3` (PBKDF2, `--aes-v3-iterations` 300000), so the next run silently changes the on-disk format mid-set; the design never states whether PASSPHRASE is random (with 32 random chars both KDFs are irrelevant; with a human phrase v2 is GPU-crackable). | `duplicati-cli help aes`; header bytes | MEDIUM | Add a cloud threat table; pin `--aes-version` explicitly (keep 2 for a homogeneous set, or accept mixed and extend AC-4 to one pre- and one post-upgrade fileset); record passphrase provenance (generator + length).

**F-14** | (d), (b), gid-139 processes | §7.5 rule 1 (an operating rule, not a threat) | Anyone who can write into `Yamaguchi/` — the Dropbox account, any linked device, any pcalnon process — drops one file and the guard exits 5: no backup until a human removes it, noticed only by the watchdog (26 h). `find -P -maxdepth 1` does not follow symlinks (fine); the name is echoed via `echo >&2`, no evaluation (newline injection into the log only); `-x` is vacuous under the capability, `-w` is not; a file named `duplicati-*.dblock.zip.aes` passes the guard and Duplicati's own foreign-file check aborts identically — the stray check duplicates Duplicati; the guard's real value is mount + TargetURL. | guard text; `find` semantics | LOW–MEDIUM (availability) | State "write access to `Yamaguchi/` = backup DoS"; F-6's model removes the pcalnon/Dropbox write path.

**F-15** | (a) any local user; token holders | §7.3.6 loopback + password; S-6 "none needed" | The sign-in URL is printed only while the password is autogenerated (Program.cs: `if (Origin == "Server" && AutogeneratedPassphrase) … CreateSigninToken`), lifetime `--webservice-tokenduration`; 5 such lines in the journal and 5 in syslog since 09-17 — every restart before P0 step 9 mints a fresh one into adm-readable logs, and with the capability a token is root-read for its lifetime. `--webservice-disable-signin-tokens` exists and is absent from the design. `duplicati-server-util` defaults `--server-datafolder` to `/home/pcalnon/.config/Duplicati/` (the orphaned profile) and takes `--password` on argv (`/proc/*/cmdline` is world-readable) — the design's `change-password <new>` puts the new UI password on a command line. | server-util help; Program.cs | MEDIUM | Set the password on the *first* start (`--webservice-password-init` from a credential/secret provider, or `--settings-file`), then `--webservice-disable-signin-tokens`; never `change-password` with the value on argv.

**F-16** | root/pcalnon readers of the DB and its copies | P1.1 two-start re-key; §7.7 snapshot lane | Start #1 (`--disable-db-encryption`) decrypts every field in place: the passphrase is cleartext in `Duplicati-server.sqlite` between the starts and stays in free pages/WAL after re-encryption (no VACUUM in the procedure; `duplicati-database-tool` offers only `wipe-encryption`, no re-key, and it "creates a backup of the database first" beside it). If the 13:45 UTC snapshot fires in the window, cleartext lands in `~/.local/state` and in the next fileset. | dbtool help; SQLite semantics | MEDIUM | Stop the snapshot timer during re-key; both starts back-to-back; with the server stopped run `PRAGMA wal_checkpoint(TRUNCATE); VACUUM;` after re-encryption and after any wipe; 0600 and remove the tool's backup copy.

**F-17** | root shell history; the probe itself | P0 step 3 Procedure A | "Write each candidate into a 0600 file with `printf '%s' '<value>' > file`" typed at a root prompt re-creates the exposure the design itself names (root history). The probe opens the *real* job DB copy on :8399 as duplicati; the design assumes `startup-delay` keeps the job paused — unverified (O-1); a schedule in the past with no delay would run a backup against the real destination from a copied index. | design text | LOW–MEDIUM | `read -rs key; printf '%s' "$key" > file` (or `HISTCONTROL=ignorespace`); verify `startup-delay` in the snapshot's Option rows before probing; add `--webservice-disable-signin-tokens` to the probe.

**F-18** | process | P1 "same week", P2 "same week", D-8 "after P1" | The HIGH items above (F-7 chown/symlink, F-8 root timer, F-9 scrub incl. syslog, F-11 re-key, F-12 escrow delete-forever) have no dependency on recovery and take minutes; the plan leaves them open for days while the recovered server runs. | §8 | MEDIUM (process) | Add a "P0.5 — same session as recovery" bucket containing them.

**F-19** | trust-domain framing | §7.3.1 | "duplicati uid inside the unit" and "duplicati uid outside" differ only by the capability and the mount view; the crossings are: `.env` env injection (F-5), `/proc/<serverpid>/environ` (key), DB+key → JWT → API → run-script (F-3), entry replacement in `/usr/lib/duplicati` (F-7). Who can be duplicati: root (`su`), the service, run-scripts, and today anyone via the 0777 dirs. | — | LOW (documentation, but it is the frame F-1..F-8 need) | One table in §7.3.1.

**F-20** | (c) | §7.3.2 credential file root:root 0600 | With `CAP_DAC_READ_SEARCH` the service can read `/etc/credstore/*` directly; `LoadCredentialEncrypted` would not help: no TPM2 (`systemd-analyze has-tpm2`: partial, no firmware/driver) and `/var/lib/systemd/credential.secret` absent, so an encrypted credential binds to a host key readable with the same capability. The credentials directory is readable by every process in the unit, run-scripts included (`man systemd.exec`). | host | LOW | State it; it is another argument for F-1's confinement and F-4's secret-provider channel.

## Top five

1. **F-12** — the passphrase escrow is synced to Dropbox ("up to date"); `dropbox exclude add` would remove nothing from the cloud; D-2 was decided without this. The account holds ciphertext + key + the old-archive dlist key. Delete-forever, audit the account, re-decide D-2.
2. **F-9 (+F-10)** — the scrub misses `/var/log/syslog*` (99 key-bearing lines, 12 passphrase lines, adm-readable for ~10 more days) plus a list of uninventoried sinks; "local readers" means every pcalnon process.
3. **F-7 + F-8** — ordering: the duplicati-owned install directory (entry replacement → trojaned CLI run by root/pcalnon), the world-writable `bin/` and data folder, and a root timer executing a pcalnon-writable checkout script daily are all left to P2 while P0 starts and uses the tools.
4. **F-1 + F-3 (+F-2)** — `CAP_DAC_READ_SEARCH` with unrestricted network and no syscall filter turns "duplicati uid" and "UI credential holder" into host-wide readers with an exfiltration channel; D-4 states no trade-off; the UI credential lives in pcalnon space (today world-readable).
5. **F-6 (+F-4)** — the group model grants write/delete on all ciphertext to every pcalnon process and to Dropbox, beyond R-8's read-only ask, so cloud/ransomware deletions propagate into the local master; and the guard receives `DUPLICATI__passphrase` plus the settings key in its environment.

## Open items and the observation that settles each

- **O-1** Does the 09-20 snapshot's Option table carry `startup-delay`? Option-name print from `util/ad-hoc/2026-09-21_duplicati_server_db_forensics.py` on the copy. Decides whether Procedure A's probe can trigger a live run.
- **O-2** Can `duplicati-server-util --server-datafolder=<copy>` obtain a token as the server's uid *without* `--password`? Run it against the P0 probe instance. Decides whether "same uid = UI access" needs no environ read.
- **O-3** Does Duplicati 2.4.0.0 run under `SystemCallFilter=@system-service`, `IPAddressDeny=any`, `ProtectProc=invisible`, `PrivateDevices=yes`? `systemd-run -p … --uid=duplicati /usr/bin/duplicati-server …` against the probe copy, then `systemd-analyze security`. Decides F-1/F-2's fix list.
- **O-4** Do Claude transcripts hold the journal lines? `grep -rlc 'Exporting Environment Variable' ~/.claude/projects/-home-pcalnon-Development-python-Juniper-juniper-ml/` (counts only). Decides whether a cloud-linked sink exists.
- **O-5** CUPS job-file retention for the escrow-sheet print jobs: `grep -i PreserveJobFiles /etc/cups/cupsd.conf`; `sudo ls -la /var/spool/cups`.
- **O-6** Does the primary checkout `.env` (0664) hold `PASSPHRASE=` lines? `util/ad-hoc/duplicati_api.py:58-70` says it "carries both a new archive passphrase and the UI password"; the owner runs `grep -c '^PASSPHRASE=' …/.env`. If yes, S-3 has been world-readable since 2026-08-25, not "group duplicati/adm".
- **O-7** Dropbox account: 2FA on? which devices/apps are linked? which plan (30- or 180-day retention)? The dropbox.com Security page. Sets F-12's severity.
- **O-8** AES Crypt v2 KDF (8192 × SHA-256, password UTF-16LE) — confirm from SharpAESCrypt `GenerateKey` (my fetches of guessed source paths returned 404). Only matters if PASSPHRASE is not random.
- **O-9** Root's `.viminfo` / `.bash_history` and the unit swap: root-only count greps for `SETTINGS_ENCRYPTION_KEY=`.
- **O-10** The two live duplicati shells' in-memory history is unobservable until exit; the instruction is `history -c; unset HISTFILE` before closing them.
- **O-11** Does RunScript's `SendStdOutToLogs` store the guard's stderr (which carries the attacker-chosen stray-file *name*) verbatim in the server DB/log? Read `RunScript.cs` `SendStdOutToLogs`. A log-injection nuisance only.

Nothing was written into the repository; the only scratch artefacts are under `/tmp/claude-1000/…/scratchpad/valB3/` (help-text captures, the extracted unit, the security-analysis output, and a copy of the archived owner prompt used for a count-only scan).

<!-- markdownlint-enable -->
