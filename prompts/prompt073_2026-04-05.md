# Thread Handoff

Continue Phase 1 release-critical quality fixes for juniper-canopy v0.4.0.

## Completed so far

### Phase 0 (7 tasks) — DONE

committed on fix/pre-release-security-audit, pushed to origin

- Path traversal, timing attack, exception info leak, rate limiter memory leak, CallbackContextAdapter contextvars, Event race, FSM locking
- 4,233 passed, 0 failed

### Phase 1 (15 tasks) — CODE COMPLETE but NOT COMMITTED

All changes were verified (4,237 passed, 0 failed) but were lost due to a branch switching issue. They need to be re-applied on branch fix/release-critical-quality.

## Remaining work

Re-apply all 15 Phase 1 fixes on fix/release-critical-quality branch. The changes are well-defined:

### Step 1.1 — API/WebSocket (7 tasks)

1. src/main.py ~1892: /ws endpoint — remove inner try/except sleep loop, catch WebSocketDisconnect + generic Exception separately
2. src/communication/websocket_manager.py ~236: Add max_connections check before accept() in connect()
3. src/communication/websocket_manager.py ~332: Copy message dict before adding timestamp: message = {**message, "timestamp": ...}
4. src/main.py ~2044: Remove duplicate "cn_patience" from cn_keys list
5. src/main.py ~1996: Add SetParamsRequest Pydantic model, change api_set_params(params: dict) to api_set_params(body: SetParamsRequest), add params = body.model_dump(exclude_none=True). Note: cn_training_complete must be str, not bool
6. src/middleware.py ~75: Wrap int(content_length) in try/except (ValueError, OverflowError), return 400
7. src/main.py ~247: Change CORS allow_methods=["*"] to ["GET", "POST", "DELETE", "OPTIONS"], allow_headers to ["X-API-Key", "Content-Type", "Accept"]

### Step 1.2 — Configuration (5 tasks)

8. src/main.py: Add import importlib.metadata, define APP_VERSION from metadata, replace two version="0.3.0" with APP_VERSION
9. conf/app_config.yaml line 44: version: 0.1.0 → 0.4.0
10. pyproject.toml line 7: # Version: 0.3.0 → 0.4.0
11. src/security.py get_rate_limiter(): Replace os.environ.get() with from settings import get_settings; _settings.rate_limit_*
12. conf/app_config.yaml ~360: Fix CORS allowed_origins from > block scalar to proper YAML list

### Step 1.3 — Frontend (3 tasks)

13. src/frontend/dashboard_manager.py: Add self._api_base_url = f"<http://127.0.0.1:{self._settings.server.port}>" in __init__, change _api_url() to use self._api_base_url instead of Flask request.scheme/request.host
14. src/frontend/components/network_visualizer.py ~222: Remove "filename" from toImageButtonOptions
15. src/frontend/components/metrics_panel.py ~1710-1745: Replace inline phase band logic with self._add_phase_bg_bands()

### Test updates needed (already verified)

- test_callback_context_coverage.py: 3 assertions using adapter._test_trigger/_test_mode → public API
- test_error_response.py: "kaboom" in body["detail"] → body["detail"] == "An unexpected error occurred."
- Multiple _api_url tests: Remove Flask request context, assert <http://127.0.0.1>: instead
- test_main_endpoints_coverage.py: version == "0.3.0" → main.APP_VERSION
- test_security.py + test_rate_limit_default.py: Mock settings.get_settings instead of env vars
- test_main_api_coverage.py: Pass SetParamsRequest(...) instead of raw dicts
- test_main_coverage.py: Invalid type returns 422 not 500
- test_network_visualizer_coverage.py: Assert "filename" not in config

## Key context

- Branch fix/release-critical-quality exists, branched from fix/pre-release-security-audit
- Use conda activate JuniperCanopy / Python at /opt/miniforge3/envs/JuniperCanopy/bin/python
- Run tests: cd /home/pcalnon/Development/python/Juniper/juniper-canopy && /opt/miniforge3/envs/JuniperCanopy/bin/python -m pytest src/tests/ --tb=short -q
- CRITICAL: After git checkout fix/release-critical-quality, verify with git branch --show-current BEFORE making any edits. The working directory tends to get switched by other processes.

## Git status

- fix/pre-release-security-audit: Clean, pushed to origin, Phase 0 complete
- fix/release-critical-quality: Clean, exists locally, needs Phase 1 changes applied
- cursor/codebase-documentation-alignment-a0e4: Has unrelated doc work, may have some Phase 1 artifacts

## Verification

git -C /home/pcalnon/Development/python/Juniper/juniper-canopy checkout fix/release-critical-quality
git -C /home/pcalnon/Development/python/Juniper/juniper-canopy branch --show-current

---
