# Thread Handoff: Hardcoded Values Refactor — Begin Execution Wave 1

**Handoff Date**: 2026-04-09
**From Thread**: Juniper Project hardcoded values audit and planning (planning-only thread)
**Worktree**: `binary-bubbling-mochi`
**Branch**: `main` (planning thread did not create a working branch)

---

Begin working on **Execution Wave 1** of the Juniper Project hardcoded values refactor. The complete plan is documented in `juniper-ml/notes/HARDCODED_VALUES_REFACTOR_ROADMAP.md`. Wave 1 focuses on **creating constants infrastructure** across all 8 Juniper repositories. **No application logic changes in this wave** — only new constants files/modules and configuration scaffolding that hold the exact same literal values currently hardcoded in the source.

---

## Prior Thread Completed

A planning-only thread performed a comprehensive code review and audit of all 8 Juniper repositories for hardcoded values and produced the following deliverables (no source code was modified):

**Per-repository documents** (in each app's `notes/` directory):

| Repository            | Analysis Doc                         | Plan Doc                                  |
|-----------------------|--------------------------------------|-------------------------------------------|
| juniper-cascor        | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-data          | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-data-client   | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-cascor-client | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-cascor-worker | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-canopy        | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-deploy        | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |
| juniper-ml            | `notes/HARDCODED_VALUES_ANALYSIS.md` | `notes/HARDCODED_VALUES_REFACTOR_PLAN.md` |

**Consolidated roadmap**: `juniper-ml/notes/HARDCODED_VALUES_REFACTOR_ROADMAP.md` — defines 6 execution waves, scope summary, dependency graph, cross-repo coordination points, risk register, and success criteria.

**Audit totals**: ~624 hardcoded values identified across all 8 repos; ~120 already covered by existing constants; **~504 remain to extract**. Wave 1 creates the infrastructure to hold these constants.

---

## Wave 1 Tasks (from Roadmap Section "Execution Wave 1")

| #    | Repository            | Task                                                                  | New Files | Constants |
|------|-----------------------|-----------------------------------------------------------------------|-----------|-----------|
| 1.1  | juniper-cascor        | Create `cascor_constants/constants_api/` module                       | 2         | 43        |
| 1.2  | juniper-cascor        | Extend existing constants modules                                     | 0         | 5         |
| 1.3  | juniper-data          | Create `api/constants.py`                                             | 1         | 25        |
| 1.4  | juniper-data          | Create `storage/constants.py`                                         | 1         | 10        |
| 1.5  | juniper-data          | Create `core/constants.py`                                            | 1         | 6         |
| 1.6  | juniper-data          | Create per-generator `defaults.py` (7 generators)                     | 7         | 51        |
| 1.7  | juniper-data-client   | Create `constants.py`                                                 | 1         | 60        |
| 1.8  | juniper-cascor-client | Create `constants.py` + `testing/constants.py`                        | 2         | 95        |
| 1.9  | juniper-cascor-worker | Create `constants.py`                                                 | 1         | 30        |
| 1.10 | juniper-canopy        | Add `SecurityConstants` + `BackendConstants` to `canopy_constants.py` | 0         | 27        |
| 1.11 | juniper-canopy        | Extend `DashboardConstants` + `ServerConstants`                       | 0         | 8         |
| 1.12 | juniper-deploy        | Expand `.env.example` with new variables                              | 0         | 20        |
| 1.13 | juniper-deploy        | Create `scripts/config.sh`                                            | 1         | 8         |
| 1.14 | juniper-deploy        | Add Compose extension fields (`x-healthcheck-*`)                      | 0         | 4         |

**Note**: juniper-ml has no Wave 1 task (utility script changes are in Wave 3).

**Total**: 17 new files, ~392 constants defined.

---

## Acceptance Criteria for Wave 1

- All new constants files exist in the locations specified in the analysis docs
- Every constant holds the **exact same literal value** as the corresponding hardcoded value in the source (verified line-by-line against the analysis docs)
- Naming conventions match existing patterns per repo:
  - juniper-cascor: `_PROJECT_*` prefix consistent with `constants_model`
  - juniper-canopy: `typing.Final` annotations consistent with existing classes
  - Other repos: standard `UPPER_SNAKE_CASE`
- All files importable (no syntax errors, no import cycles)
- All existing test suites still pass (no behavioral changes expected since no application code is modified)
- All pre-commit hooks pass
- **No application logic files are touched** — that is Wave 2

---

## Recommended Execution Strategy

1. **Create one worktree per repo** following the standard Juniper worktree procedure (`util/worktree_new.bash`). Each repo's Wave 1 work is independent and can be parallelized across worktrees.
2. **Branch naming**: `feature/hardcoded-values-wave1` in each repo
3. **Order of execution** (highest impact first):
   - juniper-cascor-client (Task 1.8 — 95 constants, most needed since no existing infrastructure)
   - juniper-data-client (Task 1.7 — 60 constants, no existing infrastructure)
   - juniper-cascor (Tasks 1.1, 1.2 — extends mature pattern)
   - juniper-data (Tasks 1.3-1.6 — multiple new files)
   - juniper-cascor-worker (Task 1.9)
   - juniper-canopy (Tasks 1.10, 1.11 — extends existing classes)
   - juniper-deploy (Tasks 1.12-1.14 — config-only, not Python)
4. **Read the per-repo analysis doc first** before creating constants — it contains the exact line numbers and proposed constant names.
5. **Read the per-repo plan doc** for the recommended file structure and naming.

---

## Key Context from Prior Thread

- **This is a planning-validated effort.** All 17 deliverables (8 analyses + 8 plans + 1 roadmap) were validated for completeness, internal consistency, and arithmetic correctness. Three executive-summary inconsistencies were identified and corrected.
- **Approach A is the chosen remediation strategy** for most repos: extend existing constants infrastructure where it exists; create new modules where it doesn't. The plan docs document the rationale.
- **Generator math constants** (e.g., `2 * np.pi`) are intentionally NOT in scope — these are mathematical, not configuration.
- **HTTP status codes in juniper-data**: prefer `starlette.status` over custom constants (decision documented in juniper-data plan doc, Phase 3).
- **Cross-repo coordination**: Protocol message strings between `juniper-cascor` (server) and `juniper-cascor-worker` (client) must remain identical. Endpoint paths between `juniper-*-client` and corresponding servers must remain identical. This is verified in Wave 5.
- **No source code was modified by the planning thread** — current branch is `main` and clean.
- **Audit totals corrected**: The roadmap scope summary uses `~624 / ~120 / ~504`. juniper-canopy was corrected from `57/30+/27` to `51/30/21`.

---

## Verification Commands

```bash
# Verify worktree state
cd /home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/binary-bubbling-mochi
git status
git log --oneline -5

# Verify all 17 planning deliverables exist
for repo in juniper-cascor juniper-data juniper-data-client juniper-cascor-client juniper-cascor-worker juniper-canopy juniper-deploy juniper-ml; do
  ls /home/pcalnon/Development/python/Juniper/$repo/notes/HARDCODED_VALUES_ANALYSIS.md
  ls /home/pcalnon/Development/python/Juniper/$repo/notes/HARDCODED_VALUES_REFACTOR_PLAN.md
done
ls /home/pcalnon/Development/python/Juniper/juniper-ml/notes/HARDCODED_VALUES_REFACTOR_ROADMAP.md

# Read the roadmap before starting
cat /home/pcalnon/Development/python/Juniper/juniper-ml/notes/HARDCODED_VALUES_REFACTOR_ROADMAP.md
```

---

## Out of Scope for This Thread

- Wave 2 (Application Core Refactor) — only after Wave 1 is fully complete and committed
- Wave 3 (Testing & Support Code) — depends on Wave 2
- Wave 4 (Infrastructure & Deployment) — can parallel Wave 3
- Wave 5 (Validation & QA)
- Wave 6 (Documentation & Release)
- Modifying any application logic files
- Changing any literal values (constants must be byte-identical to current hardcoded values)

---

## When This Wave Is Complete

Wave 1 is complete when:

1. All 17 new files exist in the correct locations
2. All ~392 constants are defined with exact literal values matching the analysis docs
3. All test suites pass in all 8 repos
4. All pre-commit hooks pass
5. PRs are created for each repo (one per repo, on `feature/hardcoded-values-wave1` branch)
6. The roadmap document is updated to mark Wave 1 tasks complete

Then create a follow-up handoff prompt to begin Wave 2.
