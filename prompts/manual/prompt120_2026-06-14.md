# Handoff Prompt: juniper-model-core

Design the juniper-model-core shared interface package (the WS-3 workstream).

## Overarching Goal

Design first (Paul prefers design discussion before code — lead with a proposal, not a scaffold), then scaffold.
juniper-model-core is the shared model-abstraction package that lives as a juniper-ml SUBDIRECTORY (juniper-ml/juniper-model-core/, alongside juniper-observability/, juniper-ci-tools/, etc.).
"-core" = genuinely-shared abstraction (D4 ratified).

## Naming Conventions

TrainableModel + GrowableModel ABCs; the TrainingEvent vocabulary (training_start/end, epoch_end, unit_added, phase_change) with the cascor event mapping; ModelSerializer interface; the describe_topology() schema (the model-agnostic seam canopy renders); and the reusable conformance test kit.

## Design Directives

Design against ≥2 implementers to avoid over-abstraction (RK-4):

1. cascor's CascadeCorrelationNetwork — a GrowableModel (grow_step / unit_added / freeze), today 2-D classification.
2. the recurrence LMU — TrainableModel ONLY (fixed-order, task_type="regression", describe_topology kind="memory").
    - It already exists at /home/pcalnon/Development/python/Juniper/juniper-recurrence/juniper-recurrence-model/ (juniper_recurrence_model.units.VariableStepLMUMemory).

Also guard RK-6: the conformance kit must drive a REGRESSION model through every generic route (no argmax / accuracy assumptions).

## Canonical Specifications

Canonical spec to build from (do not re-derive):

- notes/JUNIPER_MODEL_MIDDLEWARE_REFACTOR_DESIGN_AND_PLAN_2026-05-31.md §2.3 (juniper-model-core: the ABCs, TrainingEvent mapping, ModelSerializer, TrainingLifecycleBase, describe_topology() schema, conformance kit §3.3) + Part 4 risks (RK-4/RK-6) + Part 5 open questions (OQ-10/OQ-12).
- notes/JUNIPER_RECURRENCE_MODEL_DETAILED_DESIGN_2026-06-14.md §6.3 (how the LMU plugs in).
- notes/JUNIPER_CODE_ORGANIZATION_STRATEGY_2026-06-05.md + JUNIPER_PACKAGE_PLACEMENT_AND_RELOCATION_PLAN_2026-06-09.md (placement; mechanism = subclass + inject, NOT entry-point registry).

## Design and Implementation Constraints

**Constraints:**

- C1 (first-principles, inspectable — no black-box base classes)
- C2–C5;
- Python>=3.12
- line length 512
- pytest 80%+
- WS-0 (overall design ratification) is NOT yet ratified — this is pre-implementation design.

## Current Status

Status going in (all 2026-06-14):

- juniper-recurrence repo EXISTS (public, github.com/pcalnon/juniper-recurrence): juniper-recurrence-model package scaffolded (verified Δt-native LMU, CI green, deploy key set, publish infra ready — trusted publishers + GitHub envs done).
- juniper-model-core / juniper-service-core do NOT exist yet (no subdir, no design doc beyond the refactor doc §2.3).
- cascor-core → juniper-cascor-model relocation is a SEPARATE pending track (not this).
- Memory: project_juniper_recurrence_packaging_structure_2026-06-14, project_juniper_recurse_design_plan_2026-05-31, project_juniper_package_placement_plan_2026-06-09.

### Design and Implementation Steps

**Step 1:** Read refactor doc §2.3 + §3.3, then propose the juniper-model-core interface (ABC surface + describe_topology schema + conformance-kit shape) for review BEFORE writing code.

**Step 2:** Work in juniper-ml: start a fresh worktree/branch off updated main (PR #409 is merged).
Verify state: `gh repo view pcalnon/juniper-recurrence`; `ls juniper-ml/` (no juniper-model-core yet); read the two design docs above.

## Git status

- juniper-recurrence: main, clean, pushed; commits f94c254 (scaffold) + d2b5588 (CI); origin over SSH deploy key.
- juniper-ml (this session, worktree serene-splashing-meadow): PR #409 merged to main; nothing uncommitted that matters. The model-core work belongs on a fresh branch off main.

---
