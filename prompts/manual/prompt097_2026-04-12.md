# Thread Handoff — Phase B Implementation

Continue implementing Phase B (P6) — the P0 win: polling elimination.

## Completed so far

- Phase 0-cascor (PR P1, pcalnon/juniper-cascor#125): seq, replay, resume, coalescer — MERGED
- Phase A-SDK (PR P2, pcalnon/juniper-cascor-client#18): set_params with command_id — MERGED
- Phase B-pre-a cascor (PR P3, pcalnon/juniper-cascor#126): origin, per-IP, idle timeout — MERGED
- Phase B-pre-a canopy (PR P4, pcalnon/juniper-canopy#145): origin, per-IP, audit logger — MERGED
- Phase B implementation plan written: juniper-ml/notes/PHASE_B_IMPLEMENTATION_PLAN.md

## Remaining work

1. Create canopy worktree for phase-b-canopy-drain-wiring branch
2. Add two-flag settings (enable_browser_ws_bridge, disable_ws_bridge) to src/settings.py
3. Modify websocket_client.js: seq tracking, resume protocol, jitter-backoff, remove 10-attempt cap
4. Create ws_dash_bridge.js: bounded ring buffers, drain methods, handler registration
5. Modify dashboard_manager.py: new stores, replace old WS callbacks (lines 1490-1565), polling toggle on metrics/state/topology handlers
6. Add /api/ws_latency and /api/ws_browser_errors POST endpoints to main.py
7. Create connection indicator component
8. Demo mode parity (RISK-08)
9. Tests, commit, push, open PR P6

## Key context

- The detailed plan is in juniper-ml/notes/PHASE_B_IMPLEMENTATION_PLAN.md
- The canonical plan §S7 (lines 920-1128 of notes/interface_proposals/R5-01_canonical_development_plan.md) has full specs
- All 4 repos are clean with P1-P4 merged to main
- Existing worktrees from this session are in /home/pcalnon/Development/python/Juniper/worktrees/
- Conda env for canopy: JuniperCanopy
- dashboard_manager.py is ~2500 lines — the old WS globals are at lines 1490-1527, drain callbacks at 1532-1565, metrics handler at 2388-2422
- websocket_client.js has CascorWebSocket class with globals window.cascorWS and window.cascorControlWS
- Default: enable_browser_ws_bridge=False — behavior unchanged until flag is flipped (P7)

## Verification commands

cd /home/pcalnon/Development/python/Juniper/juniper-canopy
git fetch origin && git pull origin main
git log --oneline -5  # should show P4 merge

```bash
# After implementation:
cd src && conda activate JuniperCanopy
pytest tests/unit/ -m "unit and not slow" --timeout=30 -q
```

## Git state

- juniper-cascor main: has P1 + P3 merged
- juniper-cascor-client main: has P2 merged
- juniper-canopy main: has P4 merged
- juniper-ml main: has notes/PHASE_B_IMPLEMENTATION_PLAN.md (uncommitted)

---
