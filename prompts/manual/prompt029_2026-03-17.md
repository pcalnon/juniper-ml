# Thread Handoff

Continue fixing parallel candidate training in juniper-cascor — completing Phase 3 testing and plan document update.

## Completed so far

### Phase 1 (RC-1: PyTorch thread contention) — DONE, all tests pass

- Added torch.set_num_threads() + OMP/MKL/OPENBLAS env vars to _worker_loop() entry point in src/cascade_correlation/cascade_correlation.py
- Set BLAS env vars early in src/main.py (before any torch/numpy import)
- Added configurable _PROJECT_MODEL_WORKER_THREAD_COUNT = 1 constant in src/cascor_constants/constants_model/constants_model.py and aliased in src/cascor_constants/constants.py
- Added worker_thread_count parameter to CascadeCorrelationConfig in src/cascade_correlation/cascade_correlation_config/cascade_correlation_config.py
- Set parent process thread count in _init_multiprocessing()
- _worker_loop() signature updated with worker_thread_count param; Process args updated to pass it

### Phase 2 (RC-2+RC-3: Manager queue bottleneck + redundant data) — DONE, all tests pass

- Replaced BaseManager-proxied queues with direct self._mp_ctx.Queue() in _execute_parallel_training()
- Commented out _start_manager() / _stop_manager() calls in the parallel training path
- Added shared_training_inputs param to _worker_loop() — shared tensors passed once instead of N times
- Worker reconstructs full task tuple from lightweight (index, candidate_data) + shared data
- Original code commented out (not deleted) per user instruction

### Phase 3 (RC-4: Persistent worker pool) — CODE DONE, TESTS NOT YET RUN

- Added _persistent_workers, _persistent_task_queue, _persistent_result_queue, _persistent_pool_size to _init_multiprocessing()
- Added _ensure_worker_pool() method — lazy-creates or reuses pool
- Added _shutdown_worker_pool() method — sentinel-based shutdown with graceful/terminate/SIGKILL phases
- Refactored _execute_parallel_training() to use _ensure_worker_pool() instead of per-round spawn/terminate
- Added result queue size check in wait loop for early exit
- Excluded persistent pool attributes from __getstate__ (pickle safety)
- NOTE: RC-3 shared_training_inputs optimization deferred in persistent pool mode (training data changes each round); full tasks sent through direct queue

## Remaining work

1. Run unit tests to verify Phase 3: cd src/tests && conda run -n JuniperCascor bash scripts/run_tests.bash -u
2. Fix any test failures from Phase 3 persistent pool changes
3. Update plan document (notes/PARALLEL_CANDIDATE_TRAINING_FIX_PLAN.md) with Phase 3 results and final status
4. Optionally: commit changes (all work is on main branch, uncommitted)

## Key context

- User explicitly requested: comment out original lines rather than deleting; provide meaningful comments for new/modified lines
- All changes tagged with PARALLEL-FIX (RC-N) comments identifying which root cause they address
- Plan document at notes/PARALLEL_CANDIDATE_TRAINING_FIX_PLAN.md tracks all changes in a change log table
- The _start_manager() and _stop_manager() methods are retained (not deleted) for backward compatibility but are no longer called in the parallel training path
- CandidateTrainingManager class and its registrations are also retained
- Working directory: /home/pcalnon/Development/python/Juniper/juniper-cascor on branch main
- Conda env: JuniperCascor

## Verification commands

cd /home/pcalnon/Development/python/Juniper/juniper-cascor/src/tests && conda run -n JuniperCascor bash scripts/run_tests.bash -u
git status  # 7 modified files + 1 new file (plan doc)

---
