# Thread Handoff — Canopy Network Topology Display Fix (Continued)

Continue implementing the network topology display fix plan for juniper-canopy. Phases 1-3 are complete and committed. Phase 4 (weight-centric topology toggle) and Phase 5 (quality improvements) remain.

Completed so far

- Phase 1 (OI-1, OI-4): Fixed all 8 store handler error paths in dashboard_manager.py to return dash.no_update instead of {} / None, preserving last known good display state on transient API failures. Added warning-level logging to the silent except in extract_network_topology(). Committed:
b4131ab.
- Phase 2 (OI-3): Added hidden-to-hidden cascade connections to demo_backend.py:get_network_topology(), matching the service adapter's transform_topology() logic. Added 2 cascade connection tests. Committed: b4131ab (same commit as Phase 1).
- Phase 3 (OI-2): Wired WebSocket topology push into Dash by adding ws-topology-buffer dcc.Store, extending the existing WS onmessage clientside callback to capture type === "topology" messages, adding a drain clientside callback, and modifying update_topology_store to accept WebSocket input
 with priority over REST polling. NOT YET COMMITTED — changes are unstaged in juniper-canopy working tree.
- Analysis document: notes/NETWORK_TOPOLOGY_DISPLAY_ANALYSIS_AND_FIXES.md written, validated (20/20 code refs correct, all fixes validated), committed and pushed to juniper-ml branch worktree-radiant-inventing-boot at 8d3a5db.

Remaining work

1. Commit Phase 3 changes in juniper-canopy and push
2. Phase 4 (OF-1): Weight-centric topology view toggle (~4-6 hours, juniper-canopy only)
    - Add get_raw_topology() to cascor_service_adapter.py (skips _transform_topology())
    - Add /api/topology/raw endpoint in main.py
    - Add network-visualizer-raw-topology-store + lazy poll callback in dashboard_manager.py
    - Add Node Graph / Weight Matrix toggle in network_visualizer.py:get_layout()
    - Implement _create_weight_heatmap() in network_visualizer.py
    - Modify update_network_graph callback to switch views
    - Unit tests for heatmap rendering
    - CasCor already returns raw weight-oriented format — no CasCor changes needed
3. Phase 5 (OI-5, OI-6): Quality improvements (~1 hour)
    - Push initial synced topology to Dash store
    - Broaden exception handling in get_decision_boundary

Key context

- The analysis document at notes/NETWORK_TOPOLOGY_DISPLAY_ANALYSIS_AND_FIXES.md has the full issue descriptions, fix approaches with code, and validation results — read it first
- _transform_topology() is at cascor_service_adapter.py:583-654 — converts CasCor's weight-oriented {input_size, output_size, hidden_units: [{weights, bias}], output_weights} to graph-oriented {input_units, output_units, hidden_units: int, nodes, connections}
- CasCor's /v1/network/topology already returns raw weight-oriented format — the graph transformation is Canopy-side only
- NetworkVisualizer already supports 2D/3D views with layout selector dropdown and show-weights checkbox — the weight toggle should follow the same control pattern
- Plotly go.Heatmap and make_subplots are both available in canopy deps
- Pre-existing dirty files in juniper-canopy working tree: conf/layouts/metrics_layouts.json, pyproject.toml, several test files from Sprint 6 work — do not commit those
- Full unit suite: 3201 passed, 0 failed as of Phase 3

Git status

- juniper-canopy: branch main, Phase 3 changes unstaged in src/frontend/dashboard_manager.py
- juniper-ml: branch worktree-radiant-inventing-boot (worktree at /home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/radiant-inventing-boot), clean

Verification

```bash
# Canopy tests
cd /home/pcalnon/Development/python/Juniper/juniper-canopy/src && conda run -n JuniperPython pytest tests/unit/ -q --timeout=30

# Full service launch
${HOME}/Development/python/Juniper/juniper-ml/util/launch_full_monty.bash

# Dashboard topology tab
# http://localhost:8050/dashboard → Network Topology tab
```
