# Juniper Project:  Application regressions

The juniper project has seen critical regressions introduced in the following applications:

- juniper-canopy
- juniper-cascor

## Juniper Project

The juniper project is a research platform for investigating dynamic neural networks and novel learning algorithms.
The juniper project employs a microservices architecture that can be run as host-level services or as docker compose/kubernets managed containers.
The juniper project exposes a REST API with explicitely defined contracts and utilzes a websocket framework for near-realtime communications.
The juniper project is designed for high performance and is architectured for distributed processing using remote worker clients.
The juniper project implements concurent programming features using python's multiprocessing module with forkserver and advanced, interactive queueing.

## Role

You are an experienced, principal software engineer working as the lead developer on the Juniper project.
You specialize in debugging complex, multi-faceted problems in unique and challenging projects.
You are careful, delibrate and methodical in your approach, but you creative, adaptable, and intuitive in your analysis.
Your conclusions are always logical, rigorous, and supported by extensive evidence and detailed analysis.

## Resources

Utilize all available resources in the performance of tasks including, but not limited to, the following:

- The AGENTS.md files in the affected applications' root directories.
- The contents of the notes/ directories for the affected applications including:
  - Analysis documents
  - Development Roadmap documents
  - Planning documents
  - Audit Result documents
- The project documentation files in the docs/ directories of the affected applications.
- The documented procedures located in the notes/ directories.
- The attached MCP servers and their corresponding tools, including but not limited to, the following:
  - The Serena MCP server
  - The chrome-devtools MCP server
- The locally installed and avilable utilities and toolsets.
- The externally available resources, including web resources available online via browser.

## Background

The following juniper project applications have experienced significant regressions:

- juniper-canopy
- juniper-cascor

These regressions have caused the following, critical issues:

- a running juniper-cascor instance fails shortly after beginning the first epoch.
- when monitoring a running juniper-cascor instance with the juniper-canopy application:
  - Training progress stops without errors being generated
  - The top tab menu has tabs in an incorrect order, which should be:
    - Training Metrics
    - Candidate Metrics
    - Network Topology
    - Decision Boundary
    - Dataset View
    - Workers
    - Parameters
    - HDF5 Snapshots
    - Redis
    - Cassandra
    - Tutorial
    - About
- The canopy application top status bar:
  - the Top status bar element: Status, shows "Failed"
- The canopy application Training Metrics Tab:
  - Candidate Epoch progress bar stalls
  - Left menu, Network parameters section, Neural Network sub-section values all appear to be incorrect based on defaults from consants class
  - Left menu, Network parameters section, Neural Growth Triggers sub-section, Convergence Threshold value is incorrect and appears to be filled with the Number of Epochs field value
  - Left menu, Network Information section, fields are incorrect (all zeros)
    - Input Nodes
    - Output Nodes
  - Left menu, Network Information section, Network Information Details sub-section, all fields (except for Threshold Function and Optimizer) are incorrect (all zeros)
  - Training Metrics tab, Training loss over time graph display is incorrect
  - Training Metrics tab, Training accuracy over Time graph is empty
  - Training Metrics tab, Learning rate field in the graph section heading status area does not display the same value as the Left Menu, Network Parameters section, Neural Network sub-section, Learning Rate field.
- The canopy application Candidate Metrics Tab:
  - Candidate Epoch Progress bar is stalled
  - The Pool Details section does not show any information for top candidates
  - The Pool Training Metrics section has invalid values for all fields (all zeros)
  - The Candidate Training Loss graph is not populated
  - the Candidate Training loss graph has a white background with dark text when in dark mode
- The canopy application Network Topology Tab:
  - the displayed network graph includes two output nodes even though network topology requirements and settings specify one output node
- The canopy application Decision Boundary Tab:
  - The Decision boundary display area should have an appropriate height to display the Decision boundary visualziation with its native aspect ratio rather than stretching the image width without changeing the image height
  - The Decision boundary visualization should have the ability to display visualization for each previous hidden node addition, as well as for the current number of hidden nodes.
  - The boundary decision evolution should be able to be "replayed" from its initial state to its current state based on the current number of hidden nodes
- The canopy application Dataset View Tab:
  - The Dataset Visualization display area should have an appropriate height to display the Dataset Scatter Plot visualziation with its native aspect ratio rather than stretching the image width without changeing the image height
  - The Dataset dropdown field in the Dataset Visualization top menu should be populated with all available dataset genrators from the juniper-data application
  - The dataset dropdown field in the Dataset visualization top menu should be pre-populated with the current dataset being used.
  - The left menu Dataset parameters section, spiral dataset sub-section should:
    - have a sub-section heading that is not associated with a specific dataset: Current Dataset
    - should have the fields appropriate for the currently selected dataset
    - should change the available fields as appropriate for a change in dataset selection from the Dataset View tab, Dataset Visualization top menu Dataset dropdown
  - Clicking the Generate Dataset button, after a new dataset has been selected from the dropdown and configured in the left menu Dataset parameters section, should cause the following:
    - stop training
    - display the new dataset in the Dataset Scatter Plot graph
    - update the Feature Distributions section as appropriate
    - if the current neural network is compatible with the new dataset, training should be able to be started using the new dataset
    - if the current neural network is NOT compatible with the new dataset, training should not attempt to restart
      - the user should be prompted to fix the new dataset for neural network compatibility
      - or to begin new training with a new, compatible neural network, for the new dataset
- The canopy application's HDF5 Snapshots tab:
  - HDF5 Snapshots tab should be renamed, "Snapshots"
  - HDF5 Snapshots tab should be renamed, "Snapshots"
  - The Refresh button and corresponding status message (e.g., No snapshots available) should be moved to Available snapshots section heading
- The canopy application's Cassandra tab:
  - An error message is currently displayed indicating an api url problem, this should be fixed
- The canopy application's Parameters tab:
  - all sections of the Meta Parameterstables still show a white background in dark mode.
- The canopy application's Tutorials tab:
  - The Dashboard Components topic, Dashboard Tabs section table still shows a white background in dark mode
  - The Parameters Reference topic, Key Paremters section table still shows a white background in dark mode

## Objectives

1. Document and address all identified issues with the juniper-canopy and juniper-cascor applications.
2. Addresss, with the highest possible priority, the critical, training failure problem.
    - This issue is catastrophic and blocks all current releases, deployments, and future development.

## Directives

Utilizing appropriately specialized sub-agents, perform the following planning tasks:

- Analyses
  - Investigate and identify the specific problems occurring in the Juniper Project.
  - Perform an in-depth analysis of the identified issues and the Juniper Project codebase.
  - Identify the root causes of all identified issues.
  - Validate all aspects of the analysis performed, and make any changes or updates necessary.
  - Write all analysis into an appropriately named markdown file in the notes/ directory.
- Remediations
  - Based on the analyses performed and the planning carried out, address all identified root causes by developing the following:
    - fixes
    - changes
    - updates
    - enhancements
  - Develop these detailed and specific code changes or additions needed to meet all stated and implied requirements.
    - When applicable, present multiple approaches with corresponding fixes for each root cause addressed.
    - Include detailed analyses of the strengths, weakness, risks, and guardrails of all fixes and approaches.
    - Provide recommended approaches based on careful weighting of the analyses performed.
  - Validate all aspects of the work performed and fixes developed.
  - Write all fix development and related analyses to an appropriately named markdown file in the notes/ directory.

## Constraints

Thread compactions must be avoided at all costs due to extreemly poor preservation of thread context's state and status.
Instead, when thread context utilization exceeds 80%, perform the following steps:

- Pause current work
- Create a Thread Handoff prompt in accordance with the documented thread handoff procedure
- Write the prompt to an appropriately named and timestampped, markdown file in the juniper-ml/prompts/thread-handoff_automated-prompts directory
- Call the juniper-ml/scripts/wake_the_claude.bash script passing in the thread handoff prompt's filename and path
- Include the following additional parameters in the call to wake_the_claude.bash
  - include the current worktree name as a parameter with the "--worktree" switch
  - include the "--effort" switch with the "high" parameter
  - include the "--dangerously-skip-permissions" flag
- Cease operation once the wake_the_claude.md script has been called successfully

## Deliverables

As applicable, generate the following documents in markdown format, in the application's notes/ directory.

- a rigorous and comprehensive analysis document addressing specified directives and the Juniper Project's codebase.
- a planning document outlining the phases, steps, and tasks needed to complete the prompt's Objectives and Directives.
- a development roadmap to document all required work as detailed and prioritized phases, steps, and tasks.

## Finalize

1. Using appropriately specialized sub-agents, validate all aspects of the work performed for this prompt.
2. Make any changes needed as a result of this validation process to the following:
    - Documents
      - Analysis documents
      - Plan documents
      - Development roadmaps
      - User Documentation files
    - Code
      - Codebase contents
      - Testing files
      - Automation scripts
    - Infrastructure
      - Testing
      - Monitoring
      - Configuration
      - Automation
      - Deployment
      - CI/CD
3. Ensure that the full test suites pass for all affected Juniper Project applications.
4. Commit all changes and push the working branches to the applicable, github repositories.
5. Create pull requests for the applicable changes to the affected Applications.
6. When pull requests have been successfully merged, perform the worktree cleanup v2 procedure, as documented.

---
