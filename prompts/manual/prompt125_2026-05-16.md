# Handoff prompt

Continue building the juniper-recurrence WS-4b FastAPI/CLI app, per the ratified plan notes/JUNIPER_RECURRENCE_WS4B_APP_BUILD_PLAN_2026-06-15.md (juniper-ml main).

## Completed

WS-4a model shipped + published juniper-recurrence-model 0.1.0 (PyPI + GitHub release); §9.1c loader (load_sequence_npz) in 0.1.0; validated end-to-end on irregular_sine (test R² 0.988, Δt-win confirmed).
Repo juniper-recurrence @ main 58e63a3, clean (only main).

## Remaining

**Remaining work — 3 PRs:**

1. (worktree off juniper-recurrence main): PR-1 skeleton
    - new juniper_recurrence/ app dist (D-WS4b-1):
      - pyproject (editable sibling deps),
      - settings.py (Settings
        - (SettingsBase),
        - prefix JUNIPER_RECURRENCE_,
        - port 8210,
        - _FILE secrets,
        - no env_file=)
      - build_app() → create_app(routers=…) + attach SecurityHeaders/RequestBodyLimit/SecurityMiddleware,
      - main.py serve
      - ci-recurrence-app.yml
        - (gh workflow run it)
2. PR-2
    - state.py/events.py/data.py (data-client→3-D NPZ) + routers train/predict/model/dataset + synchronous TrainingLifecycle(LMURegressor(**hp), on_event=sink).run(...)
    - (D-WS4b-2; no FSM/threads) + train CLI + §12 tests.
3. PR-3
    - publish-recurrence-app.yml + docs
    - align publish-recurrence-model.yml to --no-deps no-fallback

## Key context

- as-built service-core 0.1.0 (juniper-ml juniper-service-core/, not yet on PyPI) exports:
  - create_app(*,title,version,routers=()) (health-only)
  - SettingsBase
  - APIKeyAuth/RateLimiter/build_api_key_auth/build_rate_limiter
  - get_secret
  - the 3 middlewares
- TrainingLifecycle/EventCollector — build against the as-built API, not refactor-doc §2.3.
- Routes are regression-generic (RK-6: no argmax/accuracy)
- Publish-first: service-core must hit PyPI before the app's docker pin (recurrence-model already published)
- Dev env: editable sibling installs in JuniperCascor1 (numpy + model-core present)
- Standing rules:
  - gh pr list + gh run list --branch main before assuming a red PR is yours (heavy concurrent sessions)
  - no merge without explicit signal + gh pr view `<N>` MERGED.

## Verify start

Verify juniper cd Development/python/Juniper/juniper-ml; conda activate JuniperCascor1; l; lproject state and environment before starting

- verify all context assumptions before beginning work
- gh pr list -R pcalnon/juniper-recurrence (expect none)
- git -C juniper-recurrence log --oneline -1 (58e63a3)
- read the plan doc first

---


  Continue building the juniper-recurrence WS-4b FastAPI/CLI app, per the ratified plan
  notes/JUNIPER_RECURRENCE_WS4B_APP_BUILD_PLAN_2026-06-15.md (juniper-ml main).

  COMPLETED: WS-4a model shipped + published juniper-recurrence-model 0.1.0 (PyPI + GitHub
  release); §9.1c loader (load_sequence_npz/SequenceData) in 0.1.0; validated end-to-end on
  the irregular_sine dataset (test R² 0.988; shuffle-dt 0.898 confirms the Δt channel is used).
  Repo pcalnon/juniper-recurrence @ main 58e63a3, clean (only `main`; all feature branches merged+deleted).

  REMAINING — 3 PRs (worktree off juniper-recurrence main, per worktree procedures):
   • PR-1 skeleton — new juniper_recurrence/ app dist (D-WS4b-1): pyproject (editable sibling
     deps), settings.py (Settings(SettingsBase), env_prefix JUNIPER_RECURRENCE_, port 8210,
     _FILE secrets via get_secret, NO env_file= on Settings), app.py build_app()→create_app(routers=…)
     + attach SecurityHeadersMiddleware/RequestBodyLimitMiddleware/SecurityMiddleware,
     main.py `serve`, ci-recurrence-app.yml (gh workflow run it immediately — RK-10).
   • PR-2 routes — state.py/events.py/data.py (juniper-data-client → 3-D equities_seq NPZ →
     fit/predict kwargs) + routers train/predict/model/dataset + synchronous
     TrainingLifecycle(LMURegressor(**hp), on_event=sink).run(...) (D-WS4b-2; no FSM/threads/WS) +
     main.py `train` + the §12 test matrix.
   • PR-3 publish+docs — publish-recurrence-app.yml (tag juniper-recurrence-v*, --no-deps no-fallback
     TestPyPI verify), README/CHANGELOG; align publish-recurrence-model.yml to no-fallback too.

  KEY CONTEXT: Build on the AS-BUILT juniper-service-core 0.1.0 (juniper-ml juniper-service-core/,
  NOT yet on PyPI), exports: create_app(*,title,version,routers=()) [wires health only],
  SettingsBase, APIKeyAuth/RateLimiter/api_key_header/build_api_key_auth/build_rate_limiter,
  get_secret, {SecurityHeaders,RequestBodyLimit,Security}Middleware, TrainingLifecycle/EventCollector
  — use the as-built API, NOT refactor-doc §2.3. Routes regression-generic (RK-6: no argmax/accuracy).
  Publish-first: juniper-service-core must reach PyPI before the app's docker pin (recurrence-model
  already published); build against editable sibling installs until then; pin+lock in the same PR.
  Dev env JuniperCascor1 (numpy + juniper-model-core present). Port host 8211→ctr 8210.
  STANDING RULES: gh pr list + gh run list --branch main before assuming a red PR is yours
  (heavy concurrent-session activity on these repos); NO merge without my explicit per-PR signal
  + gh pr view <N> MERGED confirmation.

  VERIFY START: gh pr list -R pcalnon/juniper-recurrence (expect none);
  git -C <...>/juniper-recurrence log --oneline -1 (expect 58e63a3);
  read the WS-4b plan doc first, then read the as-built service-core modules
  (app/settings/security/middleware/secrets/health/lifecycle.py) before scaffolding PR-1.

  State is checkpointed for a clean restart: juniper-recurrence @ 58e63a3 (0.1.0 published + GitHub-released), no open PRs, all merged branches cleaned; memory updated with the WS-4b state. One loose note for whenever convenient: the irregular_sine validation script lives at
  juniper-ml/util/ad-hoc/verify_recurrence_on_irregular_sine.py (untracked — safe on disk, not /tmp; commit it later if you want it in the record).

