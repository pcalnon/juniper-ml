# Thread Handoff

Implement OPT-5 (Shared Memory Training Tensors) in juniper-cascor following the plan at Juniper/juniper-cascor/notes/OPT5_SHARED_MEMORY_PLAN.md. Read the full plan before starting.

## Completed so far

- OPT-6, OPT-4, OPT-2, OPT-1 all implemented and committed (latest: a854e65)
- OPT-5 plan written, validated by 4 sub-agents, critical bugs found and fixed in plan
- 2425 unit tests passing, 26 targeted performance tests passing
- Benchmarking confirmed: GET-side tensor reconstruction = 97% of queue overhead (~9ms/get cross-process); lightweight tasks reduce this to ~0.5ms/get

## Remaining work — implement in priority order (P0 through P7)

- P0: Add SharedTrainingMemory helper class (create/reconstruct/cleanup SharedMemory blocks with JNPR header format)
- P1: Modify _generate_candidate_tasks() to create SharedMemory block, fall back to tuple on failure
- P1b: Modify _execute_candidate_training() lines 1768-1769 to handle dict-type training_inputs
- P2: Modify _execute_parallel_training() — add SharedMemory cleanup in finally block
- P3: Modify _build_candidate_inputs() to detect dict vs tuple, reconstruct from SharedMemory, return shm_handle
- P3b: Modify _process_worker_task() to close shm_handle in finally after training
- P4: Add cleanup in _shutdown_worker_pool()
- P5: Add atexit handler and _active_shm_blocks tracking in __init__()
- P6: Check RestrictedUnpickler.ALLOWED_CLASSES for new types
- P7: Write tests in tests/performance/test_shared_memory.py (10 tests specified in plan)

## Key context

- Working directory: /home/pcalnon/Development/python/Juniper/juniper-cascor/
- Conda environment: JuniperCascor
- Core file: src/cascade_correlation/cascade_correlation.py (~3000+ lines)
- Start method: forkserver with preload (production default)
- Training tensors are READ-ONLY in all worker paths (validated) — safe for sharing
- Critical: shm.close() must be deferred until after training completes (zero-copy views reference the buffer)
- Critical: Python 3.12 resource tracker needs resource_tracker.unregister() in workers to prevent premature unlink
- Critical: Remote workers cannot access local /dev/shm — fall back to full tensor tasks for remote dispatch

## Uncommitted files in juniper-cascor

- notes/OPT5_SHARED_MEMORY_PLAN.md (new — the plan, 887 lines)
- notes/PERFORMANCE_TESTING_PLAN.md (modified — OPT-2/OPT-1 marked done)
- src/tests/performance/baselines/baseline_20260401.json (modified — baseline data)

## Verification commands

```bash
cd /home/pcalnon/Development/python/Juniper/juniper-cascor
git status --short
cat notes/OPT5_SHARED_MEMORY_PLAN.md | head -20
conda run -n JuniperCascor python -m pytest src/tests/ -m "unit and not slow" --timeout=60 --ignore=src/tests/performance -q --tb=no
```

---
