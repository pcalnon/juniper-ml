# OUT-12 — Build the juniper-cascor Golden / Snapshot Regression Suite (Thread Handoff)

**Date**: 2026-06-17
**Author**: Paul Calnon
**Type**: Thread-handoff prompt — continue OUT-12 implementation in a fresh thread
**Origin**: "juniper environment work" session (platform/template-cluster state reconciliation + roadmap)

---

## Continue

Implement the **juniper-cascor golden / snapshot regression suite** per the ratified build plan `notes/JUNIPER_CASCOR_GOLDEN_REGRESSION_SUITE_BUILD_PLAN_2026-06-17.md` (on `main`, shipped via juniper-ml#443). This is **OUT-12** in `notes/JUNIPER_PLATFORM_ENVIRONMENT_STATE_AND_ROADMAP_2026-06-17.md` and the **WS-6 trigger-gate**: WS-6 (refactoring cascor onto `juniper-service-core` / `juniper-model-core`) is accepted only if this suite + the model-core conformance kit stay green.

The suite lands in the **`juniper-cascor` repo** (`/home/pcalnon/Development/python/Juniper/juniper-cascor`). This is environment-dependent build work — it needs the `JuniperCascor1` conda env to actually train cascor and capture / calibrate the goldens.

## Completed so far (design done, build pending)

- Build plan written, sub-agent-scoped, and **merged** (juniper-ml#443).
- **OQ-13 resolved → tolerance-based comparison, NOT hashing.** Evidence: candidate training is multiprocessing-parallel (non-bit-stable float reduction); the resume-determinism test is `xfail`; every existing numeric assertion uses tolerances; BLAS pinned to 1 only under xdist.
- Dup-guard (2026-06-17): no in-flight cascor golden / WS-6 work; no open cascor PR touched this.

## Remaining work (the build)

1. Add a `golden` pytest marker to `juniper-cascor/pyproject.toml [tool.pytest.ini_options].markers`, plus a **serial** golden CI lane (NEVER xdist; GIL env only).
2. Write the deterministic capture fixture: `random_seed=42`, `CASCOR_NUM_PROCESSES=1` (forces the sequential candidate path), explicit BLAS=1 (`OMP/MKL/OPENBLAS/VECLIB/NUMEXPR_NUM_THREADS=1` + `torch.set_num_threads(1)`) — do not rely on the xdist-only block. Plus a **recursive volatile-field scrubber** helper.
3. Freeze the dataset: `SpiralDataGenerator.generate_2_spiral(n_per_spiral=30, noise=0.05, seed=42)` → `src/tests/fixtures/golden/two_spiral_seed42.npz` (the dir does not exist yet).
4. Implement the 3 test modules in **capture mode** first (write goldens, eyeball for sanity), then flip to **assert mode**:
   - `src/tests/integration/test_golden_trajectory.py` — fixed-seed two-spiral train; assert `network.history` float series within tolerance (`rtol=1e-3, atol=1e-4`); growth-count sequence + `_completion_reason` compared **exactly**.
   - `src/tests/integration/test_golden_serialization_roundtrip.py` — trained `predict` → `CascadeHDF5Serializer.save_network` / `load_network` → `predict` → `torch.allclose(atol=1e-6)`. Leave resume-training determinism `xfail`.
   - `src/tests/integration/api/test_golden_api_snapshots.py` — `create_app(Settings(auto_start=False))` under `TestClient` (reuse the daemon-thread `__exit__` fixture from `integration/api/test_api_full_lifecycle.py:45-72`); snapshot the fresh no-network routes, then create a network + tiny fixed-seed train and snapshot the post-train routes; scrub volatile fields.
5. Calibrate tolerances over 2–3 runs (and ideally the docker image); document chosen tolerances next to each fixture; record torch / numpy versions alongside the goldens.
6. `gh workflow run` the new lane immediately (lint-green ≠ run-green — a repeatedly-observed ecosystem failure mode).
7. PR into `juniper-cascor` (`test(golden): WS-6 pre-refactor regression baseline`), referencing the plan + OUT-12.

## Key context

- **Read the plan first.** §2–§5 carry the file layout, exact-vs-tolerance per signal, the API routes to snapshot + the full volatile-field scrub list (`meta.timestamp`, `git_sha`, `build_date`, `version`, `duration_ms`, `uptime_seconds`, `*_total` counters, `uuid`, `server_instance_id`, `snapshot_seq`, ...), and the CI-affordable config.
- **Capture goldens on cascor `origin/main` BEFORE any WS-6 refactor** — the baseline is only valid pre-refactor (insurance).
- Run on the **GIL env (`JuniperCascor1`)** — the free-threading guard aborts the lane under `Py_GIL_DISABLED`.
- Existing ~14 `*snapshot*` tests cover the HDF5 serializer **feature**, not trajectories / API; predict-after-load round-trip is partially covered (untrained network) — add the **trained** case.
- Anchors (verify before relying on them — heavy concurrent-session activity): trajectory surface = `network.history` (`src/cascade_correlation/cascade_correlation.py:741-746`); sequential-path trigger (`cascade_correlation.py:2336-2339`); two-spiral generator (`src/tests/unit/test_data/generators.py:42`); HDF5 serializer (`src/snapshots/snapshot_serializer.py`); BLAS-pin block (`src/tests/conftest.py:137-143`).
- Standing rules: Paul approves merges + PyPI / deploy gates (drive to the gate, then hand off). `gh pr list` before assuming a red PR is yours.

## Verify starting state

```bash
cd /home/pcalnon/Development/python/Juniper/juniper-cascor
git fetch origin && git log --oneline -3 origin/main      # confirm baseline
conda activate JuniperCascor1
sed -n '1,60p' ../juniper-ml/notes/JUNIPER_CASCOR_GOLDEN_REGRESSION_SUITE_BUILD_PLAN_2026-06-17.md
gh pr list -R pcalnon/juniper-cascor --state open          # dup-guard
```

## Git / branch state

- Plan + roadmap are on `juniper-ml` `main` (#435 roadmap, #443 plan, #440 / #441 doc-hygiene — all merged 2026-06-17).
- The implementation lands in **`juniper-cascor`** on a new feature branch / worktree. No work-in-progress exists for OUT-12 yet — this is a greenfield build against the plan.
