# Code change problem

## Current Status

PRs have been merged except for juniepr-canopy PR 141.
Specific portions of this work need to be reconsidered.

## Revert specific classes of changes

begin by reverting all changes affecting "dead" netsted metrics/network_topology keys.  this functionality, and the tradeoffs surounding its use, need to be considered carefully and analysed rigorously.
Restore any code removed as part of P5-RC-05, or other tasks, related to the frontend WebSocket consumption feature set.

## Requirements Clarification

this code is "genuinely missing, in the sense that it hasn't yet been completed.
canopy requirements include the use of the WebSocket set_params command to, at a minimum, return meta parameter and settings changes from the canopy frontend back to the running cascor instance to modify/update its state/training characteristics.
canopy set_params integration is work that STILL NEEDS TO BE DONE or hasn't been completed.
this is NOT unneeded functionality or dead code.
additionally, there are high volume and/or low-latency metrics and plots being displayed in canopy that need to use WebSockets for these plots, metrics, or statuses to be displayed properly.
if needed, the juniper-cascor-client needs to expose the missing functionality.
any additional changes that are needed to support or enable this websocket functionality need to be made as well
this work should not be deferred indefinitely or marked "won't do".  this is critical functionality needed to fully meet project requirements
any challenges related to verifying browser-side clientside_callback wiring, need to be documented, analysed, and addressed.

## Next steps

perform a deep dive into the juniper-canopy and juniper-cascor codebases.
perform a rigorous analysis of the state of the following feature sets and functionality:

- websocket code implementation, correctness, and performance.
- websocket code's bidirectional message contracts, contents, and performance expectations
- use of nested vs flat data structures used in messaging architecture
- develop a latency tolerance matrix for all editable data elements in the juniper canopy frontend based on UX effectiveness, meaningful metric streams, display correctness
- adjust code so that messaging between canopy <-> cascor uses messaging technology appropriate for that specific display element's latency tolerance in its UI context.

write this data, analysis, design, and status into a markdown file in the notes/ directory
apply a group of no fewer than 10 subagents to independantly evaluate, audit, and validate the generated design and analysis document.
integrate the sub-agent feedback as correct and meaningful.
when the updated document has been changed as appropriate, perform a second round of sub-agent validation.
continue this process until the document has stabelized.
continue this process for one round after the document has stablized.

## Minor, Unrelated Changes

the following Constants need to be updated:

- epochs_max should be set to 100000000000
- iterations_max shold be set to 1000000
- hidden nodes max should be set to 10000
