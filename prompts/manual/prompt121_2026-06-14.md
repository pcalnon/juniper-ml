# thread handoff

Just verified: juniper-observability 0.4.0 is live on PyPI (publish run success). Service PRs unblocked.

Done already: deploy #116/#117 (merged); design → juniper-ml #412 (notes/BUILD_PROVENANCE_DESIGN_2026-06-14.md, scope Maximal, 5 OQs all ratified yes); obs 0.4.0 → #414 merged + published; worker → cascor-worker #103 (open, the proven Dockerfile + /v1/health template); deploy wiring → #118 (open:
build.args ×7 + Makefile PROVENANCE_ENV + compose regression test).

Remaining:

1. 3 service PRs (data, cascor, canopy) per #412 §5.2, each: Dockerfile ARG GIT_SHA/BUILD_DATE/APP_VERSION → OCI revision/created/version labels + ENV JUNIPER_<X>_GIT_SHA/_BUILD_DATE; provenance accessor; git_sha/build_date on /v1/health; pass both into the existing set_build_info(...) and
ReadinessResponse(...); OQ-1 migrate hardcoded version → importlib.metadata; bump juniper-observability>=0.4.0 + regen lockfile; tests + CHANGELOG.
2. deploy PR 2 (#118 follow-up): make doctor — compare each running image's org.opencontainers.image.revision label (docker inspect, NOT /v1/health — data isn't host-published) vs git -C ../<repo> rev-parse --short HEAD → FRESH/STALE/UNKNOWN; + git_sha/STALE column in scripts/health_check.sh.
3. Rebuild stack + verify make doctor green.

Gotchas:

- Use cascor-worker #103 as the Dockerfile + /v1/health template.
- data: __version__ juniper_data/__init__.py:17; set_build_info api/app.py:43; /v1/health api/routes/health.py:117; obs pin pyproject.toml:76, lockfile requirements.lock:27 (==0.3.1).
- cascor: _API_VERSION src/api/routes/health.py:35; set_build_info src/api/app.py:164; /v1/health :53.
- canopy: already importlib (src/main.py:96-100); set_build_info main.py:156; /v1/health main.py:832.
- Lockfile regen (footgun): uv 0.11.8 + constraint-mode + /tmp+mv (memory: "uv lockfile self-pin trap", "Lockfile refresh after pyproject min bump").
- Full per-repo spec: design #412 + memory project_build_provenance_effort_2026-06-14.md.

Verify start: curl -so/dev/null -w '%{http_code}\n' <https://pypi.org/pypi/juniper-observability/0.4.0/json> → 200; gh -R pcalnon/juniper-data pr list --state open → none.

Git state: Open PRs: #412, #103, #118. Merged (worktrees cleanable): #414, #116, #117. Use a worktree per repo (feat/build-provenance, branch off origin/main).

---
