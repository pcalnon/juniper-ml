Lint shell scripts.......................................................Failed
- hook id: shellcheck
- exit code: 1

In scripts/activate_conda_env.bash line 26:
source "${conda_activate_script}"
       ^------------------------^ SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In scripts/activate_conda_env.bash line 36:
source "${conda_activate_script}"
       ^------------------------^ SC1090 (warning): ShellCheck can't follow non-constant source. Use a directive to specify location.


In scripts/cleanup_open_worktrees.bash line 5:
OLD_IFS="${IFS}"
^-----^ SC2034 (warning): OLD_IFS appears unused. Verify use (or export if used externally).


In scripts/cleanup_open_worktrees.bash line 13:
    cd "${ORIGINAL_PWD}"
    ^------------------^ SC2164 (warning): Use 'cd ... || exit' or 'cd ... || return' in case cd fails.

Did you mean:
    cd "${ORIGINAL_PWD}" || exit


In scripts/cleanup_open_worktrees.bash line 22:
        cd "${WORK_DIR}"
        ^--------------^ SC2164 (warning): Use 'cd ... || exit' or 'cd ... || return' in case cd fails.

Did you mean:
        cd "${WORK_DIR}" || exit


In scripts/wake_the_claude.bash line 538:
            EFFORT_VALUE="${CLAUDE_EFFORT_FLAGS} ${1}"
            ^----------^ SC2034 (warning): EFFORT_VALUE appears unused. Verify use (or export if used externally).


In scripts/wake_the_claude.bash line 550:
            MODEL_VALUE="${CLAUDE_MODEL_FLAGS} ${1}"
            ^---------^ SC2034 (warning): MODEL_VALUE appears unused. Verify use (or export if used externally).

For more information:
  https://www.shellcheck.net/wiki/SC1090 -- ShellCheck can't follow non-const...
  https://www.shellcheck.net/wiki/SC2034 -- EFFORT_VALUE appears unused. Veri...
  https://www.shellcheck.net/wiki/SC2164 -- Use 'cd ... || exit' or 'cd ... |...

Lint Markdown files......................................................Passed
Lint YAML files..........................................................Passed
Block unencrypted .env files.........................(no files to check)Skipped
