# Thread Handoff Prompt

Continue applying validation feedback to the Canopy Contextual Left Menu & Candidate Metrics Tab design document.

Completed so far:

- In-depth analysis of juniper-canopy codebase via 5 parallel research agents (sidebar structure, tab system, candidate content in metrics_panel.py, collapsible patterns, existing plans)
- Wrote comprehensive design document: notes/CANOPY_CONTEXTUAL_MENU_AND_CANDIDATE_TAB_DESIGN.md (v1.1.0)
- Ran 5 parallel validation agents (sidebar decomposition, callback compatibility, candidate extraction, tab system, contextual menu mapping)
- Applied 3 of ~12 required corrections from validation feedback:
  1. APPLIED: Expanded TAB_SIDEBAR_CONFIG to include all 12 tabs with separate network-info-status/details keys, sidebar-meta-params-card visibility, and collapse state management (nn/cn is_open + dynamic header text)
  2. APPLIED: Fixed internal html.Hr() divider handling — 4 orphan dividers (lines 499, 547, 651, 699 of dashboard_manager.py) now absorbed as first child of the wrapper div that follows them
  3. APPLIED: Updated version to 1.1.0, status to "Validated"

Remaining corrections to apply to the design document:

- FIX: Section 4.2 Content Source Analysis — resolve Move/Keep contradiction for candidate-epoch-progress (Section 4.2 says Move, Section 4.4 says Keep, Phase 6.6 says Keep). Decision: Keep in both tabs (Copy).
- ADD: Section 4.2 — add missing handler methods to Move list: _update_candidate_pool_handler, _update_progress_detail_handler (candidate-specific portions)
- ADD: Section 4.3 — add missing component IDs: candidate-metrics-pool-info, candidate-metrics-candidate-toggle, candidate-metrics-toggle-icon
- ADD: New subsection after 4.3 — "Required imports and helper methods" listing: time, dash, dbc, plotly.graph_objects, BaseComponent,_candidate_add_trace (Copy), _phase_band_color (Copy),_create_empty_plot (Copy), _get_status_style (Copy)
- ADD: Note about pre-existing bug — history pool cards have pattern-matching IDs but no toggle callback. New component must add this.
- ADD: Note about pool history store isolation — both tabs cannot independently accumulate history. Recommend shared store OR move history exclusively to Candidate Metrics tab.
- ADD: Note about _update_training_progress_handler coupling (outputs to both grow-progress and candidate-epoch-progress); splitting requires callback modification
- ADD: Specify max history entries (20) and note dcc.Store uses memory storage (history lost on refresh)
- CLARIFY: Phase 5 Step 5.5 — existing active-tab-aware data store callbacks do NOT need updating; the new CandidateMetricsPanel registers its own fetch callback via register_callbacks()
- CLARIFY: component_id constructor arg should be "candidate-metrics-panel" (not "candidate-metrics") to match naming convention
- ADD: Note that __init__.py export is unnecessary — follow existing direct-import pattern
- OPTIONAL: Add optimization note for network-info update callbacks — could gate on active_tab to avoid API calls when hidden
- OPTIONAL: Add note about new ctx-*-collapse components defaulting to is_open=True

Key context:

- Design document is at: notes/CANOPY_CONTEXTUAL_MENU_AND_CANDIDATE_TAB_DESIGN.md
- This is a juniper-ml worktree (deep-brewing-bunny) on branch worktree-deep-brewing-bunny
- The design targets juniper-canopy project at /home/pcalnon/Development/python/Juniper/juniper-canopy/
- The design covers two enhancements: (1) contextual left menu that shows/hides sidebar sections based on active tab, (2) new Candidate Metrics top-level tab
- Approach A (Visibility Toggle via Callback) was selected — keeps all components in DOM, uses display:block/none
- All 5 validators confirmed Approach A is sound; core callbacks (track_param_changes, apply_parameters, init_params_from_backend) all work with hidden inputs
- The file is large (~700 lines markdown); use offset/limit when reading specific sections

Verification:

- git branch: worktree-deep-brewing-bunny
- Untracked file: notes/CANOPY_CONTEXTUAL_MENU_AND_CANDIDATE_TAB_DESIGN.md
- No staged or modified files
