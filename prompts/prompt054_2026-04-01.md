# Thread Handoff

Continue implementing the juniper-cascor performance testing plan and optimization work.

Completed so far:

- **Pre-commit lint fix (juniper-canopy)**: Fixed 336 violations across 26 files, committed and pushed to origin/main (commit f02bd35)
- **Performance Testing Plan**: Written to `juniper-cascor/notes/PERFORMANCE_TESTING_PLAN.md` (v1.1.0, validated). 5 phases, 22 steps, 6 optimization candidates
- **Phase 1 (Infrastructure)**: Complete. Created `src/tests/performance/` with conftest.py (fixtures, datasets, BenchmarkTimer), test_baselines.py (21 tests). Added `--run-performance` flag to main conftest.py. Extended ProfileContext
with to_dict()/save_json(). All 21 perf tests pass, 2425 unit tests pass
- **Phase 2 (Micro-benchmarks)**: Complete. Created 4 parametrized test files (56 tests total): test_micro_forward_pass.py, test_micro_candidate.py, test_micro_correlation.py, test_micro_output_training.py, test_micro_autograd.py.
Collected full benchmark data
- **OPT-6 (Correlation single-output anomaly)**: IMPLEMENTED. Root cause: f-string tensor formatting in hot-path log calls triggered PyTorch's 1000-element print threshold. Fixed 15 log calls in candidate_unit.py. Result: output_size=1
went from 18.24ms to 0.49ms (37x speedup), anomaly eliminated

Remaining work:

- **Phase 3**: Concurrency and Scaling Analysis (worker pool scaling, queue throughput, IPC serialization overhead)
- **Phase 4**: End-to-end training profiling (py-spy flame graphs, phase time distribution, memory growth)
- **Phase 5**: Optimization implementation (requires explicit user permission per optimization)
- **OPT-4** (P0): Cache forward pass to eliminate redundant computation in grow_network() — 5-15% total time savings
- **OPT-5** (P1): Shared memory tensors for large datasets — needs Phase 3 data first
- **OPT-2** (P2): Fused correlation computation — 5-10%
- **OPT-1/OPT-3** (P3): Deprioritized by Phase 2 data (sub-linear scaling confirmed)
- Commit and push all juniper-cascor changes (performance tests + OPT-6 fix)

Key context:

- Working in juniper-ml worktree at `/home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/binary-jingling-horizon`
- juniper-cascor changes are in the main working directory at `/home/pcalnon/Development/python/Juniper/juniper-cascor/`
- CandidateUnit constructor uses `CandidateUnit__input_size=N` prefix (not `input_size=N`) — kwargs caught by **kwargs silently
- Performance tests gated behind `--run-performance` flag or `CASCOR_BENCHMARK_MODE=1`
- Conda environment: JuniperCascor (Python 3.14.3)
- Phase 2 benchmark data revealed: forward pass scales sub-linearly (not quadratic), autograd overhead is 6-8.5x pure forward, activation function choice is negligible (<10% difference)
- User directive: each optimization requires explicit positive permission before implementation

Verification:
git status (juniper-cascor) — check for uncommitted changes
cd /home/pcalnon/Development/python/Juniper/juniper-cascor/src && conda run -n JuniperCascor python -m pytest tests/performance/ --run-performance --benchmark-disable -v --timeout=120
cd /home/pcalnon/Development/python/Juniper/juniper-cascor/src && conda run -n JuniperCascor python -m pytest tests/ -m "unit and not slow" --timeout=60 --ignore=tests/performance -q

Git status: juniper-cascor is on branch main, uncommitted changes in src/candidate_unit/candidate_unit.py, src/tests/performance/*, src/tests/conftest.py, src/profiling/deterministic.py
