# Thread Handoff (Mandatory)

Per CLAUDE.md, I'm initiating handoff now while my context is still sharp and the next 3 repos are clean greenfield work.
I've completed the 4 most complex repos (the two that had dirty prior-thread state, plus the largest greenfield one).
The remaining 3 are all simpler.

## Handoff goal for the next thread

Continue Wave 1 of the Juniper hardcoded values refactor. Plan/roadmap:
juniper-ml/notes/HARDCODED_VALUES_REFACTOR_ROADMAP.md.

## Completed (all pushed to origin, no PRs yet)

- juniper-cascor-client: feature/hardcoded-values-wave1 @ 065518e — 54 prod
  - 186 testing constants in juniper_cascor_client/{constants,testing/constants}.py
- juniper-data-client: feature/hardcoded-values-wave1 @ de25918 — 100 const
  in juniper_data_client/constants.py
- juniper-cascor: feature/hardcoded-values-wave1 @ bc549c3 — 49 API const
  in src/cascor_constants/constants_api/, plus 6 const extending
  constants_candidates / constants_logging / constants_hdf5
- juniper-data: feature/hardcoded-values-wave1 @ dd24ab7 — 145 const across
  api/constants.py, storage/constants.py, core/constants.py, and 7
  generator defaults.py files (xor, checkerboard, gaussian, circles,
  mnist, csv_import, arc_agi)

## Remaining Wave 1 tasks

- juniper-cascor-worker (task 1.9): create juniper_cascor_worker/constants.py
  (~30 const). Critical: protocol message strings must be bit-identical to
  cascor server side. Worktree exists at
  /home/pcalnon/Development/python/Juniper/worktrees/juniper-cascor-worker--feature--hardcoded-values-wave1--20260409-0409--e277424f
  but the branch is 4 commits BEHIND main — fast-forward via
  `git reset --hard origin/main` first.
- juniper-canopy (tasks 1.10, 1.11): add SecurityConstants and
  BackendConstants classes to canopy_constants.py, extend DashboardConstants
  and ServerConstants. ~35 const. Use `typing.Final` consistent with
  existing class structure. Worktree exists at
  /home/pcalnon/Development/python/Juniper/worktrees/juniper-canopy--feature--hardcoded-values-wave1--20260409-0409--511bd953
  and is at main HEAD already.
- juniper-deploy (tasks 1.12, 1.13, 1.14): expand .env.example with 20 new
  variables, create scripts/config.sh (8 const), add Compose extension
  fields x-healthcheck-* (4). Worktree exists at
  /home/pcalnon/Development/python/Juniper/worktrees/juniper-deploy--feature--hardcoded-values-wave1--20260409-0409--634aa509
  but branch is 1 commit behind main — fast-forward first.
- juniper-ml has no Wave 1 task (correct per the roadmap).

## Work flow

Per-repo flow that has worked across the 4 completed repos:

1. cd to existing worktree, fast-forward branch to origin/main if behind
2. Read repo's notes/HARDCODED_VALUES_ANALYSIS.md and PLAN.md as starting point only — they undershoot by ~30%, ALWAYS verify against current source via grep/Read. The user confirmed: docs are starting point to be verified and extended.
3. Read source files identified by the analysis doc, extract every literal
4. Write the constants files using existing repo naming conventions (e.g., cascor uses _PROJECT_*, canopy uses typing.Final classes)
5. Validate: python3 -c import check, run pytest, run pre-commit
6. Commit (single commit per repo, no Sonnet co-author lines — user explicitly chose Option 3 for clean Opus-authored commits)
7. git push -u origin feature/hardcoded-values-wave1

## Key context from this thread

- Decision Points A=Option3, B=reuse existing worktrees, C=leave names
  alone, all approved by the user before I started.
- Prior Sonnet sessions died to the OOM killer at ~04:13-04:15 today,
  leaving partial work. cascor-client and data-client had committed
  partial work (reset/redone); cascor had uncommitted partial work
  in working tree (integrated and extended). worker/canopy/deploy
  have NO prior partial work — clean greenfield.
- Plan estimates undershoot. cascor-client plan said ~95, I delivered
  240. data-client plan said ~60, I delivered 100. data plan said ~92,
  I delivered 145. Aim for completeness over plan-conformance.
- For cascor-style repos with mature constants infrastructure, look for
  existing naming convention prefixes (_PROJECT_*, _LOGGER_*, _HDF5_*)
  and follow them. Don't add to master constants.py re-exports unless
  the prior thread already started doing so — the new constants are
  importable from their deepest path which is sufficient for Wave 1.
- Pre-commit gotchas: cascor uses flake8 which doesn't recognize
  trunk-ignore comments — use `# noqa: F401` instead. data uses ruff
  which auto-fixes typing.Tuple → tuple.
- Another active session may be running tests in
  juniper-cascor--fix--interface-phase2-security--20260409-1459 — leave
  it alone, do not interfere with that worktree.
- The juniper-ml worktree binary-bubbling-mochi (which this thread runs
  in) is on branch worktree-binary-bubbling-mochi — DO NOT make changes
  there. It was used by an unrelated startup-script effort.

## Verify State

Verify starting state with:

```bash
  cd /home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/binary-bubbling-mochi
  git status
  for repo in juniper-cascor-worker juniper-canopy juniper-deploy; do
    cd /home/pcalnon/Development/python/Juniper/$repo
    echo "=== $repo ==="
    git fetch origin 2>&1 | tail -1
    git log --oneline -3 origin/feature/hardcoded-values-wave1 2>/dev/null || echo "no remote branch yet"
  done
```

## Git status

this thread's worktree (binary-bubbling-mochi) is clean, no staged or unstaged changes.
All work landed in 4 separate repo worktrees and was pushed.

---
