# HANDOFF 2026-09-24 — dynamic-workflow harness design: relaunch consensus round 2, apply it, open the PR

**Session**: `dynamic workflow` (worktree `structured-inventing-fairy`, session `bb39e9fe-5809-451e-aacc-ec9d703a5fe0`)
**Predecessor**: none (first handoff of this arc). Written without verification at the owner's request; every fact below is as the predecessor left it, not re-probed.

## Goal statement

Continue the dynamic-workflow harness design arc: relaunch consensus round 2 (both round-2 agents were killed by a 5-hour usage limit seconds after launch and never reported), archive and reconcile its reports, apply them as design v4, and open the PR.

**Completed so far:**
- `notes/JUNIPER_2026-09-22_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-DESIGN.md` — **v3**, ~106 KB. Answers the owner's seed (Appendix A; the seed text lives on branch `design/dynamic-workflow` at `27ea09a1` as `notes/dynamic_workflow_design.out`). Round 1 complete: Lane A ×3 (repo / docs+state / execution) and Lane B ×2 (architecture refutation / amputation+actionability); every finding applied or recorded (Appendix D = Lane A map, 29 rows; Appendix E = Lane B map, 30 rows). Largest change in v3: the controller is a systemd timer running a pure-code tick that launches one `claude -p "/juniper-cycle <plan>"` child per cycle (the saved workflow runs inside the child); worktrees are created by code in the task's repo; V3's B3 gate is amended to a patch-id test (D-12). Twelve owner decisions D-1..D-12 in §10 (D-7 retired).
- `notes/JUNIPER_2026-09-23_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-CONSENSUS-ROUND-1-RECORD.md` — the five round-1 reports verbatim plus two reconciliation sections (Lane A, Lane B).
- `notes/JUNIPER_2026-09-23_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-GROUNDING-INVENTORIES-RECORD.md` — the five grounding inventories verbatim (the design's §2 and §4.2 cite them as INV I1–I5).
- `notes/JUNIPER_2026-09-23_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-CONSENSUS-ROUND-2-RECORD.md` — **header only**; no reports yet.
- `util/ad-hoc/2026-09-22_rate_limit_record_probe.py` (transcript `quotaLimits` probe; Appendix B evidence) and `util/ad-hoc/2026-09-23_archive_subagent_reports.py` (copies a subagent's final message verbatim from its transcript into a record file; idempotent by section label).
- Local gates passed on v3 and both records: `python3 util/ad-hoc/2026-09-05_markdown_structure_check.py <files>` → 0 problems; all relative links resolve; fences balanced; the §6.2 script parses when wrapped as `async function run(args, agent, pipeline, parallel, log) { … }` with `export const meta` → `const meta`; five phases declared = five used.
- Memory: `project_dynamic_workflow_harness_design_2026-09-23.md` saved and indexed at line 2 of `MEMORY.md`.

**Remaining work:**
1. **Relaunch round 2** (two `general-purpose` agents, launched together, no more than three at a time; read-only w.r.t. the repo and `~/.claude`; scratch under the session scratchpad; the artifact stays frozen while they run). Briefs, condensed:
   - *Lane A — execute the corrections*: (1) extract the ```javascript fence from §6.2, wrap as above, `node --check`; run a stub runtime (stages get `(prev, item, index)`; throw → null and skip; `parallel` → null on failure) for three scenarios: execution task PASS + 3×`refuted:false` → reaches Maintain with `launched=3`; executor returns `decisions_raised` → Verify short-circuited, Maintain gets `decision`; `prompt:<path>` planning task → no executor, `prompt_validated:true`, one lens; list undefined identifiers; judge whether §6.4's "⌈launched/2⌉+… concretely 2 of 3, or 1 of 1" is a computable rule. (2) implement §8.1–§8.2 exactly as written; fixture (a) `u5=23.5 r5=4h, u7=41.2 r7=3d, k=100000`, tasks S,S,M,L, `m=1.2`, timer 600 s, 40 ticks with no owner consumption → first S/M dispatch tick; (f) a 5-hour reset between ticks → is "`A_w := 0` then accrue only from `resets_at_old`" unambiguous; (g) `u7=92, r7=26h` and `u7=100` → exit 3 `weekly_wall`; (h) stale odometer with a future fallback reset → is "that window is at its wall until then" computable; (i) spend > A → floor 0; list every invented detail. (3) §7.3 patch-id test on the six squash-merged branches Lane B1 named (`analysis/soak-stratum-predictors-main` #1854, `arc/canopy-e2e-phase1-seg10` #1113, `arc/canopy-e2e-phase1-seg9` #1106, `chore/ad-hoc-ceiling-bump-opener` #1459, `chore/ad-hoc-signing-arc-status` #1110, `chore/adhoc-changelog-reorder-tool` #1792): `git diff origin/main...<branch> | git patch-id --stable` vs `git show $(gh pr view N --json mergeCommit --jq .mergeCommit.oid) | git patch-id --stable`; report pass/fail honestly and, if merge-base drift breaks it, what to compare instead (`<mergeCommit>^` as base). (4) `util/open_signed_pr.py`: confirm the DUP-GUARD is keyed on branch name (`find_open_pr` at `:175`, called `:251`), base resolved from the remote ref (`:83-84`), existing branch refused (`:276`), and that `--append`, `--reuse`, `--expect-base` do not exist. (5) `scripts/claude_interactive.bash:17` (`DEBUG`) and `:84` (`--dangerously-skip-permissions`); `claudey` symlink. (6) §5.1 merge rule on three synthetic session files (stale re-render with lower % and older hash; fresh; passed window). (7) grep all tracked `notes/*.md` first-30-line Status lines for ` — (COMPLETE|SUPERSEDED) \d{4}-\d{2}-\d{2}$` (design claims 0) and count `**Supersedes…**:` lines naming plausible ledger docs. (8) Appendix E rows 3, 5, 11, 13, 15, 30 resolve to text that says what they claim; D-7 "retired" referenced nowhere else; §0 states P0's exemption; `~/.claude/juniper-dispatch/` paths consistent across §3.1, §4.1, §6.1, §6.9, §8.5. Output: results per check with outputs; underspecified/broken list; demonstrated failures; instrument adequacy; cannot-verify.
   - *Lane B — refute the corrections* (grant §2 and round 1; "the fix pass is the least trustworthy part"; may fetch `code.claude.com/docs/en/<page>.md`): (1) the timer + `-p` child surface: does `-p` start a saved project workflow by name; is anything needed in bypass mode; does the child stay open with `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0`; at a 429 do agents fail → null and can `--resume` continue the *workflow* or only the turn (§8.2 step 7 assumes); any write from the briefs that lands in the main checkout; cross-session delivery direction for a bypass sender → bypass receiver (§9.2). (2) the heartbeat (§5.1): does a status line render with no project; does a `/loop` turn produce an API response with fresh `rate_limits`; user CLAUDE.md/MEMORY.md still load; daily token cost vs "one small turn per 45 min"; is it an 8th/9th session. (3) code-created worktrees + `dispatch-executor` without isolation: where a relative-path habit writes into the main checkout or tests the wrong tree; regression vs v2's isolation for juniper-ml tasks. (4) docs-branch model (§6.5) across the first squash merge: conflicts, docs screen / Sequence Safety on the reopened PR, who resets the branch; `one_pr_per_work_unit`; PR-storm lessons (`notes/JUNIPER_2026-07-28_JUNIPER-ML_CURSOR-PR-FLOOD-REMEDIATION-ANALYSIS.md`). (5) §4.3 case 2 vs case 3 liveness (is case 2 dead code; can a human produce the suffix accidentally). (6) `VERIFY_PENDING` + one refuter in the docs pilot: a single false refutation blocks a task; what the owner sees. (7) own-token debit vs account-wide `H_w`: construct the case where `A_w` exceeds true headroom after heavy owner use; post-reset front-loading; jitter for 5-hour resets. (8) calibration resolution: what `k` range a 250 K burst can resolve given the status line's decimal granularity (`23.5` observed); does D-10's threshold fall inside it. (9) is "loses the `/workflows` live view" over- or under-claimed (stream-json subagent messages, `parent_tool_use_id`). (10) Appendix E rows 2, 4, 14, 16, 19, 27 vs text. Output: numbered refutations with severity/quote/scenario/evidence/repair; corrections that hold (≤12 lines); new risks; cannot-judge.
   - If a launch dies with `You've hit your session limit · resets HH:MM`, wait for the reset and `SendMessage` the same agent ids ("continue from where you stopped, deliver the FINAL REPORT"); do not relaunch in the same session. (The predecessor's dead round-2 ids cannot be resumed from a new session.)
2. **Archive and reconcile**: `python3 util/ad-hoc/2026-09-23_archive_subagent_reports.py --session-dir ~/.claude/projects/<slug>/<session-id> --out notes/…ROUND-2-RECORD.md --agent <id>="A — round 2 — execution" --agent <id>="B — round 2 — refutation of the corrections"`; the slug for a worktree session is `-home-pcalnon-Development-python-Juniper-juniper-ml--claude-worktrees-<name>`. Add a "Reconciliation" section (CON §5: re-derive every lone load-bearing finding by opening the artifact; record dissent). Apply all accepted findings to the design **in one pass** as v4 with an **Appendix F** map; update the header (Version, Status → "DRAFT — rounds 1–2 applied; awaiting owner rulings D-1..D-12"), §0's round table, §12 (round 2 row + the CON §7 minimum record: instruments, sample size, agents/entry points, iterations and what the last changed, unresolved dissent, what the evidence cannot support). Run round 3 only if round 2 changed a number, a disposition or an action (CON §4).
3. **Gates before the PR**: structure check on all five notes files; relative links; fence count even; §6.2 script wrapped-parse (`node --check`); `juniper-check-doc-links --exclude templates --exclude history --exclude legacy --exclude pull_requests --exclude releases --exclude analysis --exclude fixes --exclude development --exclude CHANGELOG.md --cross-repo skip` (binary at `/opt/miniforge3/envs/JuniperCascor1/bin/`). `notes/` is exempt from markdownlint.
4. **Open the PR** with `util/open_signed_pr.py` from `origin/main` (local commits would hang on signing; the branch must not already exist): `--repo juniper-ml --branch design/dynamic-workflow-harness --add <local>:<repo path>` for the six new files plus this handoff, `--title "docs(design): dynamic-workflow harness — limit-paced, goal-driven agent dispatch (consensus rounds 1-2)"`, `--body-file <scratch>/body.md`. Body: summary; the twelve decisions as the ask; **name every document created** (the design, three records, this handoff) and the two scripts; note the seed branch `design/dynamic-workflow` (`27ea09a1`) is superseded by Appendix A and can be deleted after merge (owner's call); `## Requirements`: no JR id identified — say so rather than invent one; end with the attribution lines in force. Do not merge; do not delete any branch.
5. After the PR: update `project_dynamic_workflow_harness_design_2026-09-23.md` (state → PR number, v4) and its `MEMORY.md` line; leave the worktree in place (nothing in it is committed; the PR carries the files by upload).

**Key context (traps the predecessor hit):**
- The account is limit-bound: the 5-hour window killed three round-1 validators and both round-2 validators within a minute of launch; the weekly window was exhausted 2026-09-22 (reset Sep 23 10pm Chicago). Launch ≤3 agents at once; resume, never relaunch, inside a session.
- Freeze the design while a lane runs; apply all findings in one pass with a finding → resolution map so the next round can be briefed on the corrections alone.
- Archive reports by copying from transcripts (the script), never by re-emitting them.
- The worktree isolation shim refuses compound shell in the worktree (heredocs, `$(…)` feeding a command, `awk`); put such commands in a script under the scratchpad and run it. Heredocs that only touch `~/.claude` were allowed.
- `util/ad-hoc/2026-09-05_markdown_structure_check.py` is the single-file structure gate (`util/markdown_structure_delta.py` needs a base/head and cannot see untracked files).
- The predecessor's snippet checker lived in the session scratchpad and is gone: recreate (extract the fence with a regex, wrap, `node --check`; node v24 and jq 1.8 are on the host).
- Design facts a successor must not re-derive wrongly: `quotaLimits` sits at the transcript line's root (sibling of `message`); transcript records lag a hit by hours; a self-paced `/loop` is not restored on `--resume`; `autoContinueAtUsageLimit` in a project/local settings file turns auto-continue off; `isolation: worktree` is single-repo; V3's B1/B3 refuse squash merges 6/6; six CI tests pin agents and skills to opus+max; every `claudey` session runs with `--dangerously-skip-permissions`.
- `main` has moved since `25b78f55` (juniper-ml 0.10.0 shipped 2026-09-23); keep "Measured at: 25b78f55" in the design — it is the measurement instant, not the PR base.

## Verify starting state

```bash
cd /home/pcalnon/Development/python/Juniper/juniper-ml/.claude/worktrees/structured-inventing-fairy
git status --short            # expect ?? for the 6 files + this handoff; nothing staged
git log -1 --oneline           # 25b78f55
ls notes/JUNIPER_2026-09-2[23]_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-*.md
python3 util/ad-hoc/2026-09-05_markdown_structure_check.py notes/JUNIPER_2026-09-22_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-DESIGN.md
git show design/dynamic-workflow --stat | head -5   # 27ea09a1, notes/dynamic_workflow_design.out
```

## Git status at handoff

Branch `worktree-structured-inventing-fairy` at `25b78f55` (= `main` at the time). Untracked, uncommitted: `notes/JUNIPER_2026-09-22_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-DESIGN.md`, `notes/JUNIPER_2026-09-23_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-CONSENSUS-ROUND-1-RECORD.md`, `notes/JUNIPER_2026-09-23_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-CONSENSUS-ROUND-2-RECORD.md`, `notes/JUNIPER_2026-09-23_JUNIPER-ML_DYNAMIC-WORKFLOW-HARNESS-GROUNDING-INVENTORIES-RECORD.md`, `util/ad-hoc/2026-09-22_rate_limit_record_probe.py`, `util/ad-hoc/2026-09-23_archive_subagent_reports.py`, and this file. Nothing staged; no PR open; seed branch `design/dynamic-workflow` (`27ea09a1`) untouched.
