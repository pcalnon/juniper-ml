# Handoff prompt

🧵 Thread Handoff — Juniper-Canopy Audit / UI-Harness / Model-Selection

✅ Completed & committed (local only — NOT pushed)

- Plan approved + persisted: juniper-ml/notes/JUNIPER_CANOPY_AUDIT_AND_HARNESS_PLAN_2026-06-15.md (619a499).
- Harness L1 — util/ui_control_graph.py + src/tests/unit/test_control_graph_lint.py. Runtime-introspects the Dash app; rule: Button/Upload need a callback Input, value-carriers need Input-or-State. 2 tests green.
- Harness L2 — src/tests/ui_contract/control_manifest.py + src/tests/integration/test_control_manifest_behavioral.py. Declarative control→endpoint contracts, in-process. 8 tests green (committed 4fc6b68).
- Empirical findings (verified, not asserted): 3 dead controls — restart-with-new-dataset-button (dashboard_manager.py:1409); nn-init-output-weights-dropdown (:880, also missing from SetParamsRequest :2831-2876 → double break); dataset-plotter-dataset-selector (dynamic id, empty options). Backend
is healthy (L2 green) → regressions are frontend callback-graph holes. Caught a recon hallucination (the init-weights dropdown was claimed wired).

⏭️  Remaining (plan order)

1. Fix restart button (Task 7, stacked PR fix/orphan-dataset-restart-button off the harness branch): wire callback → POST /api/train/start?reset=true → close banner (mirror apply_dataset/cancel at dashboard_manager.py:3510-3581) + demo-parity (clear _pending_dataset_config in demo's start path —
demo has no consumption, so reconcile_pending_dataset_banner :3588 would reopen the banner) + remove it from KNOWN_ORPHANS + regression test. Other 2 orphans stay deferred (non-trivial).
2. L3 Playwright POC (Task 6): corrected native-value-setter (bubbling input+change+blur, honor 350 ms debounce) to un-xfail src/tests/ui/test_apply_button_flow.py; fallback dash[testing] (selenium not installed).
3. Model-selection design (Task 8, doc-only) — registry/task_type gating per the plan doc.
4. Literature → juniper-ml/papers/ (Task 9); deliverable doc (Task 10); validation fleet (Task 11); CI + requirements.lock reconcile + PRs (Task 12).

⚠️  Gotchas

- Env = JuniperCanopy1 (JuniperCanopy is deprecated); activate via source /opt/miniforge3/etc/profile.d/conda.sh && conda activate JuniperCanopy1.
- requirements.lock stale (dash 4.2.0 vs installed 4.1.0; plotly, starlette) — reconcile before CI pins matter.
- Running :8050/:8201 are the Docker stack — repro against local ./demo.
- util/ad-hoc/2026-06-15_introspect_dash_tree.py is untracked (throwaway).

Git status

- canopy: test/ui-regression-harness @ 4fc6b68 — worktree …/worktrees/juniper-canopy--test--ui-regression-harness--20260615-1944--c07dab86
- juniper-ml: docs/canopy-audit-recurrence-integration @ 619a499 — worktree …/juniper-ml/.claude/worktrees/atomic-painting-phoenix

Verify starting state

source /opt/miniforge3/etc/profile.d/conda.sh && conda activate JuniperCanopy1
cd <canopy-WT>/src
python -m pytest tests/unit/test_control_graph_lint.py tests/integration/test_control_manifest_behavioral.py -q   # 10 passed
python ../util/ui_control_graph.py    # prints the 3 orphans, exit 1

📋 Continuation prompt (paste into a fresh thread)

▎ Continue the juniper-canopy audit + UI-regression-harness + model-selection work. Harness L1 (util/ui_control_graph.py) and L2 (src/tests/ui_contract/ + src/tests/integration/test_control_manifest_behavioral.py) are built and green (committed 4fc6b68 on test/ui-regression-harness); the working
▎ plan is juniper-ml/notes/JUNIPER_CANOPY_AUDIT_AND_HARNESS_PLAN_2026-06-15.md (619a499). Verify state with the commands in that doc. Then: (1) implement the restart-button fix as a stacked PR — wire restart-with-new-dataset-button → POST /api/train/start?reset=true + banner close (mirror
▎ dashboard_manager.py:3510-3581), add demo-parity clearing _pending_dataset_config on demo start, remove it from KNOWN_ORPHANS, add a regression test; (2) run the L3 Playwright value-setter POC to un-xfail test_apply_button_flow.py; (3) write the deliverable
▎ juniper-ml/notes/JUNIPER_CANOPY_AUDIT_REGRESSIONS_AND_MODEL_SELECTION_2026-06-15.md (model-selection design + regression catalog + harness + validation appendix); (4) retrieve UI/accessibility + Dash-testing literature into juniper-ml/papers/; (5) run the independent-sub-agent validation pass;
▎ (6) reconcile requirements.lock, confirm CI runs L1+L2, open the harness/fix/doc PRs. Env: JuniperCanopy1. Keep every claim file:line-backed and POC-verified.

---
