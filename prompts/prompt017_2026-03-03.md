# thread handoff

## thread handoff goal

perform a thread handoff with the following goal

1. first, address failing tests:
   - investigate the 29 failing tests.
   - determine root causes of the failures.
   - design fixes for these root causes.
   - implement the fixes to resolve each of the errors.
2. next, address warnings:
    - investigate the following filtered warnings:

    | Files                                                | Warnings                                                                         |
    | ---------------------------------------------------- | -------------------------------------------------------------------------------- |
    | src/tests/unit/test_demo_mode_comprehensive.py       | Added filterwarnings on 4 legacy env var tests (2 invalid-value + 2 valid-value) |
    | src/tests/integration/test_setup.py                  | Added filterwarnings("ignore::ResourceWarning") + import pytest on test_imports  |
    | src/tests/integration/test_main_coverage.py          | Added filterwarnings("ignore::RuntimeWarning") on 2 schedule_broadcast tests     |
    | src/tests/integration/test_network_stats_endpoint.py | Added filterwarnings("ignore::ResourceWarning") on test_no_backend_available     |

    - determine root causes of these warnings
    - design fixes for these root causes
    - implement fixes to resolve warnings

  ---
