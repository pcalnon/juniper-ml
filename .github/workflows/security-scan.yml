name: Scheduled Security Scan

on:
  schedule:
    - cron: "0 6 * * 1"  # Weekly on Monday at 06:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  security-scan:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd  # v6.0.2

      - uses: actions/setup-python@a309ff8b426b58ec0e2a45f0f869d46889d02405  # v6.2.0
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit
          pip install -e .

      - name: Run pip-audit (Dependency Vulnerabilities)
        # --ignore-vuln CVE-2026-3219: pip 26.0.1 (pre-installed on the
        # GitHub Actions runner image) is flagged for the concatenated
        # tar/ZIP confusion issue. As of 2026-04-29 there is no fixed pip
        # release. Re-evaluate and remove this flag when pip publishes a
        # fix.
        run: pip-audit --strict --desc on --ignore-vuln CVE-2026-3219
